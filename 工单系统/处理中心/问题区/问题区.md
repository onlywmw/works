# 问题区

> 各角色阻塞/疑问 + 审验员审验发现的证据链问题（红线 16：必须登记本区），设计师集中处理（解决/升级/打回）。

| 日期 | 角色 | 问题（工单 + 证据链问题 + 建议） | 设计师处理 | 状态 |
|---|---|---|---|---|
| 2026-08-28 | 审验员 | **S-08 线上领先于入库（未入库部署）**：审验对账发现生产站 `/home/market/` 页脚 = 「赣公网安备36073202361013号」+ `/home/assets/beian.png`（200，1403B），而验收链 `feat/s08-market-page @ 471ae65` 页脚仍「公安备案：办理中」且仓库 assets 无 beian.png——备案改动**已上生产但未入库**（mov-s08 worktree 内页脚改动为未提交脏状态，beian.png 连本地工作区都没有，仅存服务器）。风险：下次从仓库部署将**回退**线上备案号+破图。建议：施工者把页脚改动 + beian.png 补提交入库（可挂 S-08 分支或小单），并在工单表注记；S-08 本单验收范围不含此改动，不影响本单审验通过结论。 | ✅设计师已处理 @2026-08-28：备案批次入库 `f6d6fb6`（9 页页脚 + verify ②b 断言 + beian.png）——市场页脚从 mov-s08 脏状态逐字搬入同笔；S-08 合并 b6b7972 后复跑 verify 23 项全绿 exit=0，回退风险消除 | ✅已销 |
| 2026-08-29 | 设计师 | **memory.save 双名单观察（UPG-05 R1 验收参考）**：UPG-02+04 合并单溯源复核时发现——main 上 `McpToolScheduler.kt` 的 `memory.save` 同时出现在 writeTools（:103）和 harmlessTools（:117）（b62cee8 B 方案三级分诊引入），而判定顺序 harmless 优先于 writeTools（:184-188）→ **main 上 save 实际已是自动放行**。这与 UPG-05 验收实测「save 弹审批窗」（打回 D3 的前提）表面矛盾——可能 feat/upg05 基线与 main 名单状态不同步，或弹窗来自其他路径。建议：验收员 R1 差异面验收时核对该名单状态（feat/upg05 合并后 save 应在 harmless 且仅一处），R1 的 scheduler 改动与 main 现状 diff 需对齐口径。 | ✅设计师销项 @2026-08-29：查验员审验已顺带实证（见下行）——feat/upg05 tip 与 main 同形态、harmless 优先=ALLOW、R1 后口径一致，弹窗实为 R1 前旧基线行为；writeTools 内 save 系冗余死项（main 同病，非 R1 引入），列入权限名单收口捎清项 | ✅已销 |

| 2026-08-29 | 查验员 | **工单表.xlsx 含非法 XML 字符引用（解析链断裂风险）**：UPG-05 行 E6 单元格「🔨C 处理中」的 🔨 被写成 UTF-16 代理对数字引用 `&#55357;&#56616;`（合法写法应为 `&#128296;`），XML 规范禁止代理区码点——Python/openpyxl 全文件解析失败（`reference to invalid character number` @col 4432）。引入窗口：今晚（23:20 的 UPG20R2 备份 VALID，00:0x 的 UPG17通过 备份已 INVALID）。当前 SheetJS/Excel 是否受影响未实测，但任何严格 XML 工具读主表即炸。建议：用合法引用替换修复（内容零变化，纯编码修正），或从 VALID 备份重放今晚编辑；修复前各角色写表工具若再经同一编码路径会带病扩散。 | ✅用户拍板查验员修复 @2026-08-29：`&#55357;&#56616;`→`&#128296;` 单点替换（内容零变化），改前备份 `_备份归档\工单表_backup_查验员_修复非法XML引用前_2026-08-29.xlsx`；修后 openpyxl 全表 21 行解析通过、UPG-05/UPG-17 行抽核内容不变 | ✅已销 |
| 2026-08-29 | 查验员 | **UPG-05 装配路径 COVER_HIT 打点断线（注释承诺未实现，「函数在、链断」同款）**：审验 UPG-05 R1 时数据面对账发现——`MainActivity.kt:3553`（feat/upg05，步3 8c8724d 引入）注释明写「COVER_HIT 打点：turnId=cover-<指纹>，整段只计 1 次引用」，但 `memoryCoverPromptSegment()`→`MemoryCoverManager.currentCover` 装配注入路径**全链无任何 recordCoverHits/memoryRef 调用**（grep 实证：recordCoverHits 唯一调用者=MemoryMcpTools.cover()，即仅 AI 显式调 memory.cover 工具才记）。真机互证：emulator-5558 E/R 行为面 cover 注入确定发生（E1 3跑3过=注入生效铁证），但 14 个 session journal **memory/ref 零事件**、显化页全部「引用 0 次」。影响：v5.1 晋升语义「save→同轮 search+1→下轮 cover+1」的「下轮 cover+1」在生产路径永不发生 → 晋升流量枯竭 → draft 14 天衰减兜底清场——**死穴 1（晋升证据链断裂）只修了一半**（AI 自发 search 且 verbatim 命中才有 ref）。验收缺口同步登记：L 组无「装配注入→ref 增长」断言（L2 验的是工具直调路径），验收员按 v5.2 剧本执行无失职，属剧本+实现双缺口。建议：①currentCover 命中处按注释既有口径补 recordCoverHits（turnId="cover-"+指纹）；②L 组补断言「新 session 装配注入→memory/ref +1（每 session 每条 ≤1，对齐 Freeze）」。已同步挂账（P1，不阻塞 R1 合 main——非 R1 回归、步3 引入）。**顺带实证设计师「memory.save 双名单观察」**：feat/upg05 tip 与 main 同形态（save 同列 writeTools:102+harmlessTools:117，harmless 优先=ALLOW；upg05 新增 memory.delete 仅 writeTools=ASK 正确）——R1 后口径一致，writeTools 内 save 为冗余死项（main 同病，非 R1 引入）。 | ✅设计师接单 @2026-08-29：挂账 P1 在册、不阻塞 R1（已合 main @66244f4）；两点修法采纳（①currentCover 命中处按注释口径补 recordCoverHits[turnId="cover-"+指纹] ②L 组补「装配注入→memory/ref 增长」断言[每 session 每条 ≤1 对齐 Freeze]）→ **已出单：UPG-22 记忆尾单 @2026-08-29**（规则 21 溯源复核已过；捎 R3 释法+writeTools 冗余 save 清理+tools/ 四临时物；开工前置=UPG-02+04 合 main） | 📋已转工单 UPG-22（待认领） |
| 2026-08-29 | 查验员 | **工单表 UPG-02+04 验收结论登错行（UPG-07 错卡同款复发）**：UPG-02+04 合并单验收结论（012784e 通过段）写进了 **row 4（UPG-03 行）H 列尾部**，而正确位置按 UPG-04 卡「施工/验收/登记均走 UPG-02 主卡」应为 **row 3（UPG-02）F3=✅通过+H3 注记**——现状 F3 空白、H3 无验收段、row 5（UPG-04）无并入验收标注。验收员自报「row4 H4 追加」即错行自认（疑按旧行号记忆或数错行）。工单库侧正确（UPG-02 卡尾有验收段、UPG-04 卡并入销）——主表与库不一致。建议：验收员改前备份后修表——结论挪 row3（F3=✅通过+H3），row4 误挂段清除（UPG-03 行恢复至「闭环@2026-08-29」止），row5 H 列补一句「验收走 UPG-02 主卡（012784e）」；修后我读回对账。 | —（转验收员修表） | ⏳待验收员 |
| 2026-08-29 | 设计师 | **工单表非法 XML 字符引用第二起（同款复发，工具病根未除）**：首案（UPG-05 E6 🔨 代理对引用，查验员修复已销）后，C 认领 UPG-06 写 E7「🔨C 处理中」时同款复发——`&#55357;&#56616;` 代理对引用致 openpyxl 全表解析失败（`reference to invalid character number` @col 24446），设计师合 UPG-02+04 登记时发现。根因：某写表路径把 emoji 按 UTF-16 代理对拆成两个数字引用而非码点引用，严格 XML 工具读主表即炸。建议（机制项）：**写表统一走 openpyxl API，禁裸 XML/字符串拼写**；或写表工具入库前过一道「非法引用扫描」（代理区码点引用必拒）。 | ✅设计师修复 @2026-08-29：代理对合并为合法码点引用（`&#55357;&#56616;`→`&#128296;`，内容零变化、C 的认领登记完整保留），修后 openpyxl 全表 22 行解析通过；改前备份 `_备份归档\工单表_backup_设计师_UPG01溯源_2026-08-29.xlsx`（注：该备份是坏表副本，仅作事故留痕） | ✅病例已销；⏳机制项（写表统一 openpyxl）待用户拍板 |
| 2026-08-29 | 设计师 | **工单表.xlsx 被旧副本整体覆盖（多会话落档丢失+乱码）**：晚间的 live 表被某会话用旧副本覆盖——丢 UPG-22/UPG-23 两行、丢 UPG-01 v2.2/UPG-02+04 合 main 落档（G3/G5）、row7 出现乱码。已恢复：以最完好备份（UPG22前置解除，24 行）为基+重放 E23 前置解除+C 批1 进度（E7/H7）+补 UPG-24/25 行；被覆盖文件留证 `_备份归档\工单表_被旧副本覆盖_丢行乱码_2026-08-29.xlsx`。另发现两起错行写：①验收员把 UPG-02+04 验收明细写进 UPG-03 行 H4（内容保留，G3/G5 状态已在，不搬移防断引用）；②C 把 UPG-22 认领写进 UPG-06 行 H7（已挪正至 E23）。**纪律重申：写表只许「读当前 live→改→写回」，禁止用任何旧备份整体覆盖；认领/落档先按 A 列工单号定位行号再下笔。** | ✅设计师已恢复 @2026-08-29（openpyxl 全表 26 行解析+FFFD 零残留验证） | ✅已销（纪律提醒留档） |
| 2026-08-29 | 查验员 | **S-05 落档两面不同步**：验收通过节已落 ACCEPTANCE_LOG（67cee93，公网+服务器双实证我复核全吻合），但 ①工单库 S-05 卡状态行仍停「✅方案完成待派单」（无施工/验收任何记录）②维度工单表底稿/市场.md S-05 行同停「方案完成待派单」。两个状态面与落档脱节，后续按库/表排程会误判本单未派。建议：验收员或设计师补登两处状态行（内容照 ACCEPTANCE_LOG 口径：数据层撤 demo 2 包、公网=服务器同源 md5 7d0b18be…、回滚备份在架）。 | —（转验收员/设计师补登） | ⏳待补登 |
| 2026-08-30 | 审验员 | **UPG-27 R1 打回项复验通过但 L2 端到端未验 + 证据区缺独立产物**：审验 UPG-27 R1（ca47f01）——CodeModeWiringContractTest 锚真实性亲核通过（E3 装配点 `if(presentationMode==CODE){` @:34=MainActivity:4444 精确匹配、buildSdkSection 调用锚 @:39=:4446、outputHint 诚实化 ToolSdkGenerator:142 恒「output 声明待登记」+ CodeModeWiring:61-68 禁「顶层返回键含」锁死）+ M1 亲杀红逻辑自洽（0efda79 无锚假红→R1 加锚必红）+ L1 全量 388/0/0。**问题①（受限项）**：L2 真机四项（code 模式真实任务/双门实测/tool.help 直呼/塌缩自纠）标 ⏳ 待真机补验——代码层打回项虽过，UPG-27 整体 L2 端到端未验，**整单合 main 前置未满足**（需 L2 真机补验后销，走真机补验挂账区）；**问题②（证据链）**：证据区 `UPG27\` 仅文本记录 upg27_r1_evidence.md，无 L1 XML/变异 FAILED 输出独立落盘。建议：①L2 四项入真机补验挂账区，补验后销（受限于平板 AI 发送链不稳，同 UPG-28）②补落盘 L1 XML + 变异 FAILED 输出。 | —（转设计师定夺） | ⏳待补验/补证 |
| 2026-08-30 | 审验员 | **批3 系列 & UPG-18 证据区产物缺失 + 三类受限项待补验**：①**证据链**——批3-1/3-3 仅 evidence.md、批3-2 & UPG-18 证据目录 NOT FOUND，统一缺 L1 XML/变异输出/测试日志独立落盘产物（元数据/死代码单 L1 判据由测试+变异保证，但证据链缺可复核产物，建议统一补落盘 L1 XML+变异 FAILED 输出）；②**受限项（L1 过、L2/实测待验证）**——UPG-25 v2 L2 真机 13 项截图+L3 换肤真机零回归 待装机补验（UI 视觉项）；UPG-07 批2 弹窗基线数值挂账待补（adb 基础设施 5037 半死/离线受阻，脚本+任务定义已交付）；批3-4 Token/KV 上界 +8~11K/轮 待 L2 真机快照实测（推导上界非实测）。建议：受限项按⏳待补验标注，不混入直接可合批；证据产物统一补落盘。 | —（转设计师定夺） | ⏳待补验/补证 |
| 2026-08-30 | 审验员 | **UPG-01 批4 R1 L3复验 审验发现两条登记不规范（不阻塞证据链通过结论）**：审验 L3 复验证据链闭环坐实（seq 3910 `tool/call ext_MockServer_hello {"who":"Galaxy"}` 带参 → seq 3911 `tool/result` mock-hello 回显 isError=false 同 turn → seq 3948 assistant 确认；环境链 mcp_servers.json+adb reverse+MOV-Boot 156→158 实核；6574b93 落档干净仅 ACCEPTANCE_LOG）——但发现 **①挂账登记表.md 状态文本自相矛盾**（同段并存「本挂账销项（ACCEPTANCE_LOG 6574b93）」与「挂账维持 ⏳ 不销；R2 建议=补可观测日志」，销项登记不干净，旧 R2 建议段残留未删）**②工单表.xlsx UPG-01 验收员列未更新**（验收员自报「工单表已登记批4-R1 ✅ 验收通过」，但实测 row2 F2 仍是旧状态「真机 L3 ❌ 未闭环→维持打回」，与自报不符）。建议：设计师/验收员①清理挂账登记表矛盾文本（删残留 R2 段，统一为「销项」）②补登工单表 UPG-01 批4-R1 ✅ 通过状态（含 15:2x 复验通过与 L1/L2/L3 全过口径），改前备份。 | —（转设计师/验收员） | ⏳待清理 |
| 2026-08-30 | 审验员 | **UPG-41 v2 市场包详情开关切换 UI 不即时同步（disable 实测）**：模拟器复验 MockServer 已装包详情——点头卡 ToggleButton（van-switch）触发 disable，toast「已停用 MockServer」+ store `enabled:false` 落库成功，**但 switch aria-checked 仍保持 on**（`MarketPackDetail.vue` toggle→`onTogglePkg→ue()` 调 `market.disable` 后仅 toast+刷新总览，未更新本地 `pkg.enabled`；退出详情重进才显示正确——重进实测 off+「已停用」）。影响：用户切换后界面误导（显示开实际已停）。验收启停实操未完成（P3）故未暴露。建议：`ue()` 成功后同步 `l.enabled`（或详情页 switch 状态改由 store 响应式驱动）。非本单打回项范围（P1 分流/P2 键集均已坐实），不阻塞 UPG-41 v2 复验结论。 | —（转设计师定夺） | ⏳待设计师 |
| 2026-08-30 | 审验员 | **UPG-41 v2 enable 不可达时 store 先写 true 但 toast 操作失败（状态不一致）**：模拟器复验——MockServer（URL https://mow.kim/mock/mcp 不可达）启停：disable 成功落库 false；enable 时 `McpMarket.setEnabled` **先 `store.setEnabled(true)` 再 discoverServer**，discover 抛 `MARKET_SERVER_UNREACHABLE` 后 catch 返回 ok=false **不回滚 store** → 实测 store 已 `enabled:true` 但用户 toast「操作失败: MARKET_SERVER_UNREACHABLE」。影响：用户以为启用失败，实际已启用（状态机与用户感知背离），且下次刷新 UI 显示启用态。建议：enable discover 失败时回滚 store（setEnabled(false)）或返回 ok=true+reachable=false 口径对齐。非本单打回项范围，不阻塞 UPG-41 v2 复验结论。 | —（转设计师定夺） | ⏳待设计师 |
| 2026-08-30 | 审验员 | **UPG-07 批2 证据区缺 L1/变异独立落盘产物（建议补证，不阻塞结论）**：审验 UPG-07 批2 大白话弹窗修复（feat/upg07-b2 e7104e3）——证据链核验全过（源码 setItems 0/ContractTest 6-0/全量 57 类 424-0-0(+1跳过) 干净重跑确认/变异 2-2 亲杀独立复跑/L2 journal 现场拉真机 21770d7d 原始 session.jsonl 逐条核对 seq 7655/7748/7749/7359 真实），但**证据区 `UPG07批2R1\` 仅复验记录 + 节点树 XML，无 L1 XML/变异 FAILED 输出独立落盘**——L2 journal 我可现场拉原始 jsonl 复核（设备上仍在），但 L1/变异产物缺可复核留档（同 UPG-27 问题②同款）。建议：补落盘 L1 XML + 变异 FAILED 输出到证据区（供 P5 并后抽审复核）。不影响本单通过结论。 | —（转设计师定夺） | ⏳待补证 |
| 2026-08-30 | 审验员 | **UPG-27 挂账「upg27-sdk工具名点号下划线错位」登记数据与设备实测不符（登记质量审验提示，不阻塞撤回结论）**：审验 UP6-27 L2 撤回判定（验收员撤回「P1 点号直呼塌缩」）时，实物对账发现——**挂账登记所述「点号 12 次塌缩」与设备实测不符**：设备 9 房间 journal 全量统计**点号 tool/call = 0 次**；TOOL_COLLAPSED 事件仅 3 次（seq 8053/12180/13629），**全部为下划线形式（shell_exec）且所属面=native**（native 面正确塌缩语义）；code 面 shell 调用 10 次全部成功、0 塌缩，both 面 3 次成功——**code 面无点号塌缩证据**。「12 次」溯源=验收员初次 grep 计数（含 approval/asked 文本出现），交付报告已自我澄清「12 次多为 native 面正确塌缩」，落档样本=3 个 seq。影响：此数据不实**反而更支持挂账误报注销**——实测点号直呼 0 次，「点号下划线错位致塌缩」登记前提不成立；程序员已补 SchedulerDotNameBehaviorTest（ce5fac9 纯新增 54 行，锚=点号/下划线直呼均不塌缩+native 面塌缩保留）锁定正确行为。建议：登记侧数据口径与实测对齐（挂账注销段标注「点号调用实测 0 次，12 次系初次 grep 误计」），供 P5 并后抽审复核。 | —（转设计师定夺） | ⏳待设计师 |
| 2026-08-30 | 审验员 | **UPG-27 单A 边界契约项存 3 条问题（P1 边界违例 + boundary ②③ 假绿）**：审验单A Memory API 工程契约（feat/ai-model-ui 3bf598f）——L1 全量复跑 core 8/0+api 14/0+app 427/0/0 坐实、变异 M1/M2 亲杀红（均还原），主体功能可信；但**边界契约项独立核验发现 3 条**：**①P1 边界违例**——`MainActivity.kt:582/597/598` 用**全限定名**直引 `com.hermes.mov.memory.core.MemoryStore/MemoryStatus`（`memory-api/build.gradle.kts` 用 `api(project(":memory-core"))` 使 core 对 app 编译面传递可见，故编译通过），boundary ② 检查 `s.contains("com.hermes.mov.memory.core")`——**路径修好后单A 自身必红**：「呈现层零触 core」契约语义（L3 硬门槛）被源码直接违反（Gradle 依赖边界成立、源码边界违约）。建议：MemoryApiService 提供工厂/默认装配入口，MainActivity 不直接构造 core 内部类（或契约显式允许 composition-root 注入但调整 boundary 语义）；**②boundary ③ 假绿（验收员误判「③有效」）**——`MemoryDependencyBoundaryTest.kt:44` `File("memory-core/src/main/kotlin")` 同 ② workingDir=app/ 病根解析成 `app/memory-core/...` 不存在→空扫恒绿；且豁免 `!s.contains("零 Android 依赖")` 排除全部 3 个 core 文件（JsonMini/MemoryEntity/MemoryStore 均含该注释）→**双重失效恒绿**。验收落档「①Gradle ③core 零 Android 两锚有效」判断有误；**③boundary ② 假绿（验收已报 P3，复核属实）**——`File("app/src/main")` 解析成 `app/app/src/main` 不存在→恒绿，M3 变异复现仍绿+app/ 下路径实证坐实。**L3 硬门槛红线测试 ②③ 双双失效**，边界契约核心交付点实际未验证且 ① 违例。建议：修 boundary 测试路径（`src/main` 相对 workingDir）+ 处理 ① 装配违例后复验该锚。不阻塞单A 状态机/持久化/Envelope 主体功能，但**边界契约项判定=未通过**，处理后再合 main。 | —（转设计师定夺） | ✅销项 @2026-08-30（审验员复核 feat/ai-model-ui 0aa0c07 修复全坐实——①P1 违例清除：`MemoryApiService.create(baseDir)` 门面工厂装配移入 api 包 + SeedEntry.status 净化（core 枚举→API 术语字符串+内部 when 转换），grep `com.hermes.mov.memory.core` app/src/main **零命中**，原 :582/597/598 直引点全清；②boundary ②③ 真绿化：路径双探测（../app/src/main|src/main）+**基准不存在即断言失败**（杜绝空目录假绿）+豁免收窄仅注释行；③L1 复跑 468/0/0（core 8/0+api 14/0+app 62 类 446/0/0+1 跳过+boundary 3/0）+变异 3 态亲杀（①直引 core→②红 ②core 注释外 android→③红 ③注释行→豁免不红）全部还原；落档 2e0c874 与实测一致。详情见审验状态表 UPG-27 单A 行） |
| 2026-08-30 | 审验员 | **UPG-47 挂账复核「main 基线编译红」确认属实（阻塞 capsule rebase 合 main）**：审验 UPG-47（feat/capsule-system）时按挂账复核——`origin/main` 4bfddd8 `:app:compileDebugKotlin` **FAILED**：MainActivity.kt:**6707 行 `<<<<<<< HEAD` 冲突标记残留**（UPG-18 merge 未清净）+ **Unresolved references**（MarkstreamView / destroyWebViewsIn / launchCamera，6103bb8 尾部冲突解决不完整）——与验收员 L2 复核 P0 挂账结论一致。UPG-47 停靠 0aa0c07 绿基线合理。建议：设计师清理冲突标记 + 尸体引用修复主基线后，capsule rebase 合入。非本单代码缺陷（UPG-18 引入），不阻塞本单审验通过结论。 | ✅设计师修复 @2026-08-31：`4f25633`（main，单行 diff）——①MainActivity.kt:6705 删 `<<<<<<< HEAD` 孤立冲突标记 ②:615 run{ 块补缺失 `}`（UPG-46 merge 残留 memoryApi run{} 右括号）；**审验员独立复核坐实 @2026-08-31**——git grep `<<<<<<< HEAD` origin/main 零命中 + 4f25633 diff 逐行核对两处修复到位 + **main HEAD 56a0e92 `:app:compileDebugKotlin --rerun-tasks` 强制重编译 BUILD SUCCESSFUL（1m12s，20 tasks）**——主基线编译红真销。capsule/upg51/upg52 现均可基于新 main rebase | ✅已销（审验员复核编译绿坐实） |
| 2026-08-31 | 审验员 | **UPG-53 场景7 记住偏好对 vault.get 明文读取「伪放行」（审计免弹但业务拒绝，UI 承诺与行为不符）——P1 挂账建议**：L2 模拟器实测坐实（emulator-5554，全程未动真机）——`vault.get` handler（MainActivity:3546-3594）**强制每次弹窗且只认 `allowed-once`**（:3568 `if (outcome != OUTCOME_ALLOWED_ONCE)` 否则返回「用户拒绝」），但 `permissionTier(vault.get)`=free（guard 不拦）→ 弹窗「记住此偏好」勾选可显示可勾（MainActivity:6616-6617 canRemember=free≠gate=true）→ **实测闭环**：勾选记住+同意 → `mov_security.xml remembered_tools=[vault.get]` 落盘 ✓ → 下次 vault.get **豁免链命中 allowed-remembered**（session.jsonl seq10/11「已记住同类偏好」审计）→ 但 handler 判 ≠allowed-once → **curl 实测返回 `{ok=false, error=用户拒绝}`**。影响：用户勾选「记住此偏好，以后这类操作不再询问」后，vault.get 明文读取变成**「免询问但拒绝」**——与弹窗承诺直接矛盾；且**审计显示 allowed-remembered（看似放行）实际业务拒绝**——审计与行为不一致（与挂账 P1「dispatch 白名单缺 allowed-goal」同类：豁免审计与执行断裂）。同类受影响（代码推断同构）：turn 豁免（这次对话同类同意）/goal 豁免对 vault.get 同样伪放行；browser 支付/登录审批（browser.click/fillForm/login 只认 allowed-once，:3123/:3198/:3224）同病。**安全方向正确**（明文未泄露，handler fail-closed），但功能承诺失效。根因：handler 层强制「每弹 only-allowed-once」的工具，UI 层豁免勾选未按此禁用。建议：①vault.get handler 兼容 allowed-remembered/turn 语义（或豁免命中时审计+行为对齐）②弹窗对该类工具隐藏/禁用 turn/goal/记住勾选③补测试锚。不阻塞 UPG-53 审验通过结论（安全方向正确、JVM 锚全绿），登记挂账待设计师定夺。 | —（转设计师定夺） | ⏳待设计师 |

| 2026-08-31 | 用户实测+设计师溯源 | **UPG-62 候选：多轮对话后输入框失焦（键盘弹起但不能输入，需再点输入框）**：现象=多轮对话后键盘弹起但输入框无光标不可输入，需再点一次。溯源（@main f975437）：AI 回复用 markstream WebView 渲染（MainActivity:6158/:6734）——WebView 可聚焦，挂载渲染回复时抢走窗口焦点→原生 EditText input（:1775）失焦，IME 键盘未同步收回保持弹起→「键盘在、焦点不在」；短回复走 markwon 原生不触发（解释「多轮后」）；hideSoftKeyboard 的 clearFocus（:4344）为独立同症状源。建议修法：①markstream WebView isFocusable=false/descendantFocusability=BLOCK（渲染面板无需键盘交互——根治）②回复渲染完成后焦点归还原 input.requestFocus（打字中途回复到达无感）③hideSoftKeyboard clearFocus 保留 | ⏳待设计师 | 已立 UPG-62 |

| 2026-08-31 | 设计师 | **C7BaselineGenerationTest 生成非确定性输出（每次全量重跑 z5-*.jsonl/baseline_summary.md 全部变 M）**：现象=每次 --rerun-tasks 全量后 docs/c7_baseline_UPG63/ 31 文件必变 M（时间戳/顺序差异），干扰 git 干净态判定与分支切换（UPG-58/59/60/66 四轮合单均被挡）。根因=生成器输出含非确定性字段（时间戳/迭代顺序）。建议=生成器注入固定时钟+确定排序（同 EvalStabilityTest 思路），或测试生成到 build/ 目录不入库。不阻塞合单（恢复 HEAD 版即可），属流程摩擦类。 | ⏳待设计师 | 已登（随下批顺手修） |

## [问题-2026-09-01 | UPG-61/62 验收流程缺口] 已合 main 但验收记录零落档
- **发现者**：验收员（2026-09-01 工单表盘点时发现）
- **现象**：UPG-61（vault.get 伪放行修，570c921）、UPG-62（输入框失焦修，82c778b）均已于 2026-08-31 合 main，但 ①`docs/ACCEPTANCE_LOG.md` 两单零记录（grep 计数=0）②工单表验收员列均为「—」——违反红线 1（合 main 前提=验收通过）与红线 2（验收结论落 ACCEPTANCE_LOG）
- **证据**：`grep -c "UPG-61" docs/ACCEPTANCE_LOG.md` = 0、`grep -c "UPG-62" ...` = 0；工单表 UPG-61/61 行验收员列「—」vs 合main列「✅已合 main @2026-08-31」
- **影响**：合 main 的两单无验收判定与证据链可查；审验员红线 17（未审验不视为可信）同样无法对账
- **建议**：设计师确认两单验收口径（UPG-61 备注「P3×4 观察项不阻塞」、UPG-62 为用户授权设计师直修）→ 若认可现状，补登记「追认」记录进 ACCEPTANCE_LOG + 工单表验收员列；若不认可，走补验
- **登记人**：验收员 ｜ **日期**：2026-09-01 ｜ **状态**：⏳待设计师处理

## [问题-2026-09-01 | 交付绑定 manifest_sha 口径] evidence_manifest_sha 不可复现 + 绑定规范文件缺失（UPG-49 R2 交付对账发现）
- **发现者**：验收员（UPG-49 R2 复验红线 23 对账时发现）
- **现象**：①DEL-UPG49-20260901-001 的 evidence_manifest_sha 申报 16004595…，manifest 文件原样 sha256 实测 03b3504a…、JSON 规范化口径亦无法复现申报值——计算口径未定义；②红线 23 所指细则文件 `SYS\交付绑定规范.md` 实际不存在（SYS 目录无此文件），对账无仲裁依据；③artifact_sha（APK）因程序员产物未留档+构建非确定性无法独立复现
- **影响**：红线 23「三重 hash 必须同时匹配」中两重（manifest_sha/artifact_sha）无法机器对账，交付绑定降级为「code_commit_sha+逐条 evidence sha」两级可信
- **证据**：ACCEPTANCE_LOG §P20-R2 §3；manifest 文件 `程序员/交付报告/DELIVERY_UPG49_R2_2026-09-01_manifest.json`（8 条 evidence 逐条 sha 已核全一致）
- **建议**：①补建 `SYS\交付绑定规范.md`（定义 manifest_sha=文件原样 sha256 或规范化口径+artifact 留档要求）②后续交付 manifest 落盘后**不得再改**（改=新 delivery_id）③artifact_sha 附产物路径+产物留档
- **登记人**：验收员 ｜ **日期**：2026-09-01 ｜ **状态**：⏳待设计师处理（P3 治理，不阻塞 UPG-49 结论）

| 2026-09-01 | 审验员 | **UPG-49 无 STD 冻结标准（红线 22 对账缺口 · 存量单过渡）**：审验 UPG-49 R2b（fe04668）时对账 standard_id——DEL-002 manifest `standard_id = "我的记忆_验收方案_v1（SYS-01 冻结区未收录，沿用归档版）"`，**非 `STD-UPG-49-v<n>` 冻结版**；`验收标准冻结区\` 仅有 UPG-55，UPG-49 无冻结区 → 红线 22「验收/审验/合main抽查三处对账 content_sha256」无法对账。注：红线 22 于 2026-09-01（SYS-01 P0-1）当日生效，UPG-49 为存量单未走冻结流程。建议：设计师裁决——UPG-49 补冻结 `STD-UPG-49-v1`（L1/L2/L3+变异锚+测试匹配档）或明确存量单豁免声明；合 main 抽查对账前需处理。不阻塞本单证据链通过结论（功能正确性已实物坐实：像素 72px 高亮 + amber 0 + patch-id 等价）。 | ⏳待设计师 | — |
| 2026-09-01 | 审验员 | **UPG-49 DEL-002 code_commit hash 漂移（红线 23 治理）**：DEL-002 `code_commit_sha=3751a99`，当前 main 上实际为 **fe04668**（rebase 重写 hash）——**patch-id 等价实证**：两 commit 全量 patch-id 均 `34293c8915762f1d7537e4bbb21ef4415eb0f05c`（内容逐字节等价，比验收员申报的「app/ 部分 38243d84」更强）。按红线 23「交付后 code/artifact/evidence 任一变化→标失效+新建 delivery」，严格口径 DEL-002 应标 ⏳失效并新建 DEL-003（绑 fe04668）。验收员已标 P3-4 治理项。建议：设计师定夺——①新建 DEL-003 绑定 fe04668 ②或明确「patch-id 等价豁免」声明（内容未变仅 hash 重写，工单表 delivery_id 列注记双 hash）。不阻塞本单证据链通过（patch-id 等价已实证内容一致）。 | ✅**裁决：patch-id 等价豁免 @2026-09-02（设计师）**——3751a99≈fe04668 全量 patch-id 34293c89 逐字节等价=内容同一（rebase 重写仅 hash 变），**新建 DEL-003 将制造同内容双记录**（更乱）；豁免=工单库/交付报告注记「等效 hash 对」+问题区留案；红线 23 的 hash 漂移根因（rebase 重写未同步=信息滞后的老洞）已随 SYS-02 引子挂账（挂账-sys02-UPG49hash漂移引子） | — |
| 2026-09-01 | 审验员 | **UPG-50 混线分离产物损坏——main 分支 assets.html 含未解决 Git 冲突标记（P1，与 §P21「分离合入无损」声明不符）**：审验 UPG-50 1B（176606d）复核混线分离（51449d3→bb1e6d7 独立合 main）时发现——`main` 上 bb1e6d7 的 `app/src/main/assets/pages/assets/assets.html` **第 7/13/19 行含 `<<<<<<< HEAD` / `=======` / `>>>>>>> 51449d3 (fix...)` 未解决冲突标记**（cherry-pick 冲突未解决即 `git add` 提交）。对照：51449d3 原版 assets.html 干净（引用 assets-Bec3hfLh.js+tokens-BeOAjKr5.js 等），bb1e6d7 实际引用 HEAD 侧资源（assets-Ces3wOit.js+tokens-Ds3IUCZ5.js 等，均在树中）。源文件层（AssetsSheet.kt/SettingsSheet.kt/AssetsPage.vue）两提交**逐字节一致**，分离语义内容等价——**但 HTML 产物带冲突标记**。影响：①浏览器将 head 中冲突文本移至 body 顶部=资产页**乱码文本** ②实际加载 HEAD 侧 js（assets-Ces3wOit.js 与 51449d3 重建 assets-Bec3hfLh.js blob 相同 `863157e3…`，功能影响有限）③51449d3 侧 modulepreload 文件（tokens-BeOAjKr5.js 等）不在 main 树中。与裁决注记「分离合入无损 ✅」**不一致**。建议：设计师修复 main 上 assets.html（清冲突标记+统一引用一侧资源），真机验证「我的资产」页正常后销项；**1B 合 main 终审前需处理**（涉 main 基线健康）。非 1B 代码缺陷（设计师分离操作引入）。 | — | ⏳待设计师 |
| 2026-09-01 | 审验员 | **UPG-50 STD 三文件缺冻结头（红线 22 对账缺口 · 新冻结单非存量豁免）**：审验 UPG-50 对账 `验收标准冻结区\UPG-50\` 三文件——`STD-UPG50-v1_2026-09-02.md`（标题自称 v1.1）+`STD-UPG50_阶段1_增补`+`STD-UPG50_阶段2_增补` **均无冻结头**（standard_id/content_sha256/frozen_at/frozen_by/approved_by），红线 22「验收/审验/合main抽查三处对账 content_sha256」无法执行。UPG-50 为红线 22（2026-09-01 SYS-01 P0-1）**生效后冻结的新单**（不同于 UPG-49 存量单豁免）——STD 文件存在但未按冻结区 README §二 落冻结头；阶段1/阶段2 增补为独立文件而非 `STD-<单>-v<n+1>` 派生版。建议：设计师按冻结区 README 补冻结头（含 content_sha256）或明确豁免声明；1B 合 main 抽查对账前需处理。不阻塞本单证据链通过结论（判据内容已实物坐实：L2-9/L2-10 像素复核+manifest 一致+文件面一致）。 | — | ⏳待设计师 |
| 2026-09-02 | 设计师 | **问题A 销项**：main 资产页冲突标记已修复（e988ad6——assets.html 冲突清除+资产目录整体取 51449d3 版/HEAD 旧 hash 删除）；审验员对应问题可闭环 |
