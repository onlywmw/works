# 建议：用 Archify 给 MOV 生成交互式系统架构图（缓办·用户叫停待重启）

> 登记：设计师B @2026-09-03 ｜ 状态：✅ 已完成 @2026-09-03（用户提供本地 zip 绕开网络；产物=设计预览\MOV_运行时架构图_2026-09-03.html，showcase 校验 9 检 0 错 0 警 + deliver 双 sha256 在档）

## 是什么

Archify（[github.com/tt-a1i/archify](https://github.com/tt-a1i/archify)，MIT）：把代码库/系统描述变成可交互系统图的 agent skill——typed JSON IR → 确定性编译成自包含 HTML/SVG/PNG，五种图型（架构/工作流/时序/数据流/生命周期）、深/浅主题、节点溯源（可挂 Git 验证的源码行）、路线追踪、Before/Delta/After 架构对比（适合评审用）。

## 原定用途

给 0027-mov 出一张高层运行时架构图（agentloop/工具调度/审批面/exec-engine/记忆/市场 + DeepSeek 云/MCP/信任边界），挂在 README 或设计网络入口——审批体系刚收口，正是出总图的好时点。

## 重启步骤（当时已走到第 3 步）

1. 装 skill：`npx skills add tt-a1i/archify -g`（本机 npx 缺失——用 bun x 或 git clone 后把 `archify/` 子目录拷进 `~/.agents/skills/archify`）
2. 读契约：`archify/SKILL.md`（JSON IR 规范 + validate/deliver 命令）
3. 作者化 JSON IR（MOV 8-12 核心组件+主路径+外部依赖+信任边界）
4. `node archify/bin/archify.mjs validate architecture <json> --quality showcase --json` → `deliver ... --open`
5. 产物落 `设计师\设计预览\`（HTML 单文件，可直接双击）

## 当时卡点

网络克隆慢被叫停（非技术阻塞）。
