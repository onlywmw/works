# STD-UPG-100-v1 验收标准冻结版

> 工单：UPG-100 ｜ 标题：锚②半弱锚升级（count/match 组合断言）｜ 本文件 = 验收「什么是合格」的**唯一正式冻结版**

---

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-100-v1`
- **content_sha256**: `96c6bce10e0cdbfe1bcf31e5eda7989222d40e8e26bcd6711c11f8f417685daa`（= 冻结区正文哈希，非整文件哈希；按文末命令计算）
- **frozen_at**: `2026-09-05T10:15:00`
- **frozen_by**: 设计师
- **approved_by**: 审验员（追认会签待补——本单为交付先行后补冻，见追加说明区）

---

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L1 低 | 纯测试锚升级（零业务改动）→ diff 精读 + 变异亲杀 + 全量回归 | 锚②升级为计数/行为级断言后：单点破坏必红、双点破坏必红、全删必红、原状绿 |

### 变异锚

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| A1-1 单点短路锚 | 恢复原审验变异（chunk 转发单处短路） | 升级版锚②**必红**（亲杀——原缺陷=单点短路仍绿） |
| A1-2 多点锚 | 双处短路 / 全删 | 均必红；原状态绿 |
| flaky 防护锚 | 升级后连跑 ≥3 遍 | 全等（红线：不得引入 flaky） |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 定向用例 | HomeDeliveryContractTest 全绿（含升级版锚②），XML 计数>0 |
| 全量回归 | `testDebugUnitTest --rerun-tasks` 0 失败（基线 843±1 @31640fc8 口径注记） |
| 零业务改动 | diff 仅测试文件/文档（+业务文件零改动） |
| A2 方法论入库 | 测试锚强度规范落档（知识库/规范文档引用可查） |
| A3 同族排查 | contains 组合锚抽检结论表（4~8 处：锚/强度/是否升级） |

### 销项条件（下列全满足）

1. A1-1/A1-2 变异亲杀全红→还原复绿 + 连跑 3 遍全等（零 flaky）
2. 全量 0 失败 + 零业务改动实证
3. A2 规范落档 + A3 排查结论表在案
4. 交付绑定 DEL-UPG-100-*（manifest 硬闸产出）+两表登记

---

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-05 | 设计师 | STD 补冻追认 | 本单按卡面判据直接施工（用户指令直接领单），STD 未随派冻结——程序员用卡面段实测 sha 2f3e20ea 临时代入 manifest 并如实申报；本 v1=卡面 A1-A3+验收锚原样固化追认 |

---

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-100-v1.md"
```
