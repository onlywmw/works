# STD-UPG-69-v1 验收标准冻结版

> 工单：UPG-69 ｜ 标题：WebMCP 站点试点（mow.kim 工单站「站点=业务工具」第一站）｜ 本文件 = 验收「什么是合格」的**唯一正式冻结版**

---

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-69-v1`
- **content_sha256**: `be6a06a06c77ecd0b8e26932c802b5981df7d41b85d27708b74ce1d764088f13`（= 冻结区正文哈希，非整文件哈希；按文末命令计算）
- **frozen_at**: `2026-09-05T10:20:00`
- **frozen_by**: 设计师
- **approved_by**: 审验员（追认会签待补——交付先行后补冻，见追加说明区）

---

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L2 中 | 站点+App 行为实测 | W1 登记表 3 工具齐全 / W2 AI 工具面出现 web.mow.kim.* 命名空间 / W3 只读调用返回真实数据且卡片投影可见 |
| L3 高 | 安全闸 diff 精读+变异 | W4 写类过审批闸（无审批上下文=ASK/拒绝，不静默放行）/ W6 跨站隔离（非 mow.kim 页零注入） |

### 变异锚

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| W4 闸锚 | 移除/绕过 mov_openTicket 审批闸 | 「写工具必过闸」用例必红 |
| W6 隔离锚 | 注入泄漏到非注册域 | 「跨站零注入」用例必红 |
| W5 契约锚 | 站点登记与 App 侧解析不同步（删一工具） | 机器对账必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| W1 | 页面内 window.mov_webApi 登记表 3 工具齐全（schema 完整） |
| W2 | App 内浏览器打开站点 → AI 工具面出现 web.mow.kim.mov_openTicket/mov_queryTicket/mov_listOrders |
| W3 | AI 调 mov_queryTicket（只读）→ 真实数据回程 + 卡片投影可见（UPG-40 视觉纪律）；端到端受限可转持有（ConnectWeb 通道受限如实申报+替代验证路径在案） |
| W5 | 命名/契约与 UPG-43a 同版本基准（WEBMCP_PROTOCOL v0.1），站点登记↔App 解析机器对账一致 |
| 定向用例 | WebMcpHubTest W4/W5 对账锚全绿，计数申报 |
| 全量回归 | 0 失败基线 |
| 证据链 | 站点实证（curl/截图）+ App 侧调用链 journal + 变异亲杀记录 |

### 销项条件（下列全满足）

1. W1-W6 逐条实证（端到端受限项按转持有口径如实申报）
2. 变异 3 锚亲杀全红→还原复绿
3. 写类过闸+跨站隔离 diff 精读通过
4. DEL 绑定闭环（STD 冻结后 deliver-gen 硬闸产出）+两表登记

---

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-05 | 设计师 | STD 补冻 | 本单由用户直接外派施工，STD 未随派冻结；程序员 deliver-gen 硬闸拒绑并申报「判据 W1-W6 为设计师定稿建议固化」——本 v1=卡面 W1-W6 原样固化 |
| 2026-09-05 | 设计师 | 背景注记 | 同日官网双层设计拍板后本单曾挂起改向，用户随后直接外派维持原口径——验收以本 STD 为准，挂起注记仅历史 |

---

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-69-v1.md"
```
