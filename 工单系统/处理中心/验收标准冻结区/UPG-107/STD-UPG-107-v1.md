# STD-UPG-107-v1 验收标准冻结版

> 工单：UPG-107 ｜ 标题：Memory OS 类型化 payload（Semantic 池子结构化演进）｜ 本文件 = 验收「什么是合格」的**唯一正式冻结版**
> 验收执行细则：`设计师\方案设计\04_记忆\Memory_OS_类型化payload_验收脚本_v1_2026-09-05.md`（八步走查，与本 STD 一一对应）

---

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-107-v1`
- **content_sha256**: `235736101497215325714ded45bab6e6f797aa14865b4467d31f672ddedf7c30`（= 冻结区正文哈希，非整文件哈希；按文末命令计算）
- **frozen_at**: `2026-09-05T07:30:00`
- **frozen_by**: 设计师
- **approved_by**: 审验员 ✅ 会签 @2026-09-05（证据链审验通过——STD 重算一致 23573610/DEL-UPG107-20260905-001 manifest ok:True/变异 3 锚链+落盘铁证核物；UPG-103/UPG-46 先例口径）

> ⚠️ 冻结区填完、content_sha256 回填后，**冻结区任何改动都视为标准变更**，只能走修订派生 v2，不得原地改。

---

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L1 低 | 结构面+单测 | SemanticEntry payload 字段实物+六类模板注册表+抽取/校验/降级用例全绿（计数>0 防假绿） |
| L2 中 | 真机行为实测 | 5 场景：同义去重出合并候选/冲突检出 RE_EVALUATE/画像对齐双页一致/降级条目可视/用户主权（可见可删+Timeline 有痕） |
| L3 高 | 契约面 | memory-api 零改/Semantic 只读/Timeline append-only/原子写入一致；UPG-52 既有 27 用例+打点链零退化 |

### 变异锚（3 锚亲杀，逐个「改→必红→还原→复绿」）

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| M1 权限锚 | 抽取结果默认状态 PROPOSED 改成 ACTIVE | 「AI 草案禁直写 ACTIVE」用例必红 |
| M2 降级锚 | 抽取失败路径改为抛异常 | 「抽取失败降级纯文本入库」用例必红 |
| M3 冲突锚 | 删字段级冲突比对（退回 type 层互斥） | 「同场景异取向必检出」用例必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 基线 | 施工前记录 memory-os/app 全量计数；基线红=停 |
| 定向用例 | 抽取/校验/降级/去重/冲突用例全绿，**XML 计数>0**（已知坑 #6） |
| 全量回归 | `:memory-os:test` + `:app:testDebugUnitTest` 0 失败；新增计数申报 |
| 构建 | `:app:assembleDebug` 绿；提交版=验证版 |
| 证据链 | grep 实物输出 + XML + 真机截图/录屏（瞬态 UI 用录屏法，已知坑 #8）+ logcat 事件链 |
| 申报 | Token/KV 两节（payload 抽取走 LLM 通道，token 增量必报）+《共享面影响清单》coverage_status（SemanticEntry=全局数据结构，红线 24） |

### 销项条件（本单「合格」= 下列全满足）

- [ ] 验收脚本第 0-7 步全走通，每步证据在档
- [ ] 变异 3 锚亲杀全红→还原复绿
- [ ] 真机 5 场景实证（截图/录屏）
- [ ] 红线全守：payload 永不直写 ACTIVE/校验失败=降级不报错/模板只加不改/五条 P0 不破
- [ ] 交付绑定 DEL-UPG-107-*（deliver-gen 机制产出+verify-hash HASH_OK）+两表登记（先表后库）

### 一票否决（任一命中=不通过）

1. 基线红继续施工 2. 变异锚没亲杀 3. 0 测试假绿未察觉 4. payload 绕过 PROPOSED 直写 ACTIVE 5. 抽取失败致条目丢失/阻塞 6. UPG-52 既有用例退化

---

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

> 追加格式：`| 时间 | 作者 | 触发原因 | 说明 |`。追加只入本区，**不得回填进冻结区**。

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-05 | 设计师 | 立卡冻结 | 依据设计 v1（v2 冻结补丁）+验收脚本 v1；知识库命中坑#6/#8 已写入脚本 |

---

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-107-v1.md"
```

> 校验：验收 / 审验 / 合 main 抽查时重跑同命令比对；不一致 = 标准被改，阻断并走修订。
