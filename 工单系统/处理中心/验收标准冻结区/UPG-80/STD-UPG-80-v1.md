# STD-UPG-80-v1 验收标准冻结版

> 工单：UPG-80 ｜ 标题：AI 对话链路诊断修复（模拟器 DeepSeek 链路）+ 联动补验 ｜ 本文件 = 验收「什么是合格」的**唯一正式冻结版**

---

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-80-v1`
- **content_sha256**: `d666e636a0c32eee9ab39d17f828fcce41be691644145e38ed18ad63b223cd96`
- **frozen_at**: `2026-09-03T06:20:00`
- **frozen_by**: 设计师B
- **approved_by**: 审验员（派单后确认，验收启动前补齐）

> ⚠️ 冻结区任何改动都视为标准变更，只能走修订派生 v2，不得原地改。

---

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L3 高 | 真机端到端 + 凭据安全面 → 诊断实证 + 真机多场景 | ① 诊断结论有 logcat/命令证据（key 注入/配置加载/网络可达/HTTP 状态，不许猜）；② 修复后真机 AI 对话成功往返（截图+logcat 含时间戳）；③ 联动补验：UPG-76 L3 四场景按 STD-UPG-76-v2 执行通过；④ 凭据脱敏：`git grep sk-` 零命中、证据无 key 明文 |

### 亲杀锚

| 锚点 | 动作 | 期望 |
|---|---|---|
| 失败如实 | 注入错误 key/断网 → 发起对话 | 错误如实呈现（可诊断的错误信息/日志），**不误报成功、不静默挂死** |
| 修复实证 | 修复后真机对话 | AI 回复真实到达（非模拟/非缓存——新会话新问题，logcat 可见请求发出+响应到达） |
| 脱敏闸 | 全证据链扫描 | `git grep sk-` 零命中；截图/logcat 摘录无 key 明文（打码实证） |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 真机 L3 | ① 诊断记录（logcat 关键行+结论）；② 新会话对话往返（截图含时间戳）；③ UPG-76 L3 四场景全过（多步出单/部分批准/MONEY 实时/过期转新窗——按 STD-UPG-76-v2 测试匹配档） |
| 全量回归 | 若涉代码改动：`testDebugUnitTest` 绿（计数如实）；纯配置/环境修复则声明「零代码改动」 |
| 证据链 | logcat + 截图 + 命令输出 四环节完整；key 相关帧一律打码 |

### 销项条件（下列全满足）

- [ ] 诊断结论落交付报告（根因+证据行），非「换 key 就好了」的裸结论
- [ ] 真机 AI 对话往返实证（新会话新问题，截图+logcat）
- [ ] 挂账-upg76-L3真机补验四场景 销项（四场景证据齐）
- [ ] 挂账-模拟器AI未回复 销项
- [ ] `git grep sk-` 零命中 + 证据脱敏自查声明
- [ ] 交付报告含 Token/KV Cache 两节（若动请求链路必须详报；纯配置写 0/0）

---

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-03 | 设计师B | 冻结依据 | 挂账-模拟器AI未回复（P1，2026-08-26 起验证环境根）+ 挂账-upg76-L3真机补验四场景（合 main 联动裁决，同环境根）；运营口令查 docs/ACCEPTANCE_LOG.md（AGENTS.md 规则 5），key 绝不落盘（红线 4） |

---

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-80-v1.md"
```
