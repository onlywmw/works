# STD-UPG55-v1 验收标准冻结版

> 工单：UPG-55 ｜ 标题：我的资产（资产管理项注册体系 · 67-A 框架+迁移批） ｜ 本文件 = 验收「什么是合格」的**唯一正式冻结版**
> 依据：设计 v3 收敛版（唯一施工口径）+ 验收方案 v1（07_资产/我的资产_验收方案_v1_2026-09-01.md）——本冻结版为其固化形态。

---

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG55-v1`
- **content_sha256**: `75aec218e6c47c8e794b7df05f67e84ca9bd40ed290319e45781b35b65c4dcc6`
- **frozen_at**: `2026-09-01`
- **frozen_by**: 设计师
- **approved_by**: 审验员

> ⚠️ 冻结区填完、content_sha256 回填后，冻结区任何改动都视为标准变更，只能走修订派生 v2，不得原地改。

---

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L1 中 | 契约/纯函数（AssetRegistry/迁移器/各守卫）→ JVM 测试 + 变异亲杀 | J-1~J-9 判据 + M55-1~5 变异锚 |
| L2 高 | 真机行为（迁移无感/资产首页/凭据页）→ 真机实测 | R55-1~7 判据 |
| L3 高 | 凭据安全（only-once/明文不出/egress guard）+ 迁移数据完整性 | J-3/J-6/J-8 重点 + 真机迁移实证 |

### L1 判据（机器验收）

| 编号 | 判据 | 通过标准 |
|---|---|---|
| J-1 | 注册制不破坏既有 | 注册新 kind → 既有三类零变化（变异：改通用模型 → 红） |
| J-2 | 每类规则独立 | 三类 schema 行为测试互不影响 |
| J-3 | 迁移三等式 + declaredCount | COUNT/ID_SET/HASH 三等式全过；**ID_SET/HASH 为逐条内容对账（非基数比较）**——P2 打回项，验收时按修复后口径核 |
| J-4 | 迁移幂等+中断续跑 | 连跑两次一致；kill 重跑补完不重复 |
| J-5 | MCP 写门控 | wardrobe 可写；credential 拒写（AI/MCP 双路径=拒绝+审计） |
| J-6 | 凭据明文不出 | 全路径 grep 零明文（审批放行返回体除外） |
| J-7 | 注册即用 | 注册新 kind → 首页卡片出现 |
| J-8 | egress guard | 资产数据外发路径被拦+审计 |
| J-9 | 兼容期老入口只读 | 老「我的信息」入口写入=被拒+引导 |

### 变异锚

| 编号 | 变异 | 预期红 |
|---|---|---|
| M55-1 | 迁移器丢条目（declaredCount 计数泄漏） | J-3 红 |
| M55-2 | 迁移器重复迁 | J-4 幂等红 |
| M55-3 | Registry 治理失效（删 require(audit)） | J-1 红（验收已独立复验 ✅） |
| M55-4 | egress guard 移除 | J-8 红 |
| M55-5 | 兼容期老入口可写 | J-9 红 |

### L2/L3 真机判据（R55-1~7）

| 编号 | 判据 | 预期 |
|---|---|---|
| R55-1 | 资产首页 | 类目卡片实时渲染+凭据卡首位+灰显诚实 |
| R55-2 | 凭据类目页 | 旧数据全在+平台分组+明文 only-once |
| R55-3 | 迁移无感（**覆盖安装→老数据全在**） | 用户数据零丢失真机实证 |
| R55-4 | 证件照子页签 | 子页签在+「同等安全等级」小字+查看 |
| R55-5 | 到期提醒 | 临期条目首页 ⏰（实时计算） |
| R55-6 | 老入口只读 | 只读+引导 |
| R55-7 | 多轮对话回归（UPG-62） | 焦点不回退 |

### 追加说明区

- **P2/P3 挂账（验收打回时登记）**：P2 verify() 三等式内容对账未实现（基数比较=名不副实）｜P3 asset.credentials 假脱敏｜P3 asset.peekPhoto 桥未注册——**随 R1 修复后本冻结区不变**（修复后重验按原判据）
- **L2 阻塞**（打回时）：真机断连/5554 损坏——R55-1~7 待真机恢复后补验
- **合规补齐项**（打回时登记）：能力护栏裁决（共享面=MainActivity 注册表）/STD 冻结（本文件）/delivery_id 绑定——本次冻结已补
