# STD-UPG-102-v1 验收标准冻结版

> 工单：UPG-102 ｜ 标题：MainActivity 拆分·批③ 页面桥面搬移（→ pages/ 模块）｜ 唯一正式冻结版

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-102-v1`
- **content_sha256**: `3eaa908be80ea7a8647bfafea079841bdcefd3371773fc836c130e788a773c5b`
- **frozen_at**: `2026-09-04T19:40:00`
- **frozen_by**: 设计师B
- **approved_by**: 审验员（派单后确认，验收启动前补齐）

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L3 高 | 上帝文件拆分批③ → diff 精读 + 变异亲杀 + 保真对账 + 真机冒烟 | ① 页面桥面（browserHandlers 注册块 :2332-2515 + WebMcpHub.mountCallback 块 :2517-2557，行号=main@d11509cc 重核）搬入 pages/ 模块，MainActivity 壳内只留装配点；② 纯搬移零逻辑改动（块归一化 sha256 前后全等）；③ 军规 7/8 合规（零新增壳内代码/pages/ 零 Activity 长期持有）；④ 搬出函数一律显式 internal（批② 3 函数漏写 internal 事故不再犯）；⑤ 锚定面迁移收口=全量 0 失败硬门槛（批②教训） |

### 变异锚（L3 必填）

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| 保真锚 | 搬移块内改一行逻辑 | 「块归一化 sha256 前后全等」断言必红 |
| 名单锚 | 多挂一个伪页面桥函数（或漏搬一个） | 「页面桥面名单完备」契约锚必红 |
| 唯一写点锚 | MainActivity 壳内残留页面桥面第二实现（伪搬移=复制不删） | 「壳内零残留/唯一写点」锚必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 定向用例 | PagesSplitContractTest 拆分契约锚（名单/保真/唯一写点/直呼接线/军规8——照批② MarketSplitContractTest 模式）+ 页面桥面直呼/功能契约（拆前绿→拆后绿） |
| 全量回归 | `testDebugUnitTest --rerun-tasks` 绿（0 失败基线，/1 口径）；UPG-93/98 拆分锚零回归；锚定面迁移逐条注记 |
| 构建 | `:app:assembleDebug` 绿；**提交版=验证版**（全新检出复跑——批② §P51b 教训） |
| 证据链 | 保真锚全等清单（hash 列表）+ internal 提升清单 + 锚迁移清单 + logcat/截图（含时间戳）+ journal 四环节 |
| 真机 L3 | ①侧边栏打开正常（PagesBridge 白名单链路）②browser.* 直呼一条 ③web.* 页面挂载（开网页页后 web.* 入面）——logcat 留证 |
| 冷启动锚 | 拆前后各 5 次采样取中位，Δ>10% 标红须解释 |

### 销项条件（下列全满足）

- [ ] 页面桥两块搬入 pages/ 模块；MainActivity 壳内只留装配点（行数下降实测：5929→预期值申报）
- [ ] 保真锚：搬移块归一化 sha256 前后全等清单在档（冻结值=拆前真值，可独立抽查）
- [ ] 军规 7/8 合规：零新增壳内代码（装配点+豁免注记除外）；pages/ 模块零 Activity 长期持有；搬出函数全显式 internal
- [ ] 3 变异锚亲杀红→还原复绿；全量绿+assembleDebug 绿；冷启动锚在容忍带
- [ ] 真机三场景冒烟通过；UPG-93/98 拆分锚零回归；锚定面迁移收口（全量 0 失败）
- [ ] 债务清单联动（asset.* 转调 vault.* 耦合等批③相关条目只记录不修）
- [ ] Token/KV 两节申报

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-04 | 设计师B | 冻结依据 | 拆分串行纪律：一批一派、合后再基；批③=页面桥面（蓝图 §二「页面桥」原 :2906 搬移前锚，本单行号已按 main 顶 d11509cc 重核：browserHandlers 块 :2332-2515 / mountCallback 块 :2517-2557）；形态/锚法照抄批①②（UPG-93/98 已合先例）；批②两条教训入红线：提交版=验证版（§P51b）、搬出函数显式 internal（审验 3 函数漏写事故）；批④（chips/胶囊）待本批合后派 |

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-102-v1.md"
```
