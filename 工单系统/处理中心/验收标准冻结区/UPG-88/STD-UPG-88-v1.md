# STD-UPG-88-v1 验收标准冻结版

> 工单：UPG-88 ｜ 标题：极简批阶段 1——ASR 语音输入 + 极简主页生产化（引擎第一模块）+ 两态开关点亮 + 快速提示词 ｜ 唯一正式冻结版

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-88-v1`
- **content_sha256**: `f64ec0e288ecbe7ae14abaea2d325dc7036b5d89ccaf121e77ef782892575bec`
- **frozen_at**: `2026-09-03T10:05:00`
- **frozen_by**: 设计师B
- **approved_by**: 审验员（派单后确认，验收启动前补齐）

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L3 高 | 新主页/语音/模式切换 → diff 精读 + 变异亲杀 + 真机多场景 | ① 极简主页生产化：HeroVisual（默认 MOV 竖眼 logo）+ 双通道输入（点 logo=直接说 / 输入框打字）+ 结果呈现走现有 Markstream 链（schema2ui 归阶段 2 不做）；② ASR 接入（Android SpeechRecognizer）：权限/听写/回填/错误诚实空态；③ 两态开关点亮：顶部按钮「经典⇄极简」真实切换（UPG-84 占位转正），选择持久化重启保持；④ 极简=快速档（reasoning off + 简洁提示词节，UPG-84 A5 绑定落地）、经典=深度思考不变；⑤ 能力零缩减（工具面/审批/记忆全量一致） |

### 变异锚（L3 必填）

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| 模式切换 | 两态开关实际不切主页（或切了不持久化） | 「切换→极简主页呈现+重启保持」测试/真机锚必红 |
| ASR 回填 | 听写结果不回填输入框（假听写） | 「partial/final 结果→输入框真实文本」断言必红 |
| 档位绑定 | 极简模式 reasoning 未关（或经典被误关） | 「极简=off / 经典=high」装配断言必红 |
| 能力零缩减 | 极简模式工具面收缩 | 「两模式工具面=同一全量集」断言必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 定向用例 | 模式切换/持久化 + ASR 回填（可单测面）+ 档位绑定装配断言 + 工具面一致断言 |
| 全量回归 | `testDebugUnitTest --rerun-tasks` 绿（0 失败基线）；审批系/UPG-84 收敛套件零回归 |
| 构建 | `:app:assembleDebug` 绿 |
| 证据链 | 真机截图/录屏要点（含时间戳）+ logcat + 权限流截图 四环节 |
| 真机 L3 | ① 点顶部按钮 → 极简主页（HeroVisual+双通道）→ 切回经典（重启保持）；② 点 logo → 录音权限流（首次）→ 说话「我要吃辣椒炒肉」→ 转写回填 → 发送 → 结果呈现；③ 极简下对话请求 reasoning off 实证（日志/装配锚）；④ ASR 拒绝权限/无识别 → 诚实空态不卡死 |

### 销项条件（下列全满足）

- [ ] 极简主页=引擎第一模块（HeroVisual 可注入抽象，默认 MOV 竖眼 logo；点 logo=直接说）；主页无菜单/胶囊/抽屉
- [ ] ASR 接入：权限流/听写/partial 回填/final 发送/错误诚实空态（状态矩阵照送审稿 §3.6）
- [ ] 两态开关点亮（经典⇄极简真实切换+持久化；UPG-84 占位转正）
- [ ] 档位绑定落地：极简=快速（reasoning off+简洁提示词节）、经典=深度思考（high）——装配锚+实证
- [ ] 能力零缩减：两模式工具面同一全量集（断言）；审批/记忆/市场能力一致
- [ ] 四个变异锚亲杀全红、还原复绿
- [ ] Token/KV 两节申报（简洁提示词节新增+模式绑定——量级+前缀稳定性说明）
- [ ] 结果页本阶段走现有 Markstream 呈现链（schema2ui/商家卡归阶段 2，不在本单）

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-03 | 设计师B | 冻结依据 | 设计文=`设计师\方案设计\01_外观\极简模式_对话优先主页_设计_送审_2026-09-02.md`（送审稿；决策点以用户拍板为准：进入=顶部两态开关/ HeroVisual 可注入默认竖眼/点 logo=直接说/付款键占位）+ demo `设计师\设计预览\极简模式_demo_v1_2026-09-02.html`；用户红线：「极简模式只是画面的极简，不能阉割能力」；依赖 UPG-84 A5 档位绑定（已合 main 8647ee9） |

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-88-v1.md"
```
