> **STD 冻结头**（2026-09-02 补 · 红线 22）：standard_id= ｜ content_sha256= ｜ 冻结人=设计师 ｜ 冻结日=2026-09-02
# STD-UPG50 阶段 2 验收判据增补（2026-09-02）

> **配套**：`01_外观\UI_Component_Registration_Contract_v2.1_2026-09-02.md`（注册契约）+ `UI_Component_Runtime_契约_v5.0`（Runtime 九字段）+ `外观UI组件体系_合并发送版`（体系）+ 大神 v4.1 裁决（四件钉死[Contract/Capability/State Ownership/Override Resolution]+六建议全采纳+**Registry 不再扩**）。
> **阶段 1（全量铺开）→ 阶段 2（注册制开放）**：官方组件形态已验证 → 本阶段打开「组件注册制」——**MCP/第三方组件可注册可替换**（不破注册制不可绕过的安全边界）。

## 一、前置修复（P2-1 · 阶段 1 挂账）

- P2-1: classic vs capsule 视觉可辨——composer 容器高约 115px 圆角钳制（classic 24dp 圆角=72px>半高 57.5px 被 clamp 全圆）
- 修法：composer 增高 或 classic 圆角调小至 ≤57.5px（或 RoundRect 钳制修正）
- 判据：三形态视觉可辨（default/capsule 截图肉眼可区分；不得再依赖 DOM 属性断言）

## 二、阶段 2 判据（引用大神 V4-1~11 + 注册制）

| # | 判据 |
|---|---|
| L1-15 | **ComponentRegistry 注册/发现/卸载**：注册即选择页出现（来源分区=官方/MCP注册/扩展）；卸载即消失；重启持久；重复注册幂等 |
| L1-16 | **热替换**：同 component_id 注册新 impl → 页面即时换（零重启）；冲突=拒绝+提示 |
| L1-17 | **回滚**：新 impl 失败 → 回退 default（Capability fallback 诚实降级链；required 缺失→fallback default；optional 缺失→入口灰显） |
| L1-18 | **Token Namespace 隔离**：第三方组件 token 前缀隔离（不污染全局 --primary/--s0..s4/--text1..3）；grep 断言无全局 token 覆盖 |
| L1-19 | **注册契约校验**：ComponentContract 九字段（component_id/semantic_type/variant/impl/capabilities/状态/版本/权限/声明）缺一=CI 红；不符合命名规范=CI 红 |
| L1-20 | **单执行通道不变**（A7）：注册组件只经 Dispatcher→Guard→Handler；注册面不直达 impl（Registry 不再扩=大神底线） |
| L2-11 | 真机：注册一个 MCP 组件 → 选择页新分区出现 + 切换生效（前后截图） |
| L2-12 | 真机：卸载/冲突/热替换各一（截图+零崩溃） |
| M-U50-10 | 注册契约缺字段→L1-19 红（变异亲杀） |
| M-U50-11 | Token 隔离破坏（第三方覆盖全局）→L1-18 红 |

**红线**：Registry 不再扩（大神底线——不加新机制只做登记制开放）；Token 契约不变；A7 单通道；范围外不做（商业支付/上架市场=后续单）。

**L3**：明暗×状态×来源分区 回归（注册组件在浅深两主题均可用）。
