> **STD 冻结头**（2026-09-02 补 · 红线 22）：standard_id= ｜ content_sha256= ｜ 冻结人=设计师 ｜ 冻结日=2026-09-02
# STD-UPG50 阶段 1 验收判据增补（2026-09-02）

> **配套**：`01_外观\外观组件库_阶段1_形态矩阵规格_v1_2026-09-02.md`（20 条×75 形态档+族状态矩阵=唯一基准）+ `STD-UPG50-v1.1`（阶段 0 判据，保持有效）。
> **阶段 0 → 阶段 1**：机制已验证（单实例打样），阶段 1=全量铺开（20 条组件全部纳入组件外观）。

## 一、判据（新增 L1-11~14 / L2-8~10 / M-U50-8~9）

| # | 判据 | 判定 |
|---|---|---|
| L1-11 | 20 条编号组件全部有形态规格档（default+≥1 变体——基准=形态矩阵规格 v1；机器校验条数=20+变体总数=75） | 缺任一条=FAIL |
| L1-12 | 每组件变体解析到真实 DOM（阶段 0 Resolver 扩展——单测每条组件解析到对应 class） | 任一组件解析断=FAIL |
| L1-13 | 组件族状态矩阵共享（grep 断言无组件级重复状态表——族模板单源：INPUT/行列表/卡片/按钮图标 4 族） | 重复表=FAIL |
| L1-14 | 全组件切换=单 API（AppearanceProfile.components 唯一写点；20 条无硬编码独立态——grep） | 双写点=FAIL |
| L2-8 | 选择页 20 条组件全部可见（部位分组+栏折叠——截图 20 条全览） | 缺组件=FAIL |
| L2-9 | 任选 3 条高频（CHAT-BUBBLE/SETTINGS-ROW/WORKBENCH-CARD）切换形态 → 实例即时变（前后截图对照） | 任一没变=FAIL |
| L2-10 | 明暗 × 状态抽查 3 态（default/pressed/disabled）× 2 明暗 = 6 截图（无崩溃/无错色） | 任一异常=FAIL |
| M-U50-8 | 组件规格表缺 1 条（改 20→15）→ L1-11 红 | 不红=FAIL（亲杀） |
| M-U50-9 | 非 INPUT 组件被误伤（切换 BUBBLE 影响 BUBBLE 以外）→ L1-14 红 | 不红=FAIL（亲杀） |

## 二、红线（阶段 1）

1. Token 契约不变；2. 单实例（每次切换=单组件生效——20 条互不污染）；3. UI 直调豁免（不动调度器/审批面）；4. 范围外不做（MCP 注册制=阶段 3；P2-1 伴随单；排版 UI=并行小单）；5. 排版 Token 消费（禁写死字号/字族/字重——L1-10 延续）。

## 三、批次验收口径

- **1A（机制扩展）**：L1-11~14 + L2-8（选择页 20 条全览）
- **1B（高频视觉）**：1A 全过 + L2-9（3 条高频）+ L2-10（6 截图）
- **1C（余下组件）**：1B 全过 + 20 条逐条 L2-9 扩展（全组件切换验证）

## 四、L3 / 端到端（延引）

分辨率/字体大小/横竖屏/平板 前后对照回归（组件全量铺开后）。
