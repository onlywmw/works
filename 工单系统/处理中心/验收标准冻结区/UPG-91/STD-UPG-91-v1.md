# STD-UPG-91-v1 验收标准冻结版

> 工单：UPG-91 ｜ 标题：我的资产页注册制重排（tab 导航+二级收缩裸列表+点值脱敏+诚实空态+假数据回落删除+令牌迁移）｜ 唯一正式冻结版

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-91-v1`
- **content_sha256**: `03b497ecd4dbc74ae77ab1ca0de9aab3523bacf5abbd9a8a548edb78450456ae`
- **frozen_at**: `2026-09-03T13:00:00`
- **frozen_by**: 设计师B
- **approved_by**: 审验员（派单后确认，验收启动前补齐）

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L2 中 | UI 重排+安全面（假数据回落）→ diff 精读 + 契约锚 + 真机走查 | ① 视觉=demo v6 终版（`设计预览\我的资产_重设计_demo_v6_2026-09-03.html`——tab 导航[类目+计数]/二级收缩组行/点值脱敏往返/纯文字线框零图标/诚实空态）；② 假数据回落删除（`demoBridge` 回落删——桥空=空态，永不演示凭据充数）；③ 令牌迁移（AssetsPage.vue:169-173 等硬编码色值全清，绿横幅消亡，过 tokens 单源）；④ 分级由机制承载（点值=脱敏往返交互；读取审批链 vault.get 不动） |

### 变异锚（L2 必填）

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| 假数据回落（demoBridge） | 恢复「桥空回落 demo 数据」 | 「桥空必空态」测试必红（红线 18 同族） |
| 硬编码色值 | 塞回硬编码 hex（#eef7f4 等） | 令牌锚（UPG-70 模式扫描/契约断言）必红 |
| 二级收缩 | 组行默认展开 | 「组行默认收起」断言必红 |
| tab 计数 | tab 无计数/计数与数据不符 | tab 计数=真实条目数断言必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 定向 | 资产页契约套件（桥空空态/无假数据/令牌锚/收缩/tab 计数）+ UPG-50/70 组件契约套件零回归 |
| 全量回归 | `testDebugUnitTest --rerun-tasks` 绿（0 失败基线） |
| 构建 | vite build + sync-pages 产物入库（红线哨兵零越界）+ assembleDebug 绿 |
| 真机 L3 | 平板实拍：有数据态（tab+组行收起）/组行展开明细/点值脱敏往返/空态/深浅色——截图含时间戳 |

### 销项条件（下列全满足）

- [ ] 视觉=demo v6（tab 导航+组行收缩+点值往返+零图标+空态诚实），登记行不建
- [ ] demoBridge 假数据回落删除（桥空=空态）
- [ ] 硬编码色值清零（UPG-70 归层纪律，令牌单源）
- [ ] 4 变异锚亲杀全红还原复绿；全量 0 失败绿
- [ ] 未上线类目 tab 灰显在册、内容区诚实「即将上线」一行
- [ ] Token/KV 两节申报（0/0）

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-03 | 设计师B | 冻结依据 | 六轮迭代定稿（v1 卡框版→v2 至简→v3 注册制摊开→v4 +导航栏→v5 二级收缩+去 emoji→v6 登记行删）；用户拍板点：注册制结构归 tab 栏、分级归机制（点值需确认）、图标全删、点值脱敏；假数据回落=红线 18 同族根治 |

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-91-v1.md"
```
