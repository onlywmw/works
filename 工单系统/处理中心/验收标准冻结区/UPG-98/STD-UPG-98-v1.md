# STD-UPG-98-v1 验收标准冻结版

> 工单：UPG-98 ｜ 标题：MainActivity 拆分·批② 市场面搬移（→ market/ 模块）｜ 唯一正式冻结版

## 头部（身份 + 完整性 · 冻结后不可改）

- **standard_id**: `STD-UPG-98-v1`
- **content_sha256**: `1aa228227177fd9284a71117935b7586f2b104b9c46b7b3ad3f20f2d895a6ed7`
- **frozen_at**: `2026-09-04T06:40:00`
- **frozen_by**: 设计师B
- **approved_by**: 审验员（派单后确认，验收启动前补齐）

## 冻结区（不可改 · 只读）

### 验收定级

| 定级 | 本单验收方式 | 验收要点 |
|---|---|---|
| L3 高 | 上帝文件拆分批② → diff 精读 + 变异亲杀 + 保真对账 + 真机冒烟 | ① 市场总览区（buildLocalOverview/启停/安装投影）搬入 market/ 模块，MainActivity 壳内只留装配点；② 纯搬移零逻辑改动（块归一化 sha256 前后全等）；③ 军规 7/8 合规（零新增壳内代码/Feature 零持有 Activity）；④ 行号以开工时 main 重核（蓝图行号为搬移前锚） |

### 变异锚（L3 必填）

| 锚点 | 变异动作 | 期望 |
|---|---|---|
| 保真锚 | 搬移块内改一行逻辑 | 「块归一化 sha256 前后全等」断言必红 |
| 名单锚 | 搬漏一个市场面函数（从 market/ 模块删除） | 「市场面名单完备」契约锚必红 |
| 唯一写点锚 | MainActivity 壳内残留市场面第二实现（伪搬移=复制不删） | 「壳内零残留/唯一写点」锚必红 |

### 测试匹配档

| 项 | 期望 |
|---|---|
| 定向用例 | 拆分契约锚（保真/名单/唯一写点/装配顺序——照批① ToolsSplitContractTest 模式）+ 市场面直呼/功能契约（拆前绿→拆后绿） |
| 全量回归 | `testDebugUnitTest --rerun-tasks` 绿（0 失败基线，/1 口径）；UPG-93 拆分锚零回归 |
| 构建 | `:app:assembleDebug` 绿 |
| 证据链 | 保真锚全等清单（hash 列表）+ logcat/截图（含时间戳）+ journal 四环节 |
| 真机 L3 | ①MCP 市场页打开正常 ②内置包启停一次（禁用→面收缩/启用→恢复）③市场工具直呼一条（如 market.status）——logcat 留证 |
| 冷启动锚 | 拆前后各 5 次采样取中位，Δ>10% 标红须解释 |

### 销项条件（下列全满足）

- [ ] 市场总览区搬入 market/ 模块；MainActivity 壳内只留装配点（行数下降实测：6016→预期值申报）
- [ ] 保真锚：搬移块归一化 sha256 前后全等清单在档
- [ ] 军规 7/8 合规：零新增壳内代码（装配点+豁免注记除外）；market/ 模块零 Activity 长期持有
- [ ] 3 变异锚亲杀红→还原复绿；全量绿+assembleDebug 绿；冷启动锚在容忍带
- [ ] 真机三场景冒烟通过；UPG-93 拆分锚零回归
- [ ] 债务清单联动（批②相关条目只记录不修）
- [ ] Token/KV 两节申报

## 追加说明区（任何人可加 · 不作为合格判定正向证据）

| 时间 | 作者 | 触发原因 | 说明 |
|---|---|---|---|
| 2026-09-04 | 设计师B | 冻结依据 | 拆分串行纪律（UPG-95 真冲突实证后立）：一批一派、合后再基；批②=市场面（蓝图 §二「市场总览 :7266」搬移前锚，开工时以 main 顶重核）；形态/锚法照抄批①（UPG-93 已合 3315bff0 先例：9 域扩展+ToolsRegistry 只聚合+保真锚法）；批③（页面桥）待本批合后派 |

## content_sha256 生成方法（本段仅供校验 · 不属于冻结区）

```bash
python -c "import hashlib,sys; t=open(sys.argv[1],encoding='utf-8').read(); s=t.split('## 冻结区',1)[1].split('## 追加说明区',1)[0]; print(hashlib.sha256(s.encode()).hexdigest())" "STD-UPG-98-v1.md"
```
