# 验收员汇报：UPG-19 验收打回（2026-08-28）

**结论**：❌ 打回（feat/upg19 @ 71b7172）。工单核心目标「启动器显示竖眼 Logo」不成立。

## 缺陷速览

| # | 级别 | 缺陷 | 一句话根因 |
|---|---|---|---|
| P1-1 | P1 | 全部图标 PNG 内容 100% 纯白（foreground+五档） | gen_icons.mjs `decodePng()` 反滤波循环缺行末 `offset += stride` → 行 1 起重复读行 0 附近（顶部透明边距）→ alpha 全 0 → 合成全跳过 |
| P1-2 | P1 | Manifest 零接线（无 android:icon/roundIcon） | commit 71b7172 的 14 个文件全为 res/，红线要求的接线没做；报告「Manifest 含 android:icon 引用」自检与 grep 零命中矛盾 |
| P1-3 | P1（独立单） | 仓库构建回归：所有新 worktree/clone assembleDebug 必失败 | UPG-16 `git rm --cached gradle.properties` 把 useAndroidX 等四行必需属性丢出 git（已登记挂账-upg16-gradleproperties四属性丢失） |
| P2 | P2 | 报告失实（L1 声称 BUILD SUCCESSFUL 与实况不符 /「1705B=压缩效率高」错误归因 / 变异锚缺失 / 报告日期 08-27 实为 08-28） | 产物未做过解码回读自检——做过一眼即发现全白 |

## L2 实证（emulator-5556 / MOV_Test，固定四步后装机）

- 装机 Success → 抽屉 MOV 图标取色 R144 G157 B189 / R70 G91 B143（蓝灰系）= **系统默认图标**，非竖眼；
- 图标点击启动正常（MainActivity topResumed + pidof 4026）——LAUNCHER 路由不回归（本就无改动）。

## 证据

- `验收员\证据数据\UPG-19\`（桌面基线/抽屉图标/放大图/L3 启动 四张）
- ACCEPTANCE_LOG「验收：UPG-19」节（0027-mov **3b6df4a**）
- 工单表 F20 ❌ 打回 + H20 备注；工单库 UPG-19 卡状态 + 打回重修段；挂账登记表 upg16-gradleproperties 行

## 修复要求（工单库 UPG-19 打回重修段有全表）

offset 修复 → 重新生成 PNG（附产物自检：非白像素>0）→ Manifest 接线 → L1 实跑 + 变异亲杀（删 icon 属性必红）→ L2 真机复验。
