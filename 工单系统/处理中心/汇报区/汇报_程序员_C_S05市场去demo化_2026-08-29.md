# 程序员汇报：S-05 市场去 demo 化（2026-08-29）

**执行人**：程序员 C ｜ **性质**：用户直令施工（无正式卡，S-05 为历史批次号——「去 demo 化」范围经溯源实测后按推荐路径执行，登记汇报供设计师追认/立案）

## 溯源实测（动手前）

- market-web 各页（index/explorer/details/upload/guide/merchant/admin）**无假数据**——S-08 已做 registry 同源 + 诚实空态，表单 placeholder 属正常 UI；
- demo 残留在**数据层**：线上 `/var/www/market/registry.json` 3 包中 2 个为 demo：
  1. `wx-merchant-onboard`（微信商户入驻助手，desc=「演示剧本：对话式入驻」，workflowId=**demo-onboard**，步骤数据 demo-user、open URL=**127.0.0.1:8899/spa-form.html** 本地测试页——公网用户装上即调不通）；
  2. `hello-world`（cat=**demo**，byo）；
  3. `browser-automation`（builtin 真包，保留）。

## 处置（按「两包全撤」推荐路径）

- 服务器 `/var/www/market/registry.json` **原子改写**（tmp+rename，对齐 review-service 形态；12306 撤卡同通道——「直接改静态文件即持久」），改前备份 `registry.json.bak_demo撤卡前_2026-08-29`（1522B 原样）；
- 撤后线上：**packages=1（browser-automation）**，`demo` 字样零残留，updatedAt=2026-08-29T13:33:07Z；curl 公网实证；
- 消费面即时生效（同源 fetch，无需重部署）：`/home/market/`（S-08 页）、`/tools/explorer.html`（线上 data.js `REGISTRY_URL="/market/registry.json"` 实核）、`/tools/`；
- 诚实空态兜底：S-08 交付的空态逻辑在案，用户视角无破绽；
- **App 侧零改动**：`app/src/main/assets/workflows/` 三个内置 demo 剧本（demo-onboard/demo-mood-capsule/demo-vault）为 workflow 引擎演示资产（WorkflowEngineTest 引用），不在「市场」范畴，本单不动（如需清 Engine 演示资产请设计师另立单）。

## 遗留提示

- `tools/submit-declarative.js`（S5 演示提交脚本）仍在 repo——它就是两个 demo 包当年的提交来源；后续若再跑会把 demo 包重新提交上架（有 confirm 硬闸+审核口挡着），建议设计师定夺：脚本加「勿重复执行」注释头或挪 _备份归档；
- demo 撤卡属数据面变更，如需回滚：`cp registry.json.bak_demo撤卡前_2026-08-29 registry.json`（服务器上）即恢复 3 包。

## 关联

- 溯源通道依据：`/var/www/market/registry.json` 由 review-service approve/publish 原子重写、approved 后 pending 清空 → 直接改静态文件即持久（乱码修复先例）；部署 ssh root@mow.kim（MOTD banner 淹没输出，命令 tail）。
- 今日同日关联交付：UPG-23 R1（0bfa4fa 已合 main）、M3-R2（5c113cb）、UPG-01 批1（067aa8e）批2（d33c112）、UPG-22 R1（09b9a79）。
