# 汇报 · SYS-01 工单系统自身改造（第一阶段 P0-1 + P0-2）

> 类型：SYS 系统自改进（独立编号，不混入 UPG 主线）
> 日期：2026-09-01 ｜ 依据：`SYS\改造计划_工单流转中心防伪抗倒退_2026-09-01.md`（v3.1 实施定稿，评审 9.4/10）
> 治理归属：工单系统自身精装修，**零 APP 影响**（未碰 0027-mov）
> 状态：✅ 已完成第一阶段（P0-1 + P0-2），P0-3 ~ P2 待后续阶段

---

## 一、本阶段交付内容

### P0-1 验收标准「冻结」机制（先建可信对象）
| 项 | 落点 | 说明 |
|---|---|---|
| 冻结区规则 | `处理中心\验收标准冻结区\README.md` | 冻结流程 / STD 结构 / 冻结区 vs 追加说明区 / 修订派生 / 三处对账 |
| STD 冻结模板 | `处理中心\验收标准冻结区\_模板\STD_模板.md` | `standard_id/content_sha256/frozen_at/frozen_by/approved_by` + 冻结区（L1/L2/L3+变异锚+测试匹配档）+ 追加说明区 + sha256 重算命令 |
| 红线 | 根 `README.md` 红线 22 | 标准冻结必做、修订派生、三处对账 |

### P0-2 交付 hash「绑定」机制（先建可信对象）
| 项 | 落点 | 说明 |
|---|---|---|
| 交付绑定规范 | `SYS\交付绑定规范.md` | `delivery_id` 格式 + 不可原地改 / 三重 hash（commit/artifact/evidence_manifest_sha）/ evidence_manifest 不可变清单结构 / 失效标记 / 登记流程 |
| 工单表列 | `工单表.xlsx`「升级工单表」新增 **delivery_id（I 列）** | 已合 main 历史行标「—（合前交付，未绑定）」49 行；未合工单留空待新交付 |
| 红线 | 根 `README.md` 红线 23 | 交付必绑 delivery_id + 三重 hash、失效→新建 |

## 二、联动更新（引用收敛）
- `处理中心\README.md`：文件夹结构加「验收标准冻结区」
- `设计师\README.md`：§三 职责加「7. 标准冻结」
- `程序员\README.md`：职责加「交付绑定」登记要求
- `验收员\README.md`：加「交付绑定对账（P0-2）」节
- `审验员\README.md`：审验检查点表加「标准冻结对账 / 交付绑定对账」两行

## 三、工单表写入对账（红线 9 五步铁律）
- ① 写前重新读最新文件 ✅ ② 备份：`_备份归档\工单表_backup_SYS01_delivery_id列_2026-09-01.xlsx` ✅
- ③ 行定位内容匹配，未动既有 A-H 列 ✅ ④ 无新增行（仅加列，无 !ref 问题）✅
- ⑤ 写后全量读回对账：表头 9 列、56 个非空工单号行完整、UPG-28 E 列 hash `443b288` 在位、已合 49 行标记 + 未合 7 行留空、无乱码新增、行 59 为原始尾部空行（保存 trim，无数据丢失）✅

## 四、范围与红线遵守
- 只动 `Desktop\MOV\工单系统`，**未碰 0027-mov APP 源码** ✅
- 未新增机制/角色/审批节点/库/评分体系/防伪分类（仅补 P0-1/P0-2 两个契约，均复用既有角色职责）✅
- Hash≠身份：标准冻结区用 `standard_id` 管身份、`content_sha256` 管完整性；交付用 `delivery_id` 管身份、三重 hash 管完整性 ✅
- 不可变对象：STD 修订派生、delivery_id 不可原地改、证据清单不可变 ✅
- machine 只出 flag：本阶段只落规则 + 可手动重跑命令，未写自动化脚本（自动比对属 P2）✅

## 五、遗留（非本阶段范围）
- P0-3 反伪审验（integrity_flags 两层分离 + 人工裁决）→ 阶段二
- P1-1 能力护栏（coverage_status 三档）→ 阶段三
- P1-2 坑位/复用库 → 阶段四
- P2 AI 机械活（审验.py hash 自动比对 / 伪通过预筛）→ 阶段五
- 备注：现状 `审验员\审验.py` 未实际存在（README 引用但无文件），P2 阶段落地时可一并处理

## 六、建议
- 阶段二开工时，建议给**首个新工单**（如 UPG-68）做一次「标准冻结 + 交付绑定」全流程试点，验证模板可用性再铺开。
