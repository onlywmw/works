# 已知坑库（回答「什么东西以前坑过我们？」）

> 收录条件：**已解决**（现象→根因→药方齐）。未解决走 `处理中心\挂账登记表.md`（待审池），见 `README.md` 三库边界。
> 出单前必查本库（派单文本填「已查坑位库/复用件库」字段），命中 → 施工范围引用/规避。

| # | 坑名（一句话） | 适用版本/日期 | 关联工单 | 现象 → 根因 → 药方 | 解决状态 | 最后验证时间 |
|---|---|---|---|---|---|---|
| 1 | 工作台状态语义「验收中」无法表达「已通过待合」 | 2026-08-26 | `挂账-upg08-workbench状态语义` → 转工单 UPG-10 | 现象：UPG-08 已验收通过仍显示「验收中」；根因：sync-workbench.mjs 仅以「列有 ✅」定状态，验收员列 ✅ 一律输出 status=验收中；药方：状态推导区分「✅ 通过/打回」，验收员「通过」→待合 main、「打回」→打回修复中，合 main ✅→已完成 | 已验证解决 | 2026-08-26 |
| 2 | 工单表并发写入丢行 / 行回退（多 agent 撞车） | 2026-08-28~08-31 | UPG-54（覆盖）· UPG-53（emoji 截断）· UPG-56/57（丢失）· UPG-60（回退） | 现象：并行 agent 改 `工单表.xlsx` 互相覆盖/丢行/回退；根因：xlsx 库不自动扩展 !ref、行号硬编码漂移、读旧副本后写入；药方：**写入五步铁律**（①写前重读 ②写前备份 ③内容匹配定位禁行号 ④新增行先扩 !ref ⑤写后全量读回对账），违反任一步=登记无效 | 已验证解决（红线 9 固化） | 2026-08-31 |
| 3 | 演示数据残留污染生产态 | 2026-08-26 | UPG-08（P2-2 打回补强） | 现象：交付/验收演示的临时数据（假状态/假阻塞/假账目）残留到生产态；根因：演示后未还原即报完成；药方：**演示数据必须还原 + 交付报告声明「演示数据已还原且生产态已复核」**，验收员发现残留直接记 P2 打回 | 已验证解决（红线 18 固化） | 2026-08-26 |
| 4 | Windows 文件名含尖括号 `<` 导致 ENOENT | 2026-09-01 | SYS-01 P0-1（STD 模板建卡） | 现象：模板文件名 `STD_<工单号>-v<n>_模板.md` 创建报 ENOENT；根因：Windows 文件名禁止 `<` `>`；药方：文件名用占位词（如 `STD_模板.md`），尖括号只出现在文档内容说明 | 已验证解决 | 2026-09-01 |
| 5 | 截图文件名紧凑格式时间戳识别失效 | 2026-09-01 | SYS-01 P0-3（审验.py 边界用例）→ P2 修复 | 现象：`shot_20260829_220000.png`（HHMMSS 无分隔符）时间戳解析退化「仅日期」，time_inconsistency 检测失效；根因：ts_from_filename 正则要求时分秒间有分隔符；药方：审验正则已支持紧凑格式（P2 落地 SYS-01 阶段五，实测 `shot_20260829_220000.png` 识别 22:00:00），截图用分隔符 `2026-08-29_22-00-00` 或紧凑格式 `20260829_220000` 均可识别 | 已验证解决（P2 正则落地 @2026-09-01） | 2026-09-01（P2 验收） |
| 6 | `--tests` 类名/通配不中 → 0 测试 BUILD SUCCESSFUL 假象 | 2026-09-03 | UPG-84/85/87 三角色四现（程序员/验收员/审验员均踩） | 现象：`--tests` 过滤器未命中任何测试类时 Gradle 报 SUCCESSFUL（0 测试≠红）→「假绿」；根因：通配不匹配测试实际包名/类名时执行集为空，BUILD 不失败；药方：**变异复杀强制步——`--tests` 执行后必须核 XML fresh + tests 计数>0**；**注入点选「单行删除/纯函数改值」**（勿选需编译配套的删法——验收员 87/88 三轮未复现系注入点/工具适配问题，审验员改注入点后全达真红）；测试类名先 grep 定位再过滤 | 已验证解决（纪律+药方入册） | 2026-09-03 |
| 7 | 合并冲突把 ACCEPTANCE_LOG 解向旧版 → 验收落档成批丢失 | 2026-09-05 | 问题区 P1（UPG-46 合 main 7ccf0446）→ 恢复 e7e0b2ed | 现象：合 main 时 docs/ACCEPTANCE_LOG.md 冲突「取 theirs」解向旧版，P22–P57 共 40 段（898 行）验收落档在 main 全灭（内容只挂 feat/upg50-ph1 分支历史）；根因：该文件是**高频追加型共享文件**，任何 merge 冲突自动/半自动解决都可能反向丢段；药方：**ACCEPTANCE_LOG 合并冲突一律手工 diff 确认行数只增不减**（或 union 策略），合 main 前 `git show <base>:docs/ACCEPTANCE_LOG.md \| grep -c "^## P"` 与合并后比对段数；丢失修复=恢复性追加（逐字取自完整历史版，追加后逐段字节一致机器校验） | 已验证解决（e7e0b2ed 恢复+逐段校验；硬规则 4 补强口径） | 2026-09-05 |
| 8 | 瞬态 UI（chips/气泡/toast）截图走查粒度不够 → 假对照/误判风险 | 2026-09-05 | UPG-103 场景①（burst_2/3 逐像素同帧）· UPG-94 P2-B 同族前科 | 现象：走查截的「疑似瞬态帧」实为同一稳态画面截两次（逐像素 diff=0.000%），按「瞬态对照」解读即误判；根因：chips 类气泡是 tool_call 期间**亚秒级瞬态**，事后 DOM=0、连帧手速截图覆盖不到；药方：**①疑似瞬态帧目测第一步=逐像素 diff（同帧=对照无效，直接出结论不误判）②瞬态 UI 走查一律录屏法**（adb screenrecord/连帧分析），截图只证稳态；行为面铁证走 logcat 事件链（tool_call→tool_result 闭环）不依赖截图 | 已验证解决（验收员 UPG-103 目测闭环+方法沉淀） | 2026-09-05 |

## 沉淀来源（双向链接）

- `挂账登记表.md` 条目解决并转工单完成 → 半自动提取到本表（挂账 → 已知坑）
- 本表「关联工单」列 = 反向可追溯（已知坑 ← 工单）
- 沉淀规则：`README.md` §三 + `处理中心\挂账登记与转工单规则.md` §四
| 9 | 验收/打回落段落错卡片（多卡并行验收时张冠李戴） | 2026-09-05 | §P39→UPG-81 卡（实为 UPG-85）· §P54→UPG-47 卡（实为 UPG-103）· 9e764fb 打回段→UPG-65 卡（实为 UPG-63）——三现 | 现象：一个验收 commit 覆盖多单时，登记人把 A 单的验收/打回段粘贴进 B 单卡面，B 单状态被污染（UPG-65 因此被误判「回炉待修」挂了 5 天）；根因：验收落档与卡面登记是两步手工动作，靠人记归属；药方：**①卡面登记段落时首行必写分支名+hash，与卡片工单号不符即停手**（本族三次错挂的段落里都写着别人的分支名）②验收员落档后自核 `grep -n "feat/" 卡面段` 归属 ③设计师合批前抽查「卡↔段」归属（同 §六抽查一并做）；修复=错挂注记不删原文（历史保留）+归位注记 | 已验证解决（三处错挂全部注记归位 @2026-09-05） | 2026-09-05 |
| 10 | 多点同文本 contains 组合锚只防全删（单点破坏免疫） | 2026-09-05 | UPG-96 审验发现① → UPG-100 | 现象：HomeDeliveryContractTest 锚② `contains(A)&&contains(B)` 组合，被护文本 `if (homeDeliveryActive)` 在 MainActivity 两处出现，验收员单处条件反转复杀锚仍绿（第二处同文本命中兜底），行为已部分受损（chunk 不回流=流式出卡缺失）锚不报——双处同时破坏才红；根因：contains 只证「存在≥1」，多点同文本时对单点破坏免疫；药方：**①多点同文本锚一律升级「计数+局部性」组合断言**（出现次数==N 冻结双点+每处块内局部性校验，UPG-100 锚②已落地：单点短路/空壳/双点/全删四变异全红）②单点文本（次数==1）contains 锚强度足够不动 ③行为级测试优先于计数锚（计数是 JVM 无法行为化时的次优强锚）④Vue 侧同族两档更弱形态在案（UPG-100 A3 抽检 18 处疑似）：「定义兜底命中」（xxxCls computed 定义行必中+模板使用处再中——断言护模板绑定却删绑定仍绿）与「注释令牌污染」（UI-X 令牌首次命中来自注释行——删真实读点仍绿）；升级方向=活行口径（hasLiveLine 先例）/锚读点形态/`:class=` 正则；抽检结论表见 DELIVERY_UPG100 | 已验证解决（UPG-100 锚②升级+四变异亲杀+A3 同族抽检 8 文件结论表） | 2026-09-05 |
