# UPG-55 共享面影响清单 + 能力护栏裁决（红线 24 · SYS-01 P1-1）

> 设计师 @2026-09-01 ｜ 补交时点：验收打回（能力护栏节缺失）后按红线 24 补。依据：交付报告 DELIVERY_UPG55_2026-09-01.md + 交付 hash e1f241e + main 040c9d9 基线。

## 一、共享面变更清单（本次交付触碰的）

| # | 共享面 | 具体变更 | 影响面 |
|---|---|---|---|
| 1 | **MainActivity 注册表** | 新增 handler：asset.catalog / asset.credentials / asset.credPeek（复用 vault.get only-once）+ ui.openAssets；vault.set 迁移完成拒写引导（J-9） | MCP 面加 4 工具；AI 工具面相应增 |
| 2 | **工具面契约** | asset.* 进 mcpHandlers（AI 可发现可调）；vault.set 行为变更（迁移完成后拒写） | 既有 vault.set 调用方行为变化 |
| 3 | **页面桥白名单** | AssetsSheet allowedPrefixes 增 asset.+ui.（WebView 栈） | 页面桥面 |
| 4 | **Warmup 红线** | WebViewWarmupTest exclude 增 assets（sync-pages 受控通道） | 预热面 |
| 5 | **协议/接口定义** | AssetRegistry/Asset 公共模型/AssetIdRules/VaultAssetMigrator（tool-orch 新包） | 工具面数据契约新类型 |

## 二、coverage_status：**PARTIAL**

理由：变更面集中（资产框架新增+既有 vault.set 行为调整），但**未覆盖维度存在**——见 coverage_decision。

## 三、coverage_decision（PARTIAL 必须含设计师显式裁决）

| 字段 | 值 |
|---|---|
| **uncovered** | ①asset.* 新工具与既有审批链（ApprovalService/only-once）的**多工具交叉场景**未整链验证（单工具 only-once 已测，双工具链未测）②MCP 8389 外部面对 asset.* 的调用路径未验证（页面桥面已验证）③assets 产物经 sync-pages 受控通道，但**生产安装包**（不经过 worktree 拷贝通道）的 assets 页更新路径未验证 |
| **risk** | 中（①若 asset.credPeek 绕过 only-once 链=凭据泄露——已复用 vault.get 的 only-once handler 降低风险；②8389 面新工具落 else→free 静默放行——**asset.* 查询类只读，可接受但需在审验/AI 面抽查**；③生产安装路径 sync-pages 未跑=产物体积/路径偏差风险） |
| **merge_decision** | **允许合 main**——但以下前置：a)③项 sync-pages 生产通道验证补充（临时——随 67-B 同步）b)①纳入 R1 修复后验收重验时补测 c)8389 面 asset.* 在审验抽查 |
| **reason** | 变更本质=资产框架新增（向后兼容：既有 vault 数据迁移后语义保持+老入口只读=回滚窗口），既有工具面仅 vault.set 一处行为收紧（拒写导引=安全增益非回退）；未覆盖维度均为新能力面的边界验证，不构成既有能力倒退 |
| **decided_by** | 设计师（本文件=裁决记录） |
| **decided_at** | 2026-09-01 |

## 四、全量回归说明

- 全量 0 新增失败（验收员已证 AssetFrameworksTest 8/8 + 全绿灯）——按红线 24「全量回归 0 新增失败 ≠ 能力没倒退」，未测维度见上 §三 uncovered 三项
- 本裁决随本文件入工单库卡（备注引用），供验收/审验/合 main 三方对账
