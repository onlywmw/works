# 《审批体系 总纲 v1》评审增补（设计师B · 2026-09-02）

> 依据：对 main@014c10f 的全链只读审计（六路探查，全部结论带行号，可复核）。
> 态度：**总纲框架成立**（没成体系=属实；分层终态=R1 组件化呈现层=该做；R3 改判=与本文结论一致）。
> 但 §1 核心判断有一处与代码证据冲突，不修正则实施序错位：R1 做完，P0 明文洞仍在。

## 1 对 §1「L0 已稳、L2 唯一黑洞」的修正

**L0 不稳：审批判定是双写的，且两份已经漂移。**

- 管线 1（对话调度链）：`McpToolScheduler.kt:317-319`——only-once 在 FULL_ACCESS 下也强制 ASK（UPG-68 MEDIUM-1 修复）。
- 管线 2（MCP :8389 面）：`McpServer.kt:110-113`——tools/call 直查 `guard.guard()`，**没有这份覆写**。open 模式下 vault.get（登记 `ask`，`PermissionRegistryData.kt:198`）被 `guard` 判 ALLOW（`McpToolScheduler.kt:192-200`，`isHighRisk` 只认 shell.exec/credentials）→ `McpServer.kt:151` 直接执行。
- UPG-68 D 收口已把 handler 内层闸拆掉（`MainActivity.kt:4010-4012` 注释「审批由调度层统一门控」）——调度层管不到 MCP 面，**open 模式下零点击明文直出**。这是 L0/L1 边界缺陷，不是 L2。

**「呈现层从未跑通」的归因要拆开：**

- 管线 1 的呈现层是**健康**的（状态机全审计：ALLOWED_ONCE 仅三个真实点击来源——弹窗 `MainActivity.kt:7541-7551` / 通知广播 `NotificationAnswerer.kt:133-142` / chip `:7394`；超时/取消/异常全 fail-closed；chip 在 `fifo.add` 即亮 `ApprovalService.kt:418-420`）。
- 真机「三面静默」是因为调用**从未进入 ApprovalService**：管线 2 的 pending 存在 `PermissionGuard.pending`（`McpToolScheduler.kt:51`），chip 只读 `ApprovalService.pendingList`，UPG-75 A3 又删了旧 takePending 接管，`permission.approve` 在 main 上**无任何 UI 调用点**——req-N 是设计内死信。**统一 ApprovalSurface 若仍只接 ApprovalService 队列，做完照样看不见 req-N。**
- 铁证：LLM 报的 `APPROVAL_REQUIRED requestId=req-N` 全仓唯一产地 `McpServer.kt:133`（对话链失败文案是 `APPROVAL_DENIED`）——req-N 出现 = 该轮必走 :8389 面。

**总纲遗漏的第三缺口（LLM 入口契约层）：**

- `ToolSdkGenerator.kt:215-217` 在 code 模式系统提示里逐字教 LLM 用 `shell.exec curl 127.0.0.1:8389` 调任意工具——这是 LLM 落入管线 2 的匝道。
- `ToolSdkGenerator.kt:221` 宣称「标 ask 的写类会再弹一次」——对 MCP 面是**虚假承诺**，直接喂养了「LLM 报告已申请审批、用户零感知」的幻觉话术。

## 2 修正后的分层图（只加一层入口面）

```
LLM 工具调用
   │ 入口面（双通道——总纲 v1 未画）：
   │  ① native dispatch（ReactLoopAgent→McpToolScheduler）
   │  ② MCP :8389（直连/经 shell.exec curl——SDK 教学路径）
   ▼
L0 决策层（判定须单源——当前双写已漂移，本增补 R0 收口）
L1 请求/队列层（ApprovalService FIFO——健康；PermissionGuard.pending 死信待处置）
L2 呈现层（ApprovalSurface 组件化——总纲 R1，成立）
L3 审计层（已稳）
```

## 3 实施序修正（§5 前置一档 R0）

- **R0 判定单源化 + 死信处置 + SDK 纠偏（P0，小 diff，先行）**：
  ① MCP 面 tools/call 复用调度层的 only-once 强制 ASK 判定——抽公共判定或让 McpServer 走 dispatch，消除双写（不是再抄一个 if）；
  ② `PermissionGuard.pending` 并入 ApprovalService FIFO 呈现，或 MCP 面 ASK 直接返回拒绝+引导文案，消灭永远批不了的 req-N；`permission.approve` 无 UI 调用点一并裁决；
  ③ 修正 `ToolSdkGenerator.kt:221` 的虚假承诺，写明 MCP 面 ask 类工具的实时语义。
- **R1 呈现层总实现（P1，总纲原案）**：ApprovalSurface 单组件 + 组件级测试。与 R0 的合成关系：R0② 若选「并入 FIFO」，R1 的一源投影**天然覆盖 req-N**，两案不冲突。
- **R2 预审单（UPG-76）**、**R3 历史改判**：照总纲。
- 附带挂账（不进 R0/R1）：弹窗僵尸竞态（`MainActivity.kt:4381` 赋值先于 `:4382` 建窗的窗口期，P2）；通知权限被拒静默丢弃+日志谎称已发出（`NotificationAnswerer.kt:113-114`，P2）；冷启动通知按钮 action 丢失（onCreate 不处理 ACTION_ALLOW/DENY，P2）。

## 4 追加评审点（并入总纲 §6）

5. R0 与 R1 的先后与边界：R0 是堵洞（判定层），R1 是体系（呈现层）——R0 小 diff 先合，R1 按总纲节奏。
6. open 模式下 only-once 语义拍板：vault.get 是否允许被 open 豁免——建议**否**（与 `McpToolScheduler.kt:317-319` 单源对齐），写进 STD 冻结。

## 5 未核实项（防幻觉声明）

- 真机当时的权限模式（default/open）与 LLM 实际调用路径（native vs shell.exec curl）为运行时态，代码层无法断定。
- 零成本取证：logcat 三行判别——`MOV-Vault:4022`（明文读取）出现而 `ApprovalVis:4374`（answerer 被调）缺席 = 坐实管线 2；查会话 journal 有无 `shell.exec curl ...:8389`。
