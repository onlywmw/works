# UPG-95 派单：个性化槽位选择（care profile 提炼 + 可选槽池 + LLM 编辑层 + ticketCard/rideCard 骨架落地）

> **派单时间**：2026-09-04 ｜ **派单人**：设计师B ｜ **优先级**：P1（产品主线·极简批阶段 3 前置）
> **验收标准**：`STD-UPG-95-v1`（content_sha256 见冻结区文件）
> **设计文（唯一施工口径）**：`极简呈现_v1.2增补` 附录 B（个性化槽位选择契约）+ 种子基件 `方案设计\01_外观\个性化画像_记忆种子集_v1_2026-09-04.json` + 场景形锚 demo `设计预览\极简呈现架构_demo_v6_生活场景全图_2026-09-04.html`
> **已查坑位库/复用件库**：是。复用命中=UPG-51 MemoryPreferenceExtractor（提炼器扩展点）+ UPG-89 core.js 骨架渲染器/Registry + UPG-05 基因红线（摘要渲染不渲染原文）；坑库命中=「DRAFT 不驱动/敏感类禁碰」测试锚写法
> **依赖/防撞**：与 UPG-94 同 index.html 面（amenity chips CSS）——**建议 94 先合或同程序员串行**；不动 MainActivity（拆分棘轮军规）；不动 Memory API（只消费）

---

## 一、一句话

商户事实全量进、用户关注点从 Memory OS 提炼、LLM 在登记槽池内做编辑选择——同一张卡，内容千人千面，结构千人一面。

## 二、范围

1. **care profile 提炼层**（`personalization/MemoryPreferenceExtractor.kt` 扩展，纯函数零 LLM）：新增域规则——hotel（早餐/浴缸/停车/取消）、food（辣度/咖啡）、rail（靠窗/过道）、ride（拼车/车型）、travel（出行节奏）、shopping（性价比/品质）；输入 Memory API ListItem（content+status），输出确定性 care 键值表。**DRAFT 不驱动、敏感类绝不进（UPG-51 红线沿用）、转述句（「朋友说」）降权不驱动**。
2. **Registry 槽池扩展**（registry_seed.js）：offerCard.v1 增 `amenities` 槽（≤3 坑）、新增 ticketCard.v1/rideCard.v1 登记（**state=candidate**——无真实数据源，mock 消费者不算独立消费者）+ style_tokens 白名单同步。
3. **骨架渲染器**（core.js）：ticketCard.v1（路线头+kv+主次动作，照 demo v6 形锚）+ rideCard.v1（司机行+kv+主次动作）+ offerCard amenities chips 渲染；槽池超界截断（≤3）。
4. **LLM 编辑层契约**（系统提示词节）：「全量事实 × care profile → 从已声明槽池选择」的指令模板 + 双段输出扩展（amenities/seat_pref/ride_pref 字段）；**禁发明新槽**（不在槽池=词表闸拒收）。
5. **mock 全量事实 fixtures**：酒店（含早+浴缸+无停车）/高铁（靠窗余票）/快车（拼车/独享选项）三个商户 mock——供端到端测试，标注 MOCK 不涉真实数据源。
6. **测试**：种子集 11 条全断言（正向 8/兜底 1/负向 2/敏感 1）+ 变异锚 4（STD）。

## 三、红线（违反=打回）

1. 军规不变：LLM 选事实=编辑权放行，发明槽/样式=拒收；PV-01/词表/Danger 三道闸零回归。
2. 敏感类（宗教/健康/政治/性取向）绝不进画像——UPG-51 L1④ 锚延续，本单种子 M-11 为实证锚。
3. 基因/画像红线：提示词注入的是提炼标签，**不注入记忆原文**（UPG-05 v4/v5 定案沿用）。
4. 无新框架/新依赖；core.js 纯 HTML5/JS；index.html 改动面=amenity chips CSS 一处。
5. Token/KV 两节必报（care profile 注入节的字节量实测）。

## 四、测试与真机（STD-UPG-95-v1）

- 种子集驱动：load 种子 JSON → 提炼 → 画像断言 → mock 事实灌槽 → 渲染断言（酒店卡 amenities=[含双早,浴缸]/电影卡=默认槽/DRAFT 不驱动/敏感拦截）。
- 4 变异锚亲杀 + 全量 0 失败基线 + assembleDebug 绿。
- 真机/模拟器：coverage PARTIAL 申报位（homeWeb agent 接线属后续批——本单实证到引擎渲染层+bun/JVM，真机出卡走 CDP 注入实证路径即可，截图四场景）。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG95_<日期>.md`（种子集 11 断言全录+4 亲杀+Token/KV 两节+「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）+ manifest 硬闸自检（ok:True 输出附报告）；DEL 绑分支头；共享面=呈现面/系统提示面/记忆提炼面，附 coverage_status。
