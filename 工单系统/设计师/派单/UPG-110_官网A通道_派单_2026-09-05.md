# UPG-110 官网容器面·批二 A 通道 · 派单

> **派单人**：设计师 ｜ **日期**：2026-09-05 ｜ **优先级**：P1
> **设计口径**：`设计师\方案设计\工单方案\官网容器面_给AI看内容_设计_v3_2026-09-04.md` §4.5（验证层）/§4.6（A 通道运营）/§5（工程边界）+ 工单库 UPG-110 卡
> **验收标准（冻结版）**：`STD-UPG-110-v1`（content_sha256=99b78881…，冻结区 UPG-110/）
> **已查坑位库/复用件库**：是 ｜ 命中：坑#7（ACCEPTANCE_LOG 合并纪律——本单涉服务器部署文档落档注意）；复用件=scene-mcp/server.mjs 形态模板（12306 MCP server，同构直接照骨架）
> **溯源复核**：已过 @2026-09-05（官网服务形态 market-web 静态+Caddy/Node 在案；批一产物 llms.txt/tools-schema.json/sync-manifest.json 在 main 可复用；六工具数据源=capability-registry.json 同源）

---

## 一、基线与认领

- 主仓 `E:\mov归档\0027-mov`；`git fetch origin` 后基最新 main 开 **分支 `feat/upg110` / worktree `mov-upg110`**
- 认领登记（开工前必做）：`python 审验员/set-status.py UPG-110 --phase claimed --branch feat/upg110 --actor <你>`（SYS-04 写入闸已生效，手写散文不产生权威状态）

## 二、施工范围（四件）

1. **端点模块**：`market-web/` 新建容器面 MCP 服务（照 `scene-mcp/server.mjs` 骨架：JSON-RPC tools/list+tools/call+统一错误信封 `{ok:false,error:{code,message}}`）——六工具实现（mov_site_info/capabilities/tools/search/market_catalog/guide），数据**读 capability-registry.json 生成，禁手抄**
2. **验证戳**：全部工具返回含 `registry_version`+`last_sync_time`（与 sync-manifest.json 同源）
3. **A 通道运营**：UA 约定 `MOV-AI-Client/1.0`（统计用不强制）+ 响应缓存头（CDN 5min 口径）+ IP+UA 限流锚（不建 token 体系）
4. **部署上线**：systemd 服务 + Caddy 反代挂载 mov-ai.cn；重启窗口告知用户；回滚=关服务

## 三、红线

只读（零写路径）；A 面零凭据/会话/隐私；install_url 指向人操作界面；诚实空态（市场 0 包如实）；同源生成（手抄 registry 内容=打回）。

## 四、验收

STD-UPG-110-v1：三变异锚亲杀（同源/验证戳/限流）+ 生产 curl 三连实证 + 同源对账零差异 + 零凭据核查。

## 五、交付

报告 `程序员\交付报告\DELIVERY_UPG110_*.md`（含 curl 实证含时间戳/部署记录/Token·KV 两节/共享面影响清单/「已登记两个表」）；manifest=`审验员\deliver-gen.mjs --ticket UPG-110 --branch feat/upg110 --hash <sha> --app-path "E:\mov归档\0027-mov" --evidence …` 机制产出 → 审验.py --manifest ok:True → verify-hash HASH_OK → set-status delivered 登记。

**候选施工**：任意线可领，先到先得
