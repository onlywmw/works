# UPG-92 派单：manifest 硬闸化（deliver-gen 源头合规 + 自检内置）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（同族第六现——红线 23 自检步声称未执行，执行闸机制性闭环）
> **验收标准**：`STD-UPG-92-v1`（content_sha256=`a2c0fcd7b2afb725db48172d9a86dd23b188b0005599554c418e09effe1d9bf7`）
> **已查坑位库/复用件库**：是。复用命中=脚本-审验.py（--manifest 校验）+ deliver-gen.mjs（SYS-02 阶段二件，本单升级它）；坑 #6 适用
> **并行安全**：工单系统侧工具链（deliver-gen.mjs/审验.py）——零 app 代码，与全部在施单不撞

## 一、一句话

manifest 合规不靠人记得跑自检——deliver-gen 从源头产出合规 manifest（路径裸串/条带 sha256/绑定值写入），产出后自跑校验，不合规=交付生成直接失败（硬闸，非警告）。

## 二、范围

1. **deliver-gen 升级**（`审验员\deliver-gen.mjs`）：产出 manifest 时——路径裸串（禁嵌注释）、每条 evidence 带 sha256、manifest_sha 绑定值写入文件（可重算对账）；产出后**内置自跑** `审验.py --manifest` 自检，**不合规=拒绝产出/交付生成失败**。
2. **亲杀**（STD 两锚）：产出合规可重算一致；注入不合规内容 → 拒绝产出。
3. **回归**：审验.py 全部子命令 + deliver-gen 骨架生成功能零回归（fixture 自测 PASS）。

## 三、红线

机器产出即合规（不靠人）；硬闸=拒绝不是警告；不动 0027-mov；Token/KV 0/0 申报。

## 四、交付

报告落 `程序员\交付报告\DELIVERY_UPG92_<日期>.md`（产出合规输出+亲杀拒绝输出+「已登记两个表」）；库加交付块 → sync 投影；本单自身 manifest 必须经新闸产出（吃自己的狗粮）。
