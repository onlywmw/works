# UPG-41 派单：市场页「本地」二级页化 （本机能力总览下沉）

> 设计师 ｜ 2026-08-30 ｜ 优先级 P2 ｜ 串行约束=UPG-40 合 main 后开工 → **UPG-40 已合 main @c9298d0，前置已解除，本单可派**
> 设计文档：`设计师/方案设计/UPG-41_市场本地二级页化_方案设计_2026-08-30.md`

## 一、一句话

MarketPage.vue「本地」tab 收缩为概览入口卡；本机能力总览（UPG-23 概览卡+钉选槽+三层分组+工具级下钻）下沉全屏二级页。**纯结构重组，不动数据面/构建链。**

## 二、施工范围

1. **一级「本地」tab 收缩**：概览入口卡（本机能力 N 类 M 工具统计 + 钉选常用工具快捷行 ≤3 + 「查看全部」入口）
2. **总览下沉全屏二级弹层**：`van-popup` 全屏 + 新组件 `LocalOverviewPage.vue`（UPG-23 内容整体迁移）
3. **不改数据面**：`market.localOverview()` 聚合读面 / `ui.getPins` 钉选 / 安装启停逻辑零改动
4. **不动构建链**：不加 html 入口，不动 vite 多页构建 / sync-pages 链；SPA 内跳转返回保留 tab 态
5. 零新增样式（视觉 token 随 UPG-40 已换主色）

## 三、红线

1. 纯结构重组：不改数据面 / 安装 / 启停 / 权限逻辑；UPG-23 风格硬规继承
2. markstream 冻结项不涉及
3. 不引新依赖、不加 html 入口

## 四、验收

- **L1**：总览功能等价断言（分组数/工具数/钉选态 迁移前后一致；demo.js 兼容）；变异：删入口卡 → 必红
- **L2 真机**：截图（入口卡 + 二级页顶部/底部）× 深浅双色；操作链实测（入口→二级→下钻→返回→tab 态保留）
- **L3**：与 UPG-40 合流后 market 风格走查（token 无回归，候选C 冷调近黑仍在）
- **Token/KV 申报**：无新增（读面不变）

## 五、施工 / 认领

- 认领：工单表 UPG-41 行备注追加 `认领: <agent> worktree=mov-upg41 branch=feat/upg41 @<时间>`（规则19：开工先 fetch + 看表，基于最新 main 重切）
- 完成后**登记两个表**（先表后库）：`工单表.xlsx` 程序员列 `✅UPG-41 完成` + 备注 `feat/upg41 <hash>（报告 DELIVERY_UPG41_*.md）`；`工单库.md` 状态改「程序员✅完成，待验收」；报告落 `程序员/交付报告/DELIVERY_UPG41_*.md` 写明「已登记两个表」
- 产物提交 `0027-mov` 分支 `feat/upg41`

## 六、复核锚（@届时 main）

- `MarketPage.vue`（:11-83 本地 tab 模板 = 迁移源；本地 tab 收缩后入口卡）
- `market.localOverview()` / `ui.getPins`（数据面复用，零改动）
