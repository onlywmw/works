# UPG-70 UI 组件化改造 · 地基 —— 派单文本（2026-09-02）

## 单号
UPG-70 ｜ P2 ｜ M8 UI/交互（标签 M2 体系/治理）

## 一句话
把 UI 组件化体系的地基修好：组件目录单一数据源（消除 Kotlin/Vue 双写）、形态类归层、规范与实现对账。

## 依据（用户拍板）
2026-09-02 用户要求先调研 GitHub 成熟方案（Vant4 / Ark+UnoCSS / shadcn-vue）——调研结论：外部库解决「组件丰富度」，MOV 真正病根是本地工程结构（无单一数据源/形态类分散/无统一宿主/规范脱节），用户拍板**先修地基**（A 方案），引库留到下一阶段。

## 范围
- **A1（核心）**：`前端设计/mov-vue/src/catalog/ui-components.json` 为组件目录唯一源（id/name/kind/prefix/variants/defaultVariant/site/order/provider/semanticType）；`scripts/gen-ui-catalog.mjs` 生成：
  1. `app/src/main/java/com/mov/android/ui/catalog/UiComponentCatalog.kt`（CatalogEntry/ComponentVariant/ComponentSpec + 20 编号常量 + components 列表）
  2. `AppearanceApp.vue` 的 `COMPONENTS` 数组段
  3. 对账断言（schema⇄Kotlin⇄Vue 全等，脚本可加入 pre-commit）
- **A2**：SETTINGS 族形态类从 `SettingsPage.vue` scoped 迁入 tokens.css 形态层；规范与 token 对账（--primary 等漂移项处置：以实物定标或改实物，按设计师裁决）；产出 `tools/ui-catalog-audit.mjs`（未接入页面清册）。
- **A3（B 阶段，本单不做）**：统一页面宿主——A2 验收后再拆单。

## 不做
- 不引 Vant/Ark/UnoCSS（下一阶段另议）
- 不改 20 条编号/52 档数据（只改来源）
- 不改组件视觉（收拢版 UI 已合 main，3eb8372/b3fdcf9）
- 不动 UPG-50 阶段 2 已派工作（feat/upg50-ph2）

## 验收锚
- A1-1 唯一目录源（grep 断言无第二份目录数据）
- A1-2 改 schema → 重新生成 → Kotlin+Vue 同步（端到端实证）
- A1-3 手改生成物 → 断言红（亲杀）
- A2-1 SETTINGS 形态类在 tokens.css、SettingsPage.vue scoped 无残留
- A2-2 规范/token 对账表产出（漂移项全部登记处置）

## 交付物
- schema + 生成脚本 + 生成物（带 DO NOT EDIT 头）+ 断言脚本
- 对账表（A2）
- 交付报告（含 Code-LOC 申报）

## 基线
main 顶 `15e53e6`（含外观收拢版 b3fdcf9）
