# 【派单 SYS-02 阶段一】协作执行层 · E2 hash 一致性自动闸

**级别**：P2（基建）｜ **依据**：`公司完整体系设计\改造计划_工单流转中心防伪抗倒退_2026-09-01.md` §八（SYS-02 设计——E1 单源/E2 自动闸/E4 仪表盘三组件；本阶段=E2 最小闭环）
**引子**：UPG-49 交付报告 hash「9fd39b6」不存在（rebase 重写后未更新）——红线 23 登记前台无机器校验 → 漂移进表。SYS-01 防伪（真假）已落地，SYS-02 补一致性（同步）。

## 一、范围（阶段一 · E2 最小闭环）

1. **审验.py 扩展 `--verify-hash <branch> <reported_hash>`**：
   - ① git cat-file 校验 hash 存在（不存在→REJECT）
   - ② 祖先链校验：reported_hash 在 `origin/main` 祖先（`git merge-base --is-ancestor`）→ 不在=REJECT（防旧 hash/未合内容冒充）
   - ③ 输出：`HASH_OK` / `HASH_REJECT <reason>`（reason∈missing/not-ancestor/ambiguous）
2. **红线 23 流程接入**：交付绑定登记前**必须**先跑 `--verify-hash`；REJECT=登记拒绝（提示用 `git log --oneline branch` 取当前真 hash）——红线 23 文本补一句（登记前强制校验）
3. **回归自测**（同 SYS-01 风格）：构造 U-49 案例重放（9fd39b6→REJECT missing；2a13dcd→OK）——测试锚=脚本内 fixture + 手动命令双跑

## 二、验收判据

- S2-1 不存在 hash → `HASH_REJECT missing`（9fd39b6 实重放）
- S2-2 rebase 重写 hash（2a13dcd 在链/9fd39b6 不在）→ OK/REJECT 判定正确
- S2-3 红线 23 流程：登记前缺校验=拒绝（流程文本提证）
- S2-4 零 App 代码（git diff 无 0027-mov 路径）
- S2-5 审验.py 原有 `--coverage`/证据校验功能零回归（SYS-01 P2 物不倒退）

## 三、红线

1. **只动**：`工单系统\审验员\审验.py` + `工单系统\README.md`（红线 23 文本）+ 汇报文——**零 0027-mov/App 代码**
2. 不破坏 SYS-01 六阶段物（审验.py 旧功能回归 + manifest/STD 校验照旧）
3. 不写 UPG 工单表（SYS-xx 独立追踪——规则不变）

## 四、交付

分支 `feat/sys02-ph1`（基底=工单系统仓库 main）；报告 `DELIVERY_SYS02_阶段一_<日期>.md`（S2-1~5 每条证据）；登记：SYS 追踪（README §六追加 SYS-02 阶段一 ✅）+ 挂账登记表（引子行状态改 ✅ 已实施）。

## 五、备注

- 阶段二（E1 状态单源）/ 阶段三（E4 仪表盘）随本阶段通过后拆派
- 参考 SYS-01 阶段五（P2）审验.py 扩覆盖的先例（README §六）
