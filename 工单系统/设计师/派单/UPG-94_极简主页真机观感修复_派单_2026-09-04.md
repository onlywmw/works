# UPG-94 派单：极简主页真机观感修复（dock 隐藏 + logo 去框去影 + hint 删除 + 发送钮归 token）

> **派单时间**：2026-09-04 ｜ **派单人**：设计师B ｜ **优先级**：P1（真机实测体验缺陷，用户本人踩中）
> **验收标准**：`STD-UPG-94-v1`（content_sha256=`4fca8e5dae1c9d69b27970f6f4f0e2d88b398b5af222e21b5d19043d492ada28`）
> **触发**：用户真机实测（2026-09-04 03:27 装的包）极简模式顶部浮出经典 dock（chips 行+输入条+免责行）；logo 显示为带框牌子（应为透明底无边框）；logo 下方 hint 文字用户拍板删除
> **设计口径**：呈现体系 v1.1 §三 L0 + v1.2 增补 §一（卡面/pill token）；本单含用户两条新拍板（heroHint 删除、logo 零装饰）

---

## 一、一句话

极简模式主页必须是干净底座——经典 dock 在极简态不可见，logo 直接贴画布（零边框零投影），hint 行删除，发送钮归 v1.2 纯黑 pill。

## 二、范围（四件，全是小改）

1. **dock 隐藏路由**（MainActivity.kt）：`applyPresentationMode` 极简分支补 `dockView` GONE、经典分支 VISIBLE——根因：dock（:923 构建、:1772 addView）从未字段化，applyPresentationMode（:7395）只关 topbar+scrollHost；homeWeb append 在 dock 后（:7540）导致 dock 飘顶。**军规 7 豁免注记**：本改=修既有装配行的可见性路由（dock 字段化 1 行 + 路由 2 行），非新增业务面。
2. **logo 零装饰**（assets/home/index.html `#heroLogo`）：删 `border-radius: 32px` 与 `box-shadow`（含 `body.listening` 的绿环投影——listening 态保留 scale 呼吸，投影改无或中性灰）；logo.png 透明底直接贴画布。
3. **heroHint 删除**：删 `#heroHint` 行及其 CSS（用户拍板 @2026-09-04：「logo 下面的文字不需要」）；input placeholder 保留（输入框占位提示是功能件）。
4. **发送钮归 token**：`#btnSend` `background: var(--brand)` → v1.2 §一 pill 主钮纯黑口径（`--primary` 系黑/白双主题），守拍板④。

## 三、红线（违反=打回）

1. MainActivity.kt 纯 CRLF；只动 applyPresentationMode/dock 字段化两处，不碰其他任何行（UPG-93 拆分在途，工具注册区零交集）。
2. home/index.html 改动面=上述 3 处样式/结构，引擎（core.js/registry_seed）零触碰。
3. 能力零缩减；经典模式 dock 显示零回归（经典态 dock 必须原样可见）。

## 四、测试与真机（STD-UPG-94-v1）

- JVM 源码锚 2：①applyPresentationMode 极简分支含 dock GONE 路由；②home/index.html 无 heroHint 节点+heroLogo 无 box-shadow/border-radius+btnSend 非 --brand。
- 全量 0 失败基线绿 + assembleDebug 绿。
- 真机/模拟器四场景截图：①极简主页=dock 不可见（顶部干净）②logo 透明贴画布无牌无影 ③无 hint 行 ④切回经典 dock 原样恢复。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG94_<日期>.md`（锚复绿+四场景截图+Token/KV 两节+「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）+ manifest 自检（硬闸 ok:True 输出附报告）；DEL 绑定分支头（新硬规则：分支头推进=重绑）。
