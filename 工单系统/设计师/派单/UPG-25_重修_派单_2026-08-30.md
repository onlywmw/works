# UPG-25 打回重修派单：UI 瑕疵批修（在当前 main 上重做）

> 出单：设计师 ｜ 日期：2026-08-30 ｜ 打回类型：与已合 UPG-40（换肤）源码/产物双撞，无法干净合入 → 打回在现 main 重做 v2

## 一、为什么打回

UPG-25 原交付（feat/upg25 62986c2）与 **已合入 main 的 UPG-40（换肤标准 v1.1）**：
- **源码同文件双改**：`WorkbenchPage.vue` / `demo.js` / `MainActivity.kt` / `PhotoAskSheet.kt` 两边都改了（审验员「基本无重叠」判断被证伪）
- **全站产物重建碰撞**：两边都跑过 sync-pages，产物 hash 全冲突
- → 无法 rebase/ff-only 干净合入；且 upg25 绝大多数项**仍缺失**（upg40 只覆盖「颜色/token/皮肤」宏层面，UI 细节未动）

## 二、重做范围（规范 v1 §七 13 项，upg40 未覆盖项为主）

| # | 项 | 规范值 | 当前 main 现状锚点 |
|---|---|---|---|
| 1 | chips ▾ | 去除 | MainActivity.kt:1276/1283 `" ▾"`；ChatChips.kt:9/57 |
| 2 | 房间列表时间行 | 固宽不换行+行高固定 | MainActivity.kt:4409 |
| 3 | 设置页结构 | 我的记忆融列表/API Key 挪 AI 模型组/退出登录挪账号卡 | SettingsSheet.kt:134-184 原生段未拆 |
| 4 | 设置行右值 | 单行 ellipsis | SettingsPage.vue cell |
| 5 | sheet 底色 | 全 s0 | SettingsSheet/VaultSheet/WebPageSheet |
| 6 | 原生文本色并轨 | 契约值 | MainActivity 19 处/WebPageSheet 4 处 `0xFF...` 直写 |
| 7 | 字号档 | 10/12/13 | MainActivity.kt:696/5368/5379/5521 |
| 8 | sheet/弹层圆角 | 16/12/8dp | PhotoAskSheet:117；MainActivity:498/4906/5354/5488 |
| 9 | 裸 px padding | dp2px | SettingsSheet:137/143/158/164；MainActivity:408/4450/4910/5078 |
| 10 | JS 直写 danger | 单源 | ModelPage:216/SidebarNav:275,378/VaultPage:408,485 `#d92d20` |
| 11 | 游离色 | primary 系/标准阴影 | WorkbenchPage:257；ChatPage:186 |
| 12 | 顶栏高度 | 46 | tokens.css:155 `.topbar`（56）|
| 13 | MemoryPage ellipsize | 标题1行/摘要2行 | MemoryPageActivity:72,77,147,153,165,176 |

## 三、红线

- **保留 upg40 换肤成果**：tokens.css 契约段 / 候选C 皮肤值段 / colors.xml 运动_ 单源 / RoomApp 气泡 token —— 只追加 upg25 的 UI 细节，**不覆盖不回退 upg40**
- 不引 new 依赖；web 用 CSS 变量，原生用 UiTokens（规范硬规）
- 同文件冲突（WorkbenchPage.vue/demo.js/MainActivity.kt/PhotoAskSheet.kt）需**解冲突保留两边**（upg40 换肤 + upg25 修复），不覆盖

## 四、施工

```
改源码(Vue/Kt) → bun run build（前端设计/mov-vue）+ markstream 重建 → node scripts/sync-pages.mjs → gradle :app:assembleDebug
```

认领：`worktree=mov-upg25v2  branch=feat/upg25-v2`（基于最新 main 重切，规则19：先 fetch+看表）

## 五、验收

- **L1**：全量 `--rerun-tasks` 绿 + design_token_assert / 契约断言
- **L2**：真机 walk-through，每项对照规范值截图（chips无▾/顶栏46/设置三改/字号圆角/ellipsis…）
- **L3**：换肤零回归（候选C 冷调近黑仍在，无品牌绿）

## 六、完成后登记（先表后库）

`工单表.xlsx` 程序员列 `✅UPG-25v2 完成` + 备注 `feat/upg25-v2 <hash>（报告 DELIVERY_UPG25v2_*.md）`；`工单库.md` 状态改「程序员✅完成，待验收」；报告落 `程序员\交付报告\` 写明「已登记两个表」。

---

## 附：仍缺失审计依据（@2026-08-30 当前 main，git grep 实证，供验收对照）

- 项1 chips ▾：`git grep " ▾" main -- app/src/main/java/com/mov/android/ChatChips.kt MainActivity.kt` → 命中 :9/:57/:863/:1276/:1283
- 项6 原生色直写：`git grep -c "0xFF1F2329\|0xFF8A919C\|0xFF5C6470" main -- app/src/main/java/` → MainActivity 19 / MarkstreamView 1 / PhotoAskSheet 3 / WebPageSheet 4
- 项10 danger 直写：`git grep "#d92d20" main -- 前端设计/mov-vue/src/` → ModelPage:216 / SidebarNav:275,378 / VaultPage:408,485
- 项12 顶栏：tokens.css:155 `.topbar` 高 56（规范 46）
- 项3 设置页：SettingsSheet.kt 含「信息管理」原生孤立行未拆
