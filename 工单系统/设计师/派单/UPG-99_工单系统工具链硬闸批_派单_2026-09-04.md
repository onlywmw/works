# UPG-99 派单：工单系统工具链硬闸批（DEL 分支头校验 + standard_id 交叉校验 + 报告模板强制段 + 红线 23 落规）

> **派单时间**：2026-09-04 ｜ **派单人**：设计师B ｜ **优先级**：P2（治理/卫生——止住同族回潮）｜ **施工仓**：工单系统仓（works.git）——**与 MOV 仓在飞单（96/97/98）物理零冲突**
> **验收标准**：`STD-UPG-99-v1`（content_sha256 见冻结区文件）
> **依据（全部为设计师已立裁决的机制化落地）**：①DEL 绑定漂移三连（89/91/94）→ 裁决「sync 投影机器校验 delivery_id 绑定 hash==分支头」；②standard_id 名/指纹张冠李戴（UPG-93 manifest）→ 裁决「审验.py 加交叉校验」；③UPG-94 自纠不报 → 裁决「报告模板加重大回归与自纠强制段+截图随追加」；④manifest 六连回潮根因=手工绕闸 → 裁决「manifest 必须 deliver-gen 机制产出，手工件不收」

---

## 一、一句话

把四条已立裁决从纸面纪律落成机器闸——以后这类回潮由工具拦截，不靠人盯。

## 二、范围（全在工单系统仓）

1. **sync-orders.mjs**：sync 投影时校验库卡 `DEL-*` 绑定 code hash == 对应分支头（git ls-remote 可查）；不一致 → 投影标 `⏳失效` 并输出清单（不阻断投影，但 --check 非零）；显式豁免注记（库卡含「豁免」字样）识别跳过。
2. **审验.py --manifest**：新增 standard_id 交叉校验——manifest 内嵌 `content_sha256` 与 `验收标准冻结区\<单>\` 对应版本 STD 文件重算值比对，名/指纹不符 → ok:False + 明确报错（UPG-93 型笔误机器拦截）。
3. **deliver-gen.mjs**：交付报告骨架模板加两节——「施工期重大回归与自纠」（无则填「无」，不许删节）+「视觉类追加=截图随追加」提示行。
4. **文档落规**：工单系统 README/红线 23 细则明文两条——「manifest 必须 deliver-gen 机制产出，手工件不收」+「分支头推进（交付后追加提交）=DEL 重绑或显式豁免注记，验收/审验/合 main 三处只认分支头」。
5. **自测**：每项带正反案自测（照既有 --self-test/--manifest-self-test 模式扩展）。

## 三、红线（违反=打回）

1. 只动工单系统仓；MOV 仓零行。
2. 既有闸零回归：deliver-gen 硬闸（MANIFEST_REJECT）/审验.py 既有校验/sync 投影 diff=0 纪律全部保持——新校验只加不破。
3. 校验报错人话化（谁、哪条、怎么修）——不产天书。
4. 改库卡类文件前备份 `_备份归档\`（本仓纪律）；sync 后必 --check diff=0。

## 四、测试（STD-UPG-99-v1）

- 四件各自正反案自测全绿 + 既有 self-test（4+2 案）零回归。
- 实战过闸：用一份历史违规 manifest（UPG-93 型指纹错位）喂审验.py → 拦截实证；用一份 DEL 漂移库卡喂 sync-orders → ⏳失效标注实证。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG99_<日期>.md`（四件自测全录+实战过闸两实证+「已登记两个表」）；本仓提交走既有 pre-commit 闸；库加交付块 → sync 投影；DEL 绑分支头。
