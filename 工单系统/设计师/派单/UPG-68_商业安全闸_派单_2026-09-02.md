# 【派单 UPG-68】商业安全闸（W2 白名单自动闸 + 商业面 fail-open 集中收口）

**设计师** @2026-09-02 ｜ **级别**：P0（安全） ｜ **依据**：设计 v3 定稿（`05_审批\审批白名单机器可读化与注册即校验_设计_v1_2026-09-01.md`，二轮大神 9.1/10 补 A7 后冻结）+ 商业模块漏洞预扫描（`契约管理\商业模块_漏洞预扫描_2026-09-01.md`）

**范围（A~E 五子项 + 商业门禁项）**：

- **A 白名单自动闸（原案主体）**：CT-07 机器可读化——单源 `docs/ApprovalRegistry.json`（生成物）+ schema 校验 + `UNKNOWN→ASK` fail-closed（else→ALLOW 即缺陷）+ CI 注册即校验（新 handler 未登记→CI 红）
- **B biz.\* 补登记**：task/booking/onboard 全套写类入名单（onboardSubmit/onboardSet 敏感级——AI 免审批进件=最严重缺口）
- **C PARTNER_AUTH_TOKEN 去硬编码**：迁 CredentialStore（Keystore）+ 服务端下发（源码零硬编码断言）；**partnerHttp 只接受 Credential 句柄/对象**（V69-2a——不可接受裸 String token）
- **D vault 全族收口**：vault.set/credSet/setPhoto/scanPhoto 补登记 + vault.get 伪放行修复（allowed-remembered/turn 一致——完整状态机测试 NONE/ASK/ALLOWED_ONCE/消费→NONE + ALLOWED_REMEMBERED 跨轮 + TURN 失效跨 turn/round/session 回归）
- **E 商业凭证明文治理**：mov_biz/mov_login token 迁 Keystore（与 F4 方针对齐）
- **A7 单执行通道不变量**：Agent→Dispatcher→Guard→Handler；禁 Agent→Handler/Biz API/Vault API/partnerHttp 绕行；CI/静态检查=任何 agent 可达 handler 不经过统一 dispatch/guard → CI 红

**验收判据**（STD 见 05_审批 设计 §四 + 大神 A7 补丁）：
- V68-1 UNKNOWN→ASK fail-closed（else→ALLOW 变异必红）
- V68-2 registry 全量登记（条目数=工具面数，schema 校验 0 错）
- V68-3 注册即校验（新 handler 未登记→CI 红）
- V68-4 既有语义不变（全量 500+ 绿；BP-03 两历史案例→ASK；UI 直调豁免不变）
- V68-5 单源（guard() 不依赖代码内三名单；docs/contracts/ 不存在）
- V68-6 只读不误弹（read 类已登记→FREE）
- V68-7 单执行通道（A7——构造绕行入口→CI 红）
- V69-1 biz.\* 全部登记（摘除名单→红）
- V69-2 PARTNER_TOKEN 源码零硬编码（grep 断言+CredentialStore 加载）
- V69-3 vault 全族收口（伪放行状态机全测+补登记摘除→红）
- **V68-8（增补 @2026-09-02 安全审查补位）** open 模式 fail-closed 不失效：open/FULL_ACCESS 下未登记仍 ASK（变异 else→ALLOW 必红）+ isHighRisk 高危子集照旧 + 模式无关性（registry 判定与 setMode 无关）
- **V69-4（增补 @2026-09-02 安全审查补位）** token 下发链单入口：唯一入口=CredentialStore（拆串/资源/Base64 绕行→CI 红）+ 服务端下发径直接收落 Keystore（无 prefs/明文/日志）+ partnerHttp 只收句柄 + 短期轮换无旧值重放
- **Commercial Entry Gate 六条**：①UNKNOWN→ASK ②注册面⊆Registry ③所有 Agent 副作用统一 Guard ④PARTNER Credential 单入口+Keystore ⑤商业敏感操作 DENY ⑥HTTP Host Allowlist 已落地 → **Commercial Ready = TRUE/FALSE 单判据**（⑥=硬门禁，B5 升级，不并批）

**红线**：不扩功能（不新增模式/名单类型/不动 approval UI）；UI 直调豁免保持；FULL_ACCESS isHighRisk 语义不变；main rebase 后 ff-only 合入。

**交付要求**：分支 `feat/upg68`（基底 main 8e73e8d+已合内容）；交付报告 `DELIVERY_UPG68_<日期>.md` + 变异亲杀记录（V68-1/V68-7/V69-1~3 每条还原必红）+ 登记两表。

**判定**：与 UI 线（feat/upg50-ph1 1A/1B/1C）**文件不相交**（涉及 MainActivity 中部 mcpHandlers——**同线 B 无冲突**；若遇 MainActivity 空白交错=与设计师协调排队）。
