# UPG-01 批 3-2 · 业务与数据安全（26 工具）——派单文本

> 出单：设计师 @2026-08-30 ｜ 优先级：P0（紧随 B3-1，可与 B3-3/B3-4 并行）｜ 方案：`设计师\方案设计\UPG-01_批3批4_方案设计_v1_2026-08-29.md` **v1.1**（评审 P1/P2 修正 + P3 吸收，以此版为准）
> 规则 21 溯源复核：✅ 已过（@main af68d22）。本单 = 批 3 中 **26** 工具，精确清单以派单当天重跑 §一 grep 基线为准（规则 21）。

## 一、交付清单（hostToolMetaB2 登记）

1. **hostToolMetaB2 登记（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB2.kt`，`data class HostToolMetaEntry(description, schema, output)` + `val hostToolMetaB2: Map<String, HostToolMetaEntry>`，收录附录 A 的 26 工具。
   - **desc** ≤60 字：功能 > 限制 > 场景；近邻工具必须写差异点（附录 B）
   - **schema** `paramSchemaDesc` 字段级真描述 ≤15 字/字段 + 「来自 xxx.kt:行」；真无参 = 空 properties
   - **output** 点名顶层返回键（有错误码才附语义）
   - **不碰 MainActivity**：本单纯新文件；聚合由 B3-1（首并入）+ B3-4（收口并入）负责，本单不认领编辑窗口
2. **无额外先行任务**：output 试点只在 B3-1（方案 §1.4 件 3），本单 output 随登记照写（非空断言入 L1）

## 二、验收口径（测试方案，给验收员）

- **L1**（`ToolMetaTest` 扩展）：
  - hostToolMetaB2 遍历：desc 非模板串/无「占位/待补充」黑名单/≥20 字；字段级描述非参数名占位；**output 非空非占位（自动断言）**，顶层键正确性归人工抽查/L2（评审 P3-6）；真无参合法
  - **回落收敛**：本单 26 工具从 MOV-Boot 回落清单消失
  - **变异亲杀两条**：①任一 desc 改占位 → 必红；②接线改回模板串 → 必红
- **L2 真机**（抽样 4 件）：回落清单不含本单工具 + 抽样 AI 面 desc 真描述；截图入 `docs/ACCEPTANCE_LOG.md`
- **L3 AI 实证**：journal 可见正确选参（vault.set 正确传 keys；biz.onboardSubmit 正确走流程）
- **计数断言**：不变量式，不写绝对数
- **全量绿 + assembleDebug + `node scripts/check-token-effect.mjs`**；Token/KV 两节申报（实测校准）

## 三、红线（方案 §五全套，复述硬项）

1. 不改工具行为/调用签名；`McpServer` 方法签名不可变。
2. 描述必须真实（字段语义查源码核实）；拿不准标「待核实」进挂账登记表——**同时挂账 ≤8**，L1 报告随附清单；禁编造、禁「占位/待补充」字样。
3. desc ≤60 字，三段优先级**功能 > 限制 > 场景**；近邻工具须写差异点。
4. 现有已验收描述只增不改（除非事实性错误）。
5. **登记层与接线同批交付**（本单聚合由 B3-1/B3-4 承担，登记完成即接线生效，验收时确认在面）。
6. 请求前缀恒定：不得新增 tools 字段会话中途变动点。
7. 禁新建平行登记结构 / 禁元数据落盘。
8. 回滚 = revert 单 commit。
9. `MainActivity.kt` 纯 CRLF（本单不碰，但动到一律遵守）；Kotlin 块注释内不得嵌 `/* */`；Edit 前必 Read。

## 四、交接段（程序员领单按此执行）

1. 开工前 `git fetch origin` + 看工单库/工单表本单状态（规则 19）；认领登记：工单表本行备注追加 `认领: <agent> worktree=mov-upg01-b3-2 branch=feat/upg01-b3-2 @<时间>`，基于最新 main 切分支。
2. **串行约束**：本单**不碰 MainActivity**（纯新文件 `meta/HostToolMetaB2.kt`），与其余子单零文件冲突；聚合接线由 B3-1/B3-4 负责，若需先行本地验证接线，用独立 worktree 对齐 B3-1 合并后的 `buildToolRegistry`。
3. 完成后登记两个表（先表后库）：`工单表.xlsx` 程序员列 `✅完成` + 备注 `分支名 hash（报告 DELIVERY_UPG01_批3-2_*.md）`；`工单库.md` 状态同步。
4. 交付报告落点 `程序员\交付报告\DELIVERY_UPG01_批3-2_2026-08-30.md`，末尾写「已登记两个表」，含 Token/KV 两节申报 + 回落清单对账。
5. 产物提交 `0027-mov` 分支 `feat/upg01-b3-2`，交付时报 hash。

## 五、附录 A：本单 26 工具 + env/cond（§1.6）

**全部 env=mobile / cond=none**（业务流/保险柜/凭据均为设备本地能力）：

| 工具（26） | env | cond |
|---|---|---|
| biz.onboardStart / Set / Photo / Scan / Submit / Status（6） | mobile | none |
| biz.profInfo（1） | mobile | none |
| biz.bookingAction / biz.bookingMine（2） | mobile | none |
| biz.taskAction / biz.taskClaim / biz.taskMine / biz.taskOpen（4） | mobile | none |
| workflow.run / resume / status（3） | mobile | none |
| vault.list / set / delete / get / setPhoto / scanPhoto / credSet / credDelete（8） | mobile | none |
| credential.getKey / credential.setKey（2） | mobile | none |

## 六、附录 B：近邻差异点速查（撰写必读）

- **biz.onboard\* 6**：Start=发起入驻 / Set=填资料 / Photo=传照 / Scan=扫证 / Submit=提交 / Status=查状态——流程各步，desc 写清「第几步、何时用」
- **biz.task\* 4**：Open=打开任务 / Mine=我的任务列表 / Claim=认领 / Action=执行动作
- **biz.booking\* 2**：Mine=我的预约列表 / Action=对预约执行动作
- **vault.\* 8**：list=枚举 / set=写入条目 / get=读取 / delete=删除 / setPhoto=存照 / scanPhoto=扫描识别照片 / credSet=存凭据 / credDelete=删凭据——**get 与 peek 区分**（peek 是 uiOnly 不上面）；setPhoto/scanPhoto 与 camera.* 的边界（vault 处理已存照片，不调相机）
- **credential.\* 2**：getKey=取密钥 / setKey=设密钥——凭据读取涉及安全，desc 写清「不落明文到返回值」
- 每条 desc 附「来自 xxx.kt:行」锚点，字段语义以 handler 实现为准（红线 2）
