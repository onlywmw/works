# UPG-90 派单：尾巴批修（S-06 打回项 ②⑧ + C7 基线测试非确定性）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P2（尾巴清零批）
> **验收标准**：`STD-UPG-90-v1`（content_sha256=`59e79222de1f4c8dfeb6b4997099742b7c06b10d083a8fcd6adc72273cea0793`）
> **已查坑位库/复用件库**：是。命中：坑 #6「--tests 不中→0 测试假象」（亲杀后必核 XML fresh+tests>0）；坑 #5（时间戳格式）相关
> **并行安全**：本单零 app 代码面（market-web 站 + 测试产物治理）——与 UPG-86/88/89 全不撞

## 一、交接

- 主仓库 `0027-mov`，基最新 main（fea2fae+）开 `feat/upg90` / `mov-upg90`
- 对象：`market-web/`（S-06 ②⑧，mow.kim 侧）+ `app/src/test/java/com/mov/android/c7/C7BaselineGenerationTest.kt` + `docs/c7_baseline_UPG63/`

## 二、范围

1. **S-06 ②**：`market-web/index.html:199` `href="explorer.html>` 引号未闭合修复（修复后提取 href 可解析断言 + 部署/本地点验不 404）。
2. **S-06 ⑧**：`market-web/sms-probe.js`、`make_guide.py`、`make_merchant.py`、`run-pc.sh` 四死文件 `git rm`（真实删除，非留注释）。
3. **C7 非确定性**：`C7BaselineGenerationTest`（:106-107 产物落 `docs/c7_baseline_UPG63/`）每次全量重跑改写 30 会话 jsonl+汇总（时间戳字段）→ 治理二选一（交付报告声明取舍）：①断言/产物剔除时间戳类字段；②产物归 `build/` 目录（不入库）。判据=全量连跑两遍 git status 零 M。

## 三、红线

不动业务逻辑；S-06 其余组（①③④⑤⑥⑦⑨）不在本单（已修/另案）；C7 治理不得降测试有效性（亲杀锚：恢复时间戳写→两跑零 M 断言红）；Token/KV 两节（0/0）。

## 四、交付

报告落 `程序员\交付报告\DELIVERY_UPG90_<日期>.md`（②⑧ 修复实证+C7 两跑零 M 输出+亲杀 2 锚+「已登记两个表」）；S-06 卡闭环销项 + 挂账-C7 销项逐条对应；sync 投影；verify-hash + manifest 自检（红线 23 双闸）。
