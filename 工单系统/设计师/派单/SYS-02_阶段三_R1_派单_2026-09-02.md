# 【派单 SYS-02 阶段三 R1】sync-orders 解析器增强（旧卡字段提取兜底）

**角色**：C 线（基建·延续阶段三）｜ **级别**：P2（迁移前置）｜ **依据**：阶段三设计侧验收裁决（2026-09-02——302 处 diff 大比例为解析器提取不足，非表滞后；3B 迁移暂缓待 R1）

## 一、问题

`sync-orders.mjs` 从工单库卡提取「优先级/设计师/程序员/验收员/合main」字段时，**旧卡（长文本状态段）提取失败**：
- UPG-08 库文本含「P1」「设计师✅」「程序员✅R2」——sync-orders 报 `优先级缺失` / `库=—`
- 8 张卡（UPG-01/03/05/08/09/21/49/68）优先级缺失——**文本在库但提取不到**

## 二、范围

1. **字段提取规则增强**（`工单系统\审验员\sync-orders.mjs`）：
   - 优先级：多形态锚（`优先级：P0/P1/P2/P3` / `**优先级**：P1` / 卡标题附近 `（P1·…` / 状态行内 `｜ 优先级：P1`）
   - 设计师/程序员/验收员/合main：卡文本锚（`出单人` / `✅C 完成…` / `✅ 验收通过…` / `✅**已合 main**` / `✅*已合 main` 等形态）+ 长文本段兜底（按关键词窗口截取）
   - **提取失败→输出警告（保持不猜）**——但至少覆盖现有已知形态
2. **回归**：8 张卡提取命中（UPG-01/03/05/08/09/21/49/68——优先级+主要字段）
3. **重评估**：真实表 `--check` → diff 报告（预期 302→显著下降；**仍不 --sync**——由设计师 R1 后裁决 3B）

## 三、验收判据

- R1-1 八张卡优先级均提取成功（此前报缺失）
- R1-2 UPG-08 设计师/程序员/验收员提取与表一致（抽样）
- R1-3 真实表 --check diff 数 < 302 且报告含「提取失败剩余」清单（诚实）
- R1-4 未对真实表 --sync（红线上守）；副本表验证完整（--table 副本）
- R1-5 审验.py 零回归

## 四、红线

1. 只动 sync-orders.mjs（审验员目录）；0027-mov 零接触
2. 提取失败=警告不猜（不造值）
3. 真实表不 --sync（3B=设计师裁决后单独执行）

## 五、交付

分支 `feat/sys02-ph3`（继续——R1 commit 追加）；报告 `DELIVERY_SYS02_阶段三_R1_<日期>.md`；README §六 阶段三行标注 R1 进行中/完成。

## 六、备注

- 迁移顺序参考：R1 通过 → 设计师裁决 3B（--sync 前自动备份 _备份归档\）→ E4 阶段四（依赖 E1 ts + 干净表基线）
