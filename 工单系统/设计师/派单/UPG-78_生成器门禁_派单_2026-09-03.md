# UPG-78 派单：生成器派生项入库保鲜 + CI diff=0 门禁（ApprovalRegistry 系）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P2
> **验收标准**：`STD-UPG-78-v1`（content_sha256=`26266fe8bc20830cc3117e871116255bc91e2962f7a74f5d9f543dbcf9417670`）——三处对账
> **已查坑位库/复用件库**：是。命中条目：坑 #2「工单表并发写入五步铁律」（适用登记环节）；复用命中=UPG-70 A1-2 门禁模式（`upg70-catalog.yml`：gen+`git diff --exit-code`+JVM test，本单推广同款到 ApprovalRegistry 系）
> **溯源复核**（红线 20/21，@main 6dd9161 已做）：分层溯源图见工单库 UPG-78 卡；关键锚——`ApprovalRegistryGeneratorTest.kt:27`（读 `build/inventory/tools.txt`）/ `app/build.gradle*` 无 collect 引用（grep 实证空=断链）/ `approval-inventory-collect.mjs:57-58`（产出 tools.txt）/ `upg68-registry.yml:30-40`（collect→生成器测试→verify→单通道→全量，**无 git diff 步**）

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（工作树停在 feat/upg50-ph1 且有未提交改动——**开工先 `git fetch origin` + 基于最新 main（6dd9161）重开分支**，红线 19） |
| 分支 / worktree | `feat/upg78` / `mov-upg78`（开工前工单表备注登记认领） |
| 构建 / 单测 | `gradle :app:assembleDebug` / `gradle :app:testDebugUnitTest`（ANDROID_HOME 需指向 `D:\Android\Sdk` 或有 local.properties——worktree 无 local.properties 时必现 SDK location not found） |
| 关键文件 | `app/src/test/java/com/mov/android/ApprovalRegistryGeneratorTest.kt`、`scripts/approval-inventory-collect.mjs`、`scripts/approval-registry-verify.mjs`、`app/build.gradle*`、`.github/workflows/upg68-registry.yml` |

## 二、一句话

让「生成器→生成物」链路自供电、保鲜、无污染：测试不再依赖手工先跑 collect（干净环境全量绿）；CI 加 `git diff --exit-code` 门禁（生成物漂移即红）；生成物入库后全量连跑零 M（UPG-75/76 的手工还原成为历史）。

## 三、施工范围

1. **A1 测试自供电**（二选一，优先 A，偏离需在交付报告说明）：
   - **A（建议）**：`app/build.gradle*` 注册 `approvalInventoryCollect`（Exec：`node ../scripts/approval-inventory-collect.mjs`）+ `testDebugUnitTest.dependsOn` 挂接——构建图内消解断链；
   - **B**：`ApprovalRegistryGeneratorTest` 内 tools.txt 缺失/过期时自动跑 collect 脚本（ProcessBuilder），零构建图改动。
   - 判定锚：`ApprovalRegistryGeneratorTest.kt:27` 目前直接读 `build/inventory/tools.txt`；`app/build.gradle*` grep 无 collect（断链实证）。
2. **A2 CI 漂移门禁**：`.github/workflows/upg68-registry.yml` 在「单源独立对账（verify）」之后追加一步：`git diff --exit-code -- docs/ApprovalRegistry.json docs/ApprovalRegistry.md app/src/main/kotlin/com/hermes/dsh/tools/PermissionRegistryData.kt`——生成物与代码漂移即红（UPG-70 `upg70-catalog.yml` 同款模式，照抄不新造）。
3. **A3 污染结构性消除**：A1+A2 落地后，交付报告声明「全量测试后三生成物 diff=0，本单起不再发生手工还原」；若 A1 选 A，注意 collect 任务输出仍落 `app/build/`（gitignored，不产生新污染）。

## 四、红线

1. **不改生成器逻辑**（ApprovalRegistryGeneratorTest 的生成规则/合并语义零改动）；不改 `docs/ApprovalRegistry*` 人工输入文件（semantics/categories）；不改 guard 消费侧。
2. 门禁路径**限定三生成物**——不得全仓 `git diff --exit-code`（autocrlf 假阳性坑：挂账-upg70-autocrlf误报，CI Linux 为权威）。
3. 不动 `upg70-catalog.yml`；不动 C7 基线测试（另挂账在册）。
4. **Token / KV Cache 两节申报**（预期 0/0，零请求链路，也要写）。
5. 亲杀三锚全做（STD）：断链复现红→绿 / 门禁注入非 0 / 连跑零 M。

## 五、测试方案（验收锚=STD-UPG-78-v1）

- A1：删 `app/build/inventory` 后 `:app:testDebugUnitTest --tests ApprovalRegistryGeneratorTest` 绿；删后**全量**绿。
- A2：改 handler 登记面不重跑（或改生成物一行）→ 本地复演门禁命令非 0；恢复=0。
- A3：`--rerun-tasks` 连跑两遍，三生成物 md5 前后一致、git status 零 M。
- 回归：collect/verify/single-channel 三脚本正常；assembleDebug 绿。

## 六、交付与登记（红线 7/12/23/24）

1. 交付报告落 `程序员\交付报告\DELIVERY_UPG78_<日期>.md`——含 Token/KV 两节、三亲杀输出（带时间戳）、「不再发生手工还原」声明、演示数据还原声明。
2. **完成后立即登记两个表**（库加交付块 → `node 审验员/sync-orders.mjs --sync` 投影），报告写「已登记两个表」。
3. 登记前 `审验员\审验.py --verify-hash feat/upg78 <sha>`（红线 23）。
4. 交付绑定 DEL-UPG-78-<日期>-<NNN> + 三重 hash；共享面=构建/CI 面，附《共享面影响清单》+ coverage_status（预期 FULL）。
5. CI 改动（A2）合 main 后**首跑结果**需回看并在报告/卡补记（upg68-registry 在 push main 触发）。
