# UPG-87 派单：内置包启停真实生效（宿主工具组纳入 builtin 包机制）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（市场线真 bug——挂账双案实证在册）
> **验收标准**：`STD-UPG-87-v1`（content_sha256=`805aa4e3` 前缀，全文见冻结区文件）
> **已查坑位库/复用件库**：是。复用命中=browser builtin 热挂/摘除机制（b3219a6/8fdc8d9 已验证——本单复用其通道）；坑库无直接命中
> **溯源复核**（红线 20/21，@main fe8cd45 已做）：宿主组定义 `LocalOverview.kt:32/37`（device-control/scene-12306/obsidian）；`McpMarket.setEnabled` builtin 分支 `:354-365` 只认 registry 包（宿主组不在→MARKET_NOT_INSTALLED 实证）；`unmountExtTools:5973` prefix 匹配（「device-control」摘不到「device.*」实证）；两挂账双案在册

## 一、交接

- 主仓库 `0027-mov`，基最新 main 开 `feat/upg87` / `mov-upg87`（与 UPG-82 已合/其他单零重叠）
- 关键文件：`McpMarket.kt`、`MainActivity.kt`（纯 CRLF）、`LocalOverview.kt`、`PermissionRegistryData.kt`（只读）
- 构建/单测/真机照常；真机=平板 `192.168.2.3:5555`

## 二、范围

1. **纳入 builtin 机制**：device-control/scene-12306/obsidian 三宿主组登记为 kind=builtin 包（builtinTools 名单=注册表/常量**单源**，禁手抄两份）。
2. **启停真实生效**：market.enable/disable 对三组——禁用=摘除 mcpHandlers+allowedTools+MCP 面同步+落盘（builtinStates）；启用=热挂恢复；重启保持。禁用后 agent 调用=TOOL_NOT_FOUND。
3. **默认全量启用**（能力零缩减红线：启停=用户显式动作，默认态不阉割）。
4. **测试**：启停套件（摘除/热挂/重启保持/TOOL_NOT_FOUND/名单完备）+ 3 变异锚亲杀（STD）。

## 三、红线

不改审批/guard 语义；摘除名单单源（禁双写）；MainActivity 纯 CRLF；Token/KV 两节申报（工具面随用户启停变化——会话内恒定规则不动）。

## 四、交付

报告落 `程序员\交付报告\DELIVERY_UPG87_<日期>.md`（3 亲杀+真机启停实证+两挂账销项对应+「已登记两个表」）；sync 投影；verify-hash（红线 23）；DEL 绑定；共享面=工具面，附 coverage_status。
