# UPG-81 派单：基线 2 失败契约锚同步修（AppearanceContractTest 适配收拢版现实）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P2（消灭「pre-existing」口径，恢复全量 0 失败）
> **验收标准**：`STD-UPG-81-v1`（content_sha256=`c3f7009aec19a980a893065574d18257e0a96ff4ddd74e510768667e4d348395`）
> **已查坑位库/复用件库**：是。命中条目：无直接命中（计数纪律适用：全量计数 XML 为准并注明统计时点）
> **拍板依据**：`程序员\交付报告\UPG70_裁决记录_2026-09-02.md` 裁决项 1——处置=**契约锚同步修**（断言适配收拢版硬编码 px 现实），归属阶段 1 维护单；当事提交 3eb8372/b3fdcf9 不追责

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（基最新 main（6dd9161）开 `feat/upg81` / `mov-upg81`；注意与 UPG-78/79 并行：本单只动 `app/src/test/java/com/mov/android/appearance/AppearanceContractTest.kt`，与两单零重叠） |
| 失败案（修前实证） | `AppearanceContractTest.kt:56` `L2-9 1B 高频组件切换契约_实例分发到独立应用方法` + `M-U50-5 预览卡 render-only 无交互`（近三次全量实测均为这两案，基线 b3fdcf9 对照同红） |

## 二、一句话

把两个契约断言从「旧设计稿口径」同步到「收拢版现实」——全量首次回到 0 失败，以后没人再拿 pre-existing 当借口。

## 三、施工范围

1. 复跑实证修前 2 红（留 XML/输出，注明统计时点）。
2. 逐案对账：断言期望值 vs 收拢版样式现实（`.back{font-size:20px}` 等硬编码，以实际样式源文件为准引用行号）→ 修订断言为现实口径，并在断言消息里写清「收拢版现实锚（来源文件:行号）」。
3. 修后全量 `--rerun-tasks` **0 失败**（不得用 skip/豁免消音）；亲杀锚：断言改回旧值或破坏对应样式属性 → 红。

## 四、红线

1. **零样式源码改动**——收拢版 Vue/CSS/tokens 一律不碰（拍板方向=修断言不是改样式）；只动测试文件（必要的注释除外）。
2. 断言必须保持有效性（亲杀实证），不得削成摆设。
3. Token/KV 两节申报（0/0）。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG81_<日期>.md`（修前 2 红 + 修后 0 失败 XML、diff、亲杀记录、Token/KV 两节、「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）。
