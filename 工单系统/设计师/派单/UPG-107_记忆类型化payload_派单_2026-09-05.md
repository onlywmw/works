# UPG-107 Memory OS 类型化 payload · 派单

> **派单人**：设计师 ｜ **日期**：2026-09-05 ｜ **优先级**：P1
> **设计文（唯一施工口径）**：`设计师\方案设计\04_记忆\Memory_OS_类型化payload_设计_v1_2026-09-05.md`（Memory OS v2 冻结版的补丁）
> **验收标准（冻结版）**：`STD-UPG-107-v1`（content_sha256=235736101497215325714ded45bab6e6f797aa14865b4467d31f672ddedf7c30）｜ **验收执行细则**：`设计师\方案设计\04_记忆\Memory_OS_类型化payload_验收脚本_v1_2026-09-05.md`（八步，做完照走一遍）
> **已查坑位库/复用件库**：是 ｜ 命中：已知坑 #6（--tests 0 测试假绿——验收脚本已带防护）、#8（瞬态 UI 走查=录屏法——真机步骤已引用）；复用件无域内命中

---

## 一、基线与认领

- 主仓 `E:\mov归档\0027-mov`；开工前 `git fetch origin`，基于最新 origin/main 开 **分支 `feat/upg107` / worktree `mov-upg107`**
- 认领登记（开工前必先做）：工单表 UPG-107 行备注 `认领: <你> worktree=mov-upg107 branch=feat/upg107 @时间`

## 二、施工范围（照设计 v1 §二/§三，四件）

1. **SemanticEntry 扩展**：加 `payload: Map<String, String>?` + `payloadVersion: Int?`——可选字段，null=纯文本条目仍是一等公民（`memory-os/.../semantic/SemanticEntry.kt`）
2. **六类模板注册表**：PREFERENCE{场景,取向,强度}/HABIT{行为,频率,场景}/FACT{主体,谓词,值}/IDENTITY{维度,值}/RELATION{对象,关系,强度}/PROGRESS{目标,进度,里程碑}
3. **LLM 抽取+字段校验闸**：LLM 只出模板字段草案 → 校验（超枚举拒绝）→ 状态=PROPOSED；**抽取失败=降级纯文本入库（payload=null），不得报错阻塞**
4. **池子管理三操作升级**：去重（同 type+关键字段相等→合并候选）/冲突（字段级互斥必检出→RE_EVALUATE）/合并（字段级合并提案，保留双来源链）——全部人裁决，不自动

## 三、红线（违反=打回）

- payload **永不直写 ACTIVE**（走 UPG-52 既有四态状态机，人确认才 ACTIVE）
- 模板演进**只加字段不改语义**（payloadVersion 追溯）
- UPG-52 既有 27 用例 + UPG-05/22 打点链 + memory-api 契约**零退化**
- Memory OS 五条 P0 不破（事实认知分离/历史不可变/AI 无权改/检索只读/删除不复活）
- `MainActivity.kt` 只出不进——本单零 MainActivity 改动预期，确需改先报设计师

## 四、验收

按 `STD-UPG-107-v1` + 验收脚本八步执行；3 变异锚亲杀（M1 直写 ACTIVE/M2 降级改报错/M3 删字段比对——每个「改→必红→还原→复绿」）；真机 5 场景；一票否决 6 条见 STD。

## 五、交付物与交接

1. 交付报告 `程序员\交付报告\DELIVERY_UPG107_*.md`：验收脚本八步逐步证据 + Token/KV 两节（payload 抽取走 LLM 通道，**token 增量必报**）+ 《共享面影响清单》+coverage_status（SemanticEntry=全局数据结构，红线 24）+ 「已登记两个表」声明
2. 交付绑定：`审验员\deliver-gen.mjs --ticket UPG-107 --branch feat/upg107 --hash <sha> --app-path "E:\mov归档\0027-mov" --evidence <证据…>` 机制产出 manifest → `审验.py --manifest` ok:True → `审验.py --verify-hash feat/upg107 <sha>` HASH_OK → 登记两表（先表后库，xlsx 五步铁律）
3. 证据落 `程序员\UPG107-evidence\`

**候选施工**：任意线可领，先到先得
