# UPG-79 派单：审批呈现层组件化（ApprovalSurface + CardShell v1 视觉 + 渠道三修）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（总纲 R1=审批体系最后一块）
> **验收标准**：`STD-UPG-79-v1`（content_sha256=`97f756ffb01ee27ad86e15ae8bb8ee482869dae340db0e1cc116f9e585cb91cf`）——三处对账
> **已查坑位库/复用件库**：是。命中条目：坑 #2（登记五步铁律）；复用命中=**审批卡片_组件设计_v1 + demo v2**（用户拍板视觉口径，`设计师\设计预览\审批弹窗_重设计_demo_v2_2026-09-03.html`，照抄结构/令牌不自由发挥）
> **溯源复核**（红线 20/21，@main 6dd9161 已做）：分层溯源图见工单库 UPG-79 卡；关键锚——answerer 装配 `MainActivity.kt:4361` / buildApprovalDialogView `:7772` / showApprovalPanel `:7609` / showPlanSheet `:5676` / refreshApprovalChip `:7597` / approvalDialogDeferred `:418,:4371` / closeApprovalSurface `:7585` / ACTION_ALLOW 仅 onNewIntent `:8191` / `areNotificationsEnabled` 全仓零命中（P2-a 实证）/ onQueueChanged `ApprovalService.kt:91,:282`
> **并行提示**：可与 UPG-78（生成器门禁：build.gradle/CI/scripts 面）并行施工——文件面不重叠（本单=MainActivity/NotificationAnswerer/新增 UI 组件；UPG-78 不碰这些）；同一 worktree 纪律照红线 9。**防撞硬约束：本单组件测试一律用现有测试基建（纯 JVM 断言 + 假 answerer/deferred 注入，照 ApprovalQueueTest/PlanApprovalStoreTest 写法），禁引新测试依赖、禁动 `app/build.gradle*`**——与 UPG-78 的唯一潜在撞点就是 build.gradle；施工中若确需新依赖（如 Robolectric），先停手申报，排合序（UPG-78 先合后 rebase 再动），两单不得同时改 build.gradle。合 main 单点：先合者胜，后合 rebase 重跑全量再报 hash。

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（工作树停在 feat/upg50-ph1 且有未提交改动——**开工先 `git fetch origin` + 基于最新 main（6dd9161）重开分支**，红线 19） |
| 分支 / worktree | `feat/upg79` / `mov-upg79`（开工前工单表备注登记认领） |
| 构建 / 单测 | `gradle :app:assembleDebug` / `gradle :app:testDebugUnitTest`（worktree 无 local.properties 时设 `ANDROID_HOME=D:\Android\Sdk`） |
| 真机 | 模拟器 `emulator-5556`/平板 + `adb forward tcp:8389`（curl 触发审批，**不需要 AI key**）；通知权限场景在系统设置手动开关 |
| 关键文件 | `MainActivity.kt`（纯 CRLF，Edit 前必 Read）、`NotificationAnswerer.kt`、`ApprovalService.kt`（只读数据源，不改语义）、`AndroidManifest.xml`、新增 ApprovalSurface 组件（建议 `com.mov.android.ui` 或 MainActivity 同包） |

## 二、一句话

把审批的「脸」收口成一个组件：ApprovalSurface 唯一组件、ApprovalService 队列唯一数据源、三形态（悬浮卡/通知/待办面板）+ 预审单卡四个投影；视觉按拍板的 CardShell v1；顺手修掉通知权限静默、冷启动 action 丢失、僵尸窗竞态三个 P2。

## 三、施工范围

1. **A1 ApprovalSurface 组件**：新建组件收编五处——`buildApprovalDialogView`（:7772）/`showApprovalPanel`（:7609）/`refreshApprovalChip`（:7597）/`closeApprovalSurface`（:7585）/`showPlanSheet`（:5676）；MainActivity 只留 answerer（:4361）/onQueueChanged（:4408）/presentationCanceller（:4405）装配挂点。组件对 ApprovalService 只读（pendingList/onQueueChanged/present 回调），**禁自维护第二份状态**。
2. **A2 CardShell v1 视觉落地**（照 `审批卡片_组件设计_v1` + demo v2）：居中悬浮卡（16px 圆角/s4 面/scrim）；环形倒计时（30s 消退、≤10s 变红、环心=队列 i/N、1/1 留空）；参数卡（key-value 脱敏）；豁免=单勾选「记住，同类操作不再问」（only-once 无勾选，换 🔒 一行小字）；MONEY/支付行锁定明示；按钮 **[拒绝] 左 [同意] 右**；纯黑白 + 唯一功能红；令牌引用 tokens 体系，**禁硬编码色值**（现原生 AlertDialog 大白话样式退役）。预审单卡同壳（步骤清单/勾选/支付行，机制沿用 UPG-76 不重构）。
3. **A3 渠道三修**：
   - **通知权限**：`NotificationAnswerer.show` 前查 `areNotificationsEnabled()`（API 33+）——未授权：**前台→改走弹窗分支；后台→如实 return null**（fail-closed 不变）+ 日志不谎称「已发出」+ 足迹落「通知未授权」一条。
   - **冷启动 action**：`onCreate` 处理 ACTION_ALLOW/ACTION_DENY（转发 onNewIntent :8191 同路径）；`ApprovalActionReceiver` 死代码裁决（删或启用，报告声明）。
   - **僵尸窗竞态**：`approvalDialogDeferred`（:4371 先赋值）与 UI 建窗（:4377）窗口期——建窗 UI 块内 `show()` 前查 `deferred.isCompleted`（或赋值移入 UI 块，二选一），先 cancel 后建窗不出僵尸窗。
4. **A4 组件级测试**（呈现层测试从零到有）：投影一致性（队列→chip/顶栏/面板同源）、only-once 无勾选行、超时到点=cancelled、僵尸窗守卫、通知权限检查、冷启动转发。

## 四、红线（违反=打回）

1. **L0/L1 零改动**：guard.decide/isGranted/FIFO/request 语义不动；UPG-76 机制零改动（PlanApproval 绑定/扫描编排/Store 逻辑——仅 UI 收编）；fail-closed 不变；only-once 任何模式只收 allowed-once。
2. 组件对 ApprovalService 只读——禁第二数据源、禁组件内改队列。
3. MainActivity.kt 纯 CRLF；Kotlin 块注释不嵌套；变异亲杀 5 锚全红→还原复绿。
4. **Token / KV Cache 两节申报**（预期 0/0，零请求链路——审批 UI 不进 AI 面）。
5. 演示数据必还原+生产态复核声明。

## 五、测试方案（验收锚=STD-UPG-79-v1）

- 五变异锚亲杀（见 STD）；新增 ApprovalSurface 套件；既有审批系套件（PermissionGuard/OnlyOnceGuard/ApprovalQueue/McpServerApproval/PlanApproval 32）零回归；全量 `--rerun-tasks` + assembleDebug 绿。
- **真机 L3 四场景（curl 触发，不需 AI key）**：① curl vault.get → 悬浮卡呈现（对照 demo 截图）→ 同意→明文+足迹；② 后台通知→点允许→放行 + **杀进程冷启动点通知→决策生效**；③ 系统关通知权限→后台触发→无谎言日志+足迹「通知未授权」；④ 并发 2 pending → chip=2+环心推进+超时自动拒绝→次条自动出现。
- 视觉证据：真机截图与 demo v2 对照（悬浮卡/环/参数卡/按钮序）。

## 六、交付与登记（红线 7/12/23/24）

1. 交付报告落 `程序员\交付报告\DELIVERY_UPG79_<日期>.md`——Token/KV 两节、5 变异亲杀、真机四场景证据（截图含时间戳+curl/logcat 输出）、演示数据还原声明、「已登记两个表」。
2. 库加交付块 → `node 审验员/sync-orders.mjs --sync` 投影；登记前 `审验员\审验.py --verify-hash feat/upg79 <sha>`。
3. 交付绑定 DEL-UPG-79-<日期>-<NNN> + 三重 hash；共享面=审批呈现面，附《共享面影响清单》+ coverage_status（真机交互面预期 PARTIAL，设计师裁决位留空）。
4. 三挂账（僵尸窗/通知权限/冷启动）随本单交付销项——交付报告逐条对应。

## 七、上下文（施工前必读）

- 视觉口径：`设计师\方案设计\01_外观\审批卡片_组件设计_v1_2026-09-03.md` + demo v2（**照抄结构/令牌**）
- 总纲：`审批体系_总纲_v1`（R1 定位）+ 评审增补
- 卡：`工单库.md` UPG-79（含分层溯源图）
