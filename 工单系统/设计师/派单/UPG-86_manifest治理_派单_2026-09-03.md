# UPG-86 派单：manifest 治理（存量补齐 + 审验.py 机器可验性强化）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P2（治理卫生——同族四现根治）
> **验收标准**：`STD-UPG-86-v1`（content_sha256=`4b2ce3a7` 前缀，全文见冻结区文件）——处理中心\验收标准冻结区\UPG-86\
> **已查坑位库/复用件库**：是。复用命中=脚本-审验.py（--manifest 已有，本单强化）；坑库无直接命中

## 一、交接

- 工作区：`Desktop\MOV\工单系统\`（治理面）+ `审验员\审验.py`（校验强化）——**不动 0027-mov 业务代码**
- 存量对象：`处理中心\delivery_UPG50/55/68/82/85_manifest.json` + `程序员\交付报告\*_manifest.json`（UPG-76/77/78/79/81/84 等）

## 二、范围

1. **存量补齐**：全部存量 manifest 机器可验化——路径裸串（去嵌注释）、每条 evidence 补 sha256、manifest_sha 绑定值写入文件（可重算对账）；缺失证据如实标 missing，**不造假不补造**。
2. **审验.py 强化**：`--manifest` 新增三类失效检测（路径嵌注释/缺 sha256/绑定值未写入）→ ok:False+明确诊断；fixture 三坏案红+好案绿亲杀。
3. **销项**：挂账-deliveryManifest指纹治理 + 挂账-upg70-manifest缺口。

## 三、红线

历史交付内容零改动（只修清单形态）；审验.py 其余子命令零回归（fixture 自测 PASS）；Token/KV 0/0。

## 四、交付

报告落 `程序员\交付报告\DELIVERY_UPG86_<日期>.md`（三坏案亲杀+存量自检全绿输出+「已登记两个表」）；库加交付块 → sync 投影。
