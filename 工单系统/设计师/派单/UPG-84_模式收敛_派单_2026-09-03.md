# UPG-84 派单：模式收敛 5→2（工具面去模式化 + SDK 匝道段删除）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（用户拍板：只要两种模式——经典+极简；极简只是画面极简，**不能阉割能力**）
> **验收标准**：`STD-UPG-84-v2`（content_sha256=`b279e9788beaead9e812f5583fbecd1ada3e7d9165c732ca581c4e7343bd8416`；v1 留档——v2 增补「快速/深度思考开关收敛：极简=快速、经典=深度思考」用户拍板 @2026-09-03）
> **已查坑位库/复用件库**：是。命中条目：坑库无直接命中；复用=UPG-76 扫描 READ-only 收缩机制（`preApprovalScanActive`+`PlanApprovalScan`——与模式枚举无关，本单不得误伤）
> **溯源复核**（红线 20/21，@main 5cf546d 已做）：`presentationMode` 默认 BOTH（`MainActivity.kt:350`）/ prefs 读取 `:471` / 五模式过滤 `:7426-7434` / `codeTools` `:369` / codeSdk 注入段 `:5150-5161` / `modeLabel()` `:7328`+`:5182` 拼串 / 循环切换 `:7537-7548` / `presentation.set_mode` handler `:4171-4188` / uiOnlyMcpTools `:338` / `ToolSdkGenerator.kt`+Test / `ToolPresentationMode`（`Tools.kt:202`）

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（基最新 main（5cf546d）开 `feat/upg84` / `mov-upg84`；与 UPG-80/81/82 并行零重叠） |
| 构建 / 单测 | `gradle :app:assembleDebug` / `gradle :app:testDebugUnitTest`（无 local.properties 时 `ANDROID_HOME=D:\Android\Sdk`） |
| 真机 | 平板 `192.168.2.3:5555`——**prefs 现存 code，正是回归锚设备** |

## 二、一句话

把 E3 时代的五模式循环从用户面退役：工具面固定全量（both）——经典/极简两种模式只是画面范式差异，能力不阉割；SDK curl 匝道段随 code 模式一并删除。

## 三、施工范围

1. **A1 工具面固定全量**：`rebuildAgentTools`（:7426-7434）移除五模式过滤分支（固定=非 uiOnly 全集）；`ToolPresentationMode` 枚举/循环切换逻辑（:7537-7548）/prefs 读取（:471——旧存 code 等值**自动回落 BOTH**，不写迁移数据只改读取语义）全清理；**顶部按钮保留**（=唯一模式切换入口），断开五模式循环改两态「经典⇄极简」（极简侧占位提示，见附录 D1）。
2. **A2 SDK 匝道段退役**：codeSdk 注入段（:5150-5161）整段删除；`ToolSdkGenerator.kt` + `ToolSdkGeneratorTest.kt` 删除（删除优于新增）；`codeTools`（:369）退役。
3. **A3 模式类清理**：`presentation.set_mode` handler（:4175-4188）退役 + `uiOnlyMcpTools` 名单同步；系统提示「当前工具面模式：…」声明删除（:5182 拼串——固定后无信息量）。
4. **A4 测试处置**：grep 全部引用 `ToolPresentationMode`/`codeTools`/`ToolSdkGenerator`/`presentation.set_mode` 的测试，逐个删/改并出清单（**禁 skip 消音**）。
5. **A5 快速/深度思考开关收敛**（STD v2 增补）：模型 chip 的快速/深度思考单选移除（`MainActivity.kt:943-953`/`:1189` 区）；prefs `mov_chat_mode` 的 `mode` 值退役（读取语义固定，旧值忽略）；**reasoning 档位绑定模式**——极简=快速（reasoning effort 无 + 简洁提示词节，与极简批提示词合并设计）、经典=深度思考（reasoning high，`:5113` 装配点改为按模式恒档）；**非自动分流**（关键词自动判深浅有前科被砍 `:5041`，本方案=按模式恒档，不犯同一病）；任何界面不再出现第三组模式开关。

## 四、红线（违反=打回）

1. **能力零缩减**：工具面=全量（both），任何工具不得以「极简」名义从面里移除（用户拍板红线）；审批/UPG-68/76/77/79 语义零改动；UPG-76 扫描 READ-only 收缩机制（`preApprovalScanActive` 路径）零回归。
2. **Token / KV Cache 两节必报**：system prompt 变短（删 SDK 节+模式声明）+ 跨会话前缀变化一次（量级说明；会话内恒定）。
3. MainActivity.kt 纯 CRLF；变异亲杀 2 锚全红→还原复绿；测试处置禁 skip 消音。
4. 两态开关「经典⇄极简」**不在本单**（归极简批阶段 1——极简主页建成后再接）。

## 五、测试方案（验收锚=STD-UPG-84-v1）

- 2 变异锚亲杀；工具面全集计数断言；审批系+UPG-76 扫描套件零回归；全量绿；assembleDebug 绿。
- **真机 L3**：平板（prefs 现存 code）装新包启动 → 工具面自动全量 → 输入「帮我读取保险柜里『商户名称』的凭据内容」→ 直弹敏感 only-once 卡（单次审批，无 shell.exec 写入卡）——UPG-83 场景以「模式收敛」方式回归销账。

## 六、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG84_<日期>.md`（Token/KV 两节+2 变异亲杀+测试处置清单+真机回归证据+「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）；DEL 绑定三重 hash；共享面=工具面/系统提示面，附 coverage_status；挂账-code模式curl匝道双重审批 已在册销项（根因消除）随交付引用。

## 附：模式落脚点清单（删/改/留 · 2026-09-03 全仓盘点 @main 5cf546d——按单施工，不乱删）

### 删（用户面开关 + 匝道 + 登记）

| # | 落脚点 | 处置 |
|---|---|---|
| D1 | 顶部「极简模式」按钮（`MainActivity.kt:893-897`） | **保留**（用户拍板：此钮=唯一模式切换入口，位置/图标不变）——断开五模式循环，改两态「经典⇄极简」；极简主页建成前极简侧=占位提示「极简模式即将上线」，极简批阶段 1 点亮 |
| D2 | codeSdk 注入段（`:5150-5161`）+ `ToolSdkGenerator.kt`+Test + `codeTools`（`:369`） | 整段/整文件删除 |
| D3 | `presentation.set_mode` handler（`:4175-4188`）+ 登记行（`PermissionRegistryData.kt:136`）+ 名单条目（`McpToolScheduler.kt:204` uiOnlyTools、`MainActivity.kt:338` uiOnlyMcpTools） | 三处同步删除+生成器重跑（UPG-68 注册即校验机制，UPG-78 后 dependsOn 自动） |
| D4 | 模型 chip 快速/深度思考单选（`:943-953` UI + `:1189` handler）+ ChatChips 模式名标签位（`ChatChips.kt:31`） | 删单选 UI 与分支；chip 保留「模型选择」本体 |
| D5 | 系统提示「当前工具面模式」声明（`:5182` 拼串 + `modeLabel()` `:7328` + Boot 日志 `:7466` 模式段） | 删声明（固定后无信息量）；Token/KV 申报 |

### 改（机制保留，只换数据源）

| # | 落脚点 | 处置 |
|---|---|---|
| M1 | `rebuildAgentTools` 五模式过滤（`:7426-7434`） | 固定全量（非 uiOnly 全集），删分支 |
| M2 | prefs 读取（`:471` readPresentationModePref + `mov_chat_mode.xml` mode 值） | 读取语义固定回落（旧存 code/deep 值忽略，不写迁移数据） |
| M3 | reasoning 装配（`:5041`/`:5113`） | 档位源：prefs mode → **按模式恒档**（极简=off+简洁提示词节/经典=high）；DeepSeekAdapter/AgentLoop 的 reasoning 执行机制**保留不动** |
| M4 | 测试：`CodeModeWiringContractTest.kt` / `Upg27FixContractTest.kt` 及全部模式引用 | 逐个删/改出清单（禁 skip 消音） |

### 留（不误伤·红线）

| # | 落脚点 | 理由 |
|---|---|---|
| K1 | GoalDomain / `goalmode`（目标模式） | 是功能不是模式开关（UPG-07 资产），零触碰 |
| K2 | Token 统计 reasoning 字段（`:3266-3271`） | 用量统计，与开关无关 |
| K3 | ApprovalLogic / 审批面 | 与呈现模式零耦合（grep 实证零命中） |
| K4 | `ToolPresentationMode` 枚举（`Tools.kt:202`） | 引用清零后删除；删前最后一步核对（防 UPG-76 扫描期等新引用——实证零命中） |
| K5 | assets `demo-*.js` 的「深度思考」类文案 | 演示页假数据文案，与生产开关无关，不动（demo 治理另议） |
| K6 | `preApprovalScanActive` 扫描收缩（UPG-76） | 与模式枚举无关，零回归断言兜底 |
