# UPG-80 派单：AI 对话链路诊断修复（模拟器 DeepSeek 链路）+ 联动补验

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（验证环境根，阻塞全部对话面真机验收）
> **验收标准**：`STD-UPG-80-v1`（content_sha256=`d666e636a0c32eee9ab39d17f828fcce41be691644145e38ed18ad63b223cd96`）
> **已查坑位库/复用件库**：是。命中条目：坑 #4（Windows 文件名）不适用；红线 4 脱敏纪律（key 打码、`git grep sk-` 零命中）全程适用；复用件「工具-ADBKeyboard/ui_ops.py」可用于真机操作
> **溯源复核**：挂账-模拟器AI未回复（2026-08-26，emulator-5556 对话 80s+ 无回复）+ 挂账-upg76-L3真机补验四场景（emulator-5554 无 DeepSeek key）——同环境根；配置锚：CredentialStore.kt:14（Keystore 加密落盘）/ MainActivity.kt:1051（key 输入）/ DeepSeekAdapter（com.hermes.dsh.llm）

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（诊断先行，未必改代码；如需改代码，基最新 main（6dd9161）开 `feat/upg80` / `mov-upg80`） |
| 真机 | `emulator-5556`（主）/ 平板；app 包 `com.mov.android`；`adb logcat` + `uiautomator dump` |
| 凭据 | 运营口令查 `docs/ACCEPTANCE_LOG.md`（AGENTS.md 规则 5）；**key 绝不写进任何文件/git/截图——证据一律打码** |

## 二、一句话

把模拟器上的 AI 对话修通：先诊断（logcat 证据说话，不许猜），再修复，然后真机实证 + 顺手把 UPG-76 L3 四场景补验做掉（同环境根，一次解锁两笔账）。

## 三、施工范围

1. **A1 诊断**：emulator-5556 发起对话，logcat 抓全链路——key 是否注入（CredentialStore 读取/Keystore 解密是否成功）→ 请求是否发出（DeepSeekAdapter 链路）→ 网络是否可达（DNS/ TLS/代理）→ HTTP 状态/错误体。产出：根因结论 + 证据行（logcat 关键行引用，打码）。
2. **A2 修复**：按诊断修复——key 重配/模型配置/网络栈/代码缺陷（若是代码缺陷，走正常分支+测试；若纯配置，零代码改动声明）。
3. **A3 实证 + 联动补验**：新会话新问题真机对话往返（截图含时间戳+logcat 请求/响应行）；然后按 STD-UPG-76-v2 测试匹配档执行 **L3 四场景**（多步出单/部分批准/MONEY 实时/过期与次数耗尽转新窗）——证据落盘，两挂账销项。

## 四、红线

1. **脱敏**：key 绝不落盘（`git grep sk-` 零命中）；截图/logcat 摘录 key 帧打码；报告不含任何 key 片段（前后 4 位也不行）。
2. 诊断结论必须有证据行——不接受「换 key 就好了」的裸结论；失败面也要如实（错 key/断网=可诊断错误，不静默）。
3. 代码改动才走分支；纯配置修复在报告声明「零代码改动」。若动请求链路（DeepSeekAdapter/参数），**Token/KV 两节必须详报**。
4. 演示数据还原+生产态复核声明。

## 五、交付与登记

1. 交付报告落 `程序员\交付报告\DELIVERY_UPG80_<日期>.md`——诊断结论+证据行、修复说明、对话往返实证、UPG-76 L3 四场景证据、脱敏自查声明、「已登记两个表」。
2. 两挂账销项（挂账-模拟器AI未回复 / 挂账-upg76-L3真机补验四场景）在报告中逐条对应证据。
3. 库加交付块 → `node 审验员/sync-orders.mjs --sync`；若有代码改动：verify-hash + DEL 绑定（红线 23）。
