# UPG-97 派单：侧边栏工作台三入口接线（我的记忆/我的资产/我的能力）+ comingSoon 兜底可见性

> **派单时间**：2026-09-04 ｜ **派单人**：设计师B ｜ **优先级**：P1（用户真机踩中「侧边栏有的按钮失灵」）
> **验收标准**：`STD-UPG-97-v1`（content_sha256 见冻结区文件）
> **问题陈述**：设计师真机实测复现——侧边栏五入口中 MCP 市场/我的订单可用，**我的记忆/我的资产/我的能力点击无任何页面打开**。根因：`前端设计/mov-vue/src/components/SidebarNav.vue` openPage（:244-247）只接 tasks/orders/vault 三 case，其余落 `comingSoon` toast 兜底（且 toast 真机上近乎不可见）；原生 handler（ui.openMemory=MemorySheet / ui.openAssets=AssetsSheet / ui.openWorkbench=BizSheet）与 PagesBridge 白名单（ui.*）全部齐备——纯前端分派欠账，非回归。
> **防撞**：只动 mov-vue 源 + 编译产物同步；不碰 MainActivity（棘轮军规）；与 UPG-95/96 零交集（95/96 动 home 面）

---

## 一、一句话

侧边栏工作台五入口全通——缺的三个 case 接上现成原生 handler，comingSoon 兜底改成看得见的人话提示。

## 二、范围

1. **三入口接线**（SidebarNav.vue openPage）：按槽位页键映射——记忆→`ui.openMemory`（MemorySheet，UPG-49 唯一入口口径）、资产→`ui.openAssets`（AssetsSheet，UPG-55/91 资产页）、能力→`ui.openWorkbench`（BizSheet workbench）；映射前先在源里核实三个槽位项的实际 page 键值，键值与 case 对齐（防 9-02 openAssets 分派表漏 case 同族病）。
2. **comingSoon 兜底可见性**：兜底 toast 真机实测近乎不可见——改为明确可见的人话提示（「该入口暂未开通」量级），保留给未来新槽位。
3. **编译同步**：mov-vue 构建产物 → app assets（sidebar bundle）同源同步，禁止手改产物。
4. **测试**：源码契约锚 + 全量 + 真机五点验。

## 三、红线（违反=打回）

1. 产物只从 mov-vue 构建来（禁手改 minified bundle）；构建脚本既有口径不变。
2. 房间区/钉选/profile 等既有接线零回归；PagesBridge 白名单不动。
3. 纯前端单：MainActivity/tools 零行改动（handler 已齐——若施工中发现 handler 缺失，停下报障，不许顺手加）。
4. Token/KV 两节必报（预期零变化）。

## 四、测试与真机（STD-UPG-97-v1）

- 变异锚 2 亲杀 + 全量 0 失败基线绿 + assembleDebug 绿。
- **真机五点验（本单核心）**：侧边栏五入口（MCP 市场/我的订单/我的记忆/我的资产/我的能力）逐个点击→对应页面打开，录屏/截图留证；外加一个未接线槽位（如有）→可见的「暂未开通」提示。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG97_<日期>.md`（2 亲杀+真机五点验+Token/KV 两节+「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）+ manifest 硬闸自检（deliver-gen 机制产出，手工件不收）；DEL 绑分支头。
