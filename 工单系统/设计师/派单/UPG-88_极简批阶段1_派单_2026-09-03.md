# UPG-88 派单：极简批阶段 1——ASR + 极简主页生产化 + 两态开关点亮 + 快速提示词

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P1（产品主线）
> **验收标准**：`STD-UPG-88-v1`（content_sha256=`f64ec0e2` 前缀，全文见冻结区文件）
> **设计文（唯一施工口径）**：`设计师\方案设计\01_外观\极简模式_对话优先主页_设计_送审_2026-09-02.md`（决策点以用户拍板为准：进入=顶部两态开关 / HeroVisual 可注入默认 MOV 竖眼 / 点 logo=直接说 / 付款键占位）+ demo `设计师\设计预览\极简模式_demo_v1_2026-09-02.html`（交互参照）
> **已查坑位库/复用件库**：是。命中：坑库无直接命中；依赖确认=UPG-84 A5 档位绑定已合 main（8647ee9）
> **用户红线**：「极简模式只是画面的极简，不能阉割能力」——两模式工具面/审批/记忆能力全量一致

## 一、交接

| 项 | 值 |
|---|---|
| 主仓库 | `0027-mov`，基最新 main（fe8cd45+）开 `feat/upg88` / `mov-upg88` |
| 真机 | 平板 `192.168.2.3:5555`（ASR 权限流+语音实测） |
| 关键面 | `MainActivity.kt`（纯 CRLF）+ 新增主页资产（引擎第一模块，纯 HTML5/JS，无框架无新依赖——送审稿 §四） |

## 二、范围（阶段 1=主页可用：说/打/结果走现有呈现链）

1. **极简主页生产化**：HeroVisual 组件（可注入抽象，默认 MOV 竖眼 logo）+ 双通道输入（点 logo=直接说 / 输入框打字）+ 主页无菜单/胶囊/抽屉；结果呈现走现有 Markstream 链（**schema2ui/商家卡归阶段 2，不做**）。
2. **ASR 接入**：Android SpeechRecognizer——权限流/听写/partial 回填/final 发送/错误诚实空态（状态矩阵照送审稿 §3.6）。
3. **两态开关点亮**：顶部按钮「经典⇄极简」真实切换（UPG-84 占位转正）+ 选择持久化重启保持。
4. **快速提示词**：极简=reasoning off+简洁提示词节（UPG-84 A5 档位绑定落地）；经典=深度思考不变。
5. **测试**：切换/持久化+ASR 回填（可单测面）+档位绑定装配断言+「两模式工具面=同一全量集」断言。

## 三、红线

1. **能力零缩减**（工具面同一全量集断言兜底）；审批/记忆/市场能力两模式一致；UPG-68/76/77/79/84 语义零回归。
2. LLM=提取器纪律预留（本阶段不产 HTML——结果走 Markstream）；无新框架/新 SDK/原生 UI 拼装（WebView 唯一载体）。
3. Token/KV 两节必报（简洁提示词节新增+档位绑定——量级+前缀稳定性）。
4. 变异亲杀 4 锚（STD）；演示数据还原声明。

## 四、交付

报告落 `程序员\交付报告\DELIVERY_UPG88_<日期>.md`（4 亲杀+真机四场景证据[切换/ASR 权限+听写回填/档位实证/诚实空态]+Token/KV 两节+「已登记两个表」）；sync 投影；verify-hash；DEL 绑定；共享面=主页/系统提示面，附 coverage_status（真机交互面预期 PARTIAL 留设计师裁决位）。
