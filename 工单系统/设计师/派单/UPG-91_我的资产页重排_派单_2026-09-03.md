# UPG-91 派单：我的资产页注册制重排（tab 导航+二级收缩裸列表+点值脱敏+诚实空态+假数据回落删除+令牌迁移）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P2（体验线——用户实测「体验感很差」）
> **验收标准**：`STD-UPG-91-v1`（content_sha256=`03b497ecd4dbc74ae77ab1ca0de9aab3523bacf5abbd9a8a548edb78450456ae`）
> **视觉基准（照抄不自由发挥）**：`设计师\设计预览\我的资产_重设计_demo_v6_2026-09-03.html`（六轮迭代用户拍板终版）
> **已查坑位库/复用件库**：是。命中：坑 #5/#6（时间戳/测试过滤假象——产物链注意）；复用=tokens.css/colors.xml 单源 + Icon 组件（本单零图标，不引）
> **并行安全**：文件面=AssetsPage.vue+资产页产物——与 UPG-86/88/89/90 全不撞

## 一、交接

| 项 | 值 |
|---|---|
| 主仓库 | `0027-mov`，基最新 main 开 `feat/upg91` / `mov-upg91` |
| 关键文件 | `前端设计/mov-vue/src/components/AssetsPage.vue`（源）→ vite build + sync-pages → `app/src/main/assets/pages/assets/`（产物入库） |
| 宿主 | `AssetsSheet.kt`（75% BottomSheet，白名单 asset./ui.——本单不动） |
| 真机 | 平板 `192.168.2.3:5555`（资产页走查） |

## 二、一句话

我的资产页按 demo v6 终版重排：tab 导航（类目+计数）+ 二级收缩裸列表 + 点值脱敏往返 + 诚实空态 + 假数据回落删除 + 硬编码色值清零——纯文字线框，零图标。

## 三、施工范围

1. **tab 导航**：下划线 tab 栏（凭据 3 / 证件照 0/2 / 未上线类目灰显），计数=真实条目数；点切类目。
2. **二级收缩**：平台组默认收起成一行「微信 · 2 项 ›」，点开展开明细行（label+脱敏值）；明细行 hairline 分隔。
3. **点值脱敏往返**：删 👁 按钮——点脱敏值=显示真值，再点=收回（桥走 credPeek 不变，uiOnly 免审批语义不动；vault.get 审批链不动）。
4. **空态诚实化**：桥数据空=空态（🔒 不要图标——纯文字「还没有任何资产」+说明+「添加凭据」主按钮）；**`demoBridge` 假数据回落整段删除**（红线 18 同族——桥空永不演示充数）。
5. **令牌迁移**：`:169-173` 等全部硬编码色值（绿横幅 #eef7f4/#1c7a5f、卡 #fff/#eceff4/#222/#999）清零过 tokens 单源；未上线类目灰显在册+内容区一行「即将上线」。
6. **契约测试**：桥空空态/无假数据/令牌锚/组行默认收起/tab 计数断言。

## 四、红线

1. **禁图标**（用户拍板：零图标纯文字线框；不引 Icon 组件新用法）；禁新增硬编码色值；禁 demo 数据回落任何形式复活。
2. 不动 AssetsSheet 宿主/白名单/vault.get 审批链/credPeek 桥语义；不动其他页面。
3. 产物链纪律：vite build → sync-pages → 产物入库（红线哨兵零越界）；Token/KV 两节申报（0/0）。
4. 变异亲杀 4 锚全红还原复绿（STD）。

## 五、交付

报告落 `程序员\交付报告\DELIVERY_UPG91_<日期>.md`（4 亲杀+真机走查截图[有数据/空态/tab 切换/组行展开/点值往返]+「已登记两个表」）；sync 投影；verify-hash + manifest 自检（红线 23 双闸）；DEL 绑定；共享面=资产页面，附 coverage_status。
