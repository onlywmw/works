# UPG-27 修复 · 补呈现模式 UI 切换（挂账-upg27-code模式无手控入口）· 派单文本

> 出单人：设计师 ｜ 2026-08-30 ｜ P1 ｜ 挂账来源：挂账-upg27-code模式无手控入口（✅设计师验证属实，转单修复）

## 交接基础信息
| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov` |
| 分支 | `feat/upg27`（未合 main；**基于最新 main 重开/rebase**）|
| worktree | `mov-upg27` |
| 相关文件 | `app/src/main/java/com/mov/android/MainActivity.kt` |
| 真机 | `adb -s emulator-5556`（包 `com.mov.android`）|

## 需求（挂账1 · 补 code 模式手控入口）
1. **顶部「极简模式」占位钮接线**：`MainActivity.kt:821-822` 现为占位 toast「极简模式即将上线」→ 改为调用 `togglePresentationMode()`(`:6422-6423`,已存在,环形 both→code→native→hardware→causal→both)切换呈现模式;
2. **presentToken 状态持久化**：把 `presentationMode` 持久化到 SharedPreferences,重启后保留切换结果(现无持久化,重启回 both);
3. **presentation.set_mode 维持 uiOnly**：保持铁律 1(权限/模式切换工具仅 UI),不改 MCP 直调暴露面;
4. 切换时用户可见反馈(如 toast/状态标签,呈现当前模式)。

## 验收
- **L1**：`togglePresentationMode` 出现生产调用点(顶部钮);presentationMode 持久化读写实现;变异——切回占位 toast 必红。
- **L2 真机**：点顶部「极简模式」钮 → 切到 code(截图/logcat 可见模式变化);杀进程重启 App → 仍 code(持久化生效);**复验 UPG-27 L2 两项**:code 模式真实任务(AI 仅凭 SDK 节+shell.exec 完成真实任务,journal 可见 shell.exec→MCP→工具链)+塌缩自纠(直呼非在面工具→三分语义错误)。
- **红线**：不改工具行为/签名;presentation.set_mode 仍 uiOnly;铁律 1。

## 派单后必须(规则 12)
1. 开工前 `git fetch origin` + 看表;
2. 工单表 UPG-27 备注追加 `认领: <agent> worktree=mov-upg27 branch=feat/upg27 @<时间>`(修复本单同为 UPG-27 卡,登记到 UPG-27 行);
3. 完成后登记两个表(先表后库)+ 报告落 `程序员\交付报告\DELIVERY_UPG27_修复_*.md`,说明已登记两表,含 hash+证据链。
