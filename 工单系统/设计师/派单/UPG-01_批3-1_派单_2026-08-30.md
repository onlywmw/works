# UPG-01 批 3-1 · AI 认知核心（34 工具）——派单文本

> 出单：设计师 @2026-08-30 ｜ 优先级：P0（批 3 四子单最先）｜ 方案：`设计师\方案设计\UPG-01_批3批4_方案设计_v1_2026-08-29.md` **v1.1**（评审 P1/P2 修正 + P3 吸收，以此版为准）
> 规则 21 溯源复核：✅ 已过（v1.1 随版完成 @main af68d22，锚点实测，见方案 §七）。关键基线：批 3 = 118 宿主工具，本单 = 其中 **34**；精确清单以派单当天重跑 §一 grep 基线为准（规则 21）。

## 一、交付清单（核心 = hostToolMetaB1 登记；另含两项批 3 首批任务）

1. **hostToolMetaB1 登记（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB1.kt`，定义 `data class HostToolMetaEntry(description, schema, output)` + `val hostToolMetaB1: Map<String, HostToolMetaEntry>`，收录附录 A 的 34 工具。
   - **desc** ≤60 字：功能 > 限制 > 场景；**近邻工具必须写差异点**（附录 B 提示，何时用我、何时用隔壁）
   - **schema** `paramSchemaDesc` 字段级真描述 ≤15 字/字段 + 「来自 xxx.kt:行」注释；真无参 = 空 properties（合法形态）
   - **output** 点名顶层返回键（有错误码才附语义，无则不编造）
   - **MainActivity 聚合（本单唯一触碰点）**：companion 内 `hostToolMeta` 首次并入 `hostToolMetaB1`；`buildToolRegistry` 增 `host` 参数 + `out[entry.name] = metaView(entry.description, entry.schema)`（复用既有 `metaView`）；`projectToolMeta` 契约零变化
2. **output 首清偿（批 1 欠账 22）**：provider 20 + scene 2 的 output 顶层键声明——清偿形态 = Provider `PARAM_DEFS` 增 output 声明（或迁 hostToolMeta 时补）；**批 3 首批，本单完成**；非空断言入 L1
3. **journal 频次榜（先行，开工第一件事）**：跑 `tools/journal_freq_top.mjs <sessions_dir>`（Android 侧 sessions/ pull 后）出 Top 榜——Token 拍板 + 本单撰写优先级双重用途；数据不可得 → 按领域关键度兜底（方案 §1.5），不阻塞
4. **output 试点**：本单 hostToolMeta 的 output 即试点批——B3-1 验收看 L1 断言实际价值，全量与否由用户再拍（方案 §1.4 件 3）

## 二、验收口径（测试方案，给验收员）

- **L1**（`ToolMetaTest` 扩展，测投影不测文件）：
  - hostToolMetaB1 遍历：desc 非模板串 / 无「占位/待补充」黑名单 / ≥20 字；字段级描述非参数名占位；**output 非空非占位（自动断言）**——顶层键与 handler 返回的**正确性核对归人工抽查/L2**（评审 P3-6）；真无参合法
  - **回落收敛**：本单 34 工具从 MOV-Boot 回落清单消失（`fallbackTools` 输出比对）
  - **output 清偿 22**：provider 20 + scene 2 output 非空断言
  - **变异亲杀两条**：①任一登记 desc 改占位 → 必红；②接线改回模板串 → 必红
- **L2 真机**（抽样 4 件，便宜可重跑）：MOV-Boot 回落清单不含本单工具 + 抽样工具 AI 面 desc 真描述（会话装配快照/logcat）；截图入 `docs/ACCEPTANCE_LOG.md`
- **L3 AI 实证**：journal 可见 AI 读描述后正确选参（memory.save 正确传 content；room.create 正确传 name；model.use 正确传 id）
- **计数断言**：不变量式（不存在已注册、非 uiOnly、无登记的在面工具），不写绝对数
- **全量绿 + assembleDebug + `node scripts/check-token-effect.mjs`**；Token/KV 两节申报（方案 §四三口径；B3-1 即**实测校准点**，用实测校准总账）

## 三、红线（方案 §五全套，复述硬项）

1. 不改工具行为/调用签名；`McpServer` 方法签名不可变（本单不碰 MCP 面）。
2. 描述必须真实（字段语义查源码核实）；拿不准标「待核实」进挂账登记表——**同时挂账 ≤8**，L1 报告随附清单；禁编造、禁「占位/待补充」字样。
3. desc ≤60 字，三段优先级**功能 > 限制 > 场景**；近邻工具须写差异点。
4. 现有已验收描述只增不改（除非事实性错误）。
5. **登记层与接线同批交付**（只补不接 = 白做；本单含聚合接线）。
6. 请求前缀恒定：不得新增 tools 字段会话中途变动点。
7. 禁新建平行登记结构 / 禁元数据落盘。
8. 回滚 = revert 单 commit。
9. `MainActivity.kt` 纯 CRLF；Kotlin 块注释内不得嵌 `/* */`；Edit 前必 Read。

## 四、交接段（程序员领单按此执行）

1. 开工前 `git fetch origin` + 看工单库/工单表本单状态（规则 19：以表为准）；认领登记：工单表本行备注追加 `认领: <agent> worktree=mov-upg01-b3-1 branch=feat/upg01-b3-1 @<时间>`，基于最新 main 切分支。
2. **串行约束**：本单动 MainActivity companion 聚合区（buildToolRegistry + hostToolMeta 首并入）——与 **B3-4（收口聚合）互斥**：同文件同一时间一个 agent，先完成者先合、后到 rebase（规则 9）；B3-2/B3-3 纯新文件不受影响。
3. 完成后登记两个表（先表后库）：`工单表.xlsx` 程序员列 `✅完成` + 备注 `分支名 hash（报告 DELIVERY_UPG01_批3-1_*.md）`；`工单库.md` 状态同步。
4. 交付报告落点 `程序员\交付报告\DELIVERY_UPG01_批3-1_2026-08-30.md`，报告末尾明确写「已登记两个表」，含 Token/KV 两节申报（实测校准）+ 频次榜产出物 + output 清偿清单 + 本单回落清单对账。
5. 产物提交 `0027-mov` 分支 `feat/upg01-b3-1`，交付时报 hash。

## 五、附录 A：本单 34 工具 + env/cond（§1.6）

**全部 env=mobile / cond=none**（纯设备本地能力，无硬件条件依赖）：

| 工具（34） | env | cond |
|---|---|---|
| memory.save / load / search / delete / cover / judge / list（7） | mobile | none |
| session.reference / session.search（2） | mobile | none |
| mov.journalTail / mov.roomList / mov.subagentList（3） | mobile | none |
| room.create / list / switch / rename / delete / pin / clearAll（7） | mobile | none |
| goal.set / complete / status（3） | mobile | none |
| subagent.run / agent.stop（2） | mobile | none |
| model.add / delete / list / setDefault / setEnabled / testConnection / update / use（8） | mobile | none |
| usage.summary / account.me（2） | mobile | none |

## 六、附录 B：近邻差异点速查（撰写必读）

- **memory.7**：save=写新记忆 / load=语义召回 / search=关键字检索 / list=枚举全部 / delete=删指定 / cover=压缩覆盖（budgetLines） / judge=质量自评——各自参数与返回语义查 `MemoryMcpTools` 实现
- **room.7**：create=新建 / switch=切换当前 / rename=改名 / pin=钉选 / delete=删除 / clearAll=清空——**switch 与 pin 易混**，须区分
- **model.8**：use=切换当前 / add=注册 / update=改配置 / delete=删 / list=枚举 / setDefault=设默认 / setEnabled=启停 / testConnection=连通性——**use 与 setDefault 易混**，须区分
- 每条 desc 附「来自 xxx.kt:行」锚点，字段语义以 handler 实现为准（红线 2 防编造）
