# UPG-01 批 4 · MCP 面收口 + 外部发现路径（McpExtDiscovery 补传 + 三态回落 + 死码删除 + 静态表日落）——派单文本

> 出单：设计师 @2026-08-30 ｜ 优先级：P0（分两段：前段可并行、后段串行于批 3 收口后）｜ 方案：`设计师\方案设计\UPG-01_批3批4_方案设计_v1_2026-08-29.md` **v1.1**（评审 P1/P2 修正 + P3 吸收，以此版为准）
> 规则 21 溯源复核：✅ 已过（@main af68d22）。关键现状：外部发现 `tools/list` 只取 name 丢弃 desc/inputSchema；`mountExtTools` 恒模板串「外部 MCP 工具: $name」；`McpToolProvider`/`registerMovQueryTools` 零调用。

## 〇、两段结构（评审 P1-2 定稿）

| 段 | 内容 | 并行关系 |
|---|---|---|
| **前段** | ① McpExtDiscovery 补传元数据 + 结构校验 ② 死码删除 | **可并行**（不依赖批 3；文件 = McpExtDiscovery.kt / McpToolProvider.kt / McpServer.kt） |
| **后段** | ③ mountExtTools 三态回落 ④ 静态表日落验收 | **串行于批 3 收口后**（文件 = MainActivity.kt，与批 3 聚合同文件互斥） |

## 一、交付清单

**前段（可并行）**

1. **McpExtDiscovery 补传元数据**：`Result` 增加 `meta: Map<String, Pair<String, Map<String, Any?>>>`（name → (description, inputSchema)）；`discover()` 遍历 `t` 时提取 `t["description"]`/`t["inputSchema"]`（`as? String`/`as? Map` 容错，缺省回落）
2. **外部 inputSchema 结构校验（评审 P3-4）**：`type==object` 且 `properties` 为 map 才透传；非法/超限 → 回落登记层/模板串，**不原样透传进 AI 面**——防非法外部 schema 污染工具面
3. **死码删除**：`McpToolProvider.kt`（零实例化，仅注释引用）删除文件；`McpServer.kt:234 registerMovQueryTools` 删除方法
4. 黑名单镜像注释同步：「四处」→「三处」（McpToolScheduler/McpMarket/McpExtDiscovery），随删随同步注释，不扩大本单范围

**后段（串行于批 3 收口后）**

5. **mountExtTools 三态回落（nullable 严格版，评审 P2-1）**：
   - 新增 `projectToolMetaOrNull(name, registry, patches): Pair<String, Map<String, Any?>>?`——**仅登记层/补丁真命中才非空**（不落内部回落串）
   - `mountExtTools` 加 `meta` 参数，逐工具：`meta[name] ?: projectToolMetaOrNull(...) ?: ("外部 MCP 工具: $name" to 空schema)`
   - **三态语义分家**：外部未命中回落「外部 MCP 工具:」模板串；宿主面（`rebuildAgentTools`）仍走非严格 `projectToolMeta`（回落「MOV 工具:」）——两套回落不混
   - 调用点同步：`:2452/:2469`（market install/enable，meta 空走投影）、`:6270`（browser proxy，meta 空走投影命中登记层）
   - **browser 热挂载顺带利好**：browser 14 已在 toolRegistry（键 `mcp__browser__*`），改投影后自动吃上登记层真描述
6. **静态表日落（批 3 收口后验收）**：`toolParamSchemas` 唯一消费 = `projectToolMeta` patches + `fallbackTools`——批 3 全进 hostToolMeta 后 patches 零命中 → **清空为 `emptyMap()`**（或显式例外清单逐条注释理由）；`metaConflicts` 零冲突（补丁位日落）

## 二、验收口径（测试方案，给验收员）

**前段**
- **L1**（`ToolMetaTest`/新增外部发现测试）：
  - 外部工具投影：mock `tools/list` 返回 desc+schema → discover 后挂载 desc/schema 正确（非模板串/非空）
  - **结构校验**：非法 inputSchema（`type!=object`/properties 非 map）→ 回落，不进 AI 面
  - 死码删除：编译通过 + grep `McpToolProvider`/`registerMovQueryTools` **零命中**
  - **变异**：mountExtTools 改回写死模板串 → 必红
- **L2 真机**：配置外部 MCP 服务器 → `tools/list` curl 实证 desc 真描述 + inputSchema 非空；截图/journal 入 ACCEPTANCE_LOG.md
- **L3**：AI 对外部工具正确选参（journal）

**后段（批 3 收口后）**
- **L1**：三态回落逐态断言（外部缺元数据 → 登记层 → 模板串）；browser 热挂载后 desc = 登记层真描述（非模板串）
- **L2 真机**：browser 安装后 agent 面 desc 真描述（截图/journal 入 ACCEPTANCE_LOG）
- **静态表日落**：`toolParamSchemas` = `emptyMap()`（或显式例外清单）；`metaConflicts` 零冲突
- 全量绿 + assembleDebug + `node scripts/check-token-effect.mjs`；Token/KV 两节申报

## 三、红线（方案 §五全套，复述硬项）

1. **不改工具行为/调用签名；`McpServer` 方法签名不可变（本单核心——只补传参、加 nullable 投影，ToolSpec 签名不动）**。
2. 描述必须真实；不编造、不写「占位/待补充」。
3. desc ≤60 字三段式（本单以既有描述为主，不新写）。
4. 现有已验收描述只增不改。
5. 登记层与接线同批交付。
6. 请求前缀恒定：不得新增 tools 字段会话中途变动点（mountExtTools 热挂载 rebuild = 既有变动点，不在本单扩大）。
7. 禁新建平行登记结构 / 禁元数据落盘。
8. 回滚 = revert 单 commit（前段/后段分 commit，可独立 revert）。
9. `MainActivity.kt` 纯 CRLF；Kotlin 块注释内不得嵌 `/* */`；Edit 前必 Read。

## 四、交接段（程序员领单按此执行）

1. 开工前 `git fetch origin` + 看工单库/工单表本单状态（规则 19）；认领登记：工单表本行备注追加 `认领: <agent> worktree=mov-upg01-b4 branch=feat/upg01-b4 @<时间>`，基于最新 main 切分支。
2. **两段排期**：前段（McpExtDiscovery + 死码）可立即认领并行；后段（mountExtTools + 静态表日落）**等批 3 四子单全收口后开工**（依赖 hostToolMeta 118 全进，`projectToolMetaOrNull` 对宿主全命中才有意义）——开工前在工单表备注确认批 3 状态。
3. **串行约束**：后段动 MainActivity（mountExtTools 区 :4932 + 三个调用点 + 静态表 :347-424）——与批 3 聚合收口（B3-4）**互斥**；同文件同一时间一个 agent，先完成者先合、后到 rebase（规则 9）。
4. 完成后登记两个表（先表后库）：`工单表.xlsx` 程序员列 `✅完成` + 备注 `分支名 hash（报告 DELIVERY_UPG01_批4_*.md）`；`工单库.md` 状态同步。
5. 交付报告落点 `程序员\交付报告\DELIVERY_UPG01_批4_2026-08-30.md`，末尾写「已登记两个表」，含 Token/KV 两节申报 + 死码删除清单 + 静态表日落核对 + 前段/后段各自 hash。
6. 产物提交 `0027-mov` 分支 `feat/upg01-b4`，交付时报 hash（前段/后段分 commit 报）。

## 五、溯源锚点（当前 main @af68d22 实测，出单前已核）

| 锚点 | 状态 |
|---|---|
| `McpExtDiscovery.kt` discover 只取 name（丢弃 description/inputSchema）；Result 仅 handlers+errors | ⚠️→本单前段修 |
| `mountExtTools`（MainActivity:4932-4938）恒模板串「外部 MCP 工具: $name」；三个调用点 :2452/:2469/:6270 | ⚠️→本单后段修 |
| `McpToolProvider.kt:21` 零实例化，仅注释引用（McpExtDiscovery:31/69、McpToolScheduler:94） | ❌→删除 |
| `McpServer.kt:234` `registerMovQueryTools` 零调用（mov.roomList/journalTail 已在宿主 118 注册） | ❌→删除 |
| 静态表 `toolParamSchemas` 唯一消费 = projectToolMeta patches（:3680/:6295）+ fallbackTools（:6305） | ⚠️→批 3 收口后日落 |
| `buildLocalOverview`（:6221-6239）用 providerToolMeta+sceneToolDescriptions，非静态表 | ✅（日落无隐藏消费，待核实点 T1 复检） |
