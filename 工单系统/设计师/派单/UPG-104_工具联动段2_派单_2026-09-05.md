# UPG-104 工具联动 Runtime 契约·段②（评测集+安全门+编排+记忆回流） · 派单

> **派单人**：设计师 ｜ **日期**：2026-09-05 ｜ **优先级**：P2
> **前置**：UPG-46 段①已合 main（@7ccf0446）——本单解锁
> **设计文（唯一施工口径）**：`设计师\方案设计\03_工具联动\输入与MCP工具联动机制_设计_v3.2_2026-09-05.md` §三对账表（段②资产勾连全在表内）+ 本派单 §二
> **验收标准（冻结版）**：`处理中心\验收标准冻结区\UPG-104\STD-UPG-104-v1.md`（content_sha256=e6d8a7f960dad4d12a60fa5b105898d694f908ec0fae11b5c6205b6755e2b5b5b5）
> **已查坑位库/复用件库**：是 ｜ 命中：已知坑 #10（contains 弱锚——本单新增用例一律计数+局部性强锚形态）、#6（`--tests` 0 测试假绿——计数必申报）；复用件命中：Evaluator/EvalFixture（评测基建现成）、EffectSpecs+EffectSpecRegistry（分级判据现成）、Plan 执行器段①最小版（编排底座现成）

---

## 一、基线与认领

- 主仓 `E:\mov归档\0027-mov`；开工前 `git fetch origin`，基于最新 origin/main 开 **分支 `feat/upg104` / worktree `mov-upg104`**
- 认领登记（开工前必先做）：工单表 UPG-104 行备注 `认领: <你> worktree=mov-upg104 branch=feat/upg104 @时间`

## 二、施工范围（四件，A1-A4——实物底子全在 main，先读 v3.2 §三防重复施工）

1. **A1 六指标评测集扩建**（13 → ≥25 例）：`tool-orch/.../Evaluator.kt` 六指标（selection/argument/noCallPrecision/noCallRecall/multiTool/safetyGate）+ `EvalFixture.kt` 现有 13 例——新增 NO_CALL ≥2 / MULTI_CALL ≥2 / 边界 ≥2 + 余量覆盖六指标；**P&R 跑分确定性**（同输入两次一致）；新用例一律计数+局部性强锚（坑 #10）
2. **A2 Safety Policy 确认门深化**：L0-L3 全分级实测；L2/L3 必进确认门（**PermissionGuard 审批语义不动**，走现有审批通道）；**own 分类器兜底**——EffectSpecs 未登记的工具有保守分级，不裸奔
3. **A3 多工具编排完整执行**：Parallel / Sequential / Conditional 三模式端到端各 ≥1（Plan → 按序执行 → 结果回填）；互斥/优先级规则（EffectSpecs 判据）正确——段①最小执行器升级为完整版
4. **A4 记忆回流 = 候选**：`ToolOrchModel.kt:97 TraceRecord.memoryCandidate`（现恒空串）实值填充 → 经记忆写链路落 **PROPOSED**（对接 UPG-52 四态机 / UPG-107 payload 链路）；**工具结果 ≠ 偏好，全链零自动 ACTIVE**

## 三、红线（违反=打回）

- NO_CALL 是合法决策；参数阻断四类不动；Trace 不存 CoT、不开第二写点（沿段①红线 5）
- 记忆回流只产候选，**永不自动晋升 ACTIVE**（人裁决）
- **不动 ToolOrchestrator 关键词决策器**（Evaluator baseline 依赖，段①既定）
- 不改 PermissionGuard 审批闸语义；不加新 UI（确认门走现有审批/弹窗通道）
- 段①五变异锚（循环拒绝/四类阻断/Trace 单写点/NO_CALL/确认门）复验不破

## 四、验收

按 `STD-UPG-104-v1` 执行：E1-E8 销项 + **5 变异锚亲杀**（评测集/确认门/兜底分类器/三模式/回流——每个「改→必红→还原→复绿」）；真机 L2 一轮（Plan→三模式→弹卡确认→ledger 见 PROPOSED 候选）。

## 五、交付物与交接

1. 交付报告 `程序员\交付报告\DELIVERY_UPG104_*.md`：四件逐一证据 + Token/KV 两节（评测集若走 LLM 通道跑分，**token 增量必报**）+ 《共享面影响清单》（触及 Plan 执行器+记忆写链路=全局数据面，红线 24）+ coverage_status + 「已登记两个表」声明
2. 交付绑定：`审验员\deliver-gen.mjs --ticket UPG-104 --branch feat/upg104 --hash <sha> --app-path "E:\mov归档\0027-mov" --evidence <证据…>` 机制产出 manifest → `审验.py --manifest` ok:True → `审验.py --verify-hash feat/upg104 <sha>` HASH_OK → 登记两表（先表后库，xlsx 五步铁律）
3. 证据落 `程序员\UPG104-evidence\`

**候选施工**：任意线可领，先到先得
