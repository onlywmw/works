# UPG-07 批2 修复 · 审批弹窗四键渲染（挂账-upg07批2-审批弹窗四键未渲染）· 派单文本

> 出单人：设计师 ｜ 2026-08-30 ｜ P1 ｜ 挂账来源：挂账-upg07批2-审批弹窗四键未渲染（✅设计师验证：缺陷属实但属批2 未合分支,随批2 修复）

## 交接基础信息
| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov` |
| 分支 | `feat/upg07-b2`（**431b3cb,未合 main**;基于最新 main rebase）|
| worktree | `mov-upg07-b2` |
| 相关文件 | `app/src/main/java/com/mov/android/MainActivity.kt`（审批弹窗 ~:3570 区）|
| 真机 | `adb -s emulator-5556`（包 `com.mov.android`）|

## 需求（挂账2 · 修审批弹窗按钮不可点）
现状：批2 为做「允许本目标」四键改用 `setMessage(...)+setItems(labels)` 双调用,setItems 独占内容区为 ListView,与 setMessage 冲突→按钮不渲染,审批弹窗**整体无可点按钮**(只能 60s 超时拒绝)。

修复二选一：
1. **setItems 去 setMessage**：参数摘要拼入列表项 / 改为 custom view(列表 + 摘要行);
2. **恢复标准三键 + goal 键**：setNeutralButton(允许本轮)+setPositiveButton(允许本次)+setNegativeButton(拒绝) 标准三键;有 ACTIVE 目标时加「允许本目标」(AlertDialog 原生按钮上限 3 不够四键 → 需 custom view 或四键拆分)。

要求：**有 ACTIVE 目标 → 四键全可见可点**;无 ACTIVE 目标 → 标准三键正常。

## 验收
- **L1**：弹窗构建逻辑无 setMessage+setItems 冲突(源码断言);变异——回归 setItems+setMessage 双调必红。
- **L2 真机**：uiautomator 节点树 `uvw.xml` 含 button 节点(四键/三键渲染,可见可点);**goal 豁免**「允许本目标」点击可达;journal 见 allowed-goal 链;截图存证。
- **红线**：接线现有 ApprovalService,不另起平行体系;批准不永久(goal 失效即回收、新工具必弹);不改 tools 字段/system prompt。

## 派单后必须(规则 12)
1. 开工前 `git fetch origin` + 看表;
2. 工单表 UPG-07 批2 备注追加 `认领: <agent> worktree=mov-upg07-b2 branch=feat/upg07-b2 @<时间>`;
3. 完成后登记两个表(先表后库)+ 报告落 `程序员\交付报告\DELIVERY_UPG07批2_修复_*.md`,说明已登记两表,含 hash+证据链(节点树/截图)。
