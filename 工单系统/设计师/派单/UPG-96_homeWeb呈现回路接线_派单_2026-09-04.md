# UPG-96 派单：homeWeb 呈现回路接线（极简发送不回落 + LLM 回复回流出卡 + 真机批收口）

> **派单时间**：2026-09-04 ｜ **派单人**：设计师B ｜ **优先级**：P1（产品主线·极简批阶段 2 收口——完全体）
> **验收标准**：`STD-UPG-96-v1`（content_sha256 见冻结区文件）
> **设计文（唯一施工口径）**：呈现体系 v1.1 §三/§六/§七 + v1.2 增补 + demo v4（形态基准）
> **已查坑位库/复用件库**：是。复用命中=UPG-88 sendFromHome/档位绑定 + UPG-89 MovHomeHost.onLlmChunk（JS 侧已就位）+ UPG-79 ApprovalSurface；坑库命中=「home 层未装配 fail-safe」日志点（:7401）
> **依赖/防撞**：**依赖 UPG-94 先合**（seed 合并不进引擎=卡出不来，本单接了线也是回落纯文本）；与 UPG-95 同 home 面——建议串行或同程序员；**MainActivity 棘轮军规豁免注记**：本单触碰 runChat 回复链/sendFromHome（既有路径改线，非新增业务面——Agent 装配面归 UPG-93 批⑦另拆）

---

## 一、一句话

极简主页发问不跳经典——LLM 回答经 MovHomeHost.onLlmChunk 回流 homeWeb，引擎出卡浮在底座上，全程无页面切换。

## 二、范围

1. **不回落**（MainActivity.kt `sendFromHome` :7427）：删除 `applyPresentationMode(CLASSIC, persist=false)` 切回——极简发送后**留在极简**；模式持久化零改写（档位语义维持 88 口径：发送时刻模式=极简 → reasoning off+简洁节）。
2. **回复转发**（MainActivity 回复链）：runChat/回复完成回调处，**发送源=home 且当前=极简态**时 → `homeWeb.evaluateJavascript("MovHomeHost.onLlmChunk(...)")`（jsString 转义沿用既有工具）；经典态/经典发送源零变化（markstream 呈现不动）。
3. **失败诚实**：homeWeb 未装配/JS 异常 → fail-safe 落经典视图呈现 + appendLog（沿用 :7401 日志点语义）——不许静默吞回答。
4. **仲裁接线**：审批 pending 时极简主页新输入 → Intent Router 四路（Cancel/Modify/NewTask/Defer）事件足迹原生侧可查（journal 记仲裁结果）。
5. **测试**：3 变异锚 + 全量 + 真机批（收口 UPG-88/89 持有场景）。
6. **94 转出追加两件**（STD 追加说明区注记 @2026-09-04）：①index.html 构造 engine 后合并 MovRegistrySeed 进 engine.registry（+「登记簿非空」契约锚）②timelineList 骨架 CSS（.tl/.tl-dot）入 index.html——无此两件真机出卡不成立。

## 三、红线（违反=打回）

1. 经典模式零回归（markstream 流式呈现/工具链/审批链不动）；能力零缩减。
2. MainActivity 纯 CRLF；只动 sendFromHome/回复转发点/仲裁事件三处，其余行零触碰（93 拆分在途）。
3. 回答不丢：任何异常路径都必须有呈现归宿（homeWeb 或经典 fail-safe），禁止静默。
4. Token/KV 两节必报（极简简洁节/档位不变更声明）。

## 四、测试与真机（STD-UPG-96-v1）

- 3 变异锚亲杀 + 定向套件 + 全量 0 失败基线绿 + assembleDebug 绿。
- **真机批（本单核心——88/89 持有场景收口）**：①极简主页打字发问→不跳经典→回答出卡（内容卡浮现+dock 转场）②语音发问（ASR 真人发声端到端，88 PARTIAL 销项）③出卡中审批叠卡置顶（vault.get 类）④pending 中说「算了换一个」→仲裁 Cancel+NewTask 足迹 ⑤未登记类型→纯文本回落卡 ⑥小圆片折叠→点回展开 ⑦杀进程重启=极简保持+输入入口在场。

## 五、交付与登记

报告落 `程序员\交付报告\DELIVERY_UPG96_<日期>.md`（3 亲杀+真机七场景录屏/截图+Token/KV 两节+「已登记两个表」）；库加交付块 → sync 投影；verify-hash（红线 23）+ manifest 硬闸自检；DEL 绑分支头；UPG-88/89 持有项（ASR 真人发声/真机出卡）随本单销项落表。
