# UPG-46 工具联动 Runtime 契约（Tool Orchestration）· 派单

> **瘦身重派 @2026-09-04（用户拍板「建议瘦身转派」）**：本单=**段①核心契约**（联动机制+Trace+接线+契约语义——即下文原范围）；**段②后续**（六指标评测集+Safety Policy 确认门实现+多工具编排+记忆回流）**转 UPG-104**（已立卡 P2 挂单）。**候选施工=Claude CLI（UPG-103 完成后接续；若其他程序员先领以认领段为准）**。

> **派单人**：设计师 ｜ **日期**：2026-09-02（补派单——08-30 已派未认领，补交接）｜ **优先级**：P0（用户转派）
> **设计文（唯一施工口径）**：`设计师\方案设计\03_工具联动\输入与MCP工具联动机制_设计_v3_2026-08-30.md`（v3 定稿·冻结：大神 9.3 + Trace 契约）
> **旧版**：`归档\历史文档\方案设计_旧版\`（v1/v2 仅溯源）

---

## 一、施工范围

依据设计 v3（§ 各节），**输入与 MCP 工具联动 Runtime 契约**——核心交付：

1. **联动机制**：输入事件 → 工具调用（输入触发工具/参数映射）——按 v3 的契约表
2. **Trace 契约**：联动链路可溯源（调用记录/参数快照/结果回填——v3 定义的 Trace 字段）
3. **Runtime 接线**：MainActivity/Bridge 面接入点（v3 §接线指出处的 handler/挂点）
4. **契约语义**：工具编排（Tool Orchestration）——联动规则（触发条件/优先级/互斥）

**不做**（红线）：不扩工具面（不新增工具）、不改审批闸语义、不加 UI。

## 二、判据点（v3 对应）

| # | 判据 | 来源 |
|---|---|---|
| D1 | 输入→工具触发链实测（给定输入事件→正确工具被调+参数映射正确） | v3 §契约 |
| D2 | Trace 记录完整（事件/参数/结果三字段，日志可读） | v3 §Trace |
| D3 | 互斥/优先级规则正确（冲突输入→高优先/拒绝低优先） | v3 §规则 |
| D4 | 无回归（全量 JVM 基线 +312/0/1；相关测试绿） | 基线 |

## 三、基线与仓库

- **基线**：origin/main（合入前按当前 main rebase；**分支** `feat/tool-orch`，worktree `mov-tool-orch`）
- **边界**：只改 v3 指定的文件（MainActivity.kt 为全局锁唯一占用——派单声明）
- **禁止**：改审批/安全闸（白名单/审批语义不动——CT-06/CT-08）；不动工具注册表

## 四、交付物

1. 交付报告（`程序员\交付报告\DELIVERY_UPG46_2026-09-02.md`）——含 D1-D4 证据（真机/JVM）
2. 代码提交（feat/tool-orch，**hash 申报**）
3. 派单文+设计文引用（报告申报节）

## 五、验收方式

- 验收员 L1（代码层判据 D1-D3 锚点）+ L2（真机输入→工具触发链）
- 变异亲杀（D1/D3 各 1 变异必红）
- 合 main：设计师（rebase + ff）

---

**催办说明**：P0 已派 3 天（08-30→09-02）无人认领——**本派单文补齐后放开认领**（A/B/C 任意线可领；先到先得，认领=在卡状态区标注「认领@日期+线」）。
