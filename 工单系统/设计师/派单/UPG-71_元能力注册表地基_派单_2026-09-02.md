# UPG-71 元能力注册表 · 地基 — 派单文本

> 出单人：设计师（2026-09-02）｜ 单号 UPG-71 ｜ 优先级 P1
> 施工者：程序员（认领后按此施工）｜ 验收：验收员（L1/L2/L3）→ 审验员 → 设计师合 main

---

## 一句话

把「元能力注册表」（业务语义粒度：动词+对象域+副作用契约，工具粒度之上的能力层）落成 0027-mov 正式资产 + JSON Schema + 校验脚本。**本单纯新增静态资产 + 脚本，零运行时改动、零请求链路改动**——LLM 查表接线、bundle/job 落代码都留后续接线单（有消费者再动）。

## 环境基础（全基础信息）

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（git@github.com:onlywmw/0027-mov.git，Android 主工程） |
| 分支 | 认领 `worktree=mov-upg71`，`branch=feat/upg71`，基于最新 `main`（开工前 `git fetch origin`） |
| 构建 | `cd 0027-mov && gradle :app:assembleDebug` |
| 单测 | `cd 0027-mov && gradle :app:testDebugUnitTest`（「全量绿」以此为准） |
| 真机形态 | 本单无 UI/运行时接线 → L2 为「装配级」，见验收节 |

## 设计文（唯一施工口径）

- 架构稿：`设计师\方案设计\MOV_元能力注册表_架构设计稿.md`（v0.3，评审 R2 有条件冻结 8.8：P0×3[consumer 独立性/Job 三件套/ApprovalGroup 执行语义]+P1×3[Schema Validation Gate/retry_policy 语义/bundle 最小接口]全吸收）
- 样例资产（迁移基准）：`归档\历史文档\执行链路_2026-09-02\MOV_元能力注册表_MVP示例.json`

## 对账（防重复，已核实）

- 现有 `docs/ApprovalRegistry*.json`（UPG-45，工具级审批登记）、`tool-orch` EffectSpecs/DagPlanner（UPG-46/67，工具编排/副作用）——都是**工具粒度**。
- 本单是**业务语义粒度独立地基**，不与它们混、不清改它们；两者合并评估另立单。
- 0027-mov 现无 capability-registry 资产/模块 → 新开无重复。

## 施工

1. 建 `docs/capability-registry/capability-registry.json`：迁移 4 条 MVP——
   - `fulfill.dispatch` 派单（WRITE / EXTERNAL / 幂等）
   - `fulfill.track` 追踪（READ / EXTERNAL / 幂等）
   - `settle.pay` 支付（MONEY / EXTERNAL / 幂等）
   - `sense.capture` 现场取证（WRITE / LOCAL / **非幂等** / **candidate**）
   - 字段：`id/name/semantics/domain/side_effect/env/idempotent/in/out/state/owner/evidence/consumers/impls`（对应架构稿 §2，in/out 为 JSON Schema 形态）。
2. 建 `docs/capability-registry/capability-registry.schema.json`：JSON Schema（draft-07）——枚举 `side_effect/env/state/consumer_type`、`required`、结构、domains 一致性；consumers 独立性计数（按 `domain_context` 去重）机器可读。
3. 建校验脚本 `scripts/check-capability-registry.mjs`（并列现有 `scripts/check-token-effect.mjs`），对正式资产断言：
   - schema 校验 **0 错**；
   - registered 必 ≥2 独立消费者；candidate 允许 1；
   - 枚举不越界；
   - **禁占位串残留**（扫描 `{样例` / `占位` → 红）；
   - 输出汇总（能力数 / registered / candidate / 副作用分布）供人读。
4. 交付含 `git diff` + hash + 证据链；报告落 `程序员\交付报告\DELIVERY_UPG71_*.md`，写明「已登记两表」；自跑 `node scripts/check-token-effect.mjs`。

### 占位与编造纪律（关键）

- 样例资产里的 `{样例占位}` 一律清零：`owner="MOV 作者 wmw0027"`，`evidence="UPG-71 立项·架构稿 v0.3 冻结 @2026-09-02"`。
- **禁编造 impl**：不可核实的实现方不登记——`impls` 允许空数组，空则加 `note:"待外部/自营接入（接线单补）"`。

## 验收标准

- **L1**：
  1. `node scripts/check-capability-registry.mjs` 对正式资产 0 报错、汇总数字正确；
  2. **变异亲杀**：篡改临时副本——枚举越界 / registered 独立消费者减到 1 / 删必填 / 塞回 `{样例占位}` → 脚本必红；
  3. 全量绿 `gradle :app:testDebugUnitTest` + `assembleDebug`；
  4. `node scripts/check-token-effect.mjs` 过；
  5. 零冲突：git 确认未动 `docs/ApprovalRegistry*` / `tool-orch/src` / `MainActivity.kt`。
- **L2 真机**：本单无 UI/运行时接线 → 如实标「装配级：assembleDebug APK 正常 + 校验命令输出实证」；行为级真机验收留接线单。
- **L3**：与 UPG-01 元数据 / UPG-45 ApprovalRegistry / UPG-46+67 tool-orch 语义零冲突（纯新增，无引用不清改）。

## 红线

1. **零运行时改动**：不动 `MainActivity.kt`（纯 CRLF，本单禁 Edit）/ AgentLoop / McpToolScheduler / ApprovalService / `tool-orch/src` / `docs/ApprovalRegistry*.json`。
2. **请求链路不变**：Token/KV Cache 影响 = **0/0**；AI 面 tools / system prompt 会话中途不变（请求前缀恒定）。
3. 正式资产**禁占位串/禁编造**：impl 不可核实 = 空数组 + note；语义契约与样例一致，不擅改 semantics/in-out。
4. 冻结项（room.html/markstream/二维码收费/本地大模型/视频卡片）不碰。
5. 交付必含 **Token 影响 / KV Cache 影响** 两节申报（`AGENTS.md` 硬规则 1），自跑 `node scripts/check-token-effect.mjs`。

## 派单交接段

1. 开工前 `git fetch origin` + 看表（确认最新 main）；
2. 认领：工单表 UPG-71 备注追加 `认领: <agent> worktree=mov-upg71 branch=feat/upg71 @<时间>`；
3. 完成后**先表后库**登记；报告落 `程序员\交付报告\DELIVERY_UPG71_*.md` 写明「已登记两表」，含 hash + 证据链（资产 + schema + 脚本输出 + 变异亲杀）。
