# UPG-77 派单：审批判定单源化 + MCP 面死信通道处置 + SDK 契约纠偏（P0 安全）

> **派单时间**：2026-09-03 ｜ **派单人**：设计师B ｜ **优先级**：P0
> **验收标准**：`STD-UPG-77-v1`（content_sha256=c80c5941420758336394ca0e3cf0be6ef0752b52f5f6b7b997d1fb17dd164f47）——验收/审验/合 main 三处对账，不一致→阻断
> **已查坑位库/复用件库**：是。命中条目：无直接命中（坑 #2「工单表并发写入五步铁律」适用于登记环节；复用件「脚本-审验.py」供审验员使用）
> **溯源复核**（红线 21）：分层溯源图已随卡复核 @014c10f（2026-09-03，六路审计产出，锚点见工单库 UPG-77 卡）——本单全部锚点即审计证据，无需复核漂移；施工基于最新 main 重开分支

---

## 一、交接基础信息

| 项 | 值 |
|---|---|
| 主仓库 | `C:\Users\Administrator\0027-mov`（注意：当前工作树停在 feat/upg50-ph1 且有未提交改动——**开工先 `git fetch origin` + 基于最新 main 重开分支**，红线 19） |
| 分支 / worktree | `feat/upg77` / `mov-upg77`（领单开工前先在工单表备注登记认领：agent + worktree + branch + 时间） |
| 构建 | `gradle :app:assembleDebug`（JAVA_HOME 见 AGENTS.md） |
| 单测 | `gradle :app:testDebugUnitTest`（全量绿以此为准） |
| 真机 | Android 模拟器 `emulator-5556` / 平板；MCP 面 `127.0.0.1:8389`（token 在 `filesDir/mcp_token.txt`） |
| 关键文件 | `app/src/main/kotlin/com/hermes/mov/mcp/McpServer.kt`、`app/src/main/kotlin/com/hermes/dsh/tools/McpToolScheduler.kt`、`ApprovalService.kt`、`ToolSdkGenerator.kt`（SDK 文案）、`MainActivity.kt`（纯 CRLF，Edit 前必 Read） |

## 二、施工范围（A1/A2/A3）

- **A1 判定单源化**：抽统一判定入口（含 `McpToolScheduler.kt:317-319` 的 only-once 强制 ASK 覆写），`McpToolScheduler.dispatch` 与 `McpServer.kt:110-113` tools/call 共用。消除双写，不是把 if 抄到 MCP 面。open 模式下 only-once 工具（OnlyOnceTools 名单）MCP 面同样强制 ASK。
- **A2 死信面消除**：MCP 面 ASK → 路由 `ApprovalService.request`（同一 FIFO/呈现/审计，HTTP 同步等待至多 60s，fail-closed）；删除 `registerPending`（`McpServer.kt:121`）/`PermissionGuard.pending`（`McpToolScheduler.kt:51`）/`approvePending`（`McpServer.kt:213-224`）死面；`permission.approve`/`permission.deny` handler 随退役或保留空响应+说明文案——按 grep 结果裁决并在交付报告声明。极简阶梯：删除优于新增。
- **A3 SDK 纠偏**：`ToolSdkGenerator.kt:221`「标 ask 的写类会再弹一次」改为真实语义：经 MCP 面调 ask 类工具 → 用户在 App 内实时审批、同步等待至多 60s；only-once 任何模式都需当次确认。顺手核对 :215-217 的 curl 教学段与 :221 语义一致。

## 三、红线（违反=打回）

1. 安全语义只紧不松：任何模式下 only-once 不得直出明文；fail-closed 不变；UPG-68 语义零改动。
2. 不动 UPG-75 呈现层（弹窗/通知/chip 组件化=总纲 R1 另单）；不动 UPG-76 设计。
3. **Token / KV Cache 影响必须申报**（AGENTS.md 硬规则 1）：A3 改 SDK 文案=改 system prompt 组成部分——交付报告必须含两节，说明「会话内前缀恒定、跨会话前缀变化」及量级。
4. Kotlin 块注释不嵌套；MainActivity.kt 纯 CRLF。
5. 变异亲杀：三个变异锚（见 STD）全红→还原复绿，计数如实（以 --rerun-tasks XML 为准并注明统计时点——已知坑：报告计数虚高前科）。

## 四、测试方案（验收锚=STD-UPG-77-v1 冻结版）

- 新增 MCP 面审批测试：open 模式 only-once→ASK（不直出）；default 模式 ASK 路由 ApprovalService（可批可拒）；ASK 响应不再含 `req-` 死信格式。
- 既有套件零回归：PermissionGuardTest 15 / OnlyOnceGuardTest 8 / ApprovalQueueTest 4 / ApprovalExperienceTest 8。
- 真机 L3 三场景（curl 127.0.0.1:8389 tools/call vault.get）：① default 点允许→返回明文+足迹 decided=allowed-once；② 点拒绝/超时→拒绝文案、无 req-N；③ open 模式→同样强制弹窗。命令输出带时间戳留证。
- `:app:assembleDebug` 绿；全量 `testDebugUnitTest --rerun-tasks` 绿。

## 五、交付与登记（红线 7/12/23）

1. 交付报告落点：`程序员\交付报告\DELIVERY_UPG77_<日期>.md`——必须含：Token 影响 + KV Cache 影响两节、变异亲杀记录、真机三场景证据、「演示数据已还原且生产态已复核」声明（红线 18）。
2. **完成后立即登记两个表**：`工单表.xlsx`（程序员列 ✅完成 + 备注分支/hash/报告名——表由 sync-orders.mjs 生成，登记走库）+ `工单库.md`（状态快照，先表后库——实际是：库加交付登记块，再跑 `node 审验员/sync-orders.mjs --sync` 投影到表）；**交付报告明确写「已登记两个表」**。
3. 登记前强制跑 `审验员\审验.py --verify-hash feat/upg77 <code_commit_sha>`——HASH_OK 方可登记（红线 23）。
4. 交付绑定：`DEL-UPG-77-<YYYYMMDD>-<NNN>` + 三重 hash（code/artifact/evidence_manifest）。
5. 共享面变更（McpServer/McpToolScheduler=工具面/协议面）→ 交付报告必附《共享面影响清单》+ coverage_status 三档（红线 24）。

## 六、上下文（施工前必读）

- 根因评审：`设计师\方案设计\审批体系_总纲_v1_评审增补_设计师B_2026-09-02.md`（含全部行号证据）
- 总纲：`设计师\方案设计\审批体系_总纲_v1_2026-09-02.md`（本单=R0；R1 呈现层组件化另单）
- 卡：`工单库.md` UPG-77（含分层溯源图）
- 注意：UPG-75「A3-1 环境阻塞」已改判真实缺陷（本单根因），验收员 §P30 结论以本单为准更新
