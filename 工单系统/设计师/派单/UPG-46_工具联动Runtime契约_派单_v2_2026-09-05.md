# UPG-46 工具联动 Runtime 契约（Tool Orchestration）· 派单 v2

> **派单人**：设计师 ｜ **日期**：2026-09-05（v2 治理修复版——替代 `UPG-46_工具联动Runtime契约_派单_2026-09-02.md`，旧版仅留档勿用）｜ **优先级**：P0
> **v2 修订原因**：①v3 设计文本体断链 → 口径重建 v3.2；②STD 未冻结 → 已冻结 STD-UPG-46-v1；③分层溯源图缺失 → 已按规则 20/21 复核（main @841f591d）；④分支名 feat/tool-orch 被历史雏形占用 → 改 feat/upg46；⑤基线漂移（64 提交）→ 刷新 814/0/1；⑥补知识库必填字段
> **设计文（唯一施工口径）**：`设计师\方案设计\03_工具联动\输入与MCP工具联动机制_设计_v3.2_2026-09-05.md`（含分层溯源图 + 强化/新开对账 + Token/KV 申报要求）
> **验收标准（冻结版，唯一引用）**：`STD-UPG-46-v1`（content_sha256=5038b6398e39cabf4b5edba40c4d1ec096d4ee8876dee185d8e2679c7392f4f4）｜ 文件：`处理中心\验收标准冻结区\UPG-46\STD-UPG-46-v1.md`
> **已查坑位库/复用件库**：是 ｜ 命中条目：无（工具联动/Trace/orch/DagPlanner 关键词两库均无命中）
> **瘦身边界（用户 2026-09-04 拍板）**：本单=**段①核心契约**（联动机制+Trace+接线+契约语义）；段②（评测集扩建/确认门深化/多工具编排执行/记忆回流）= **UPG-104**（P2 挂单，勿越界施工）

---

## 一、基线与认领

- **基线**：origin/main 最新顶（派单时点 = `841f591d`；**开工前必 `git fetch origin` 并按当时 main 顶重开分支**——规则 19）
- **分支**：`feat/upg46` ｜ **worktree**：`mov-upg46`（命名强绑定工单号；⚠️ **勿用 `feat/tool-orch` / `mov-tool-orch`**——该分支是 UPG-27 时代雏形，已全量在 main，占用即撞单歧义）
- **认领登记（开工前必先做）**：工单表该行备注追加 `认领: <agent> worktree=mov-upg46 branch=feat/upg46 @时间`；分支被占用 = 已被认领 → 改领其他单
- **全局锁声明**：工具调用装配面 = `MainActivity.kt` runChat 段（:3140 起）+ `tools/ToolsRegistry.kt` 注册面（UPG-93/98 拆分后注册已迁出 MainActivity）——同一时段本单独占这两处；施工前 grep 确认无其他在施单占用

## 二、施工范围（段①——照 v3.2 §一/§三）

1. **Plan 协议层（新开）**：Plan JSON schema + LLM 结构化输出解析 + 解析失败拒绝；steps 依赖由 `dependsOn` 显式表达
2. **两段式接线（新开+复用）**：决策侧复用 UPG-76 预审单先例钩子区（`MainActivity.kt:3433/3523` 口径）；执行侧 Plan 消费模式架在 `ToolCallScheduler`（ToolCalls.kt:55）之上，每步必经 `McpToolScheduler.dispatch`（唯一执行闸，不新开通道）
3. **四类阻断接线（强化）**：tool-orch `ValidationIssue`（缺参/歧义/非法/超权限）接入生产 dispatch 前段
4. **Trace 契约落地（强化+新开装配）**：`TraceRecord` 14 字段实值填充（conversationId/turnId 当前恒空串）+ **落会话 journal 新事件类型**（`Session.append` → JsonlStore；**禁止另起文件=第二写点**）；TraceRecorder 生产实例化
5. **编排规则语义层（契约层，新开最小版）**：触发条件/优先级/互斥声明式规则表；复用 EffectSpecs+EffectSpecRegistry 接线进 DagPlanner
6. **消灭死引用**：`MainActivity.kt:349-350` toolOrch/toolOrchTools 接上真实链路（或按 v3.2 口径处置）

**不做（红线）**：不扩工具面（不新增工具）/ 不动工具注册表语义 / 不改 PermissionGuard 审批名单语义 / 不加 UI / **不动 ToolOrchestrator 关键词决策器**（Evaluator baseline 依赖）/ 不开第二 Trace 写点 / 段②内容不越界

## 三、判据点（STD-UPG-46-v1 摘要，验收以冻结版为准）

| # | 判据 |
|---|---|
| D1 | Plan 两段式实测：输入 → 结构化 Plan（steps+dependsOn）→ 校验 → 按序执行，正确工具+参数映射正确 |
| D2 | Trace：journal 可见 trace 事件，14 字段齐，无 CoT，无第二写点 |
| D3 | 编排语义：互斥/优先级正确；循环依赖 Plan 被拒 |
| D4 | 四类阻断各 ≥1 用例实测阻断 |
| D5 | 无回归：全量 0 失败（基线 814/0/1 @841f591d，允许合理增量须申报）+ 定向锚用例全绿 + assembleDebug 绿 |

**变异锚 5 组**（Plan 校验/四类阻断/Trace 单写点/NO_CALL/确认门——验收员亲杀，见 STD 冻结区）。

## 四、施工注意（溯源复核新发现，必读）

1. **E3 兜底冲突**：`MainActivity.kt:3335` 附近有「口头承诺不调用就 nudge 立即调用工具」的兜底逻辑——与「先 Plan 后执行」语义打架，本单须一并改这段语义（NO_CALL 合法化后不得再 nudge 强催）
2. **旧锚作废**：`writeTools/harmlessTools` 内联名单、`toolParamSchemas` 内联 schema 在 main **已不存在**（权限名单单源化迁移 PermissionRegistryData + schema 日落）——勿按旧文档找这些符号
3. **UPG-83「tool.call 受控通道」未核实**：main 提交历史查无此工单号；以 L2 实证锚（OpenAiCompatAdapter:244 / ToolCalls.kt:55 / McpToolScheduler:235）为准
4. **Plan 模式前缀恒定**（仓库硬规则 2）：tools 字段/system prompt 会话中途不得变；两段式若用独立会话，各自前缀恒定

## 五、交付物与交接（红线 12/23 口径）

1. **交付报告** → `程序员\交付报告\DELIVERY_UPG46_*.md`，必须含：
   - D1-D5 证据（JVM+journal+真机截图）
   - **《共享面影响清单》+ coverage_status 三档**（红线 24：本单触及 MainActivity 装配段+协议层 Plan JSON，必附）
   - **Token 影响 / KV Cache 影响两节**（仓库 AGENTS.md 硬规则 1）
   - 「已登记两个表」声明
2. **交付绑定（机制产出，手工不收）**：`审验员\deliver-gen.mjs` 产出 evidence manifest（内置硬闸自检 ok:True）→ 登记 `DEL-UPG-46-<日期>-<NNN>`（工单表 delivery_id 列，绑 code_commit_sha+artifact_sha+evidence_manifest_sha 三重 hash）；登记前先跑 `审验员\审验.py --verify-hash <分支> <hash>`，HASH_OK 方可登记
3. **登记两个表（同步完成，缺一不可）**：`工单表.xlsx` 程序员列 ✅完成 + 备注（分支+hash+报告名+delivery_id）；`工单库.md` 状态快照同步（先表后库）——**xlsx 写入五步铁律自动适用**（写前重读/备份/内容定位/扩 !ref/写后全量读回对账）

## 六、验收方式

- 验收员按 **STD-UPG-46-v1** 执行：L2 行为实测 + L3（diff 精读 + 变异 5 锚亲杀 + 真机）→ 结论落 `0027-mov\docs\ACCEPTANCE_LOG.md`
- 审验员证据链审验（`审验员\审验.py`）→ 设计师按 §六抽查标准核实后合 main
- **合 main 只归设计师**；验收/审验不过 = 打回补证

---

**候选施工**：Claude CLI（UPG-103 后接续；若其他线先领，以工单表认领登记为准，先到先得）
