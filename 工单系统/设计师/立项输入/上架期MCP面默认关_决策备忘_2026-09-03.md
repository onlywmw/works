# 立项输入：上架期 MCP 面（:8389）默认关 + 开发者开关（2026-09-03 用户拍板）

> 登记：设计师B ｜ 类型：上架决策备忘（发版 checklist 项） ｜ 状态：✅ 拍板待落入发版清单

## 决策

**上架构建中 MCP 面（127.0.0.1:8389 MiniHttpServer）默认不拉起；仅在「开发者模式」开启时启动。**

- 普通用户零感知、零暴露面；开发者/验收/生态期经开发者开关启用。
- **能力不阉割**（UPG-84 红线）：收的是暴露面不是能力——开发者开关打开后全部可用。
- 生态期（桌面 agent 经 adb 桥联动等真立项）再评估转正为正式功能。

## 理由（拍板依据）

1. 上架期 99% 用户用不到它（它是测试/生态面）；
2. 持续安全面负担（token 管理/面收窄/每加工具要评估）——2026-09-02 P0（open 模式 curl vault.get 直出明文，UPG-77 修复）就是学费；
3. 验收/测试通道身份保留（开发者模式开=验收员照常 curl 真机验收）。

## 落实点（发版时转工单）

- MiniHttpServer 拉起点加开发者模式门（构建 flavor/设置项）；
- 验收通道文档同步（验收员手册：真机验收先开开发者模式）；
- release 构建 checklist 加「MCP 面默认关」验证项（启动后 8389 不应答）。
