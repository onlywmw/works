# 模式抽屉改版（两卡 + goal/审批双开关）交付

- 分支：feat/approval-mode-b；提交：`6927fbf`（含申报两节，`check-token-effect.mjs` 通过）
- 全量单测：BUILD SUCCESSFUL（PermissionGuardTest +1 用例，ChatModeTest 收敛 2）

## 用户拍板需求（2026-08-25 截图指正后）

1. 抽屉只留快速/深度思考（删编程、写作、深度思考关键词三行）
2. 选中卡灰色加深（浅色 #E8EAED / 深色 #3A3F46，✓ 由加深替代）
3. 每卡右侧竖排双开关（✓ 位置）：goal 模式 + 审批模式，默认都开、全局状态、两卡同步
4. 关键词功能彻底去除

## 开关语义（用户确认）

| 开关 | 开（默认） | 关 |
|---|---|---|
| goal 模式 | 目标驱动自动连续执行（轮次推进 + 无害级自动放行 = 现行为） | 每步写操作都弹窗确认（无害级失效——PermissionGuard.harmlessAutoAllow=false）+ 轮次不推进 |
| 审批模式 | 权限模式=默认权限（三级分诊：无害级不弹、实质级弹三键） | 权限模式=允许完全访问（写类自动放行；shell.exec/凭据/高危仍弹窗；红线 deny 不动） |

## 证据

- 真机 5556 联动矩阵：goal开+审批开 torch.on 放行 / goal关 torch.on → APPROVAL_REQUIRED / 审批关(FULL) torch.on 放行且 shell.exec 仍拦截
- 两卡开关 checked 状态同步实证（关 goal → 两组 4 个 Switch 同步 false）
- 抽屉 UI：两卡（快速/日常问答、深度思考/复杂推理）+ 各卡 goal/审批 开关行
- 模式切换：深度思考 → `MOV-Mode DEEP/pro/high`；快速 → flash（此前已验证，链路未变）

## 备注

- 审批关=完全访问时 file.write 任意路径放行（含 /data 越界写）——这是既有「允许完全访问」权限模式语义（顶栏按钮同样如此），本次只是把它接进抽屉开关；如需进一步收窄（越界写仍弹窗）可后续调整
- 关键词功能已从代码移除（ChatModeResolver 仅手动模式；deep_keywords prefs 键废弃）
