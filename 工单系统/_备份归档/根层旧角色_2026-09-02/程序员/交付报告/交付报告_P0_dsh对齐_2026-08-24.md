# 交付报告 · P0 dsh 对齐（2026-08-24）

- 分支：`feat/p0-align-dsh`（基于 origin/main 20db295）
- 范围：对比报告 P0 四项全部落地——goal 接线 / session-reference 接线 / compaction 参数校准 / token-meter overhead
- 状态：**实施完成，待验收**（真机 evidence 齐全）

## 1. goal 接线（原"已翻译未装配"）

- `goal/Goal.kt`：加 `GOAL_DEFAULT_MAX_ROUNDS = 20`（GoalRoundDriver 默认参数引用同一常量）
- MainActivity：
  - 字段 `goalDomain` + 三工具：`goal.set{id,content}`（GoalDomain.set 落 goal/change 事件）/ `goal.complete{id}` / `goal.status{}`（返回 active/goal{id,content,rounds,maxRounds}）
  - **systemPrompt 目标段注入**（每次 send 动态拼接）：`当前目标：「…」——推进到第 N/20 轮，完成时调 goal.complete…`；无目标时提示"无需 goal 工具"
- 行为：目标未设定时 **status=false / set 因会话未建立返回明确错误**（fail-closed）

## 2. session-reference 接线

- `session.reference{ids[]}` 工具（schema + handler，复用 fts5/SessionReferenceResolver，超 8 个拒错）
- **Resolver.snapshotSurface 增强**：user 原文 + assistant 文本块（问答成对，对齐 dsh surface 投影语义；原实现只投影 user——引用的上下文不连贯）
- 单测：问答成对注入 / 9 个超限 TOO_MANY_REFERENCES / 空引用

## 3. compaction 参数校准（对齐 dsh 精神）

| 参数 | 原 | 改 |
|---|---|---|
| thresholdRatio | 0.85 | **0.8**（dsh 一致：更早触发） |
| keepHeadRatio | 0.30 | **0.25** |
| keepTailRatio | 0.20 | **0.15**（总保留 50%→40%，引擎更积极） |

摘要上限保留 8K 字符（比 dsh 8192 token 更保守，注释说明取舍）。

## 4. token-meter（dsh 计价口径）

- ContextBudget 增：`TOKEN_OVERHEAD_PER_BLOCK = 4` / `TOKEN_OVERHEAD_PER_MESSAGE = 4` / `messageTokens(blocks)`（估算 + 块/消息 overhead）
- **三处应用**：BasicCompactionEngine.estimateContextTokens、AgentLoop 预算检查（原各自 `(chars+3)/4` 循环，现统一 dsh 口径）
- 单测：TokenMeterTest 3 用例（char/4 边界 + 块/消息 overhead + 空消息）

## 验证证据

**单测**：全量 gradle 绿（新增 SessionReferenceTest 3 + TokenMeterTest 3 + GoalRoundDriverTest 2；GoalRoundDriverTest 覆盖 20 轮上限 ARMED 暂停语义）

**真机（emulator-5556 / MOV_Test）**：
- 工具面：goal.set / goal.complete / goal.status / session.reference 全部注册 ✓
- UI 发消息建 agent 后：`goal.set` → `{ok=true, status=active(轮次 0)}`；`goal.status` → `{active=true, goal={id=g-p0-test, content=..., rounds=0, maxRounds=20}}`；`goal.complete` → `{ok=true, status=complete}` ✓
- `session.reference`（真实会话）→ `{ok=true, count=1}` 协议正确；9 个 id → 明确拒绝（maxReferences must not exceed 8）✓
- 会话未建立时 goal.set/complete → `{ok=false, error=会话未建立}`（fail-closed）✓

## 已知边界

- `session.reference` 快照内容依赖 fts5 readSession 已落库数据（checkpoint 后）；空会话/未建房间时回空快照（协议仍正确）——与 FTS 数据链路一致
- compaction 摘要上限未照抄 dsh（8K 字符 < 8192 token，方向更保守，避免回落成本）
- goal 轮次推进（GoalRoundDriver）在工具层保持 20 轮上限；本轮未接 AgentLoop 自动续轮（目标驱动语义由提示词+工具承担，可按需再接）
