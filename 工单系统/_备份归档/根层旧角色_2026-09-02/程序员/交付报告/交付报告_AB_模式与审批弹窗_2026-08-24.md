# A（快速/深度思考 + 关键词）+ B（审批弹窗减量）交付

- 分支：`feat/approval-mode-b`（基于 main 20db295 + e94ce28 方案文档）
- 提交：`b62cee8`（B）+ `9bafc4d`（A，含申报两节）
- 规则校验：`node scripts/check-token-effect.mjs` 通过 ✅
- 全量单测：BUILD SUCCESSFUL（+9 用例：PermissionGuardTest 5 + ApprovalServiceTurnTest 3 + ChatModeTest 4）

## B：审批弹窗减量

| 项 | 实现 | 验证 |
|---|---|---|
| 三级分诊 | PermissionGuard 拆 harmlessTools（torch/wifi/bluetooth/音量/亮度/静音/note/memory.save/causal/notification）+ file.write 仅 MOV 工作区（相对路径/公共前缀，拦 .. 与绝对越界，private 凭据写除外）→ 自动放行；shell.exec/http.post/越界/vault/凭据仍 ask；底线 deny 与 sensitive 不动（安全组13 成果） | 单测矩阵全绿；真机 torch.on 直调 `ok=true` 自动放行（不弹窗）✅ |
| 弹窗三键 | ApprovalService Answer 枚举（ALLOW_TURN/ALLOW_ONCE/REJECT）+ turnAllowSet（turn→工具集，turn 边界重置，>100 整体清空）+ OUTCOME_ALLOWED_TURN；MainActivity 前台 AlertDialog 三键（中性键=允许本轮）；后台通知保持两键（允许=本次） | 单测：同 turn 同工具不再弹/跨 turn 重置/拒绝不记录；MCP 直调保持 APPROVAL_REQUIRED 契约 |
| 目标场景 | goal 连续执行：点一次「允许本轮」→ 同 turn 同类工具全放行（弹窗排队消除） | 单测语义覆盖；agent 路径视觉验证需有效 key 设备（5556 key 失效 401，未复验弹窗 UI） |

## A：快速/深度思考模式

| 项 | 实现 | 验证 |
|---|---|---|
| 两档收敛 | ChatMode（QUICK/DEEP）：快速=flash 无 thinking；深度=deepseek-v4-pro + thinking + effort=high（固定 high） | 真机 MOV-Mode 日志：DEEP → `model=deepseek-v4-pro effort=high`；QUICK → `model=deepseek-v4-flash effort=none` ✅ |
| reasoning_effort 管线 | adapter 补发 `thinking:{type:enabled}` + `reasoning_effort`（dsh serialize.ts 字段名，有值才发）；AgentLoop.reasoningEffortOverride 装配快照（修复 f309ffb「管线死路」——原逻辑对头次请求恒 null） | 真机 JSONL `request/header` 记录 `{model:deepseek-v4-pro, reasoningEffort:"high"}` ✅ |
| 关键词临时切档 | ChatModeResolver：手动深恒深 / 命中（大小写不敏感子串）该条临时深 / 未命中走默认；下一条回默认（不全局切换——拍板推荐值实现） | 单测 4 用例全绿 |
| UI | 模式 chip（快速/深度思考，● 高亮，prefs 持久化）；长按「深度思考」chip 编辑关键词（逗号分隔）；设置页为 Vite 产物未动（关键词编辑走原生入口） | 真机 chip 切换 ● 高亮双向移动 ✅ |

## 交接/边界

- 5556 DeepSeek key 失效（****7890 401）——A/B 的 agent 执行路径（弹窗三键视觉、pro 实际回复）需有效 key 设备补验；逻辑均被单测+header/日志实证覆盖
- B 的「总是允许（持久白名单）」列为后续增强（设置页管理），本批未做
- A 的 reasoning 过程不展示（现状前端不渲染 Reasoning 块；折叠展示列后续增强）
