# UPG-18 派单文本：Android 死代码清理

> 优先级 **P2**（不阻塞，瘦身降维护成本）｜出单：设计师｜日期：2026-08-27（派前复核刷新 2026-08-29，派单 2026-08-30）
> 权威卡：工单库.md UPG-18 节｜本单方案内嵌于卡（背景/验收/红线），无独立方案文件。

---

## 〇、交接段（先读，照此执行）

**基础信息**（规则 12）
- 主仓库：`C:\Users\Administrator\0027-mov`（MOV 主工程，Android + market-web）
- 分支：`feat/upg18`｜worktree：`mov-upg18`
- 构建：`cd 0027-mov && gradle :app:assembleDebug`
- 单测（全量绿以此为准）：`cd 0027-mov && gradle :app:testDebugUnitTest`
- 真机：`emulator-5556`（`adb -s emulator-5556`，app 包 `com.mov.android`）
- 文档入口：`工单库.md` UPG-18 卡｜设计师手册 §六/§八｜验收记录 `0027-mov\docs\ACCEPTANCE_LOG.md`

**开工第 1 件事**：`git fetch origin` + 看工单库本单状态（规则 19）→ 认领：工单表 UPG-18 行备注追加
`认领: <agent> worktree=mov-upg18 branch=feat/upg18 @<时间>`。

---

## 一、派单前复核（必须，动工当天做）

以**届时 main** 重跑零引用扫描定稿删除清单——UPG-01 批1+批2 + UPG-22 R1 合 main 后**行号/符号面必漂移**：行号漂移正常（清单带 commit），语义/符号漂移回写本单。复核方法＝「定义处之外零命中」+ import 扫描；**测试引用随删随清**；所列锚点如漂移，以当天重扫为准。

---

## 二、施工范围（四批，每批独立 commit、独立回滚）

**批 1 · dsh 移植占位文件（29 个）**
`app/src/main/kotlin/com/hermes/dsh/` 下：acp/api/attachment/boot/client/coderuntime/credentials/e2b/extensions/feedback/fs/hooks/host/identity/lsp/plan/preset/runtimediagnostics/sandbox/schedule/settings/shell/skill/storage/subprocess/terminal/typert/web/workspace（**含 plan/Plan.kt**，外部 import 零命中）。

**批 2 · 整文件级（~590+ 行）**
- `llm/RetryPolicy.kt`（159 行，重试实走 DurableRetry）
- `session/persistence/sqlite/SqliteStore.kt`+`SqliteSchema.kt`（~590 行，sqlite 方案从未接线）——**sqlite-android 依赖保留**（Fts5QueryEngine 在用）
- `NoteProvider.kt`（note.open 只登记 unavailableTools）
- `McpToolProvider.kt`（零实例化，UPG-01 v2.2 划归本单删）
- **Tools.kt 死成员/死子类**：PostToolDecision 层级 + ToolCallView/ToolResultView 两接口+9 子类整层级零使用（验收员补修订①，连带 `BuiltinMcpTools.kt:11-12` 死 import、`LlmTypes` 连带 `Repair.kt:7`）

**批 3 · 零散死函数**（锚点为复核刷新后实测）
MovStorage.kt:307 displayPath｜LoginActivity.kt:485 fdp｜McpServerStore.kt:46 sanitized｜MiniJson.kt:93 bool｜AgentLoop.kt:149 inject／:101 Phase.Maintenance／:234 printStackTrace→换 Log｜SystemControlProvider bluetoothOn/bluetoothOff/wifiSet+suExec（类活于 MainActivity:2182 但四件无 handler，死链成立）｜ToolRegistry.declOf(:33)／McpToolScheduler.pendingList(:78)／Fts5QueryEngine.removeEvent(:69)｜LlmTypes 未用 Block 一批｜errorChain/isHarnessError（Error.kt:90/:115）｜MainActivity.kt:6266 togglePresentationMode（注释自述 UI 已移除）。

**批 4 · 死资源/权限**
- drawable **5 个删除**：ic_gear/ic_history/ic_market/ic_new_chat/ic_ability（app/src 全域零命中）
- Manifest **删** `CHANGE_WIFI_STATE`(:21)、`BLUETOOTH_ADMIN`(:18, maxSdk=30)；**保留 `BLUETOOTH`**（bluetoothStatus 在读）

> **六处除名项（严禁出现于删除清单）**：recordSearchHits/recordCoverHits（UPG-05/22 已接线）、phoneTail（UPG-14 已消费）、SessionReference 数据类（已接线）、drawable ic_check_white（bg_check_agree.xml:16 在用）。

---

## 三、验收标准

- **L1**：编译绿 + `gradle :app:testDebugUnitTest --rerun-tasks` 全绿（删后测试引用同步清理）；四批独立 commit，任意批回滚不影响其他。
- **L2**：真机冷启动 + 门控→登录→主界面 + 抽查 OCR/工作流/市场页功能无回归。
- **L3**：APK 体积前后对比入报告（预期瘦 ~5%）；全库重扫零引用复核（同方法学复跑，报告附清单勾选表）。

---

## 四、红线

1. 只删不改逻辑；有保留注释的（deleteRoomConfirm）不动。
2. drawable 删前复核 UPG-13 之后新引用。
3. 单测引用被删符号→测试同步删改，**禁止删断言保绿**（该补替代断言要补）。
4. 六处除名项绝不可碰。
5. Token/KV 申报「无影响」，自跑 `node scripts/check-token-effect.mjs`。

---

## 五、测试方案（规则 八 · 匹配）

代码删除/重构类 → **全量绿 + 轻变异亲杀**：①误删已接线符号（recordSearchHits 或 SessionReference 契约锚）必红 ②误删在用 drawable/权限（ic_check_white/CHANGE_WIFI_STATE 若删空则设计缺陷）。验收员会核对「测试强度＝工单性质」，不堆过度测试。

---

## 六、完成后登记（规则 12/7）

先表后库：`工单表.xlsx` 程序员列填 `✅UPG-18 完成`＋备注 `feat/upg18 <hash>（报告 DELIVERY_UPG18_*.md）`；`工单库.md` 状态改「程序员✅完成，待验收」；交付报告落 `程序员\交付报告\DELIVERY_UPG18_*.md` 并写明「已登记两个表」。
