# UPG-01 全量工具元数据补全 · 批 1（+批 2 紧随）——派单文本

> 出单：设计师 @2026-08-29 ｜ 优先级：批 1 P0 / 批 2 P0（校准批）｜ 方案：`设计师\方案设计\UPG-01_全量工具元数据补全_方案.md` **v2.3**（评审#5 收口，以此版为准）
> 规则 21 溯源复核：✅ 已过（v2.3 随版完成 @main 8af7da9，见方案 §八 + §十附注；关键结论：rebuildAgentTools 已长出 providerToolMeta 通道，批 1 从「建接线」改「收敛+推广」）

## 一、批 1 交付清单（登记层收口 + AI 面接线，六件）

**基线认知（@8af7da9，方案 §一唯一基线表）**：字面注册 147 / uiOnly 9 / 在面 145（装 browser 159）/ 模板串 ≈123 / 空 properties ≈51。现状三通道并存：`sceneToolDescriptions`（scene 2）+ `providerToolMeta`（`:241-243`，device/obsidian 20 已上面）+ 静态表 `toolParamSchemas`（71 条，字段描述全是参数名占位）——批 1 把它们收敛为「**ToolDefinition 主 + 静态表补丁位**」单源语义。

1. **开工实测（先行，半日内出清单）**：实测 provider 20 + scene 2 已上面工具的**字段级 schema 与 output 声明现状**——缺口列清单（工具名/缺哪件），**归批 3 首批清偿**，不在批 1 断言内（评审#5 时序硬伤修复口径）。
2. **三通道收敛**：`rebuildAgentTools()`（`:6153-6175`）取值链改从 ToolDefinition 投影（无登记回落模板串——回落即运行时降级路径，不另设开关）；provider 两族元数据**迁 ToolDefinition**（或显式声明例外清单，逐条注释理由）；`sceneToolDescriptions`/`providerToolMeta` 通道归并，禁新建平行结构、禁元数据落盘。
3. **`paramSchema` 支持真字段描述**（字段级中文 description ≤15 字/字段，语义查源码核实，写「来自 xxx.kt:行」注释）。
4. **快赢接入**：BuiltinMcpTools 5 条 + BrowserMcpTools 14 条既有真元数据接入取值链（现有描述只增不改，browser 字段级补全归批 2）。
5. **MCP 面补传 inputSchema**：宿主注册循环（`:3281-3290` 区）补传，`McpServer` 方法签名不可变（架构不变量）。
6. **冲突检测 + 回落清单**：ToolDefinition vs 静态表**同名异值冲突必红**（构建期/测试期）——**首用例 = 静态表 4 条 builtin 条目 vs BuiltinMcpTools 自带 schema**，批 1 落地当天验证机制真跑；测试输出「仍走模板串回落的工具清单」（登记债可见）。

## 二、批 1 验收口径（给验收员）

- **L1**：`ToolMetaTest` 遍历**登记层 + rebuildAgentTools 投影输出**（测投影不测文件）——**接线断言**：已登记 41 工具（scene 2 + provider 20 + builtin 5 + browser 14）description ≠ 模板串；回落清单输出存在；冲突检测首用例过。**三件套全绿不在批 1 断言内**（按族下放，见清单 1）。计数断言写**不变量**（「不存在已注册、非 uiOnly、无登记的在面工具」），不写绝对数。
- **变异亲杀两条**：①登记层任一描述改占位 → 必红；②把接线改回模板串 → 必红。
- **L2 真机**（便宜可重跑，抽样 4 件）：① AI 面投影实物——会话装配快照/logcat 可见真描述非模板串（scene/builtin/browser/宿主各抽 1）；② MCP 面 `tools/list` curl 实证 inputSchema 非空；截图/journal 入 `docs/ACCEPTANCE_LOG.md`（append-only）。
- **全量绿 + assembleDebug + `node scripts/check-token-effect.mjs`**；Token/KV 两节申报（估算以方案 §五推导式为量级参考，实测为准）。

## 三、批 2 紧随同发（browser 校准批，可与批 1 并行认领）

- **范围**：`BrowserMcpTools.kt` 14 工具——字段级中文描述 + output 声明（**点名顶层返回键**，有错误码才附语义）；已有 14 条一句话描述只增不改。
- **校准使命**：① 跑通「撰写+来源标注+验收断言」全流程，**测出单工具实测工时**（批 3 拆子单依据）；② **顺带产出「近 N 天 journal 工具调用频次聚合 Top 榜」**（一次性统计脚本落 `tools/` 随单入库）——Token 拍板数据 + 批 3 排序轴输入，不新打点（journal 已有 tool_call 链）。
- **验收**：browser 14 三件套全绿（字段级描述 + output 顶层键断言）；变异两条同批 1。
- **撞车评估**：与批 1 不同文件（BrowserMcpTools.kt/Tools.kt 登记位 vs MainActivity 装配区），可并行；ToolDefinition 登记格式以批 1 落地形态为准——**批 2 在批 1 取值链合并后开工，或并行时先按方案 §二契约写、合入时对齐**。

## 四、红线（方案 §六全套，复述硬项）

1. 不改工具行为/调用签名；`McpServer` 方法签名不可变。
2. 描述必须真实（字段语义查源码核实）；拿不准标「待核实」进挂账登记表——**同时挂账 ≤8 个**，L1 报告随附清单；禁编造、禁「占位/待补充」字样。
3. 描述 ≤60 字，三段优先级 **功能 > 限制 > 场景**；**近邻工具须写差异点**（何时用我、何时用隔壁）。
4. 现有已验收描述只增不改（除非事实性错误）。
5. **登记层与接线同批交付**（只补不接 = 白做）。
6. 遵守「请求前缀恒定」：不得新增 tools 字段会话中途变动点。
7. 禁新建平行登记结构 / 禁元数据落盘（平行数据源红线）。
8. 回滚 = revert 单 commit。
9. `MainActivity.kt` 纯 CRLF；Kotlin 块注释内不得嵌 `/* */`；Edit 前必 Read。

## 五、交接段（程序员领单按此执行）

1. **开工前 `git fetch origin` + 看工单库/工单表本单状态**（规则 19：以表为准）；认领登记：工单表本行备注追加 `认领: <agent> worktree=mov-upg01 branch=feat/upg01 @<时间>`（批 2 认领 `branch=feat/upg01-b2`，worktree=mov-upg01-b2），基于**最新 main** 切分支。
2. **串行约束**：UPG-22 在施 `MainActivity`（记忆装配区 :3555 区 + McpToolScheduler 名单区）——本批动 MainActivity 元数据区（`:241` 区/`:3281` 区/`:6153` 区），**同一文件同一时间一个 agent**：UPG-22 未交付前开工须各用独立 worktree 并约定 MainActivity 编辑窗口，先完成者先合、后到 rebase（规则 9）。
3. **完成后登记两个表（先表后库）**：`工单表.xlsx` 程序员列 `✅完成` + 备注 `分支名 hash（报告 DELIVERY_UPG01_*.md）`；`工单库.md` 状态快照同步。
4. 交付报告落点 `程序员\交付报告\DELIVERY_UPG01_批1_*.md`（批 2 `DELIVERY_UPG01_批2_*.md`），报告末尾明确写「**已登记两个表**」，并含 Token/KV 两节申报 + 批 1 开工实测缺口清单（批 3 首批输入）。
5. 产物提交 `0027-mov` 分支 `feat/upg01`（批 2 `feat/upg01-b2`），交付时报 hash。

## 六、溯源复核记录（规则 21，出单前已核 @8af7da9）

| 锚点 | 状态 |
|---|---|
| `rebuildAgentTools():6153-6175` 取值链三通道并存（scene/provider/模板串回落） | ✅ 实测在案，批 1 收敛对象 |
| `providerToolMeta :241-243` = DeviceProvider.allMeta(`:333`)+ObsidianProvider.allMeta(`:229`)，20 工具真描述+schema 已上面 | ✅ 通道实物 |
| 静态表 `toolParamSchemas` 71 条 `:244` 起，字段描述参数名占位 | ✅ |
| uiOnly 9（`:56-63`，含 approval.setMode/market.localOverview） | ✅ |
| MCP 面宿主注册不传 inputSchema（ToolSpec 默认空 `McpServer.kt:29`） | ❌→本单修 |
| Builtin 5（`BuiltinMcpTools.kt:107-206` 区）/Browser 14（`:32-47`）真元数据未接上面 | ⚠️→批 1/批 2 接 |
| L6 平行数据源：工具元数据零持久化零第二写点 | ✅（禁落盘维持） |

依赖节点（设计网络）：运行时接线四点 ✅、McpToolProvider 孤岛 ❌（批 4 裁决归 UPG-18 删，不复活）、registerMovQueryTools 死代码（批 4 范围）。
