# UPG-01 批 3-4 · 市场与界面（24 工具）——派单文本

> 出单：设计师 @2026-08-30 ｜ 优先级：P0（批 3 收口子单，最后）｜ 方案：`设计师\方案设计\UPG-01_批3批4_方案设计_v1_2026-08-29.md` **v1.1**（评审 P1/P2 修正 + P3 吸收，以此版为准）
> 规则 21 溯源复核：✅ 已过（@main af68d22）。本单 = 批 3 中 **24** 工具，精确清单以派单当天重跑 §一 grep 基线为准（规则 21）。
> **本单 = 批 3 收口子单**：承担 hostToolMeta 四表聚合（MainActivity 编辑窗口，与 B3-1 首并入互斥）。

## 一、交付清单（hostToolMetaB4 登记 + 批 3 聚合收口）

1. **hostToolMetaB4 登记（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB4.kt`，`data class HostToolMetaEntry(description, schema, output)` + `val hostToolMetaB4: Map<String, HostToolMetaEntry>`，收录附录 A 的 24 工具。
   - **desc** ≤60 字：功能 > 限制 > 场景；近邻工具必须写差异点（附录 B）
   - **schema** `paramSchemaDesc` 字段级真描述 ≤15 字/字段 + 「来自 xxx.kt:行」；真无参 = 空 properties
   - **output** 点名顶层返回键（有错误码才附语义）
2. **批 3 聚合收口（本单独有）**：MainActivity companion 内 `hostToolMeta = hostToolMetaB1 + hostToolMetaB2 + hostToolMetaB3 + hostToolMetaB4`（四表合并，收口批 3）——**此触碰点与 B3-1 首并入互斥**：同文件同一时间一个 agent，先完成者先合、后到 rebase（规则 9）
3. **收口断言（入 L1）**：四表合计 118 无缺漏（不变量断言，不写绝对数）

## 二、验收口径（测试方案，给验收员）

- **L1**（`ToolMetaTest` 扩展）：
  - hostToolMetaB4 遍历：desc 非模板串/无「占位/待补充」黑名单/≥20 字；字段级描述非参数名占位；**output 非空非占位（自动断言）**，顶层键正确性归人工抽查/L2（评审 P3-6）；真无参合法
  - **回落收敛**：本单 24 工具从 MOV-Boot 回落清单消失；**收口后回落=0（批 3 全量，含前序子单）**
  - **四表聚合断言**：hostToolMeta 合计 = 118（不变量式，不写绝对数）；`metaConflicts` 零冲突
  - **变异亲杀两条**：①任一 desc 改占位 → 必红；②接线改回模板串 → 必红
- **L2 真机**（抽样 4 件）：回落清单不含本单工具 + 抽样 AI 面 desc 真描述；截图入 `docs/ACCEPTANCE_LOG.md`
- **L3 AI 实证**：journal 可见正确选参（market.install 正确传 id；ui.openMarket 正确跳转）
- **计数断言**：不变量式，不写绝对数
- **全量绿 + assembleDebug + `node scripts/check-token-effect.mjs`**；Token/KV 两节申报（**批 3 收口总账实测**）

## 三、红线（方案 §五全套，复述硬项）

1. 不改工具行为/调用签名；`McpServer` 方法签名不可变。
2. 描述必须真实（字段语义查源码核实）；拿不准标「待核实」进挂账登记表——**同时挂账 ≤8**，L1 报告随附清单；禁编造、禁「占位/待补充」字样。
3. desc ≤60 字，三段优先级**功能 > 限制 > 场景**；近邻工具须写差异点。
4. 现有已验收描述只增不改（除非事实性错误）。
5. **登记层与接线同批交付**（本单含聚合收口，收口即接线全部生效）。
6. 请求前缀恒定：不得新增 tools 字段会话中途变动点。
7. 禁新建平行登记结构 / 禁元数据落盘。
8. 回滚 = revert 单 commit。
9. `MainActivity.kt` 纯 CRLF；Kotlin 块注释内不得嵌 `/* */`；Edit 前必 Read。

## 四、交接段（程序员领单按此执行）

1. 开工前 `git fetch origin` + 看工单库/工单表本单状态（规则 19）；认领登记：工单表本行备注追加 `认领: <agent> worktree=mov-upg01-b3-4 branch=feat/upg01-b3-4 @<时间>`，基于最新 main 切分支。
2. **串行约束**：本单动 MainActivity companion 聚合区（四表合并收口）——**与 B3-1 首并入互斥**（同文件同一时间一个 agent）；**开工前提 = B3-1 已合 main**（聚合变量 `hostToolMeta` 及其入参存在），B3-2/B3-3 可后到 rebase 对齐。
3. 完成后登记两个表（先表后库）：`工单表.xlsx` 程序员列 `✅完成` + 备注 `分支名 hash（报告 DELIVERY_UPG01_批3-4_*.md）`；`工单库.md` 状态同步。
4. 交付报告落点 `程序员\交付报告\DELIVERY_UPG01_批3-4_2026-08-30.md`，末尾写「已登记两个表」，含 Token/KV 两节申报（批 3 收口总账）+ 回落=0 对账 + **四表聚合 118 核对清单**。
5. 产物提交 `0027-mov` 分支 `feat/upg01-b3-4`，交付时报 hash。

## 五、附录 A：本单 24 工具 + env/cond（§1.6）

**全部 env=mobile / cond=none**（市场/UI/权限查询均为设备本地能力）：

| 工具（24） | env | cond |
|---|---|---|
| market.list / install / uninstall / enable / disable / status / refresh（7） | mobile | none |
| marketAdmin.pending / approve / reject（3） | mobile | none |
| ui.openMarket / openMarketReview / openModels / openOrders / openSettings / openVault / openWorkbench / prefillInput / getPins / setPins / setLang（11） | mobile | none |
| permission.mode / presentation.mode / approval.getMode（3） | mobile | none |

> 注：permission.set_mode / approve / deny、presentation.set_mode、approval.setMode、market.localOverview = **uiOnly 9**，不上面、不登记、排除（方案 §1.3 注）。

## 六、附录 B：近邻差异点速查（撰写必读）

- **market.7**：install=安装 / uninstall=卸载 / enable=启用（已装）/ disable=停用（已装）/ status=查状态 / list=枚举 / refresh=刷新源——**install 与 enable 易混**（装 vs 启用）、**uninstall 与 disable 易混**（卸 vs 停用）
- **marketAdmin.3**：pending=待审列表 / approve=通过 / reject=驳回——审核流三步
- **ui.open\* 11**：各开不同页（市场/市场审核/模型/订单/设置/保险柜/工作台/预填/取钉/设钉/切语言）——desc 逐页写清「打开 X 页」；**openMarket 与 openMarketReview 区分**（用户市场 vs 审核后台）
- **permission.mode / presentation.mode / approval.getMode 3**：均为**只读查询**（AI 能查不能改，改类已 uiOnly）——desc 写清「只读，改走 UI」
- 每条 desc 附「来自 xxx.kt:行」锚点，字段语义以 handler 实现为准（红线 2）
