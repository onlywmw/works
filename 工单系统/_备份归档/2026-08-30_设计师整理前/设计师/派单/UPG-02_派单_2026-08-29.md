# 派单：UPG-02 设备类工具包迁移（AVD 内置 device.*/sensor.*）· 2026-08-29

> ⚠️ **作废留档**：用户拍板 @2026-08-29 UPG-02 与 UPG-04 合并施工（共享 MainActivity 装配区），以合并派单为准：`UPG-02+04合并_派单_2026-08-29.md`。

> 派单人：设计师 ｜ 优先级：P0 ｜ 状态：已派单待认领
> 方案文档（必读，含分层溯源图）：`设计师\方案设计\UPG-02_设备类工具包_方案设计.md`（v2.1）
> 卡面：`工单库.md` UPG-02 节 ｜ 排序：UPG-03 已闭环（接线四点样板在码，feat/upg03 @ e3ea812 / merge 198e26f），本单照抄其接线模式

---

## 一、任务

迁移老版设备类工具真缺口 **13 个**进新版工具面。重复建设 5 项（battery/volume/brightness/vibrate/notify）**已拍板放弃迁移**——新版已有，不做。

## 二、工具清单（方案 §四）

- 迁移：device.network（**零权限子集**：类型/在线/计量；蜂窝信号强度列施工期预研，拿不到砍字段不砍工具）、device.storage、device.toast（ASK）、device.appList、device.appLaunch（ASK）、sensor.list、calendar.list（ASK）、calendar.add（ASK）、device.timer（ASK，**落盘**——老版内存态重启丢，SystemToolProvider.java:42 前科）
- 重写：camera.capture（ASK；FileProvider+结果回调，产物落 filesDir——老版 Uri.fromFile targetSdk≥24 崩+启动即返回拿不到照片）、screen.capture（ASK；落 filesDir——老版落公共目录违红线，新版没有此能力）
- 新开发：qr.scan（ASK；老版是 stub 永返 false；复用仓内 ML Kit 依赖）、camera.ocrCapture（ASK；老版无此工具，复接 image.ocr）

## 三、施工要点（方案 §五，照 UPG-03 已验证模式）

1. `app/src/main/java/com/mov/android/DeviceProvider.kt`——**`class DeviceProvider(ctx: Context)`**（照 HardwareProvider/SystemControlProvider 范式；**禁照 SceneTools/BuiltinMcpTools 的 object 单例**——设备工具必须持 Context）
2. **接线四点配套，缺一不进面**（UPG-03 已跑通的样板）：
   - MainActivity mcpHandlers 注册段实例化注册；
   - **schema 随 Provider 走**（DeviceProvider 自带 schemas 表，注册段一次 merge；不在 :177 静态硬编码人工同步——三单共享口径）；
   - 权限名单：写类进 `writeTools`（McpToolScheduler.kt:99-107），查询类进 harmless——**漏登记 = else 静默 ALLOW 陷阱（:187-188）**；
   - `isHardwareTool` 白名单（MainActivity.kt:5613-5618，HARDWARE 模式可见性）+ ToolRegistry 并行/排他声明（查询类 true，:1582-1593 先例）
3. **黑名单先对齐再同步**：四处镜像（McpToolProvider.kt:30 / McpMarket.kt:561 / McpExtDiscovery.kt:31 / McpToolScheduler.kt:93）内容已不一致（sms.read vs sms.send 等），且 McpToolScheduler 是 contains 子串匹配、其余三处精确匹配——先对齐现有名单，再补等价变体（sms.recent/contacts.search/location.get）四处同步
4. 产物红线：截图/照片只落 filesDir，不进公共目录

## 四、验收标准（方案 §六）

- L1：契约单测全绿 + 变异亲杀（描述改占位必红；等价敏感变体必被 dropped；**写类工具从 writeTools 移除 → 调用必须 ASK 而非静默 ALLOW**）+ 接线断言（生产迭代点存在）
- L2：真机（emulator-5556/5558）逐项激活：device.storage/sensor.list 返真值；camera.capture 触发 ASK 且产物落 filesDir（截图）
- L3：AI 对话触发 sensor.list 调用，journal 真实 tool_call 链

## 五、纪律（不可省）

1. **开工前先 `git fetch origin` + 看工单库/工单表本单状态**（规则 19——本单派单前设计师已核：无 feat/upg02 分支、表/库均待派单）；认领即注记：工单表 UPG-02 行备注追加 `认领: <agent> worktree=mov-upg02 branch=feat/upg02 @<时间>`（基于最新 main 切分支）
2. **完成后必须登记两个表**（先表后库）：工单表程序员列 `✅完成` + 备注分支/hash/报告名；工单库状态改「程序员✅完成，待验收」；报告里明确写「已登记两个表」
3. 交付报告落点：`程序员\交付报告\DELIVERY_UPG02_*.md`
4. **Token/KV 申报**（硬规则 1）：tools 字段新增 13 个 schema——**每轮请求 token 显著增加**，必须申报量级估计 + 「会话开始装配定型」合规论证；交付前自跑 `node scripts/check-token-effect.mjs`
5. 交付即 `git push origin feat/upg02`
6. 溯源发现的卡外问题 → `处理中心\挂账登记表.md`，不顺手修不隐瞒

## 六、交接基础信息

- 仓库：`C:\Users\Administrator\0027-mov`（worktree=mov-upg02 强绑定）
- 构建/测试：`export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"` + `./gradlew :app:assembleDebug :app:testDebugUnitTest`（wrapper 已入库，新 worktree 直跑）
- 老版参照仓：`C:\Users\Administrator\MOV-APP-old`（36 个设备工具实物在 HermesGapProvider/SystemToolProvider/ExtraToolProvider）
- 设计网络：本单触及 [[设计师/设计网络/BP/BP-03]]（权限门静默放行——验收变异钉死）；接线样板见 UPG-03 已闭环实物
