# 派单：UPG-03 生活场景工具（12306 内置）· 2026-08-29

> ⚠️ **作废留档**：本派单文本写就当日，施工/验收/审验/合 main 链路已由流水线在同日跑完闭环（merge 198e26f）——本文档未启用，仅作过程留档。

> 派单人：设计师 ｜ 优先级：P0 ｜ 状态：已派单待认领
> 方案文档（必读，含分层溯源图）：`设计师\方案设计\UPG-03_生活场景工具_方案设计.md`（v2.1）
> 卡面：`工单库.md` UPG-05 之后的 UPG-03 节 ｜ 派单排序说明：本单是「接线四点」样板单，UPG-02/04 排在其后照抄模式

---

## 一、任务

把已在库但孤立的 `SceneTools.kt`（scene.trainQuery / scene.stationLookup 两工具）**接线进运行时工具面**，并随单修复站表解析疑似 bug。地图线已删不做（用户拍板：老版 KEY 链从未存在，属新建设非迁移）。

## 二、施工内容（按方案 §四，要点摘录）

1. **L4 接线（核心）**：`MainActivity.kt:3121` 的 `BuiltinMcpTools.all()` 迭代后追加 `SceneTools.all()` 迭代（复用 :3159-3173 通用分支）；**schema 随 Provider 走**（SceneTools 自带 schemas 表，注册段一次 merge，不在 :177 静态硬编码）；:8389 暴露循环自动跟随。
2. **站表 bug 随单修**：`SceneTools.kt:50-57` 取 `f[0]` 当电报码 vs 老版正确取 `f[2]`（`Ticket12306Stations.java:260-268`）——以真实 12306 响应验证字段顺序后修正；不修查票必败（L2 验收天然互锁）。server.mjs 同款在 S-06 挂账，**不动**。
3. **错误分类三态移植**：老版 H1 归因（REASON_SOURCE_DOWN / PARSE_DRIFT / NOT_CONFIGURED，`Ticket12306Provider.java:46-49`）落入新版错误契约。
4. **风控节补写**：`docs/MCP_TOOL_DEV_GUIDE.md` 补 12306 风控注意事项（挂账登记表 :41 在案，随单销）。
5. **降级策略（契约）**：查票失败引导用户打开 12306 官方 App/网页；重试上限 1 次端点轮换，再败即降级，禁止无限重试。
6. 权限归因写对：scene.* 只读走 else ALLOW（非 harmless 名单），代码注释与交付报告口径一致。

## 三、验收标准（方案 §五）

- L1：契约单测全绿 + 变异亲杀 + **接线断言**（SceneTools.all() 存在生产迭代点——防「函数在、链断」）；
- L2：真机（emulator-5556/5558）`scene.trainQuery` 真实返回车次（截图+journal）——同时实证站表 bug 已修；
- L3：AI 对话「帮我查北京到上海高铁」→ journal 真实 tool_call（不能只测函数）；
- 接线通过后官网 12306 卡回补属后续动作（S-08 撤卡遗留，不在本单）。

## 四、纪律（不可省）

1. **开工前先 `git fetch origin` + 看工单库/工单表本单状态**（规则 19）；注意 `feat/upg03` 分支已在库（自 433a0b1 切出）——核实基线后决定续作还是基于最新 main 重切；认领即注记：工单表 UPG-03 行备注追加 `认领: <agent> worktree=mov-upg03 branch=feat/upg03 @<时间>`。
2. **完成后必须登记两个表**（先表后库）：工单表程序员列 `✅完成` + 备注分支/hash/报告名；工单库本单状态改「程序员✅完成，待验收」。交付报告里明确写「已登记两个表」。
3. 交付报告落点：`程序员\交付报告\DELIVERY_UPG03_*.md`。
4. **Token/KV 申报**（AGENTS.md 硬规则 1）：本单 tools 字段新增 scene.* 两个 schema——前缀有变化，须申报（会话开始装配定型即合规），交付前自跑 `node scripts/check-token-effect.mjs`。
5. 推送纪律：交付即 `git push origin feat/upg03`（UPG-05 R1 漏推的教训——合 main 链需要 origin 上有分支）。
6. 溯源发现的新问题（卡外）→ `处理中心\挂账登记表.md`，不顺手修不隐瞒（规则 10/16）。

## 五、交接基础信息

- 仓库：`C:\Users\Administrator\0027-mov`（worktree 命名强绑定工单号 `mov-upg03`）
- 构建/测试：`export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"` + `gradle :app:assembleDebug :app:testDebugUnitTest`（仓库已含 wrapper，新 worktree 直接 `./gradlew` 可用）
- 真机：emulator-5556/5558（5558 已配 deepseek_key）
- 设计网络节点：本单依赖[[设计师/设计网络/节点/运行时接线四点]]、触及 [[设计师/设计网络/BP/BP-02]]（站表一根两伤——修 Kotlin 侧时同步知会 S-06 方向）
