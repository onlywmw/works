# 挂账登记表（挂账唯一落点 · 待审池）

> **规则**：`挂账登记与转工单规则.md`（2026-08-17 改版：挂账登记独立落点，不再写工单表）
> **流程**：登记挂账（⏳挂账待审）→ 设计师验证 → ✅已落实 **转工单**（写 `工单表.xlsx`）→ 走既有工单流转
> **落点分工**：本表 = 挂账待审池；`工单表.xlsx` = 正式派单权威（只放已转工单）
> **红线**：登记即挂账，不删除他人登记（疑义走打回/销项）；不主动收集，仅工作中被动发现；改表前备份

---

## 登记方法（任何角色）

在下方表格加一行：`挂账号（挂账-<简短描述>）| 标题 | 优先级 | 现象→证据→建议 | 登记人 | 日期 | ⏳挂账待审 | —`
设计师验证后更新「设计师验证」列：✅已落实（转工单）/ 打回补证 / 不落实销项。

---

## 挂账清单（2026-08-17 迁移自工单表）

| 挂账号 | 标题 | 优先级 | 登记人 | 日期 | 设计师验证 | 施工/转工单 | 现象 → 证据 → 建议 |
| 挂账-upg08-workbench状态语义 | 工作台 UPG-08 状态显示「验收中」而非「已验收通过/待合main」：sync 将验收员列任意 ✅ 推导为「验收中」 | P3 | 验收员 | 2026-08-26 | ✅已验证属实 @2026-08-26，定夺：采纳建议——状态推导细分（验收员列「通过」→待合 main、「打回」→打回修复中、其他✅→验收中；合main列✅已合→已完成） | 转工单 UPG-10 → 已解决，转入已知坑库（坑名: 工作台状态语义「验收中」无法表达「已通过待合」） | 现象：UPG-08 已验收通过（验收员列=✅ 通过），线上工作台仍显示 UPG-08=「验收中」；根因：sync-workbench.mjs pipelineFromXlsx 仅以「列有 ✅」定状态（设计师→程序员→验收员→审验员→合main 依次覆盖），验收员列 ✅ 一律输出 status=验收中，无法表达「已通过待合」。证据：https://mow.kim/wb/workbench.json（UPG-08=验收中）+ 工单表（验收员 ✅ 通过）。建议：状态推导区分「✅ 通过/打回」，或以合 main 列为主判定终态（本缺陷不阻塞，属展示语义）。 |
| 挂账-upg11验收-报告计数虚高 | UPG-11 交付报告称 PrivacyConsentTest「13 用例 13/13 绿」，实测 @Test 共 11 个（11/11 绿） | P3 | 验收员 | 2026-08-27 | ⏳挂账待审 | 已转工单 | 现象：报告 L1 节「13 用例」与我独立复核 grep @Test 计数 11 不一致；证据：app/src/test PrivacyConsentTest.kt grep -c @Test = 11 + 测试结果 XML tests=11；建议：更正报告计数（不涉及功能，属文档一致性） | 【同族后续注记 @2026-08-29 验收员】：UPG-21 申报「274 tests」实测 272、UPG-05 申报「291」实测 297（报告计数与实测不符族第三次/第四次现身）——建议设计师定夺统一口径（报告计数以 --rerun-tasks 实跑 XML 为准并注明统计时点） |
| 挂账-upg11验收-测试锚点弱 | PrivacyConsentTest 对「双 LAUNCHER」（Gate+Main 各一个）变异不敏感——MainActivity 节点提取以 /> 为边界，自闭合改嵌套后边界漂移致断言漏检 | P3 | 验收员 | 2026-08-27 | ⏳挂账待审 | 已转工单 | 现象：标准「LAUNCHER 移回」变异（删 Gate+加 Main）能红 1，但双 LAUNCHER 变异（只加不删）不红；证据：独立变异 A 两版本对比实测；建议：R2 用节点树解析（xml 库）或 extractActivityNode 精确提取，锚定「MainActivity 不得含 LAUNCHER」 |
| 挂账-upg11验收-拒绝退场前台异常 | UPG-11 拒绝→退出应用后 topResumedActivity 显示 ChromeTabbedActivity（非 Home launcher）——退场前台还原疑异常 | P2 | 验收员 | 2026-08-27 | ✅已销项 @2026-08-29（设计师定夺：非缺陷，采纳查验员复核——「退出应用」= finishAndRemoveTask() 源码实证（PrivacyGateActivity 二次说明对话框负钮），任务移除后回前一任务（Chrome）系 Android 标准行为，非前台还原异常；合规判据「不同意不可用+零收集」验收已实证满足；finishAffinity 不改变此结果；「退必回桌面」如需要属产品决策另立单） | 已转工单 | 现象：拒绝退出后模拟器前台为 Chrome 而非桌面；证据：dumpsys topResumedActivity=com.android.chrome；建议：复测是否 finishAndRemoveTask 前台还原异常或模拟器环境噪音（不影响零收集合规——退出后 shared_prefs/files 均空）；必要时改为 finishAffinity 双保险。【查验员复核 @2026-08-29：建议销项（非缺陷）——源码实证拒绝→二次说明→退出应用=finishAndRemoveTask()（PrivacyGateActivity.kt:118-121）；任务移除后系统回前一任务（Chrome）是 Android 标准行为，非「前台还原异常」；合规判据（不同意不可用+零收集）验收已实证满足；finishAffinity 不改变退场后前台归属（仍回前一任务），「退必回桌面」若需要属产品决策另立单】 |
| 挂账-upg12观察-WebView默认错误页 | UPG-12 设置页 WebView 首加载若遇瞬时不可达，会短暂显示系统默认错误页（Something went wrong / no network connection）后再恢复 | P3 | 验收员 | 2026-08-27 | ⏳挂账待审 | 已转工单 | 现象：L3 飞行模式首复现瞬时出现系统 WebView 错误页，完整等待后正常渲染（appassets 本地资产在飞行模式下零网络请求，误报属时序）；证据：R3 连拍 + logcat 网络错误计数=0；建议：SettingsSheet 优化错误页显示（onReceiveError 时保持 s0 同色底+重试指引，或等待 content 渲染完成再揭占位），属体验优化 |
| 挂账-upg13-tab往返发码按钮消失 | 登录页 Tab 往返（验证码→密码→验证码）后「获取验证码」按钮永久消失，杀进程重进才恢复 | P1 | 审验员 | 2026-08-27 | ✅已销项 @2026-08-27（修复 @6410cb3 审验复验有效 + 设计师合 main 闭环；LoginTabSwitchContractTest 双向断言防回归） |  已施工（追加#9 @6410cb3） | 现象：emulator-5554 实测——force-stop 重启后按钮在 [733,1707][1006,1844]；点「密码登录」再点回「验证码登录」按钮即消失（「请输入验证码」输入框正常恢复）。根因：LoginActivity.applyModeUi() 只有 `if (mode == "pass") sendBtn?.visibility = View.GONE`，缺 else 恢复 VISIBLE（mov-upg13 LoginActivity.kt:372；UPG-09 772e2a5:357 同源预存，UPG-09 验收与 UPG-13 复验均未覆盖 Tab 往返）。证据：uiautomator dump 三段（重启后在→往返后消失）+ scr4 截图。建议：applyModeUi 补 `else sendBtn?.visibility = if (mode == "code") View.VISIBLE else View.GONE` 一行；修复后补 Tab 往返回归断言 |
| 挂账-upg14-AccountMe零调用 | UPG-14 account.me 桥 inline 登录态装配，AccountMe 纯函数零生产调用（仅测试引用）——双实现/假覆盖 | P1 | 验收员 | 2026-08-28 | ✅已落实销项 @2026-08-29（设计师抽查实证：main `MainActivity.kt:2142` 桥调 `AccountMe.me` 为唯一实现 + inline `mapOf(loggedIn…)` 装配 grep 零命中；R1 ce5ab0d 修复 + R2 复验已合 main 8aaa999） | 已施工（R1 @ce5ab0d） | 现象：MainActivity:1821 mcpHandlers["account.me"] inline mapOf(loggedIn/phoneTail/masked)，AccountMe.me() 未被调用；grep app/src/main AccountMe 仅 AccountMe.kt 自身。证据：生产引用计数 1 + 桥 inline vs 纯函数结构重叠 + AccountMeTest 只测纯函数/注册字符串不断言桥调用。根因：桥未接入纯函数（同 W-04/W-06 测试锚定独立副本族，第四现身）。建议：桥改 AccountMe.me(...) 为唯一实现 + 测试断言「桥源码含 AccountMe.me」防漂移；修复后重跑 L1+L2。【查验员复核 @2026-08-29：已修复，建议销项——main MainActivity:2142 桥调 AccountMe.me 为唯一实现、inline mapOf(loggedIn/phoneTail/masked) 装配 grep 零命中（本复核亲跑 git grep main）；R1 ce5ab0d 修复 + R2 复验通过已合 main（8aaa999）】 |
| 挂账-upg14-残留声明不实 | UPG-14 报告称「logoutToast 文案已随死按钮删除（源+产物零命中）」与实测不符——settings i18n 字典仍含 logoutToast/accountToast demo 值 | P2 | 验收员 | 2026-08-28 | ✅已落实销项 @2026-08-29（设计师抽查实证：git grep main 源码 i18n + 入库产物 `app/src/main/assets` logoutToast/accountToast 双零命中；R2 复修 04341d2 已合 main；残留仅在 gitignored stale dist——非交付物，其污染通路另立新挂账「挂账-syncpages不校验dist新鲜度」） | 已施工（R2 复修 @04341d2） | 现象：tokens-Cef_8huI.js settings.i18n 含 logoutToast:"Logged out (demo)" 与 accountToast:"Account profile (demo)"；UI 无按钮引用（功能去重成立），但产物非「零命中」。证据：grep tokens 命中 + SettingsPage 产物 onMounted 接 account.me 真数据。建议：清这两个 demo key（或标注未用）+ 报告更正「零命中」表述为「UI 无引用、字典残留待清」。【查验员复核 @2026-08-29：已修复，建议销项——R2 复修 04341d2（已合 main）后 git grep main 双零命中（本复核亲跑）：源码 i18n zh/en + 入库产物 app/src/main/assets 均无 logoutToast/accountToast；残留仅存在于 0027-mov 未入库 gitignored 的 stale dist/（R2 前旧 hash SettingsPage-BsK8cjDB.js/tokens-FbUJK5PZ.js）——非交付物，但 sync-pages.mjs 不校验 dist 新鲜度，建议顺手清 dist 或 vite 重建防再污染】 |
| 挂账-upg14-报告249绿不符 | UPG-14 报告「全量 249 绿」与实测不符：我的 --rerun-tasks 全量首跑 BUILD FAILED（AccountMeTest 桥注册断言 1 failure——源码桥在，属 moduleFile 路径解析 flaky） | P3 | 验收员 | 2026-08-28 | ✅已销项 @2026-08-29（设计师抽查实证：`AccountMeTest.kt:43-48` moduleFile「文件存在才读、找不到明确断言」修复已在 main；R2 复验口径一致 + UPG-17 复核全量 253/0/0 flaky 未复现） | 已施工（随 R2 @e16fae8） | 现象：全量 XML failures=1（MainActivity 缺 account.me 桥注册断言失败——源码有桥）；AccountMeTest 单类重跑 BUILD SUCCESSFUL（6/6）。证据：全量失败日志 + 单类重跑绿。建议：报告更正为「单类 6/6 + 全量 flaky（路径解析时序）」；测试 moduleFile 路径解析加「文件存在才读否则明确断言」防环境误读。【查验员复核 @2026-08-29：建议销项——R2 复验全量 250 绿口径与实测一致（e16fae8 三判据之三，moduleFile 路径解析已按「文件存在才读」修）；@2026-08-28 深夜 UPG-17 复核全量 253/0/0 该 flaky 未复现（含 UPG-14 全部测试类）】 |
| 挂账-upg16-L3真机走查 | UPG-16 release 签名 APK 装机启动走查（L3）因模拟器 adb 连接不稳降级——可用环境后补验 | P3 | 验收员 | 2026-08-28 | ⏳挂账待审 | 已转工单 | 现象：emulator-5554 device↔offline 抖动、adb reconnect 后设备消失、uninstall 时 offline——release APK 装机启动无法可靠执行；替代证据：apksigner verify 签名有效（CN=JiangXi PiDan Technology）+ INSTALL_FAILED_UPDATE_INCOMPATIBLE（signatures do not match，证明 release 签名链生效）。建议：可用环境（模拟器稳定或真机）后装 release APK 走查启动→门控→登录页→主界面；装前需卸载 debug 版（签名不同） |
| 挂账-upg14-r2源码修复未入库 | UPG-14 R2（e16fae8）提交信息声称 SettingsPage.vue 直写修复，实测该改动只在工作区未提交；HEAD 源码仍 t('settings.accountToast') 引用已删除的 i18n key——en.js/zh.js 删 key 已入库而消费方修复未入库，HEAD 状态重建即断引用 | P1 | 审验员 | 2026-08-28 |  ✅已销项 @2026-08-29（设计师验证：main HEAD SettingsPage.vue 已是直写修复形态、全仓源码 accountToast/logoutToast 零命中；修复经合 main 链带入 main，1825338 在 feat/upg14 侧） |  已施工（补提交 @1825338） | 现象：git show e16fae8 --stat 仅含 en.js(-2)/zh.js(-1)/assets 重建，无 SettingsPage.vue；git show HEAD:SettingsPage.vue 第 6 行仍是 t('settings.accountToast')；工作区有一笔未提交的直写修复。产物 assets（settings 目录）虽干净，但仓库源码 HEAD 不一致：任何干净 checkout + vite build 都会产出引用缺失 key 的包。建议：立即补提交该工作区改动（验收前置），并在 verify/CI 加「src 引用的 i18n key 必须存在于字典」断言 |
| 挂账-upg16-gradleproperties四属性丢失 | UPG-16（172e67d）`git rm --cached gradle.properties` 把构建必需四属性（android.useAndroidX/nonTransitiveRClass/overridePathCheck/jvmargs）一起丢出 git——所有新 worktree/干净 clone `:app:assembleDebug` 必 BUILD FAILED，仓库级构建回归 | P1 | 验收员 | 2026-08-28 | ✅已销项 @2026-08-29（设计师验证：跟踪版四行零口令逐字核+wrapper 四件套在库+用户级 MOV_* 四键齐[只核键名]+主仓工作区已是安全版；合 main 7ce043a 已推 origin，合后全量 --rerun-tasks 38 类 264/0/0 绿；顺带带入 UPG-20 L3 落档 dfa90d3） | 已施工（433a0b1） | 现象：mov-upg19 worktree assembleDebug 报 checkDebugAarMetadata「android.useAndroidX 未启用」；9c81ef9 跟踪版 gradle.properties 含四行属性，172e67d 后 `git show HEAD:gradle.properties` 不存在，本地残留副本只有签名四参数。证据：UPG-19 验收实测（补四行后 BUILD SUCCESSFUL 1m30s 出包 49590389B）+ ACCEPTANCE_LOG UPG-19 §3；程序员报告将 main BUILD FAILED 误归因「AGP compileSdk=36 兼容性」（实为同根因）。建议：恢复跟踪版 gradle.properties 四属性（签名参数继续 gitignore 走本地副本/模板），或仓库跟踪 gradle.properties.example；修复后任一新 worktree 直接构建即证；UPG-19 重修不被本项阻塞（worktree 本地补丁即可）。【施工证据 @2026-08-28 深夜】feat/upg16-gradle-props 433a0b1：①跟踪版 gradle.properties 四行恢复（与 9c81ef9 历史版一致）+ .gitignore 移除条目；②release 签名四参数迁用户级 ~/.gradle/gradle.properties（providers.gradleProperty 官方查找顺序可达；坑：Gradle 读用户级 properties 按 Latin-1，中文路径必须写 \uXXXX 转义——首跑 keystore 路径乱码实证后修正）③gradle wrapper 四件套入库（8.13-bin，根除 ./gradlew 不存在的静默假跑）④验收判据实测：新建 detached 临时 worktree 零补丁 ./gradlew :app:assembleRelease BUILD SUCCESSFUL 3m9s（45.5MB release 包，四行+签名+wrapper 三证合一）。存量 worktree 本地 gradle.properties（四行+签名参数）可继续用不强制迁移 |【验收员独立复核 @2026-08-29 凌晨 ✅ 通过】①文件级：跟踪版四行与 9c81ef9 逐字一致（diff 实证）+.gitignore 条目移除+主仓工作区零脏噪音；用户级 ~/.gradle/gradle.properties 签名四参数齐（jks 中文路径 54C1724C 转义写法 转义规避 Latin-1 乱码，口令不落档案）②判据复现：detached 临时 worktree 零仓库补丁（仅机器级 local.properties sdk.dir——非仓库内容，9c81ef9 时代亦未跟踪）→ ./gradlew :app:assembleRelease **BUILD SUCCESSFUL 2m54s**（52 tasks 全 executed）+ release 包 45505049B + **apksigner V2 CN=JiangXi PiDan Technology SHA-256 b7343c21…（与 UPG-16 验收同款）**——四属性+签名+wrapper 三证独立复现 ③注记 P3：程序员「零补丁」口径漏报机器级 local.properties 前置（不改变本修复有效性）④临时 worktree 已清理。销项待设计师（复核结论已备齐） |
| 挂账-upg14-r2全产物零命中虚标 | UPG-14 R2 复验判据②声称「全产物四 pattern 零命中」，实测仅 pages/settings（+review）目录干净，market(7)/model(5)/orders(5)/sidebar(5)/vault(5)/workbench(5) 共 32 个产物文件仍含 logoutToast/accountToast/demo 词（旧共享 tokens-FbUJK5PZ.js + 各页陈旧 SettingsPage chunk，market/index.html 仍引用含 pattern 的 SettingsPage-BsK8cjDB.js） | P2 | 审验员 | 2026-08-28 |  ✅已销项 @2026-08-29（设计师验证：main HEAD 产物全树七页 logoutToast/accountToast/Logged out (demo)/Account profile (demo) 四 pattern 零命中+源码零命中——全产物口径成立） |  已施工（七页全量重建+清残留 @04341d2） | 现象：审验员全 assets 树 grep 四 pattern 实测计数；R2 只重建了 settings 目录，其余六页目录未重建残留旧构建。判据②在「settings 目录」口径成立、在声称的「全产物」口径不成立（报告原文「全产物四假数据词零命中 ✓」）。建议：七页全量重建（同源 tokens 已变，旧 hash 文件清残留——与 9323975「构建同步清残留」同一要求）；验收复核按全产物口径重测 |
| 挂账-w05-F12视觉复核 | W-05 事件板块侧栏五子类+五卡渲染视觉截图（L2）因 headless Edge 卡死未成图——验收员 F12 浏览器复核项 | P3 | 验收员 | 2026-08-27 | ✅已落实（验收员 F12 视觉复核闭环 @08-27） | 已闭环（验收员 CDP 实拍） | 现象：侧栏事件节点（📌）+ 五类别子节点（备案/证件/审核/事务/运营）与卡片渲染需要真实浏览器视觉实证；程序员 headless Edge 两次 >2min 无响应。证据：verify §① vm 渲染断言（结构级）+ 生产 json events 节已定；curl 可观测层 200。建议：验收员复查侧栏 events 节点与五卡渲染（电脑手操或 CDP）后闭环 |
| 挂账-w05-事件白名单fail-loud虚标 | W-05 验收判据「事件状态非法值 → sync FAIL-LOUD exit=1」实测不成立：sync 无白名单校验，非法状态静默通过且 --deploy 可直灌生产 | P2 | 审验员 | 2026-08-27 | ✅已销项 @2026-08-27（DEPLOY-GATE 落地 --deploy 路径常驻，审验变异复验有效；设计师合 main 65cf4dc 闭环） |  已施工（DEPLOY-GATE @7877472 + verify 动态化 @4b5276a） | 现象：审验员独立变异——副本事件表 E-02 状态改「审核中断?!!」→ sync exit=0 正常生成，workbench.json 中 E-02 status=审核中断?!! 原样落盘；`--deploy` 链（sync:309）只 scp 不过 verify，非法值可直达生产。白名单校验实际只在 verify 层（verify:104 实测能红），与 ACCEPTANCE_LOG:859 节声称的 `[sync] FAIL-LOUD` 输出不符（该输出在代码中不存在，grep 零命中）。证据：变异三段（sync exit=0 / json 污染 / verify 兜住 exit=1）+ grep EVENT_STATUS 仅两处（常量+pipeline 计数，无校验调用）。建议：parseEvents 内补取值域校验（不在 ["待办","进行中","待外部","完成"] → FAIL-LOUD exit=1），或 --deploy 前强制内联跑 verify；修复后复核销项 |
| 挂账-w07-施工附录泄漏公网 | /privacy 公网页尾部渲染了 src.md 内部施工附录（含「UPG-11 施工用，程序员照抄」「按钮：同意｜拒绝（两钮同等显著）」等施工规格），且 md「#」标记未渲染原样外显 | P3 | 审验员 | 2026-08-27 | ✅已销项 @2026-08-27（make_privacy 构建期截断+四禁词反向断言，审验 MUT-B 复验有效；设计师合 main a60df86 闭环） |  已施工（附录构建期截断+校验器反向四禁词 @0fd6529） | 现象：审验员独立行比对 57 行 src 发现附录节超出政策正文范围；curl 生产页实测尾部 `<p># 附录：首启隐私弹窗摘要文案（UPG-11 施工用，程序员照抄）</p>` + 按钮/链接施工规格段落原样在页。政策正文本身 43 行全数在场不受影响。建议：make_privacy.mjs 截断附录节（以「# 附录」为界不收录），重新生成部署；check_privacy.mjs 补「附录内容不得在场」反向断言 |
| 挂账-w06-openDetail被覆盖 | W-06 详情抽屉真实点击无效：app.js `window.MovWb = {...}` 直接赋值覆盖掉 detail-drawer 先挂的 openDetail——与 W-04 挂点错位 P1-1 同族（第三次现身） | P1 | 验收员 | 2026-08-27 | ✅已落实（R1 c4142c7 验收复验通过 @08-27） | R1 已修复（Object.assign 合并挂点，F12 三步全过） | 现象：工单卡 onclick=MovWb.openDetailById(UPG-01,app-tickets) → 内部调 window.MovWb.openDetail=undefined → TypeError，抽屉不出现（hasDrawer=false）。证据：CDP 真浏览器生产域：MovWb.openDetail type=undefined、点击后 drawerVisible=null；vm 复现加载顺序（detail-drawer 后 keys=[openDetail,closeDetail,mdToHtmlSafe] → app.js 后 keys=[nav,openDrawer,closeDrawer,logout,openDetailById]——openDetail 被覆盖丢失）。根因：app.js:895 `window.MovWb = {...}` 赋值覆盖非合并。建议：改 `Object.assign(window.MovWb||{}, {...})` 或 spread 合并；verify 加「openDetail 存活」变异锚（摘掉/覆盖→红）；与 W-04 同族方法论——凡跨文件挂同一全局对象，一律合并勿赋值（第三次教训） |
| 挂账-w04-登出口恶意挂点错位 | W-04 工作台登出口真实点击无效：onclick=MovWb.logout() 但 window.MovWb 无 logout（挂在 __MOVWb），运行时 TypeError | P1 | 验收员 | 2026-08-27 | ✅已落实（R1 3a8617f 验收复验通过） | R1 已修复（验收员 2026-08-27 复验） | 现象：侧栏 #logout-row onclick=MovWb.logout()，window.MovWb 仅 nav/openDrawer/closeDrawer；真实点击 confirm 不被调、token 不清、页面不回登录页。证据：CDP 真浏览器生产域注入真实 token → MovWb.logout type=undefined / confirmCalls=0 / token 未清除；vm 复现同（MovWb keys: [nav,openDrawer,closeDrawer]）。根因：logout 挂在 window.__MOVWb（verify 钩子），入口指向 window.MovWb；verify 仅字符串检查（includes MovWb.logout()+removeItem+__MOVWb.logout），三处存在但互相错位故假绿。建议：window.MovWb.logout = doLogout（或在 894 行字面量内补 logout）+ verify 真实执行点击断言 + 变异「从 MovWb 摘走 logout → 红」锚点 |
| 挂账-upg13观察-Logo口径 | UPG-13 报告「Logo 394px≈150dp」与验收实测深色区 364×332px（≈139×126dp）有口径出入（透明 padding） | P3 | 验收员 | 2026-08-27 | ✅已销项（设计师定夺：非缺陷——代码规格 150×154dp 正确，差异系测量口径含透明 padding，忽略不更正）@2026-08-27 | 已转工单 | 现象：报告称 Logo 深色区 394px 宽（=150dp），实测同参数下非白 bbox 364px；证据：1080x2400@420dpi 截图 PIL 分析 + mov_logo.png 1108x1020 FIT_CENTER 换算；判断：代码规格 150x154dp 正确，差异为测量口径（报告量的是含 padding 的显示框），非缺陷；建议：仅更正报告数字口径或忽略 |
| 挂账-upg13观察-政策页标题消失 | UPG-13 第4条对 PrivacyPolicyActivity hide 顶栏后，「隐私政策」标题与返回箭头消失 | P3 | 验收员 | 2026-08-27 | ✅已销项（设计师定夺：维持无栏不自绘——privacy.txt 首行自带大标题，系统手势可返回；同 §五定夺）@2026-08-27 | 已转工单 | 现象：hide 后政策全文页无标题/返回箭头（仍可从系统手势返回）；证据：程序员报告第5节申报 + 页面结构代码；判断：从弹窗/设置入口进入场景短，影响可接受；建议：设计师定夺是否自绘小标题栏（不阻塞） |
| 挂账-upg19-verifyicons阈值不敏感 | UPG-19 R1 交付的 tools/verify_icons.mjs 自检脚本对「自身解码器回归」不设防：删其内部 offset += stride 后退化解码仍残存行 0 暗像素（12~108px），「暗像素>0」断言不红——若后续有人改坏脚本自身将假绿 | P3 | 验收员 | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：验收员独立变异实测——脚本内 decodePng 删 offset 行后六案仍全 OK（残存暗像素非零）；证据：UPG-19 R1 复核（ACCEPTANCE_LOG 878b98b §3 注记）。建议：阈值改「暗像素占比>0.5%」一行收口（非全白且防退化），可随任意后续单顺带；不影响 UPG-19 交付结论 |
| 挂账-upg05-r3剧本口径歧义 | E/R 行为面 R3 剧本（不张冠李戴）判据存在字面/意图歧义：验收员实测 AI 回「没有你的狗的信息——我只知道你养了只猫叫 Yuanshuai（元帅）」（正确否定+澄清已知），字面含「元帅」按原文「回答元帅=失」会误判 | P3 | 验收员 | 2026-08-29 | ✅已销项 @2026-08-29（设计师定夺：释法已出——判事实归属不判词面：挂错对象=失，归属正确或明示无记忆=过；按此口径验收员边界案判过成立，行为面无需复跑；释法随 UPG-22 卡⑤发布入验收剧本注释，UPG-22 交付后全闭环。另：UPG-06 卡面「+R3 释法」残留已核实为 UPG-05 串行，同步销项） | 随 UPG-22 卡⑤（剧本注释落地） | 现象：R3 实测回复正确区分猫/狗且未把猫事实安到狗上（理想行为），但字面命中「回答元帅」；证据：ACCEPTANCE_LOG 76a1418 R3 节 + session 转录。建议：R3 剧本口径补「按是否把房 A 事实安到被问对象上判」（同 R1 的引用判细化先例），设计师/查验员释法后行为面复跑 |
| 挂账-upg05-e3r2行为面补验通道 | E3（失败教训 occurrences≥2 注入）+R2（AVOID 不误伤）行为面待补验：模拟器环境失败场景制造受限——torch 在 AVD 成功、file.write 越界被工作区重定向保护、12306 受 AI 编排不确定性影响 | P3 | 验收员 | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：验收员三轮尝试（torch/file.write/shell_exec cat）均未建立「同一错误自然发生 2 次」场景（UI 自动化多步链路命中率低+工具环境差异）；机制面已由变异⑥（occurrences≥2 阈值）+JVM 钉死不阻塞。证据：ACCEPTANCE_LOG e24a14a 补验尝试记录。建议：程序员下次交付附 instrumented 失败注入案（同 MemoryLink 宿主同款模式）或真机人工补验（手操 2 次失败+1 次新 session 台词） |
| 挂账-upg0204-WRITE_CALENDAR重复声明 | UPG-02+04 交付的 AndroidManifest.xml 中 WRITE_CALENDAR uses-permission 重复声明两次（:11 与 :14）——无功能影响，装包合并去重，属清单整洁 | P3 | 验收员 | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：grep manifest WRITE_CALENDAR 命中 2 行；证据：UPG-02+04 验收代码审查（ACCEPTANCE_LOG 012784e §1）。建议：合并为单条声明，随任意后续动 manifest 的单顺带 |
| 挂账-upg21-源码锚契约逻辑变异局限 | UPG-21 ComposerInputContractTest 为源码字符串包含断言——对逻辑变异不敏感（验收员变异 isSend=false 短路后契约仍绿，字符串保留）——关键行为逻辑（发送闸门）无行为级测试覆盖 | P3 | 验收员 | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：验收员 M2 变异（isSend=false）后 ComposerInputContractTest 2 案仍绿（删除类变异才红）；证据：ACCEPTANCE_LOG 9553524 §3 注记。建议：composer 发送逻辑补行为级测试（Robolectric/instrumented 按 EditText+KeycodeEnter 驱动断言 send 调用），或源码锚补「isSend 短路形态禁用」反向断言 |
| 挂账-模拟器AI未回复 | emulator-5556 对话 AI 请求 80s+ 无回复（scene 工具 L3 验证受阻） | P1 | 设计师 | 2026-08-26 | ⏳挂账待审 | 已转工单 | 现象：输入 query train 2026-08-27 后 80s 无 AI 回复；证据：Monkey/am start 启动正常，UI 可见消息已发出；建议：查 DeepSeek API key/模型配置与日志（logcat），修通后补 L3 真机调用证据 |
| 挂账-GoalDomain无日志恢复 | GoalDomain 无 restoreFrom：goal/change 事件落盘但重启/会话恢复后活跃目标丢失、轮次驱动失效、落盘事件成孤儿（UPG-07 批 2 goal 级豁免的直接前置） | P1 | 设计师（UPG-07 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：goal.set/complete/status 生产接线在（MainActivity.kt:1795-1832、轮次驱动 :3790-3804），但 GoalDomain 无 restoreFrom——对比 PlanModeController 有（plan/Plan.kt:30-37）；证据：Goal.kt:17-22 + Session.kt:331-333（goal/change 落盘为通用 MemoryEvent）@dfa90d3；建议：仿 PlanModeController 补 restoreFrom 回放 goal/change 重建活跃目标，UPG-07 批 2 出卡前必修 |
| 挂账-looksLikeToolRequest关键词过宽 | E3 兜底触发词含 turn/get/set/check/list 等超宽英文词（"get it" 即误触发、凭空多发一轮模型请求），中文「列一下/帮我看看」类反而漏判 | P2 | 设计师（UPG-06 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：looksLikeToolRequest() 关键词表过宽且中英文失衡，误触发=静默多跑一次模型（token 浪费）；证据：MainActivity.kt:5586-5592 + E3 重试 :3888-3897 @dfa90d3；建议：收窄英文词表+补中文触发词，或与 UPG-06 FabricateGuard 移植合并治理（防双机制干扰） |
| 挂账-E3重试文本拼接 | E3 nudge 重试后，runChat 汇总 eventsBefore 之后全部 assistant 文本——第二轮文本与第一轮（可能是编造内容）一并拼进 [AI] 回复显示 | P3 | 设计师（UPG-06 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：E3 静默重试成功时用户看到编造段+重试段拼接；证据：MainActivity.kt:3904-3911 @dfa90d3；建议：重试轮替换而非拼接，或 UPG-06 批 1 拦截反馈形态设计时一并覆盖 |
| 挂账-压缩双阈值口径漂移 | 压缩阈值双事实源并存：ContextBudget.degradeRatio=0.85 vs BasicCompactionEngine.thresholdRatio=0.8（默认值，装配时未传参覆盖；现靠 AgentLoop 85% 先门控不爆雷）；另 MainActivity.kt:160 注释「1M/128K/5K」与实际仅 1M↔5K 两档切换（:1144）不符 | P3 | 设计师（UPG-07 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：动估算口径时两处阈值需分别重校，漂移风险；证据：ContextBudget.kt:36,47 + BasicCompactionEngine.kt:27 + MainActivity.kt:160-161,1144-1147 @dfa90d3；建议：UPG-07 批 1 收敛为单一事实源并修正注释 |
| 挂账-键盘发送键换行 | 聊天页输入框键盘发送键无法发送、回车=换行 | P1 | 用户（实测反馈） | 2026-08-29 | ✅已落实 **转工单 UPG-21** @2026-08-29（设计师源码实证根因：MainActivity.kt:1059-1071 设 TYPE_TEXT_FLAG_MULTI_LINE 且无 imeOptions → 软键盘回车固定换行；方案=去 MULTI_LINE+IME_ACTION_SEND+listener 接 send，trade-off 卡面明说） | 待施工（UPG-21 待派单） | 现象：键盘发送键无法发送、是换行；证据：用户实测 + 源码 :1059-1071 实证（无 imeOptions、无 OnEditorActionListener，唯一发送通路=发送按钮）；建议：见工单库 UPG-21 卡 |
| 挂账-syncpages不校验dist新鲜度 | sync-pages.mjs 不校验 dist 新鲜度——不重建就同步会把 stale dist（旧 hash 文件，含已清理的 demo key）重新同步进入库产物，再污染源已清净的 assets | P2 | 验收员（UPG-14 复核附带发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：`0027-mov\前端设计\mov-vue\dist\`（gitignored 未入库）残留 R2 前旧 hash SettingsPage-BsK8cjDB.js/tokens-FbUJK5PZ.js 含 logoutToast/accountToast；main 源码+入库产物已双零命中，但 sync-pages.mjs 无新鲜度校验，谁不重建就同步即再污染；证据：UPG-14 复核亲跑（git grep main 双零命中 vs stale dist 有命中）；建议：sync 前校验 dist 构建时间戳/hash 或强制 vite 重建，并顺手清现有 stale dist |
| 挂账-registerMovQueryTools死代码 | `McpServer.registerMovQueryTools()`（McpServer.kt:234）全仓零调用且语义重复——若未来有人调用，会与 MainActivity 已注册并经 server 循环入面的同名工具重复注册（后者覆盖前者，丢失真描述和真 schema） | P3 | 设计师（UPG-01 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：定义在案零调用；证据：UPG-01 溯源全仓 grep 零生产调用 @5170421；建议：转 UPG-18 死代码清理范围评估（删或接线须裁决，接线须防重复注册覆盖真描述） |
| 挂账-McpToolProvider零调用 | `McpToolProvider` 生产零调用——它是唯一保留外部工具真 description 的路径，却不在链路上；外部工具实际走 `McpExtDiscovery.kt:91-102`（只取 name，丢弃 description/inputSchema） | P3 | 设计师（UPG-01 溯源发现） | 2026-08-29 | ⏳挂账待审 | 已转工单 | 现象：McpToolProvider 仅 McpExtDiscovery 注释提及，无生产调用方；证据：UPG-01 溯源 @5170421；建议：转 UPG-18 死代码清理范围评估（与 McpExtDiscovery 功能重叠，裁决合并或删除；UPG-01 批 4 外部描述回落策略会触及此处，到时联动） |
| 挂账-upg08-r1生产json未同步 | UPG-08 R1 交付终态线上 workbench.json 未与登记后的工单表同步（UPG-08 仍显示设计师处理中） | P2 | 验收员 | 2026-08-26 | ✅已落实销项 @2026-08-26（复验代纠正 + 合 main 后设计师重新 sync --deploy，生产 16:22 与工单表一致，已核实） | 无需施工（规则 18 已立防复发） | 现象：复验拉取生产 json（15:33:54Z）UPG-08=设计师处理中、pipeline 设计师:8/程序员:0，为 R1 上表登记前的旧产物；交付报告称「线上已是修复版」。证据：https://mow.kim/wb/workbench.json 实测 vs 本地 sync 对比。建议：交付终态一律以登记后的工单表重新 sync --deploy（本缺陷已在复验中代纠正） |
| 挂账-upg08-r1-sample未接线 | UPG-08 R1 报告称前端 fallback 链含 workbench.sample.json，但前端未接线 | P3 | 验收员 | 2026-08-26 | ✅已落实销项 @2026-08-26（R2 已接线三级 fallback + verify 接线断言防回归，复验通过） | 已施工（R2 @32564fd） | 现象：grep app.js/index.html 对 workbench.sample 零命中（仅 sync 脚本生成）。证据：断网 mock 实测 SAMPLE 兜底正常无 undefined（功能不受影响）。建议：接线 sample 或删报告声明 |
| 挂账-mockServer-本地与商店展示不一致 | 已安装但不在目录的包，本地 tab 与市场商店展示不一致（MockServer 为证） | P3 | 设计师（真机对照发现） | 2026-08-30 | ⏳挂账待审 | 已转工单 | 现象：本地 tab「市场已安装」含 MockServer，市场商店（热门/全部工具）只有「浏览器自动化」，无 MockServer——已装但非目录包「本地显示、商店失踪」。代码：bubbleOverview=store 全量（`McpMarket.kt:396`）vs `list()`=registry 目录（`:197`），两处数据源不同。建议：统一「已安装包」展示数据源（商店也显示已装非目录包，或本地只显示目录内的），或标注内部测试 mock 不展示 |


## [UPG-09 | 2026-08-27] 验证码登录「未注册自动注册」— 服务端未实现（account-service）——✅已落实 @2026-08-28
- **发现者**：程序员 C（UPG-09 施工实测）
- **现象**：工单声明「验证码登录未注册自动注册」，实测 `POST /account/login` 对未注册手机号返回 404，无自动注册；`/account/send-code` 对未注册号仍可发送
- **已做**：前端按实际提示「账号不存在，请先注册」（未改后端，红线）
- **拍板与落实 @2026-08-28**：用户拍板「验证码登录自动注册」——`/account/login` 验证码校验通过后未注册即自动建号（响应带 `registered:true/false`，自动注册号 `hasPassword:false`，密码登录不受影响仍可 `/set-password` 补设）；本地六流程序列实测全过（自动注册/再登录/无密码401/错码不建号/token 有效/重注册 409）；已部署生产（`/opt/market-account/account-service.js`，备份 `.pre-autoreg`，与仓库 HEAD 零漂移确认后覆盖），health 200 mode=real。**真机端到端（真实短信）待用户实测一圈后验收员闭环**
- **待决策**：已决（见上）

## [UPG-11 | 2026-08-27] privacy_agreed_v1 随自动云备份迁移新设备将不弹窗（需产品拍板）
- **发现者**：程序员 C（UPG-11 施工 security_review MEDIUM 项）
- **现象**：同意标志落 `mov_prefs`（参与自动备份/换机迁移），新设备恢复备份后首启不再弹隐私弹窗，违背"新设备必弹"审核惯例
- **证据**：SecurityReview sa_20260826_195341 MEDIUM 项；PrivacyGateActivity.kt:43 用 getSharedPreferences(MOV_PREFS)
- **建议**：`dataExtractionRules`/fullBackupContent 排除 mov_prefs 或拆独立不备份 prefs 文件；涉及用户数据云备份策略取舍，待设计师拍板（DELIVERY_UPG11_2026-08-27.md §五）

## [W-02 | 2026-08-27] 已装 App 市场功能真机实测未完成（emulator NAT 故障降级）
- **发现者**：程序员 C（W-02 施工 L3 验证）
- **现象**：模拟器 MOV_Test 外网 NAT 故障（ICMP/TCP 双断，冷重启 QEMU 重建 NAT 无效+System UI ANR），「已装 App 打开市场页拉 registry.json 浏览」端到端实测无法执行
- **证据**：DELIVERY_W02_2026-08-27.md §三L3；服务端零影响已由 curl 矩阵+node 消费复现钉死（registry.json 200、结构完整）
- **建议**：验收员在联网可用环境对已装 App（任意版本≥市场上线版）做一次市场页浏览实证后闭环本单 L3
## [W-04 | 2026-08-27] 已登录态点击退出→回登录页的人工 F12 复核（非缺陷·验收操作项 · ✅已部署 3a8617f 等复核）
- **发现者**：程序员 C（W-04 L2 验证降级申报）
- **现象**：程序员侧无浏览器自动化工具，无法以真实已登录 localStorage 态完整演示「点退出→confirm→token 清除→回登录页」交互链
- **证据**：代码与部署已就绪（mow.kim/app.js 含 logout-row+removeItem，2026-08-27 上线）；未登录门禁已被 headless DOM 断言+curl 401 独立实证；verify 四断言含变异亲杀双案红
- **建议**：验收员按三步复核：①登录工作台 ②F12→Application→localStorage 确认有 mov_account_token→点侧栏底部退出登录→确认 ③F12 确认 mov_account_token 消失且页面回登录页（全自动路径：headless 播放脚本亦可）
- **施工进展（R1 @2026-08-27 20:40 程序员 C 追记）**：验收打回的 P1-1（MovWb.logout 挂点错位）/P1-2（verify 假覆盖）已修复并部署主域——feat/w04-logout `3a8617f`（报告 §九：真执行断言重写+MUT-R1 变异必红+hash 勘误）。原「F12 三步复核」操作项继续有效，请复跑。
## [W-06 | 2026-08-27 23:2x] 线上 workbench.json 被旧代码 sync 覆盖——详情数据短暂丢失（已恢复，防再发需合 main）
- **发现者**：程序员 C（W-06 部署后 1 分钟内线上复查发现）
- **现象**：23:08 W-06 部署（含 detail/designDoc 的 json）后，23:09 线上 json 被另一次 sync --deploy 覆盖——UPG-13 detail 消失、designDoc 归零（旧代码=feat/w06-detail 未合 main 的其他 worktree 跑的 sync）
- **证据**：线上 json 生成时间 15:09:34Z vs W-06 部署 14:54Z；diff 实测 detail/designDoc 差异；已于 23:15 从 mov-w06 重新 --deploy 恢复（UPG-13 detail 10 节+designDoc 14 条复验 ✓）
- **建议**：①feat/w06-detail 尽快合 main（根治：所有 worktree 的 sync 都带 detail 集成）；②合 main 前其他角色勿跑 sync --deploy，或统一从 mov-w06 worktree 执行

## [登录 | 2026-08-27] 登录页「输完手机号+验证码，登录钮无法点击」——设计性死锁（已转工单 UPG-15）
- **发现者**：用户实测反馈 @2026-08-27
- **现象**：验证码登录页输好手机号+验证码后，登录按钮灰色不可点，无任何提示告诉用户差在哪
- **根因（设计师代码+真机双实证）**：非功能 bug，是 UPG-09 既定规格「协议未勾禁用主按钮」的死锁副作用——`LoginActivity.refreshEnabled()`（:377-383）未勾选时 `loginBtn.isClickable=false`，而 `:430` 的 `setError("请先勾选协议")` 分支因此**永远触发不到**（按钮都不可点，doLogin 不会执行）；勾选框仅 22dp 灰细边空心圆（UPG-13 #8 样式），视觉存在感低，用户根本不知道要去点它。真机 emulator-5554 实测：输完两框 clickable=false（规格内）→ 点勾选框 checked=true → 按钮 clickable=true 恢复（机制本身无 bug，证据截图 `验收员\证据数据\UPG-15_登录钮禁用_勾选后恢复.png`）
- **修法（已随 UPG-15 出单）**：①登录钮常可点，未勾选点按走 :430 既有 setError inline 红字（视觉禁用态 alpha 0.4 保留，交互不死锁）；②协议整行点击切换勾选态（扩大 22dp 小圆命中区）；③回归断言补两案
- **状态**：已转工单 UPG-15 @2026-08-27，待派单

## [UPG-14 | 2026-08-27] 设置页账号卡/跳转真机操作复核（非缺陷·验收操作项）
- **发现者**：程序员 C（UPG-14 L2 取证降级申报——模拟器实例 input 子系统故障+reboot 卡 offline）
- **现象**：UI 驱动取证无法执行；代码层证据链完整（AccountMeTest 6 用例绿+产物 grep settings 四词零命中+APK 49518227）
- **操作可复现**：run-as 写 shared_prefs/mov_login.xml（token=任意非空 / phone_tail=4位数字）→ 重启 App → 设置页账号卡应显示「MOV 用户/****1234」无假徽章 → MCP 市场/我的能力两行点击应跳转
- **建议**：验收员在可用模拟器/真机按三步复核后闭环

## [UPG-17 | 2026-08-28] review.html 在 UPG-14 R2 被误删——审核口页面白屏风险
- **发现者**：程序员 C（UPG-17 修6 产物清点）
- **现象**：`app/src/main/assets/pages/review/review.html` 在 `04341d2`（UPG-14 R2「八页全量重建」）被删除，但 `MarketReviewActivity` 仍加载 `pages/review/review.html`（设置页连击 5 次版本号入口）→ 审核口页面白屏；`review/assets/` 下为孤儿旧 chunk（含旧 WorkbenchPage 等假数据词产物）
- **证据**：`git log -- app/src/main/assets/pages/review/review.html`（04341d2 为最后变更）；`MarketReviewActivity.kt:61` 仍 loadUrl 该页；`review/assets/` 目录现存孤儿 chunk
- **建议**：设计师定夺——恢复手写 review.html（857df30 版本 `git show 857df30:<path>` 可取）或下线审核口入口并清孤儿资产

## [UPG-17 | 2026-08-28] main 构建属性丢失——UPG-16 误删提交态 gradle.properties，新 clone/worktree 构建必红（⚠️ 同案并入「挂账-upg16-gradleproperties四属性丢失」[验收员 @2026-08-28 同日独立发现并先行登记表格行，P1 ⏳待审]——两案同根因同修法，设计师销项时按表格行主案处置，本段留作程序员侧独立发现佐证）
- **发现者**：程序员 C（UPG-17 worktree 构建排查）
- **现象**：`172e67d`（UPG-16）为口令外移 `rm --cached` 了整个提交态 `gradle.properties`，连带删掉 `org.gradle.jvmargs`/`android.useAndroidX=true`/`android.nonTransitiveRClass=true`/`android.overridePathCheck=true` 四行必需构建属性；现 main 上该文件不存在（仅本地 gitignore 版含签名参数）→ 任何新 clone/worktree 执行 gradle 即 `EvalIssueException: SDK location not found`→`android.useAndroidX not enabled` 连环红
- **证据**：`git show 172e67d -- gradle.properties`（deleted file，diff 可见原 4 行）；mov-upg17 worktree 复现红 → 本地补 4 行后 253 tests 全绿
- **建议**：恢复提交态 gradle.properties（4 行属性；签名参数仍走本地 gitignore 覆盖）——可并入 UPG-16 复验或独立小单
- **销项**：随主案一并销项 @2026-08-29（合 main 7ce043a；主案表格行已标 ✅已销项，含设计师验证明细）

## [UPG-17 | 2026-08-28] ui.openLogin 桥缺失——侧边栏未登录点按暂以 toast 过渡
- **发现者**：程序员 C（UPG-17 修1 施工）
- **现象**：工单卡修1 要求「未登录点按跳登录」，但宿主无面向 Vue 的 `ui.openLogin` 桥（LoginActivity 启动均为原生 Intent：PrivacyGate/SettingsSheet）；UPG-17 红线禁自造桥且 MainActivity.kt 当时被 UPG-20 施工占用
- **证据**：DELIVERY_UPG17_2026-08-28.md §六-③；grep `ui.open` 全清单无 login 项
- **建议**：待 MainActivity.kt 空闲补 `ui.openLogin`（跳 LoginActivity，与 ui.openMarket 同形态几行），侧边栏 onProfileClick 改调桥；门控下未登录态本不可达，属防御性显示，不阻塞上架

## [UPG-05 | 2026-08-29] 装配路径 COVER_HIT 打点断线——晋升证据链生产路径半断（P1 ✅已销项 @2026-08-30，UPG-22 R1 L2 复验通过：jsonl memory/ref source=cover turnId 对齐 + 显化页 0→1 + dedupe 不涨）
- **发现者**：查验员（UPG-05 R1 审验，5558 journal 数据面对账）
- **现象**：记忆 cover 装配注入（`MainActivity.kt:3555 memoryCoverPromptSegment()`→`MemoryCoverManager.currentCover`，feat/upg05 步3 起）不记 COVER_HIT——`recordCoverHits` 唯一调用者是 `MemoryMcpTools.cover()`（AI 显式工具调用路径）；:3553 注释承诺的「turnId=cover-<指纹> 整段计 1 次」无任何实现。后果：draft 在真实使用下引用计数恒 0（5558 实测 14 journal memory/ref 零事件、显化页全「引用 0 次」）→ 永不晋升 → 14 天衰减清场，死穴 1 只修一半。
- **证据**：grep `recordCoverHits` 全仓 3 命中（定义+注释+MemoryMcpTools.cover 唯一调用）；`adb run-as grep memory/ref` 14/14 journal 零命中；E1 3跑3过（cover 注入生效）与零 ref 互证断点位置在装配路径而非注入本身。
- **建议**：currentCover 构建命中处按 :3553 注释口径补 recordCoverHits（turnId="cover-"+指纹，同 session 恒定内容只计 1 次，对齐 Freeze+dedupe 口径）；L 组补断言「新 session 装配注入→memory/ref +1」。修复很小（约十行），可随 UPG-05 后续批或独立小单。
- **销项条件**：打点接上 + L 组断言补入 + 真机新 session 后 journal 可见 memory/ref（source=cover）。
- **转工单**：✅ 已转 **UPG-22 记忆尾单** @2026-08-29（设计师出单，规则 21 溯源复核已过；捎 R3 释法+writeTools 冗余 save 清理+tools/ 四临时物捎清；开工前置=UPG-02+04 合 main）——销项条件不变，交付验收后回到本条划销

## [UPG-02+04 | 2026-08-29] 合 main 版 obsidian 7 handler/捕获桥/usage.summary/manifest service 四处接线缺失——schema 广告在、真机调用即死（P1，修复已就绪待验收）
- **发现者**：程序员（UPG-23 施工前溯源时发现 55f9730 接线缺口；接棒后逐标记复核 main@8a205d6 实证缺口随 merge 入 main）
- **现象**：main@8a205d6 `MainActivity.kt` 无 obsidian.* 7 个 handler 注册（`obsidian.vault.detect` grep=0）、无捕获桥注入（`deviceProvider.cameraCapture/ocrCapture/qrCapture/screenCapture` 赋值=0）、无 `toolCapture`/`screenCaptureLauncher`/`obsidianTreeLauncher`；`usage.summary` 仅 paramSchema 广告（:276）无 mcpHandlers 注册；manifest 无 ScreenCaptureService 声明（类文件在库）。后果：obsidian 7 工具、qr.scan、screen.capture、usage.summary 真机调用即 TOOL_NOT_FOUND 或「捕获通道未就绪」——但合 main 时 L1 288/0/0 假绿（契约测试无接线断言，覆盖遗漏）。
- **根因**：UPG-02+04 施工中 `git checkout a02033b -- MainActivity.kt` 静默覆盖后续接线改动，55f9730 携缺口被合入 main。
- **证据**：`git show origin/main:app/src/main/java/com/mov/android/MainActivity.kt` 五标记 grep 全 0（obsidian.vault.detect/pendingToolCapture/screenCaptureLauncher/obsidianTreeLauncher/toolCapture()；`DeviceProvider.kt:35` `var screenCapture` 桥属性默认 null（桥未注入即「捕获通道未就绪」）；`McpToolScheduler.kt:255` TOOL_NOT_FOUND 兜底语义。
- **建议**：合 M3 补丁（已就绪，见下）；验收员代码层复核 + 真机抽验 obsidian.vault.detect 与 screen.capture 实调；本次「接线断链仍验收通过」建议走验收错误登记（`验收员\文档\验收错误登记表.xlsx`）。
- **修复**：✅ 已施工 **feat/upg02 70db6c6 已 push origin**——四处接线全补 + Provider 实例化（接棒时查实 obsidianProvider lateinit 未实例化，运行即崩，已补并加实例化断言）+ 契约测试接线断言固化（7 handler+4 桥+toolCapture+SAF launcher+usage.summary+实例化双锚）；L1 全量 **289/0/0** + assembleDebug 绿 + check-token-effect 通过；Token/KV 影响申报在提交信息。
- **销项条件**：M3 合 main + 全量绿 + 真机 obsidian.vault.detect/screen.capture 实调证据。
- **验收员复核 @2026-08-29**（ACCEPTANCE_LOG 已落档）：L1 289/0/0 亲跑 + 四处接线/实例化/契约断言全实证 ✅；真机 obsidian.vault.detect + SAF 全链（register/check/file.write/read/rescan）+ usage.summary 工具面 ✅ 达成；**screen.capture ❌ 打回**——授权后整屏崩溃（ScreenCaptureService.kt:58 createVirtualDisplay 前未 registerCallback，API 34+ 强制）+ `ScreenCaptureService.pending` 全仓零赋值致结果回填断链（调用方必得「捕获超时」），两处根因明确，修后仅复验 screen.capture。**销项条件未满足（capture 半），挂账维持开放。**
- **验收员复验② @2026-08-29（M3-R2 @5c113cb）**：✅**screen.capture 修复实证**——L1：registerCallback 前置（ScreenCaptureService.kt:51-55 在 :64 前）+ pending 桥接赋值（MainActivity.kt:419）+ deliver 后置空（:101），契约断言活行锚 4/4，全量 293/0/0 亲跑；真机 5556：授权弹窗→同意→返回 `{ok=true, path=…}`，PNG 产物 59669B 落盘且内容真实（非黑图），logcat 无崩溃。**真机销项证据齐——剩「M3 合 main」一环，归设计师。**（观察项：授权交互耗时与捕获等待窗口偏紧，慢操作首试「捕获超时」但 PNG 仍产出，建议后续单评估；记录不阻塞）
- **转工单**：✅设计师定夺 @2026-08-29——**不另立工单，M3-R2 续作 feat/upg02（原施工人，即刻排期）**，施工指令三件：①screen.capture 双根因——ScreenCaptureService.kt:58 `createVirtualDisplay` 前补 `registerCallback`（API 34+ 强制）+ `pending` 赋值接线补上（授权回调后调用方 Deferred 挂到 service，结果 complete 回链，消灭「捕获超时」断链）；②捎修 P1「approval.setMode 收编 uiOnlyMcpTools」（见下条，名单 :56-59 加一行+契约断言「模式类工具不进 MCP 公开面」，收编后真机点一次设置页审批模式切换确认本机桥不受影响）；③捎修 P3「file.read 未授权文案」（见下下条）。验收复验范围=三项+L1 全量亲跑（含新增契约断言），**免重复 obsidian SAF 全链**（已达成）；销项条件维持上行不变
- **✅已销项 @2026-08-29（设计师）**：三环全满足——M3+M3-R2 已合 main（rebase 后 ff-only **f1fba06** 已推 origin）+ 合批全量亲跑 50 类 362/0/0 绿 + 真机 obsidian.vault.detect/screen.capture 实调证据齐（M3 轮 SAF 全链 + M3-R2 轮 capture PNG 真实落盘，设计师 §六抽查 M3R2_17 产物亲核真实截屏）；审验通过（查验员逐项确认）

## [UPG-23 | 2026-08-29] 内置包（device-control/scene-12306/obsidian）停用不摘除工具面——market.disable 落盘有效但 device.*/scene.*/obsidian.* handler 常驻注册（⏳挂账待审）
- **发现者**：程序员（UPG-23 施工，本地 tab 启停语义核对时发现）
- **现象**：UPG-02+04 直挂工具在 MainActivity 启动块无条件注册（deviceProvider/scene 区段）；`market.disable("device-control")` 走 McpMarket.setEnabled builtin 支路只改 market_builtin.json，返回 `prefix=包id` 给 unmountExtTools，但工具名不以包 id 为前缀 → 热摘除空操作。后果：本地 tab / MCP 气泡 / 主页小按钮的「停用」显示如实反映 market_builtin.json（展示层自洽），但 AI 工具面仍可调用这些工具——展示态 ≠ 可调面。browser-automation 不受影响（mcp__browser__ 前缀真摘除）。
- **证据**：MainActivity.kt 设备/scene handler 注册无 builtinEnabled 门；McpMarket.kt setEnabled builtin 支路（仅落盘）；unmountExtTools 按工具名前缀匹配（"device-control" 匹配不到任何 device.* 工具）。
- **建议**：独立小单二选一——①启停三处同步补内置包门（rebuildAgentTools 过滤或 handler 包装层 deny，与 browser-automation 的 connect/dispose 对偶拉齐）；②明确「内置包停用仅 UI 语义」口径并改开关文案。UPG-23 红线「builtin 启停只走既有 setEnabled 支路，不新造持久化」，本卡按红线未动。
- **销项条件**：选定口径施工 + 真机「停用设备包→AI 调用 device.*」行为实证（拦截或放行，与口径一致）。

## [UPG-02+04 验收附带 | 2026-08-29] approval.setMode 在 MCP :8389 面公开——本机调用方可自助切 FULL_ACCESS 绕过全部写类 ASK（P1 ⏳挂账待审）
- **发现者**：验收员（M3 真机抽验，executeTool 通道直调时发现）
- **现象**：`approval.setMode` 未入 uiOnlyMcpTools，在本机 MCP 服务器（127.0.0.1:8389，token 在 filesDir/mcp_token.txt）公开可调——任何持 token 的本机调用方可 `approval.setMode=never`/FULL_ACCESS 自助切换，绕过全部写类工具 ASK 闸。与 MainActivity.kt:3388 注释铁律「权限/模式类工具不暴露远程」自相矛盾（permission.set_mode 已收编，approval.setMode 漏收编）。
- **证据**：M3 验收期间 register/file.write 默认 ask 模式被正确拦截为 APPROVAL_REQUIRED（r_register 返回在案），经 MCP 面 approval.setMode 切 never 后即放行——绕过链全程实证（验收员证据数据 UPG-02_04-M3 目录 payload 落盘）。
- **建议**：approval.setMode 收编入 uiOnlyMcpTools（与 permission.set_mode 同口径），或对模式切换加二次确认；修后契约测试补「模式类工具不出现在 MCP 公开面」断言。
- **销项条件**：approval.setMode 不再出现于我本机 MCP tools/list 公开面 + 契约断言落码 + 全量绿。
- **验收员复验 @2026-08-29（M3-R2 @5c113cb）**：✅**销项条件全满足**——真机 tools/list 140 工具无 approval.setMode、强呼返回 TOOL_NOT_FOUND（对照 device.network 正常；permission.approve 远程同拒）；契约断言活行锚落码（DeviceObsidianContractTest，全量 293/0/0 亲跑）；UI 面设置页审批切换功能不受影响（真机走查）。✅**已落实**，待设计师划销
- **设计师验证 @2026-08-29**：✅**已验证属实**（不信自报信实物——main 亲核：MainActivity.kt:56-59 uiOnlyMcpTools 名单无 approval.setMode（permission.set_mode/approve/deny、presentation.set_mode 在列）；:2188 mcpHandlers 注册在案；:3281 MCP 面注册循环以 uiOnlyMcpTools 过滤 → approval.setMode 必然暴露在 :8389 公开面，与 :54 铁律注释「权限/模式类工具不暴露远程」自相矛盾）。**定夺：P1 成立，随 M3-R2 捎修**（feat/upg02，名单加一行+契约断言，同上条施工指令②）
- **✅已销项 @2026-08-29（设计师）**：验收员复验销项条件全满足（tools/list 无口+强呼 TOOL_NOT_FOUND+契约活行锚+全量绿）+ 已随 M3-R2 合 main（f1fba06）；设计师 §六抽查亲核 M3R2_01 tools/list 三模式工具（approval.setMode/permission.approve/permission.set_mode）零命中、device.network 对照在面——铁律 1 漏洞闭环

## [UPG-02+04 验收附带 | 2026-08-29] obsidian.file.read 未授权态文案不区分「未登记 vault」与「文件不存在」（P3 ⏳挂账待审）
- **发现者**：验收员（M3 真机抽验）
- **现象**：未登记 vault 时调 obsidian.file.read 返回 `{ok=false, error=文件不存在: notes/test.md}`——明确错误、非静默死（达标），但文案误导：实际是 vault 未登记。对照 vault.check 的 hint「未登记 vault——先调用 obsidian.vault.detect 授权」引导语义良好。
- **证据**：验收员证据数据 UPG-02_04-M3 `07_file_read_未授权态返回.json` / `08`（vault.check 对照）。
- **建议**：ObsidianProvider.fileRead 的 resolveDoc 前置 vaultRoot==null 分支，返回「未登记 vault」引导文案（与 vault.check 同口径）。
- **销项条件**：未登记态 file.read 返回「未登记 vault」引导文案 + 全量绿。
- **验收员复验 @2026-08-29（M3-R2 @5c113cb）**：✅**销项条件满足**——真机未登记态返回「未登记 vault——先调用 obsidian.vault.detect 授权」引导文案（字节级干净 UTF-8 落盘）；已登记态读不存在文件仍正确返回「文件不存在」，正例读回成功；全量 293/0/0。注：「权限被回收」中间态真机受限未构造（需系统设置撤 SAF 授权），代码层三态分支已验。✅**已落实**，待设计师划销
- **设计师验证 @2026-08-29**：✅**已验证属实**（ObsidianProvider.kt:135 `resolveDoc(segs, create=false) ?: return "文件不存在: $path"`——resolveDoc :50 对 vaultRoot()==null 与文件缺失同回 null，两态不分；对照 :99 vault.check 引导文案在案）。**定夺：P3 成立，随 M3-R2 捎修**（fileRead 前置 vaultRoot 判空分支，文案与 vault.check 同口径）
- **✅已销项 @2026-08-29（设计师）**：验收员复验销项条件满足（未登记态引导文案字节级干净+已登记态「文件不存在」正误两例+全量绿）+ 已随 M3-R2 合 main（f1fba06）；「权限被回收」中间态真机受限如实申报、代码层三态分支在案，观察项入档不阻塞

## [UPG-02+04 验收附带 | 2026-08-29] obsidian.file.write 相对路径静默放行不弹 ASK——isHarmless 工作区特判误捕（P1 ✅已转工单 UPG-28 @2026-08-29 方案v1，验收后划销）
- **发现者**：验收员（L3 obsidian ASK 补验，5558 真实 AI 全链）
- **现象**：obsidian.file.write 名义在 writeTools 名单（ObsidianProvider.kt:19 自述「不进 harmless」），绝对路径调用确实走 ASK（弹窗实证）；但**相对路径**调用（如 `upg02_l3_probe.md`）静默放行零弹窗——journal 无 approval/asked 事件（两次实证 09:04:30/09:06:19）。根因：`McpToolScheduler.isHarmless()` 的 `name.contains("file.write")` 工作区特判把 obsidian.file.write 一并捕获（相对路径 → isMovWorkspacePath=true → ALLOW）。
- **证据**：`验收员\证据数据\UPG-02_04-M3\L3_obsidian_ASK_2026-08-29\` 全程 jsonl（10）+ 关键事件时间线（11）+ 绝对路径 ASK 弹窗对照（08）。
- **建议**：isHarmless 的 file.write 工作区特判按工具全名精确匹配（只放行 dsh 工作区 file.write，排除 obsidian.file.write），或 obsidian.* 前置排除；补契约断言「obsidian.file.write 任意路径形态都落 writeTools」。**安全面：绕过链 = AI 可不经审批写任意 vault 文件。**
- **销项条件**：相对路径调 obsidian.file.write 真机弹 ASK + 契约断言落码 + 全量绿。

## [W-05 复核 | 2026-08-29] 生产 workbench 事件五子类页面不可达——route() 缺 decodeURIComponent，中文 hash 路由失配（P1 ⏳挂账待审）
- **发现者**：验收员（W-05 P3-2 F12 复核项补验，隔离 Chrome 实例真实点击）
- **现象**：生产 https://mow.kim workbench 页，侧栏事件五子类（备案/证件/审核/事务/运营）真实点击**全部跳回 #/overview**（五连测）；总览「事件」维度卡、本周焦点 E-01 行同病。机理坐实：DOM href=`#/events-备案`（原始中文），浏览器解析后 hash 百分号编码（`#/events-%E5%A4%87%E6%A1%88`），生产 app.js `route()` 未 decodeURIComponent，`findSection` 与中文 key 不匹配 → 重置总览。ASCII key 子类（app-tickets）正常。
- **证据**：`验收员\证据数据\W-05\W05_补验_01_总览_侧栏五子类.png` / `W05_补验_02_子类点击跳回总览.png` / `W05_补验_03_控制台采集.json` / `W05_补验记录_2026-08-29.md`。
- **建议**：二选一——①app.js route() 加 decodeURIComponent 后匹配；②事件子类 key ASCII 化（对齐 app-tickets 先例）。修后真实浏览器复验五子类点击到达五卡渲染页（即原 P3-2 判据）。
- **销项条件**：生产五子类点击到达各自事件卡页（截图五连）+ 控制台零错误。
- **注**：本条即 W-05 P3-2「L2 视觉降级」的复验收口——复核后升级为 P1 功能缺陷，原「F12 闭环」记录失实部分已登记验收错误登记表（`验收员\文档\验收错误登记表.xlsx`）。

## [UPG-02+04 逐工具激活附带 | 2026-08-29] device.timer 准点触发但通知静默丢弃——No Channel found（P2 ⏳挂账待审）
- **发现者**：验收员（逐工具激活，5556）
- **现象**：device.timer set 后 timers.json 落 entry、到点准点消费实锤；但系统 logcat：`NotificationService: No Channel found for pkg=com.mov.android, channelId=mov_general`——通知被静默丢弃，用户无任何感知。timer 工具的「到点提醒」核心交付物实际不可达。
- **证据**：`…\逐工具激活_2026-08-29\` 40-44（timers.json entry + 到点消费 + NotificationService 报错段）。
- **建议**：fire 路径 notify 前 ensureChannel（创建 mov_general 渠道）；复验：set 1 分钟 timer → 到点通知真实弹出（截图）。
- **销项条件**：到点通知用户可见（截图）+ 全量绿。

## [UPG-02+04 平板真机附带 | 2026-08-29] 相机三连测期间 MainActivity ANR + 进程死亡——捕获桥阻塞主线程稳定性隐患（P2 ⏳挂账待审）
- **发现者**：验收员（平板 21770d7d 逐工具激活）
- **现象**：camera 三连测期间 MainActivity ANR（Input dispatching timed out / FocusEvent 5001ms），约 1 分钟后进程死亡、MCP 服务下线，需手动重启 App。与模拟器侧「MOV keeps stopping」史同属捕获桥/UI 等待型调用阻塞主线程的稳定性族。
- **证据**：`…\平板真机_2026-08-29\T03b_ANR崩溃_dropbox.txt`（data_app_anr 记录）。
- **建议**：UI 等待型工具调用（相机/SAF/捕获）统一移出主线程等待模型；另：此类调用未了结期间 MCP 服务对新连接直接拒绝（单线程阻塞，HTTP 000），建议一并评估。
- **销项条件**：相机/SAF 连续调用 5 轮无 ANR/进程死亡 + MCP 并发连接不被拒。

## [UPG-02+04 逐工具激活附带 | 2026-08-29] 工具契约杂项三条（P3 ⏳挂账待审）
- **发现者**：验收员（逐工具激活双线）
- **现象**：①calendar.add 报错文案说参数 begin/end，实测仅 `beginMs/endMs` 生效（报错文案误导）；②MCP tools/list 的 inputSchema 全空（4 个工具参数名靠报错试出：message/packageName/beginMs/endMs/delaySec——调用方无自描述可依据）；③obsidian.vault.register 审批弹窗 60s 自动拒绝 vs 工具 20s 超时竞态——超时后弹窗变僵尸残影（点按无效），register 连续 4 次 20s 超时（5558 L3 补验实证）。
- **证据**：`…\逐工具激活_2026-08-29\`（calendar 段）+ `…\L3_obsidian_ASK_2026-08-29\` 06/07/09（register 超时链）。
- **建议**：①报错文案对齐实际参数名；②tools/list 补 inputSchema（schema 随 Provider 走已有数据，透传即可）；③UI 等待型工具超时 ≥ 审批弹窗自动拒绝时长，或超时后主动 dismiss 僵尸弹窗。
- **销项条件**：tools/list 携带 inputSchema + calendar.add 报错文案一致 + register 不再僵尸弹窗。

## [UPG-22 R1 复验注记 | 2026-08-29] memory/ref 写侧不抑制同 key 重复——长 session jsonl 线性膨胀（P3 ⏳挂账待审）
- **发现者**：验收员（UPG-22 R1 L2 复验，5558 真机）
- **现象**：同 session 每次 cover 装配都向 jsonl 追加原始 ref 条（seq 120/121 与 0/1 同 key 重复落盘）——计数语义全靠读侧聚合层 dedupe（MemoryAggregator refSeen 三元组）兜住，语义正确但写侧无抑制，长 session 下 journal 线性膨胀。
- **建议**：写侧加「同 session 同指纹已打则跳过」一行（recordCoverHitForAssembly 或 MemoryLifecycle.recordCoverHits 内查重）；注意保持 E4a 联动语义（显式移除 invalidate → 指纹变 → 合法再 +1）不被误抑制。
- **销项条件**：同 session 同指纹重复装配不再重复落盘 + MemoryLinkInstrumentedTest 断言 D 及 dedupe 相关断言全绿 + 全量绿。


## [UPG-01 批3-3 真机补验 | 2026-08-30] hardware 5 项工具真机补验——torch.3/vibrate/bluetooth.status（✅ 已销项 @2026-08-30 真机补验通过）
- **发现者**：验收员（UPG-01 批3-3 L2，模拟器约定跳过）
- **现象**：torch.on/off/status、vibrate、bluetooth.status 依赖真实硬件（指示灯/震动/Android 31+ 蓝牙授权语义），模拟器 L2 跳过——需实体平板补验工具调用真实生效（元数据三件套已 handler 实证，仅行为面待真机确认）。
- **证据**：UPG-01 批3-3 验收（见 ACCEPTANCE_LOG UPG-01 批3-3，硬件 5 项标 ⏳）。
- **建议**：实体平板逐工具激活该 5 项，验证工具调用返回真实结果 + 无崩溃。
- **销项条件**：平板逐工具激活 5/5 真实生效（截图/journal + 结果回传）+ 全量绿。
- **注**：验收约定挂账（模拟器跳过），**不阻塞 UPG-01 批3-3 合批**；与 UPG-28/UPG-40 无涉。
- **✅ 销项 @2026-08-30（真机测验员补验，验收员核对）**：设备 21770d7d、APK main 801b8fc、MCP 通道 8389 直调——torch.on/off/status 状态机闭环（hasFlash=true），bluetooth.status 首调权限拦截真实 + pm grant 后返回真实设备名（抹零的Xiaomi Pad 6S Pro 12.4），vibrate 不在 tools/list 且系统级证据 `getprop ro.odm.mm.vibrator.motor_not_support=true` 正确剔除（MainActivity.kt:1955-1959 hasVibrator 条件注册），全部无崩溃（前台截图 hw5_前台无崩溃.png）。报告 `验收员\证据数据\UPG01批3-3\批3-3_hardware5_补验记录.md`（详见 ACCEPTANCE_LOG UPG-01 批3-3 §5）。

## [UPG-27 R1 真机补验 | 2026-08-30] L2 真机四项——code 模式真实任务/双门实测/tool.help 直呼/塌缩自纠（⚠️ 部分执行 @2026-08-30：两项✅两项⏳阻塞，发现 P1）
- **发现者**：审验员（UPG-27 R1 审验；ACCEPTANCE_LOG:2516 已标 ⏳ 待补验）
- **现象**：UPG-27 整体 L2 端到端未验——code 模式真实任务（AI 仅凭 SDK 节+shell.exec 经本机 MCP 面完成真实任务）、双门权限实测（外层 shell.exec 恒 ASK + 内层写类再 ASK）、tool.help 直呼（返回真文档）、塌缩自纠（直呼非在面工具收结构化错误且 AI 据指引自纠）——四项依赖 AI 对话在真机驱动，实体平板发送链自动化不稳（同 UPG-28），未执行。
- **证据**：UPG-27 R1 验收（见 ACCEPTANCE_LOG UPG-27 R1 §4，L2 四项标 ⏳ 待真机补验）。
- **建议**：实体平板（或稳定 AI 发送链环境）走 code 模式真实任务，验证 shell.exec→MCP 调用链 + 双门 ASK + tool.help 直呼 + 塌缩自纠，四项逐一实测。
- **销项条件**：四项真机逐一实测通过（journal 可见 shell.exec→MCP 调用链 + 双门 ASK 截图 + tool.help 真文档 + 塌缩错误自纠）+ 全量绿。
- **注**：代码层打回项已复验通过（R1），本挂账仅 L2 行为面；**不满足整单合 main 前置**（端到端未验），需补验后销；与 UPG-28 L2 受限同因（平板 AI 发送链不稳）。
- **⚠️ 部分执行 @2026-08-30（真机测验员补验，验收员核对，设备 21770d7d、APK feat/upg27 ca47f01）**：①双门权限门 ✅（`tools/call shell.exec {"command":"echo hi"}` → APPROVAL_REQUIRED requestId=req-1 恒 ASK 真实生效）；②schema 门 ⚠️ 跨批依赖（shell.exec 不在 toolRegistry——补验分支基底 main 8af7da9 早于批3 清偿 2026-08-30 合 main，非 UPG-27 回归；合 main 后新基底复验应自然通过）；③tool.help 直呼 ✅（file.read 真文档 + R1 outputHint 诚实化生效，旧「顶层返回键含: path」编造已消）；④code 模式真实任务 + 塌缩自纠 ⏳ **阻塞**——**发现 P1（见挂账-upg27-code模式无手控入口）**。本挂账维持未销（四项未全过），待手控入口修复后复验。

## [UPG-01 批3-2+批3-3 补证 | 2026-08-30] 验收证据目录缺失——L1 XML + 变异 FAILED 输出待落盘（⏳待补证，设计师登记，下次执行）
- **发现者**：审验员（`审验员\工单审验状态.md` 注记）
- **现象**：UPG-01 批3-2、批3-3 已合 main（654c88f），但验收证据目录 `验收员\证据数据\2026-08-30\UPG01批3-2\`、`UPG01批3-3\` 不存在（对照批3-1/批3-4 已有 upg01_b3x_evidence.md）；审验判据仅详记 ACCEPTANCE_LOG，未落独立审计产物。
- **证据**：`审验员\工单审验状态.md` UPG-01 批3-2 行「证据目录 UPG01批3-2\ NOT FOUND」、批3-3 行「证据目录 UPG01批3-3\ NOT FOUND」。
- **建议**：程序员补证——来源分支 `feat/upg01-b3-2`（209d191）/ `feat/upg01-b3-3`（e20f03c）或合批后 main，补：①L1 全量 XML（`app/build/test-results/testDebugUnitTest/*.xml`，两批均 381/0/1）；②变异① desc 改占位必红 FAILED 输出；③变异② host 只并 B1 必红 FAILED 输出；④证据说明 md（重跑命令/分支 hash/时间戳/变异还原说明，参照 `UPG01批3-1\upg01_b31_evidence.md` 格式）。落盘两目录。
- **状态**：⏳ 待补证（设计师登记 @2026-08-30，**下次执行**；不阻塞批3 闭环）【验收员注 @2026-08-30：`验收员\证据数据\UPG01批3-3\` 现已存在（hardware5 真机补验材料：补验记录 + hw5_前台无崩溃.png），但 L1 XML / 变异 FAILED 输出两项仍缺，本条维持 ⏳；批3-2 目录仍无】
## [挂账-upg27-code模式无手控入口 | UPG-27 | 2026-08-30] code 模式无用户手控入口（P1 · 验收员被动发现，挂账待审）
- **发现者**：验收员（UPG-27 R1 L2 真机补验核对）
- **现象**：成品 App 无任何 UI/入口可将呈现模式切到 code——`presentation.set_mode` 被铁律 1 过滤（uiOnlyMcpTools 含之，MCP 面直调实测 TOOL_NOT_FOUND）、`togglePresentationMode()` 全仓库仅定义无调用点（git grep 实证，注释「UI 按钮已移除」）、「极简模式」按钮为占位 toast「即将上线」、presentationMode 无 SharedPreferences 持久化重启回 both、无 intent/debug 入口——设计文档《UPG27_CodeMode设计_外发评审版》:27/:153 明确要求「呈现模式切换入口 uiOnly，用户手控」，实现缺失。
- **证据**：①feat/upg27 MainActivity.kt:148（uiOnlyMcpTools 含 presentation.set_mode）/ :6467 togglePresentationMode 仅 1 处=定义 / :826-827 占位 toast（验收员复核，git grep 全库调用点=0）；②真机 MCP 直调 presentation.set_mode → TOOL_NOT_FOUND；③补验记录 `验收员\证据数据\UPG27\UPG-27_R1_真机补验记录.md`。
- **后果**：UPG-27 核心交付面不可达——「code 模式真实任务」（AI 仅凭 SDK 节+shell.exec 完成真实任务）与「塌缩自纠」（直呼非在面工具→TOOL_COLLAPSED 三分语义）两项判据无法真机执行，L2 端到端未验。
- **建议**：恢复呈现模式 UI 切换按钮（顶部栏已有极简模式占位钮——将其接线到 togglePresentationMode 并补 presentToken 状态切换/持久化），或补设置页入口；修复后复验 code 模式真实任务 + 塌缩自纠（届时 schema 门应随 new 基底含批3 清偿自然通过）。
- **优先级**：P1（核心交付面无入口，阻塞 UPG-27 整体 L2 验收合 main）
- **状态**：✅**设计师验证属实 @2026-08-30**（main 亲核：presentation.set_mode `MainActivity.kt:151` uiOnlyMcpTools / `togglePresentationMode` :6423 仅定义零调用（:6422 注释 UI 按钮已移除）/ 极简模式按钮 :821-822 占位 toast）——**P1 成立**，转 UPG-27 修复（补呈现模式 UI 切换钮 + presentToken 持久化），修复后复验 L2 code 模式两项【设计师注：该缺陷现存在于 main 与 feat/upg27 双面（on-upg27 未合 main），修 main 侧即可随 UPG-27 合入】

## [挂账-upg07批2-审批弹窗四键未渲染 | UPG-07 批2 | 2026-08-30] 审批弹窗四键列表项未渲染——goal 豁免「允许本目标」UI 不可达（P1 · 验收员被动发现，挂账待审）
- **发现者**：验收员（UPG-07 批2 弹窗基线采集 L2 补验核对）
- **现象**：431b3cb 批2 把审批弹窗从标准三键（setNeutralButton/Positive/Negative）改为 `setMessage(...)` + `setItems(labels)` 双调用——**setItems 需独占内容区为 ListView，setMessage 后再 setItems 的列表项不渲染**：真机 uiautomator 节点树（ujb/ujc/ujf.xml）显示弹窗仅 2 个 TextView（alertTitle「审批请求」+ message「AI 请求执行：shell.exec…60 秒无响应将自动拒绝」），无任何 button/list 节点——有 ACTIVE 目标时四键（允许本轮/允许本目标/允许本次/拒绝）**全部不可见不可点**，用户只能等 60s 超时 fail-closed（实测 journal approval/decided=+60s，AI 回「被拒绝后不再重试」改用 echo）。
- **证据**：①节点树铁证 `验收员\证据数据\UPG07批2\ujb.xml/ujc.xml/ujf.xml`（验收员复核：无 android:id/button1、无 ListView 节点）+ 截图 dlg*.png×7；②源码 MainActivity.kt:3575-3585 `AlertDialog.Builder(this).setTitle(...).setMessage(buildApprovalMessage(info)).setItems(labels) { … }.create()`——setMessage+setItems 冲突；431b3cb diff 对照旧版标准三键正常渲染。
- **后果**：批2 核心交付「goal 级豁免（允许本目标）」UI 无入口（goalAllowSet 无法经 UI 命中）；且普通三键场景同样退化——**审批弹窗整体无任何可点按钮**（所有需审批工具调用只能超时拒绝），属审批主链路回归。
- **建议**：弹窗内容二选一——①setItems 去 setMessage（参数摘要拼入列表项/改为 custom view 列表+摘要行）；②恢复 setNeutralButton(允许本轮)+setPositiveButton(允许本次)+setNegativeButton(拒绝) 标准三键 + goal 场景加「允许本目标」独立按钮/第四键（AlertDialog 原生 buttons 上限 3 不够四键 → 需 custom view 或四键需拆分）。修复后复验：弹窗四键渲染（节点树含 button 节点）+ goal 豁免「允许本目标」点击可达 + journal allowed-goal 链。
- **优先级**：P1（审批弹窗主链路不可操作 + 批2 核心交付面不可达）
- **状态**：✅**设计师验证 @2026-08-30**：缺陷属实但**位于 feat/upg07-b2（431b3cb，未合 main）**——当前 main（2ed77f2）审批弹窗为标准三键（`MainActivity.kt:3573-3581` setNeutralButton/POSITIVE/NEGATIVE），**无** setItems 冲突；故属**批2 交付物自身引入的缺陷，随批2 修复**（①setItems 去 setMessage，或②恢复标准三键 + goal 场景加「允许本目标」键），修复后复验批2 再合 main；**非独立 main 缺陷，不另立转工单**（批2 卡内已具修复前置） → ✅ **销项 @2026-08-30**（e7104e3 大白话 v4 实现：setView 根除 setMessage+setItems 冲突；复验通过 ACCEPTANCE_LOG 65fb958：大白话 UI 真机捕获+勾选→allowed-turn+同类免弹+30s 自动取消链——**本挂账由 v4 方案取代销项**）

## [挂账-upg01批4-agent面ext元数据断链 | UPG-01 批4 | 2026-08-30] Agent 面外部工具元数据未接 extToolMeta——AI 收到模板串 desc+空 schema，选参失败（P1 · 验收员 L3 发现，挂账待审）
- **发现者**：验收员（UPG-01 批4 L3 journal 真机验收）
- **现象**：外部 MCP 工具（ext.MockServer.*）在 **MCP 面（8389 tools/list）desc 真描述+inputSchema 非空**（L2 ✅），但在 **Agent 面（rebuildAgentTools）**投影走 projectToolMeta（非严格版）→ 外部工具不在 toolRegistry → 回落「MOV 工具: ext.*」模板串 + **空 schema**；真机 AI 对话实测：AI reasoning 原文「工具描述是 "MOV 工具: ext.MockServer.echo"，没有参数定义」→ 两次调用（hello/echo）arguments 均 `{}`，mock 回显 `mock-hello:{}`——**AI 无法正确选参**。
- **证据**：①真机 journal（room-1788067399600-53fe 新房间：tool/call ext_MockServer_hello/echo arguments={} + AI reasoning 原文 + tool/result mock-hello:{}）；②源码锚：MainActivity:6233-6243 rebuildAgentTools 用 projectToolMeta 无 ext 投影；extToolMeta 仅接 MCP 面（:3647 发现挂载 / :4884 mountExtTools）；③证据目录 `验收员\证据数据\2026-08-30\UPG01批4\`。
- **建议**：①`extMetaMap`（外部发现/mount 时保存 ext.meta）成员；②rebuildAgentTools 投影改 `extToolMeta(name, extMetaMap, toolRegistry, toolParamSchemas) ?: projectToolMeta(...)`；③ToolMetaTest 补 Agent 面 ext 接线锚；④复验：AI 带参调用 + journal 参数非空。
- **优先级**：P1（外部工具接入 AI 复用的核心交付面不可用；UPG-01 批4 整体 L3 未达成 → 打回修复后复验）
- **状态**：🔧 C 已按建议①②③修复交付（feat/upg01-b4 6aaa1fc @2026-08-30：extToolMetaMap 成员+rebuildAgentTools ext.* 三态分支+ToolMetaTest 接线锚；种差：ext.* 前缀判定走 extToolMeta、宿主维持 projectToolMeta 回落不混）；**验收员复验 @2026-08-30：代码层 ✅（55 类 409/0/0+ToolMetaTest 22+变异亲杀[R1 锚必红]），真机 L3 ❌ 未闭环——AI 对 ext_MockServer_hello 仍 3 连 arguments={}（含明确要求带参指令），同会话 host 对照 obsidian.file.search 带参正常（{"keyword":"..."}）→ 模型行为正常、空参特定于 ext 工具=**R1 运行时效果未实证**；**→ ✅ 验收员复验通过 @2026-08-30（15:2x）**：干净流程（mock 127.0.0.1:9600+force-stop 重启+新对话）重新复现——MOV-Boot 15:18:52 工具面 156→158（+ext.MockServer.hello/echo）；journal `tool/call ext_MockServer_hello {\"who\":\"Galaxy\"}` → `tool/result mock-hello:{\"who\":\"Galaxy\"}`（isError=false）全链闭环；此前 3 连空参判定为环境未干净复现（mock 挂载时序/重启缺失）的伪失败——**R1 修复运行效果实证 ✅ · 本挂账销项**（ACCEPTANCE_LOG 6574b93；证据 验收员/证据数据/2026-08-30/UPG01批4R1/）——销项干净，残留旧「维持不销」段已清

## [挂账-upg41v2-内置包启停不可操作 | UPG-41 v2 | 2026-08-30] 内置包（宿主工具组）启停无效——market.disable → MARKET_NOT_INSTALLED（P1 · 验收员 L2 发现，挂账待审）
- **发现者**：验收员（UPG-41 v2 L2 真机走查）
- **现象**：设备控制详情头卡「启停开关」（UI 在、clickable）→ 点击 → `PagesBridge: invoke market.disable -> {"ok":false,"error":"MARKET_NOT_INSTALLED: 未安装: device-control"}`（refresh 重试同）→ **开关状态不变、UI 无任何反馈**——方案 B 明确「内置=有启停开关（点击切换；系统组常驻不可关）」（方案 :82-83/:105/:138）未达成。
- **根因**：`McpMarket.setEnabled(id, enabled)`（McpMarket.kt:309-311）：builtin 包（registry kind=builtin，如 browser-automation）→ builtinStates；市场服务器包（store.get(id)）→ setEnabled+discover；**其余（device-control 等宿主工具组=LocalOverview provider/scene 聚合）→ throw MARKET_NOT_INSTALLED**——宿主工具组在启停数据面无入口（非 builtin 包非市场服务器）。
- **建议**：①designer 定夺内置详情开关语义（方案修正：宿主工具组=常驻不可关（仅系统组灰显改为内置组也可灰显？）或 新建「宿主工具组启停」数据面（tools 装配开关——改动面大））；②若定夺可启停：补数据面（如 registry 内建 kind=builtin 映射 device-control→builtinStates）+ 修复后复验启停+UI 反馈（toast 与否）+ 市场包（browser-automation）启停路径 UI 实测。
- **优先级**：P1（B 需求关键点不可操作）
✅ **销项 @2026-08-30**（675970b 修复+复验通过，ACCEPTANCE_LOG e786524：设备控制详情真机「常驻|无Switch」+市场包模板 ToggleButton 在场+verify⑦5锚+变异①双红；观察项 browser-automation 启停实操待用户点审批补验 P3）

## [挂账-upg41v2-zhjs缺localDetail字典 | UPG-41 v2 | 2026-08-30] zh.js 未提交 localDetail/marketPack i18n key——重建不可复现（P2 · 验收员发现，挂账待审）
- **发现者**：验收员（UPG-41 v2 L1 核物）
- **现象**：en.js:256 已加 localDetail（+43）；**zh.js 零 diff（localDetail/marketPack grep=0）**——但入库产物 tokens-4s9L3xB4.js 含完整 zh 字典（whatFor:"这能干嘛"…）→ **源码与产物不一致**：从提交源码重跑 vite build（验收独立构建实证）→ zh dict 无 localDetail（只 whatFor:"What it does"）→ 中文界面 rebuilt 后详情页退化为裸 key（「localDetail.whatFor」）。
- **根因**：疑似程序员 build 时 zh.js 有 key、提交前回退（或产物从含 key 的本地状态同步入库）——提交树内 zh.js/产物不一致。
- **建议**：zh.js 补 localDetail/marketPack 全量中文 key（入库产物已含原文可直接抄回）+ verify 增「zh.js 含 localDetail/marketPack 全量 key」断言（本次 upg41-verify.mjs 未覆盖此面）；修复后验收员独立 build 复验（zh dict 完整+产物幂等）。
- **优先级**：P2（当前已装机产物功能正常——中文可用；缺陷=重建不可复现，后续 rebuild/合批即丢）
- **状态**：✅ **销项 @2026-08-30**（zh.js 补 localDetail 17+marketPack 20 key 与 en 逐键一致+verify⑧断言；验收员独立 bun build 产物含「这能干嘛/常驻」与提交产物逐字节等价（仅行尾差）+变异②亲杀；ACCEPTANCE_LOG e786524）

## [挂账-upg41v2-browser自动化builtin启停UI不可达 | UPG-41 v2 | 2026-08-30] browser-automation（kind=builtin 市场包）无任何 UI 启停入口——复验口径②结构性不可达（P3 · 验收员复验追加发现，挂账待审）
- **发现者**：验收员（UPG-41 v2 打回修复复验 §5 补注，独立复验追加）
- **现象**：修复构建（16:23:15 装机）本地「MCP 工具」列表仅 设备控制/12306/Obsidian/MockServer/系统基础能力——**browser-automation 缺席本地「市场已安装」组**；store tab 其卡片「已安装」为 `enabled="false"` 禁用 Button（ui_store.xml @16:03 修复构建，bounds [1675,1276][1837,1358]）→ **无任何可点启停入口**。根因=L1 已证：`LocalOverview.build()` 内置组硬编码 [device-control, scene-12306, obsidian]；market 组=bubbleOverview=store.list()；browser-automation 在 store registry 但 **kind=builtin**（setEnabled 走 builtinStates 支路，不落市场组）——已装市场包中仅 MockServer 可见可启停。
- **证据**：`验收员\证据数据\2026-08-30\UPG41R1\` ui_store.xml（已安装 Button enabled="false"）+ fixbuild_r3_mktab.png @16:24（本地列表缺 browser-automation）+ device_detail2.png @15:53（常驻锚）；ACCEPTANCE_LOG UPG-41 v2 §5。
- **后果**：交付报告 §五① 复验口径②「市场包 browser-automation 启用/停用真实生效（builtin 支路，logcat 无 MARKET_NOT_INSTALLED）」**无任何 UI 路径可验证**——既非用户点审批可解锁（原 §3 P3 观察项表述有误，已修正），也非已装可启停。builtin 支路目前仅能以 MockServer（native 支路真机 toggle 无 MARKET_NOT_INSTALLED）+ 代码层 setEnabled builtin 分支 + verify ⑦ 覆盖。
- **建议**：设计师定夺——①给 builtin 市场包补启停入口（store 卡片「已安装」接线为可点 toggle→MarketPackDetail 头卡 hd-switch 复用，走 setEnabled builtin 支路）+ 真机复验 logcat；或 ②修订复验口径②（builtin 支路仅代码层+verify 覆盖即可，UI 不承诺）。
- **优先级**：P3（非本次打回项核心阻塞——修复主目标 P1/P2 已通过；属交付声明与 UI 可达性口径差）
- **状态**：⏳ 挂账待审（设计师验证/定夺后销项或转工单）

## [挂账-upg27-sdk工具名点号下划线错位 | UPG-27 L2 复验 | 2026-08-30] Code SDK 节工具名（点号 shell.exec）与 AI 面直呼名（下划线 shell_exec）不一致——AI 按 SDK 引导直呼即 TOOL_COLLAPSED（P1 · 程序员复验发现，挂账待审）
【⚠️ **误报注销 @2026-08-30 晚**：程序员 C 证伪（TOOL_COLLAPSED 12 次全属 native 面+handlers=点号键 t.name+JVM 同构两形态均命中）+ 验收员独立复核一致（seq 8053/12180/13629 所属面=native）——点号直呼不塌缩，本挂账判定错误撤回（错因：推断 handlers 键=下划线/未核对面归属）——ACCEPTANCE_LOG 5e44347】
- **发现者**：程序员 C（UPG-27 L2 真机复验）
- **现象**：code 模式系统提示/SDK 节写「可直呼工具：shell.exec, tool.help」+目录 161 项全点号名（`- shell.exec：`）；而 AI 面真实注册名为下划线——room-1788071604100-70fd session.jsonl 全量 tool/call 统计：`shell_exec`×45、`file_read`×18、`obsidian_vault_register`×9（全部下划线，成功执行）；`shell.exec`（点号）×12 次 → 全部 `[code: TOOL_COLLAPSED] shell.exec 存在但不在当前模式直呼面——code 模式经 shell.exec 间接调用它（SDK 目录即清单）...`。**同一工具两个名字，AI 混用**。
- **后果**：①AI 照 SDK 节点号名直呼 → 塌缩（12 次）；按 schema 下划线名 → 成功（45 次）——code 模式成功率依赖 AI 猜中下划线形态；②塌缩自纠「按指引转 shell.exec」完整链被阻断（指引给的是点号名，AI 转过去还是塌缩）。
- **证据**：`验收员\证据数据\2026-08-30\UPG27\` system_code_sdk.txt（点号目录）+ turn1_chain.txt（name=shell_exec 成功）+ collapse_call.txt/collapse_result.txt（点号名 TOOL_COLLAPSED）；报告 DELIVERY_UPG27_L2复验_2026-08-30.md
- **建议**：ToolSdkGenerator 工具名输出与 AI 面直呼名对齐（调度器/agentToolSchemas 注册名=下划线形态；SDK 目录/「可直呼工具」声明同源改下划线）后复验「塌缩自纠按指引转 shell.exec」完整链
- **优先级**：P1（核心交付面命名引导错误，影响 code 模式可用性与复验项 2 闭环）
- **状态**：✅ **复核确认属实 @2026-08-30**（验收员）：独立 worktree @4bfddd8 复跑 `:app:compileDebugKotlin` FAILED；MainActivity.kt:**6707 行确见 `<<<<<<< HEAD` 冲突标记残留**；MarkstreamView/destroyWebViewsIn/launchCamera unresolved 确认；UPG-47 停靠 0aa0c07 绿基线合理（ACCEPTANCE_LOG 156d055）→ **转设计师修复主基线**（建议按挂账建议①以 0aa0c07 为基准 diff 清 72 行差异）——【⚠️ 2026-08-30 程序员 P1 证伪（派单回执）：12 次 TOOL_COLLAPSED 逐回合对照全在 native 面（journal line 730/845/1007 system=「当前工具面模式：native」）；handlers=mcpHandlers 点号键（MainActivity:3629）/known:3631/allowed:6581 均点号键；LLM sanitize（DeepSeekAdapter:360）仅影响 schema 展示；JVM 生产同构=点号/下划线直呼均不塌缩；**建议撤销本挂账**，转 P3 可选观察（SDK 节点号 vs schema 下划线一致性）——待出单人裁决；证据 DELIVERY_UPG27_P1修复_2026-08-30.md + SchedulerDotNameBehaviorTest】【✅审验员核验 @2026-08-30(撤回坐实)：调度器归一实证(mcpHandlers 点号键:152 + McpToolScheduler:257 归一 `replace('_','.')` + :277 双形态查找)→ 点号/下划线直呼**均命中，非双 miss**；设备 9 房间 journal 实测：点号 tool/call=**0 次**、TOOL_COLLAPSED 仅 3 次(8053/12180/13629 全部下划线@native 面,正确塌缩)、code 面 shell 10 次全成功 0 塌缩；程序员补验 SchedulerDotNameBehaviorTest(ce5fac9)真实存在,三断言与撤回判定逐条对齐——**「点号 12 次」与实测不符(实测 0 次,12 系初次 grep 含文本误计),此不实更支持误报注销;补注口径供 P5 复核**】

## [挂账-upg27-顶部极简模式钮真机点击无响应 | UPG-27 L2 复验 | 2026-08-30] 顶部「极简模式」钮在平板 21770d7d 上 tap 无任何效果（P2 观察 · 程序员复验发现，挂账待审）
- **发现者**：程序员 C（UPG-27 L2 真机复验）
- **现象**：rebase 后 APK（16:23:15 装机）MainActivity 顶屏 + dump 确认 content-desc=极简模式 clickable=true bounds=[1922,86][2007,171]（x 中心 1964/2032≈96.7% 贴屏幕右缘）；`input tap`×8（含中心/左部）+swipe 注入均无效果（prefs mov_presentation_mode.xml 未生成、无 toast/logcat 痕迹）；对照实验：同注入方式点「相机」钮 → OneShotImageCapture 立即打开（注入有效）；APK dex 含 mov_presentation_mode 字符串（修复在机）→ 指向特定按钮命中问题。
- **疑因**：按钮紧贴右缘（x=1922~2007，屏宽 2032）被系统边缘手势/侧边栏热区拦截（未定性；首次 start 后曾可点成功切 code——journal 内 code system 留存——后续 force-stop/重装后复现失败）。
- **后果**：code 模式 UI 入口在真机不可达（复验期切 code 靠首次可点窗口）；若用户手点同样被拦 → 修复目的（挂账-upg27-code模式无手控入口）只在部分时序可达。
- **建议**：按钮内移/留右缘 margin（或顶栏左侧/房间名右侧），修复后真机复验 tap 链路（toast+prefs+重启保留）。
- **优先级**：P2（入口存在但真机触达性不稳定；journal 已有 code 全链证据）
- **状态**：⏳ 挂账待审（设计师验证/转工单）

---

## 挂账-upg47-main基线编译红

- **现象**：origin/main = 4bfddd8（merge upg46）`compileDebugKotlin` 失败——`MainActivity.kt:248` 报 `MarkstreamView` / `launchCamera` / `destroyWebViewsIn` / `readPresentationModePref` / `openPhotoAsk` / `runOcrOnPhoto` / `closeRoomDrawer` 等 unresolved（定义在文件同体内仍不可见）；对照 `6103bb8`（merge upg18）文件尾部 6681 行存在**未解决冲突标记** `<<<<<<< HEAD`（无 =======/>>>>>>>，残段=HEAD 侧内容）；`0aa0c07`（upg27 分支）同文件编译绿。
- **证据**：`git show 4bfddd8:app/src/main/java/com/mov/android/MainActivity.kt | sed -n '6700,6735p'`（含 `<<<<<<< HEAD`）；主工作区与 mov-capsule 两处 `./gradlew :app:compileDebugKotlin` 均复现；smoke worktree @6103bb8 报 `Syntax error: Expecting member declaration`（6681 断句）。72 行 diff `0aa0c07..4bfddd8` 中 upg46 的 toolOrch 块（615-641）与 upg18 的清理块（1341-1420 等）为嫌疑区。
- **后果**：main 基线红（所有依赖 main 的分支 rebase 后编译失败）；UPG-47 被迫停靠 0aa0c07；UPG-46 之后的合批/复验全部在红基线上。
- **建议**：①主基线修复=找到 6103bb8 合并时的冲突残留并解清（建议以 0aa0c07 为内容基准 diff 核对 72 行差异）；②修复后重新跑 `assembleDebug` + 全量单测确认绿；③UPG-47 分支再 rebase 到新 main 复验。
- **优先级**：P0
- **登记人/日期**：程序员 C / 2026-08-30
- **状态**：⏳ 挂账待审（设计师验证/转工单）

---

## 挂账-upg49-jsonminiparser无上限

- **现象**：真机手工注入产品格式 entities.json（5 条 UTF-8 记忆）后，主线程在 `JsonMini$Parser.num → toDoubleOrNull → Regex.matches` 处卡死（MIUI APP_SCOUT_HANG；Utm 446s+；actresumetimeout 10.5s），界面白屏；删除注入文件后恢复；英文 1 条/中文 1 条/完整 5 条多次注入不再复现（触发条件未完全锁定）。
- **风险**：MemoryStore 解析无步数上限（容错仅 catch 异常；parser 死循环不抛异常）；正常数据由 buildPayload 写（安全），但损坏/旧版文件可能触发。
- **建议**：JsonMini parser 加元素/字符步数上限（超限=容错返回），并加解析耗时有界测试；属 UPGC-48 memory-core 面。
- **优先级**：P2 观察
- **登记人/日期**：程序员 C / 2026-08-30
- **状态**：⏳ 挂账待审

---

## 挂账-upg49-审批弹窗code面approve无效（观察）

- **现象**：今日测试会话（code 工具面）AI 调用 shell_exec 审批弹窗「同意」tap 无效（ApprovalVis answerer 循环 20s 重试；mov-step 每次 ToolCalls shell_exec；tap 坐标正中 clickable=true 节点）。
- **影响**：AI 工具链在 code 面审批交互不可用（记忆保存等）；非 UPG-49 范围内，疑 approvalRespond 桥/工具面交互链。
- **建议**：UPG-27（审批弹窗/code 模式）复核时一并排查；验收员可先用非 code 模式验证工具链。
- **优先级**：P2 观察
- **登记人/日期**：程序员 C / 2026-08-30
- **状态**：⏳ 挂账待审
| 挂账-我的资产-待安全体系前置 | 「我的资产」(资产类目注册体系) 依赖全局安全体系重构——安全不能只在资产内实现 | P1 | 设计师 | 2026-08-31 | ✅已落实·转工单 UPG-55（安全体系 v2.1 已定稿·前置解） | 已转工单 | 现象:用户拟给设置页加「我的资产」(类目管理项注册体系,含换装MCP产生资产),但安全不能只按类目在资产内实现——需先重构全局安全体系。证据:我的资产_资产管理项_设计_v1(07_资产)+安全现分散各模块(Vault/KeyVault/PermissionGuard/审批/MCP门控/网络)。建议:①「我的资产」先挂账——前置=全局安全体系重构定稿;②安全体系独立重构立项(领出加密/密钥/权限/敏感分级/审计/MCP门控为统一全局层),资产/记忆/凭据均基于其上 |

## 挂账-upg53-dispatch白名单缺allowed-goal

- **现象**：`McpToolScheduler.dispatch` 的 outcome 放行白名单只含 `allowed-once / allowed-turn / allowed-remembered`（UPG-53 新增第三项）——**`allowed-goal`（UPG-07 批2 goal 豁免）不在白名单**：AI 调度链下 goal 豁免命中返回 allowed-goal 后会被 `if (outcome != ... && ...)` 判为未通过 → `APPROVAL_DENIED` 失败返回，goal 豁免在 AI 链实际不可用。
- **证据**：McpToolScheduler.kt dispatch ASK 分支白名单条件（feat/upg53 a665349 :364-367；main 同）；ApprovalServiceGoalTest 只验了 ApprovalService 层 outcome（调度层断链未覆盖）；UPG-07 批2 复验（e7104e3/ACCEPTANCE_LOG 65fb958）走的是 allowed-turn 勾选路径。
- **后果**：goal 豁免（允许本目标）对 AI 面实际失效（每次 goal 场景弹窗后执行仍报审批未通过）；用户点「允许本目标」体验断裂。
- **建议**：dispatch 白名单加 `OUTCOME_ALLOWED_GOAL` 一行 + 调度层 goal 豁免放行测试锚（参照 UPG-53 ApprovalExperienceTest 场景7 调度链测试写法）。
- **优先级**：P1
- **登记人/日期**：程序员 C / 2026-08-31（UPG-53 施工中被动发现，未顺手修）
- **状态**：⏳ 挂账待审（设计师验证/转工单）

---

## 挂账-upg53-vault写入免审批

- **现象**：`vault.set`（写个人信息字段）/ `vault.credSet`（写平台账号密码）不在 permissionTier 任何名单 → else → **free（AI 免审批直接写入用户保险柜）**。UPG-53 已闭合删除侧（vault.delete/credDelete 补入 writeTools），写入侧同类缺口仍在。
- **证据**：McpToolScheduler.kt writeTools 名单无 vault.set/credSet（main 与 feat/upg53 同）；真机实测 AI「save phone 13800138000 to vault」免弹直接写入成功（journal tool/call vault_set→tool/result，无 approval 事件）；docs/ApprovalRegistry.json 中 vault.set/credSet approvalMode=free 与安全体系 §5.0 触发清单「写类=审批」总纲冲突。
- **后果**：AI 可静默写用户敏感数据（保险柜语义=机密级，写入比删除更高频更高风险面）。
- **建议**：vault.set/credSet/setPhoto/scanPhoto 补入 writeTools（ask）+ semantics/Registry 同步 + ToolMetaTest 锚；或随 UPG-54 安全中心「操作保护」分组统一定档（写保险柜=敏感确认）。
- **优先级**：P1
- **登记人/日期**：程序员 C / 2026-08-31（UPG-53 施工中被动发现，未顺手修——本单删除侧已闭合，写入侧留单防范围失控）
- **状态**：⏳ 挂账待审（设计师验证/转工单）

---

## 挂账-upg53-turn豁免blast-radius观察

- **现象**：同 turn 内首个写类工具被放行后，后续同工具调用全部静默通过（turnAllowSet 纯内存豁免）。真机实测：AI 在 code 塌缩面下首个 shell.exec 被放行后，同 turn 内连续 `cat` 凭据配置文件（shared_prefs/mcp_token.txt 等）+ curl 本地 MCP 端口全部零打扰通过（journal tool/call 5+ 次，无重复审批）。
- **风险**：turn 豁免的设计意图是「同类操作不重复打扰」，但对 shell.exec 这类**万能工具**，一次放行 = 该 turn 内任意命令（含读凭据/外发）静默执行；与 UPG-53 场景7「记住偏好」的用户显式勾选语义不同（turn 豁免只点一次同意，覆盖面远超单类操作预期）。
- **建议**：随 UPG-54 安全中心评估：①shell.exec/isHighRisk 类不受 turn 豁免（每次必弹）或 ②豁免命中也弹「同类已放行」轻提示（可一键禁止）或 ③豁免粒度收窄到「工具+参数前缀」。UPG-53 已有对照物：持久化记住在写入侧有 canRemember 拦 gate 级，但 turn/goal 豁免在调度层无对应闸。
- **优先级**：P2 观察
- **登记人/日期**：程序员 C / 2026-08-31（UPG-53 真机走查时被动发现）
- **状态**：⏳ 挂账待审

---

---

## 挂账-upg55-verify三等式失真

- **现象**：`MigrationManifest.verify()` 的 ID_SET/HASH 检查是基数比较（`assetIds.toSet().size != sourceCount` / `hashes.size != sourceCount`），与 COUNT（`migratedCount != sourceCount`）等价——内容对账未实现。entries() 返回 5 个错误 ID（declaredCount=5）时 verify() 仍绿。
- **证据**：tool-orch Asset.kt verify() 三行基数比较；UPG-55 验收复跑 AssetFrameworksTest 8/8 绿但无一用例覆盖「错误 ID 内容」场景（M55-1 只测丢条）。
- **后果**：交付报告宣称「三等式（COUNT/ID_SET/HASH）对账」名不副实——实际仅计数对账，迁移正确性唯一保障=条数相等，ID/哈希内容篡改检测缺失。
- **建议**：verify() 引入源侧 ID 集合+哈希映射注入，做内容等值断言；或 M55-1 变异补「错误 ID 内容」用例实证。
- **优先级**：P2
- **登记人/日期**：验收员 / 2026-09-01（UPG-55 L1 验收被动发现）
- **状态**：⏳ 挂账待审（设计师验证/转工单）

---

## 挂账-upg55-assetCredentials假脱敏

- **现象**：`asset.credentials` 桥返回 `mask: InfoVault.mask("", "")`（空串输入），恒为 `**`——脱敏投影无信息量。前端 AssetsPage 分组模式账号列显示一律 `**`，与 demoBridge 设计意图（`wx****03` 风格）不符。
- **证据**：MainActivity.kt:2571；InfoVault.mask() 空串→`**`；AssetsPage.vue loadCerts 消费 p.mask。
- **后果**：凭据页「平台+脱敏账号」中账号部分信息量为零，用户无法区分同名多账号；与交付报告 §四「凭据分组」体验承诺不符。
- **建议**：桥端用真实账号脱敏（credPlain(platform)["account"]→maskCred），或前端改仅展示平台+账号首字符。
- **优先级**：P3
- **登记人/日期**：验收员 / 2026-09-01（UPG-55 L1 验收被动发现）
- **状态**：⏳ 挂账待审

---

## 挂账-upg55-peekPhoto未接线

- **现象**：AssetsPage.vue `peekPhoto(kind)` 调 `asset.peekPhoto` 桥，但 MainActivity 未注册该桥（仅 asset.catalog/credentials/credPeek/ui.openAssets 四个）——证件照缩略图功能不可用（死调用）。
- **证据**：AssetsPage.vue:138；MainActivity.kt mcpHandlers["asset.*"] 全集 grep 无 peekPhoto；demoBridge 亦无该分支（fallthrough 返回 {ok:true}）。
- **后果**：证件照子页签点击空槽无响应/无缩略图，R55 真机验证项「证件照子页签」功能缺失。
- **建议**：补注册 asset.peekPhoto（复用 vault.getPhoto 缩略图路径），或前端移除该调用+子页签改纯占位。
- **优先级**：P3
- **登记人/日期**：验收员 / 2026-09-01（UPG-55 L1 验收被动发现）
- **状态**：⏳ 挂账待审
- **【R1 复验 2026-09-01 更新】**：桥已补注册（1f679df，asset.peekPhoto 转发 vault.peekPhoto）——但**修复不彻底**：vault.peekPhoto 返回字段为 `dataUrl`（MainActivity.kt:3779），前端 AssetsPage.vue:139 消费 `r.thumb` → 字段不匹配，缩略图仍不显示。**状态**：⏳ 待改（前端消费字段对齐 dataUrl 或桥补 thumb 字段；R55-4 真机项随之受阻）
- **【R1b 复验 2026-09-01 更新】**：前端 AssetsPage.vue:139 消费改 `r.dataUrl`（30c0d4b）——与 vault.peekPhoto 返回对齐，**源码+产物 bundle 双实证**（assets-Dsyhh6l-.js 含 `a.ok&&a.dataUrl&&(m.value={...m.value,[t]:a.dataUrl})`）；红线哨兵非 assets/settings 变更 0。**状态**：✅ 代码面已修复——缩略图渲染留 L2 真机 R55-4 实证（真机恢复后补）

---

## 挂账-upg49-Fts5冷启动ANR

- **现象**：302 条场景冷启动主线程 ANR——主线程卡 `MainActivity` onCreate 会话索引重建（MainActivity:2155 调用链 → Fts5QueryEngine 重建会话索引同步执行）。与 UPG-49 P1-2（记忆页 renderAll 全量构建 View）是**不同调用链**，非本次打回项范围。
- **证据**：UPG-49 R2 真机验证（无线 adb 192.168.2.3:44043）期间复现 302 条场景冷启动 ANR；定位 Fts5QueryEngine 会话索引重建在主线程阻塞。
- **后果**：记忆量大（300+ 条）时冷启动黑屏/ANR，影响首屏体验；与记忆页功能无直接因果，属旁路缺陷。
- **建议**：独立工单评估——会话索引重建后台线程化（首启异步）+ 首启懒加载，或增量重建避免全量重建；验证判据=302 条冷启动无 ANR + 搜索可用。
- **优先级**：P2
- **登记人/日期**：程序员 C / 2026-09-01（UPG-49 R2 施工/真机验证中被动发现，未顺手修——超出本单范围）
- **状态**：⏳ 挂账待审（设计师验证/转工单）
| 挂账-sys02-UPG49hash漂移引子【✅ 已转 SYS-02 阶段一 @2026-09-02】 | 工单系统协作执行层待评估：UPG-49 四处状态不一致（表「待认领」/卡「✅C 交付」/报告 hash 9fd39b6 不存在/功能 3 天未合 main）——「信息靠人同步」机制的又一实证 | P2 | 设计师 | 2026-09-02 | ✅已实施 @2026-09-02（SYS-02 阶段一 E2 hash 一致性自动闸落地：审验.py `--verify-hash` + 红线 23 接入强制校验 + fixture 自测 2/2，交付报告 `程序员\交付报告\DELIVERY_SYS02_阶段一_2026-09-02.md`） | SYS-02 阶段一 交付（feat/sys02-ph1） | 现象：①工单表 UPG-49 程序列=「已派单·待认领」而工单库卡=「✅C 交付」——表/卡失同步；②交付报告记 hash「9fd39b6+4e6c0d7」——git rev-parse 9fd39b6 不存在（rebase 重写后未更新）；③分支含第 3 commit（2a13dcd 记忆页改半屏 Sheet·用户拍板）交付报告完全遗漏；④「已交付」却未合 main——用户装 main 包体感=「没做」（与 UPG-02+04 同款）。证据：git merge-base/rev-parse/日志实证（2026-09-02 设计师亲查）+ 本次人工修复=rebase 合入 2a13dcd+补登记。建议：作为 **SYS-02 协作执行层**（E1 状态单源/E2 一致性自动闸/E4 全局仪表盘——工单表=生成物、hash 从 git 读、交付登记机器校验）立项引子——与已验收的 SYS-01（防伪审计线）互补；机制洞仍在（本次为人工修复），待评估后转正式工单 |
| 挂账-用户直改-侧边栏我的资产改名 | 用户直改（不进工单）：侧边栏「我的信息」→「我的资产」——语义=保险柜是资产「凭据」类目（67-A 定案）；入口重定向 vault/assets→ui.openAssets；i18n sidebar.myInfo/vault.title(凭据)+AI 提示词/工具描述术语统一 | P3 | 设计师 | 2026-09-02 | ✅已落实（合 main f9cad92 已推） | — | 现象：用户要求「侧边栏我的信息改成我的资产+全局排查入口错误」；实证：侧边栏 vault 槽走 ui.openVault（老保险柜页）→ 重定向 ui.openAssets（AssetsSheet 资产页）；保险柜=我的资产-凭据（VaultSheet 页内标题改「凭据」）。证据：commit f9cad92（chore(ui) 侧边栏「我的信息」统一为「我的资产」——11 文件+产物 3 页受控；全量 80 套件 558/0/0 绿）。建议：不转工单——用户直改留档（查账时见本行）；后续资产线（67-B/C）继承本语义（UI 一律「我的资产」，保险柜=凭据类目别名） |

| 挂账-UPG50-P2-1 | UPG-50 阶段0 classic vs capsule 视觉近似 | P2 | 设计师 | 2026-09-01 | 已挂账（合 main 附带） | 待修复小单 | composer 高约115px（density 2.75）classic 24dp 圆角=72px 超半高57.5px 被钳制成全圆与 capsule 视觉相同；数据层正确（形态ID/几何指令写入DOM已实证）程序员已申报；修法=composer 增高或 classic 圆角调小至≤57.5px；判据=三形态视觉可辨（STD-UPG50 L1-4）；建议伴随小单0.5天或并入外观后续批 |
| 挂账-deliveryManifest指纹治理 | 历史 delivery_*_manifest.json 治理：E-001~005 缺指纹（evidence 条目缺 sha/指纹字段）+ E-006/007 回填后未重算（设计侧回填条目未回算总指纹）+ 目录型路径（evidence path 有目录形态而非文件）——检出自 UPG-50 阶段 1C 交付复核 | P2 | 验收员 | 2026-09-02 | ⏳ 挂账待审 | 一次治理单（扫描全部 delivery_UPG*_manifest.json：补指纹/重算/路径规范——建议并入审验员 deliver-gen 生成器升级，新 manifest 强校验指纹必填）；不阻塞 1C（1C 复验带缺陷通过——历史 manifest 遗留已如实申报，非 1C 范围）；证据：ACCEPTANCE_LOG + UPG-50 1C 交付报告 §manifest |
| 挂账-UPG50-1C产物待重建 | UPG-50 阶段 1 合入后 sidebar 产物=main 品牌区版——1C 的 UI-SIDE-TOOL 视觉产物未随合入（SidebarNav.vue 源码=合并版已含 1C 11 处；vite 环境缺未重 build）——待 sync-pages 重建后视觉生效 | P3 | 设计师 | 2026-09-02 | ⏳ 挂账待审 | 下一次 sync-pages build（mov-vue 环境补 vite 或由 A 线交付时重建）自动带上；验收口径=STD-UPG50-v1 L2-9 全 20 组件对照（1C 真机已验——重建后抽查 3 组件无回归） | 不阻塞（1C 验收已过+合入） |

