# 【交付报告】UPG-50 阶段2：MCP 组件注册制开放

**实施方**：P2 ｜ **日期**：2026-09-02 ｜ **分支**：`feat/upg50-ph2` ｜ **依据**：`设计师/派单/UPG-50_阶段2_MCP组件注册制开放_派单_2026-09-02.md` + STD `STD-UPG50_阶段2_增补`（L2-11/12 + M-U50-10/11 冻结）

---

## 一、交付范围对照（P0 核心 5 项）

| # | 范围 | 交付 | 落点 |
|---|------|------|------|
| 1 | UI Component Catalog（合法清单） | 20 条编号组件入目录（id/contract_version/provider_id；UI-000001~000020=官方公共/UI-010001+=第三方） | `ui/registry/ComponentCatalog.kt`（`ComponentCatalogs.shared` 全局单例；zip 阶段 1 编号库自动对齐 20 条） |
| 2 | UI Component Registry（本机状态） | install/enable/version/implementation 四态；Resolver/Runtime 读注册态（未注册=不渲染） | `ui/registry/ComponentRegistry.kt`（纯状态机）+ `DisplayAppearanceResolver.resolveWhenRegistered`（fail-closed，不改核心判定） |
| 3 | 上架绑定 | MCP 市场 install 绑定 catalog ID，不可匿名创建 UI | `McpMarket.install → validateUiBinding`（`MARKET_UI_BINDING_REQUIRED` / `MARKET_UI_NOT_REGISTERED`） |
| 4 | Contract Validation + Capability Check | 注册路径必须过（CI 门禁断言） | `ui/registry/ComponentContractValidation.kt`（九字段缺一=红 + 命名规范）+ 契约测试锚定 |
| 5 | ID 规则 | component_id 永不复用（删除即作废）；版本升级同 ID | `ComponentCatalog.retire()`（`retired` 集）+ `registerThirdParty`（跳过已作废/已占用）+ `ComponentRegistry.replaceImplementation`（同 ID 热替换） |

## 二、判据达成表

| 判据 | 结论 | 证据 |
|------|------|------|
| **L2-11** 20 条 catalog 齐全 + 机器校验 | ✅ | `ComponentCatalog.validate()`（official.size!=20 红 + 缺 id/contract_version/provider_id 红 + 编号范式 UI-\d{6}）+ `AppearanceRegistryContractTest.L2-11` 锚定 |
| **L2-12** Registry 四态真实 + 未注册不渲染 | ✅ | `ComponentRegistryTest`（官方 20 预置/install 幂等/enable 启停/卸载）+ `resolveWhenRegistered`（registered=false → null） |
| **M-U50-10** 绕行注册（不入 catalog 直接 install）→ 红 | ✅ | 单测 `ComponentRegistryTest.M-U50-10`（UI-099999 绕行 install → null）+ **真机**（见第四节）+ **变异亲杀**（见第三节） |
| **M-U50-11** ID 复用（删除后重分配）→ 红 | ✅ | 单测（uninstall → isRetired true + 官方不可 retire）+ **变异亲杀**（retired += 注释掉 → 测试红） |

## 三、测试证据（单测 + 变异亲杀）

**单测**（`app/src/test/java/com/mov/android/ui/registry/`）：
- `AppearanceRegistryContractTest`（读源文件字符串锚定——变异删行必红）：L2-11 / L1-19 九字段 / L1-20 单执行通道 / L1-18 Token 隔离 / M-U50-10 / M-U50-11 / fail-closed / 前端分区
- `ComponentRegistryTest`：官方 20 预置 / L1-15 注册幂等卸载 / L1-16 热替换 / L1-17 回滚 / M-U50-10 绕行拒绝 / L2-12 fail-closed / 官方不可卸载
- `ComponentContractValidationTest`：L1-19 九字段缺一=红 + 命名规范红
- `ComponentCatalogTest`：第三方分配 / 作废不复用 / 校验

**变异亲杀 ≥2（M-U50-10/11）**：
| 锚 | 变异操作 | 结果 |
|----|---------|------|
| M-U50-10 install 校验 | `catalog.byCatalogId(catalogId) ?: return null` 伪造为直通 | 测试红 ✅ |
| M-U50-11 ID 作废 | `retired += catalogId` 注释掉 | 测试红 ✅ |
| L2-12 fail-closed | `if (registered) resolve else null` 变异为 if(true) | 测试红 ✅ |

**全量回归**：`./gradlew :app:testDebugUnitTest` + `assembleDebug` 绿（阶段 1 零回归）。

## 四、真机验证记录（模拟器 emulator-5554，交付 APK=最新 assembleDebug 无调试残留）

验证链路：冷启动 → CDP 注入 → 外观页 / 市场页实测。

| 判据 | 真机操作 | 观测结果 |
|------|---------|---------|
| L2-11 | `ui.openAppearance` → 外观页 `ui.listComponents` | 官方 20 条全部 `registered:true`；mcp/extension 分区为空 |
| L2-12 | 外观页分区渲染 + Resolver 读注册态 | 官方组件正常解析；未注册不渲染（fail-closed） |
| M-U50-10 | 市场测试包 `vendor-files`（`uiComponentId=UI-099999`，不在 catalog）→ `market.install` | **`{"ok":false,"error":"MARKET_UI_NOT_REGISTERED: 绑定的 UI catalog ID 未在合法清单: UI-099999"}`** —— 在连接服务器前 fail-closed 拒绝 |
| 解析链路 | 市场目录缓存（`mcp_market_cache.json`）加载 | 测试包正常解析为 1 条 `MarketPackage`（含 server.url 契约） |

> 说明：M-U50-10 真机验证过程中定位到市场测试包必须满足 byo 契约（顶层 `server.url` 必需，否则 parsePackage 静默丢弃）——非代码缺陷，测试包修正后全链路通过。干净代码（删临时日志后）复验一致。

## 五、挂账项（⏳ 不阻塞本单交付）

1. **第三方 catalog 登记与 retired 作废集仅内存态**：`ComponentRegistryStore` 只持久化 registry 第三方条目（filesDir/ui_component_registry.json），不持久化 Catalog 的第三方登记与作废集——App 重启后第三方 catalog 登记丢失、作废 ID 可被重新分配。建议阶段 3 补 Catalog 持久化。
2. **第三方 UI 组件成功注册的真机端到端**：需要阶段 3 市场注册开放全量 + 上传流程（Validation→新 ID）。本单仅覆盖拒绝路径（M-U50-10）与单测成功路径。
3. **P2-1 前置修复**（classic vs capsule 视觉可辨）：`resolveInput` classic `containerRadiusDp=16f`——本单红线之外的阶段 2 前置项，已交付。

## 六、交付物清单（manifest）

**新文件**：
- `app/src/main/java/com/mov/android/ui/registry/ComponentCatalog.kt`
- `app/src/main/java/com/mov/android/ui/registry/ComponentRegistry.kt`
- `app/src/main/java/com/mov/android/ui/registry/ComponentContractValidation.kt`
- `app/src/main/java/com/mov/android/ui/registry/ComponentRegistryStore.kt`
- `app/src/test/java/com/mov/android/ui/registry/`（4 个测试类）

**接线改动**：
- `app/src/main/kotlin/com/hermes/mov/mcp/McpMarket.kt`：validateUiBinding + MARKET_UI_NOT_REGISTERED/BINDING_REQUIRED + uiComponentId 透传
- `app/src/main/java/com/mov/android/appearance/DisplayAppearanceResolver.kt`：resolveWhenRegistered（fail-closed，不改核心判定）+ classic 圆角钳制
- `app/src/main/java/com/mov/android/MainActivity.kt`：ui.openAppearance / ui.listComponents 分区 / market.install 后 registry 落盘 / 侧边栏白名单
- `前端设计/mov-vue/src/AppearanceApp.vue` + `app/src/main/assets/pages/appearance/` 同步产物

**测试**：`app/src/test/java/com/mov/android/ui/registry/` 全绿 + 变异亲杀 ≥2 + 全量回归绿 + assembleDebug 通过。

---

**交付结论**：P0 核心 5 项全部交付，L2-11/L2-12/M-U50-10/M-U50-11 判据达成（含真机证据与变异亲杀），阶段 1 零回归。待设计师合 main。
