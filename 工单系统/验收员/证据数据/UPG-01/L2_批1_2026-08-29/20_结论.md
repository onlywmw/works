# UPG-01 批1 L2 抽样验收结论（emulator-5556）

日期：2026-08-29 ｜ APK：mov-upg01 app-debug.apk（067aa8e）｜ 设备：emulator-5556（5558/平板 21770d7d 全程未触碰）

## 前置处置（透明申报）
- 5556 新装无登录态；登录流程非本次验收项。采用 debug 包 run-as 直写 shared_prefs/mov_login.xml（token=占位串、phone_tail=0000，非真实凭据）打通 MainActivity 前置，见 07_login_inject_note.txt。侧栏截图显示 *****0000，无真实号码泄漏。
- 浏览器自动化能力经真实 UI（ui_ops.py 点击）从 MCP 市场安装，见 14/15/16 截图 + MOV-Market logcat「已连接（14 工具入面）」。

## 判据1 装机四步 + MOV-Boot — ✅
- install Success（01）；dumpsys lastUpdateTime=2026-08-29 12:09:01=本次装机时刻（02）；force-stop+monkey 启动（04/05/08/12 截图链：隐私门→登录页→主界面）。
- logcat MOV-Boot（03）：「工具面模式: both（agent 工具面 143 工具；登记层覆盖 94，模板串回落 49）」+「模板串回落清单： account.me,…,wifi.status」（49 件全名单）——回落清单运行时可见 ✓

## 判据2 MCP 面抽样 4 件（127.0.0.1:8389，token run-as 内存取不落盘，forward 18389）
- device.network — ✅ desc=「查询设备网络状态：在线/离线…零权限只读。」（真字段）；inputSchema properties 空=真无参正确形态（DeviceProvider.kt:317 params=emptyList）。落盘 11_tool_device_network.json
- file.read — ✅ desc=「读取指定路径的文件内容（限沙盒根目录内，拒绝路径穿越）」（真字段）；schema 字段级 required=[path]。落盘 11_tool_file_read.json
- scene.trainQuery — ✅ desc=「查询 12306 余票车次：日期/出发/到达站…」（真字段）；schema 字段级 required=[date,fromStation,toStation]。落盘 11_tool_scene_trainQuery.json
- mcp__browser__snapshot — ⚠️ 不判红（批2 范畴，按任务注记）但实况与「旧原文」预期有偏差：新装态 browser14 本不在面（143 件无 mcp__*）；市场安装后在面（157 件）。8389 面 desc=「外部 MCP 工具： mcp__browser__snapshot」（热挂载模板串，MainActivity.kt:4906 mountExtTools 硬编码），schema 空——连 BrowserMcpTools 旧原文「获取当前页面结构化快照…」都未投射。同期 MOV-Boot 显示登记层覆盖 94→108、回落 49 不变（18），即 AI 面 browser14 已被登记层覆盖而 8389 面仍是挂载模板串——两面投影在 browser 族上分歧，批2 清偿时应一并核销。落盘 11_tool_mcp_browser_snapshot.json、17_tools_list_after_browser.json

## 判据3 同投影对比（抽 file.read）— ✅
tools/list desc 与 BuiltinMcpTools.kt:114 登记层原文逐字一致（19_projection_compare.txt）。

## 总结论：✅ 通过
判据1 ✓；登记层接管族（provider/scene/builtin）3 件 desc 真+schema 字段级 ✓；browser 件按任务注记不判红，实况已如实记录（挂载模板串+空 schema，批2 清偿对象）。
