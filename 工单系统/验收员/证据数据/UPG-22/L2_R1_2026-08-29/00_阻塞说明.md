# UPG-22 R1 L2 真机复验 —— 阻塞说明（2026-08-29）

## 结论：未执行，任务前提不成立（设备状态与派单描述不符）

## 派单前提 vs 实测

| 派单前提 | 实测（emulator-5558） |
|---|---|
| 装有上轮 aacb15b 包 | `pm list packages`（含 `-u` 未安装保留列表）无 `com.mov.android`；`run-as com.mov.android` → unknown package；`dumpsys package` 无记录 |
| 已配 DeepSeek key | key 随 app 数据一同不存在 |
| 猫 draft「元帅」引用 1 次 / 花生 0 次 | 无 app 数据，draft 不存在 |

## 排查过程（全部只读，未动 5556 数据、未碰平板）

1. `adb devices`：emulator-5556 / emulator-5558 在线（无 21770d7d）。
2. `adb -s emulator-5558 emu avd name` → **Medium_Phone_GA**；`emulator-5556` → **MOV_Test**。两 AVD 序列号与上轮（MOV_Test=5558）可能已互换。
3. 5558（Medium_Phone_GA）：第三方包仅 `com.hermes.android`（v3.3.0，08-04）、`com.hermes.dsh.android`（08-25）、`com.android.adbkeyboard`（**lastUpdateTime 2026-08-29 07:42**，与上轮验收同日——佐证该机确为上轮作业机）。`com.mov.android` 在上轮后被卸载，数据（uid u0_a220 的 18 房间 sessions）随卸载删除，不可恢复。
4. 5556（MOV_Test）：`pm list packages -3` 为空、`run-as com.mov.android` unknown——该机亦无 MOV app（只读查询，未做任何写操作）。
5. 双 AVD 的 default_boot 快照 ram.img 均为 08-05 旧物，无可回滚的上轮状态。

## 为什么无法降级执行

- 判据 2/3a/3b/3c/4 全部依赖真实 AI 往返（新 session 发 hello → 装配 → COVER_HIT 打点 → AI 回复）。
- DeepSeek key 只存在于被卸载的 app 数据内；按工程纪律第 5 条 key 不落任何文件/日志，本执行者无从合法取得。
- 全新安装 + 重建 draft（猫/花生记忆提取本身需 LLM）同样卡在 key 上。

## 解锁路径（需派单方补一项）

1. 提供 DeepSeek key（走 UI 设置页录入，不落文件）；
2. 本执行者随即：install 09b9a79 → 配 key → 房间 A 重建两条 cover draft（猫 Yuanshuai / 花生过敏）→ 记基线 → 新房间发 hello 跑实证三连 → 按原判据出 ✅/❌。
   （基线将是 0 次而非上轮的 1 次，跨 session 盲区场景不变，复验有效性不受影响。）

## 附：本轮未做的事

- 未安装 APK（install -r 无意义且会混淆现场，待 key 到位一并做）；
- 未对 5556 / 平板做任何写操作；
- 未改源码、无 git 写操作。
