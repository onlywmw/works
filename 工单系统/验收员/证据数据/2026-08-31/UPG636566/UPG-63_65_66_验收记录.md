# UPG-63/65/66 第二阶段三单 —— 验收员复核记录（2026-08-31）

三单基底均为 main 顶 **2b1bb00**（60 合入后）——最新版纪律 ✓

## UPG-63 第二阶段收口 —— ⛔ 部分打回（② C-6 缺失）

**对象**：feat/upg63 = **0580fa8**
**① Z-5 弹窗基线 ✅**：docs/c7_baseline_UPG63/（31 文件=30 会话 jsonl+baseline_summary.md）；C7BaselineGenerationTest 1/0——JVM 直跑真实 ApprovalService（request→豁免链→审计同链路，answerer 注入）；**计数与 61 only-once 一致**（vault.credSet/set 不吃豁免=每弹真实）；summary=30 会话/46 弹/分布 vault_write 20·http 8·browser 6·mixed 12 与申报一致
**③ M-1 真实重放 ✅**：M1RealReplayTest 5/0——Z3-1~5 真改 EvalFixture 工具面数据走真实 orchestrate（消 P3-3 合成缺口）；Z3-5 由 VersionGuard 分防线复证
**② C-6 ⛔ 缺失**：**multiCallTurns/ratePercent/turns 统计全代码零命中**（35 变更文件全查）——baseline_summary 仅弹窗计数无 MULTI_CALL 分布——**申报与交付不符**
**L1**：app 76 类 **526/0/0** + tool-orch 6 套 42/0/0（M1RealReplay 5/0）+ memory-os 34/0/0
**变异 M-63** ✅：gate2 阈值 d>=2→d>=5 → 「Z3-1 Selection 下降且门 2 拦截」FAILED（真实重放拦截锚有效）
**打回要求**：补 C-6 journal 口径统计（turns/multiCallTurns/ratePercent）+分布落档后复验（①③不动）

## UPG-65 门 3 灰度自动化 —— ✅ 通过

**对象**：feat/upg65 = **f709ed9**
**核物** ✅：Gate3Automation——collectSignal（fabricate_hit/isError journal 投影）+ evaluate 纯函数（Threshold：minSamples=5 防误伤/fabricateHitThreshold=1 零容忍/failRatePercent=50——**宁严勿松**）+ autoDegradeIfSignal（evolution.DEGRADED **actor=system-deriver + payload trigger=auto**——A2-5 自动版替换 60 人工触发版，UPG-57 权限表消费）
**L1**：tool-orch 6 套 **43/0/0**（Gate3AutomationTest 6/0：边界/不误降/Ledger 审计）+ memory-os 34/0/0 + app 75 类 525/0/0
**变异 M-65** ✅：fabricateHitThreshold 1→5 → 「阈值宁严勿松 编造拦截 1 次即退化」FAILED

## UPG-66 A-3 Judge 扩面 —— ✅ 通过

**对象**：feat/upg66 = **1e604c7**（4 commit 含 ReplayFeed parse 双 fix——node 转义地狱自我修复痕迹）
**核物** ✅：
- **JudgeModes 四模式**（exact B1 兼容/数值：负数小数提取/枚举：|・、・, 分隔/包含：）+前缀语法 parse+**观察层红线**（判否落 journal A3-1）+模型假设注释
- **ReplayFeed**：PROPOSED 队列（queued.jsonl 幂等）→人工 accept→ACCEPTED 条目文件（docs/regression_feed/accepted/ 可溯源）——**§6.4 与 B 线同通道**；**本地正则解析**（UPG-48 边界：app 零 import memory-core——**MemoryDependencyBoundaryTest 抓住后修正申报实证**）
- **TimelineLedger.append 单调时间戳**：lastIssuedTs+maxOf(ts, last+1)——**X1-2 flaky 根修**
**L1**：app 76 类 **533/0/0**（AcceptanceJudgeExtendTest **8/0**）+ memory-os 34/0/0
**X1-2 根修复验** ✅✓：单调时间戳后全模块 **3 连跑红行数=0**（修复前 2/4 偶发红）
**变异 M-66c** ✅：numeric 判定反转（`it == want` → `it != want`）→ 「A3-1 判否落 journal 锚」+「数值判定正反例」双红
**P3**：单调时间戳无直接断言锁（M-66 单调失效变异不红——X1-2 断言间接覆盖；建议补「同 ms 严格+1」直接断言）

## 面板状态校正（程序员面板过时）

56 ✅验收｜57 ✅R1 复验通过（04fb19f）+已合 main｜58 ✅验收（7d35e3a）+已合 main｜59 ✅合 main｜60 ✅验收（9f9466d）+已合 main｜61 ✅验收（ff888be）未合 main 待审验员｜**63 ⛔部分打回（补 C-6）｜65 ✅通过｜66 ✅通过**
