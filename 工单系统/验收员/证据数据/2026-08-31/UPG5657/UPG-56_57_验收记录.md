# UPG-56/57 双单验收 —— 验收员复核记录（2026-08-31）

## UPG-56 评测集盘点+fixture 版本机制 —— ✅ 通过

**对象**：feat/upg56 = **cc86fac**（基底 main 8bcc167）
**测试构建**：现场 worktree（最新分支态）

**核物（4 项）**：
1. **盘点报告** docs/eval_inventory_UPG56.md ✅（6 节；规模修正 12 条口径+确定性声明=EvalStabilityTest 10 次重跑六指标全等+orchestrate 零 LLM/零 suspend+方差方法论保留）
2. **版本机制** ✅ EvalFixture.VERSION=1.0.0+VERSION_HISTORY 履历+EvalFixtureVersionGuard（版本不一致抛 ISE=旧 baseline 作废，非降级告警；模型假设注释在案）——UPG-58 即插即用
3. **≥40 条扩充认领** ✅（B 线反哺 ≥10 经 UPG-59 提案通道+人工 28 条，owner=程序员 C，排期 UPG-58 前）
4. **Z-3 坏改动集** ✅ docs/z3_bad_changes_UPG56.md（5 条+预期 delta 方向预登记防事后拟合+使用纪律「M-1 执行后必须 git checkout 还原」；执行待 UPG-60 M-1）

**L1**：tool-orch 全绿（EvalStabilityTest 含守卫锚）+ app **71 套件 504/0/0**
**变异复杀 U56-V1** ✅：EvalFixtureVersionGuard fail-closed 失效（if(false)）→ **2 红**（「版本守卫 fail-closed 抛出」+「变异锚——守卫降级为告警即红」）与申报一致

## UPG-57 Evolution Ledger 骨架 —— ⛔ 打回（P1 交付不完整）

**对象**：feat/upg57 = **f4bae35**（基底 main 8bcc167）

**发现（验收员实测）**：
- **TimelineLedger.kt 在 f4bae35 上零改动**（最后修改=287f9e7 upg52；git diff 8bcc167..f4bae35 该文件为空；grep 无 ACTOR_ALLOWED_EVOLUTION/namespace 分流）
- **commit message 申报「TimelineLedger 泛化（evolution.* 12 事件+ACTOR_ALLOWED_EVOLUTION+actorAllowed namespace 分流）」与 push 内容不符**
- **实测 EvolutionLedgerTest 在 f4bae35 上 5/8 红**：
  - M6 ai-proposal 只能提 CHANGE_PROPOSED —— FAILED
  - X1-2 完整链 PROPOSED→STABLE / →ROLLED_BACK —— FAILED
  - evolution 事件走同一物理账本 —— FAILED
  - X1-3 单查询 —— FAILED
  - 派生幂等 —— FAILED
- **根因**：ACTOR_ALLOWED 无 evolution.* 事件（ai-proposal 只允许 memory 事件 PROPOSED）→ `ledger.append("evolution.CHANGE_PROPOSED", actor="ai-proposal")` 被 require 拒 → 派生/幂等/单查询全链不通
- **判断**：**程序员本地存在未 push 的 TimelineLedger 泛化改动**（申报的测试绿来自本地未提交态）——push 不完整

**Deriver 本体完整** ✅：EvolutionDeriver 173 行（deriveProposals 幂等/appendLifecycleEvent 幂等跳过/explain 单查询一次归因未入流返 null/lifecycleOf）——只差 ledger 泛化落地

**打回要求**：
1. 补 push TimelineLedger.kt 泛化（ACTOR_ALLOWED_EVOLUTION 表：user 全量/ai-proposal 只 evolution.CHANGE_PROPOSED/system-deriver 派生面；actorAllowed 按 `evolution.` 前缀 namespace 分流；memory namespace 原语义一字不动——12 事件类型注册）
2. 补 push 后远端重跑 `:memory-os:test --tests "*evolution*"` 须 8/8 绿 + memory 语义回归锚全绿
3. rebase 最新 main（59 已合 4b5f65f）后再申报复验

## 结论
- **UPG-56 通过**（待审验员合 main）
- **UPG-57 打回**（P1 push 不完整；本体质量良好，补 push 后可快速复验）

---

## UPG-57 修复复验（R1）—— ✅ 通过（2026-08-31 18:2x）

**新 hash**：feat/upg57 = **fbcdd64**（force update；14502c8 rebase 4b5f65f=main 顶 59 合入后 + fbcdd64 补泛化入库）
**打回根因确认**：程序员 commit message 诚实申报「变异还原误杀同文件未提交泛化」（变异 git checkout -- TimelineLedger.kt 时把同文件未提交的泛化改动一起抹掉——与 54 xlsx 教训同款：变异前须 commit 或分文件操作）

**核物（补 push 内容）**：
- NS_EVOLUTION="evolution." + EVOLUTION_EVENT_TYPES 12 事件（CHANGE_PROPOSED/BASELINE_CAPTURED/CHANGE_APPLIED/REGRESSION_EVALUATED/ACCEPTED/REJECTED/SHADOW/CANARY/STABLE/DEGRADED/ROLLED_BACK/MODEL_UPGRADE_RECHECK——前缀消解与 memory ACCEPTED/REJECTED 重名注释在案）
- ACTOR_ALLOWED_EVOLUTION：user 全量 / ai-proposal 只 CHANGE_PROPOSED（M-6 同构）/ system-deriver 派生面（BASELINE_CAPTURED/CHANGE_APPLIED/REGRESSION_EVALUATED/DEGRADED/MODEL_UPGRADE_RECHECK）
- actorAllowed：`evolution.` 前缀分流 + 未知 evolution 类型拒 + **memory 原表一字不动**（ACTOR_ALLOWED 原样）

**L1 复验**：
- memory-os **34/0/0**（EvolutionLedgerTest **7/0**——打回前 5 红全消）
- app **73 套件 519/0/0**

**变异 U57-V1 复杀** ✅：namespace 分流失效（if(false)）→ **5 红**（X1-2/M6/同账本/X1-3/幂等——**与打回前实测完全同构**，锚决定性）

**结论**：修复复验通过 → 待审验员合 main
