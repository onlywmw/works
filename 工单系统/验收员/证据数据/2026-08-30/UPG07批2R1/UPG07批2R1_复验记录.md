# UPG-07 批2 大白话弹窗修复复验（2026-08-30 · 验收员）

**分支**：feat/upg07-b2 e7104e3（基底 origin/main c753e8a）；APK assembleDebug 16:06 装 21770d7d。

- **UI 捕获**（room-1788071604100-70fd shell.exec ls /sdcard）：💻 | AI 想帮你执行一条命令 | 参数：command=ls /sdcard | N 后 AI 会自动取消这次操作 | 同意 | 拒绝 | □ 这次对话里，同类操作都直接同意（节点树：同意@378,1650 / 拒绝@1022,1650 / 勾选@772,1708）
- **journal 链**：勾选+同意 → approval/decided **allowed-turn**（seq 7655，无 ACTIVE goal→ALLOW_TURN）；同类免弹=shellexec 二次调用 seq 7748 asked（reason=本轮已允许(1)）→ 7749 allowed-turn（同秒，不弹窗）；**30s 自动取消**=seq 7359 decided **cancelled**（弹窗到点 complete(null)+dismiss）
- **代码层**：setItems grep 0 残留；setMessage 仅（删除房间确认/后台通知接管——合理保留）；Answer 映射（同意=ALLOW_ONCE/勾选=ALLOW_GOAL|ALLOW_TURN/拒绝=REJECT）；goalIdProvider 防御（无活跃 goal→REJECT fail-closed）
- **验证**：ContractTest 6/0 + 全量 57 类 424/0/0(+1跳过) + 变异 2/2（setItems 回潮红/人话主行删红）
- 观察项：新工具（memory.delete）弹窗直抓未完成（对话流时达时不达，环境因素；代码路径 allowThisTurn 按工具名 keyed 明确新工具必弹）
