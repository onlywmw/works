# W-06 交付报告：工单详情面板（详情抽屉 + 结构化数据）

**程序员**：C ｜ **日期**：2026-08-27 ｜ **分支**：`feat/w06-detail` @ `2120098`（f5ef074 主体 → 2120098 收敛修复）
**部署**：已上线 mow.kim 主域（app.js/detail-drawer.js/index.html/workbench.json）。Token/KV 无影响。
> **已登记**：dims 基建.md W-06 行待验收认领先行（🔨→✅C 完成 待本次同步写入）。

---

## 一、实现对照方案卡四条

| # | 方案要求 | 实现 |
|---|---|---|
| 1 | sync 解析工单库.md | `ticket-details.mjs` parseTicketLibrary：一级节切分（# UPG-13/W-05…）+ h2/h3 小节结构化（追加项为独立节）+ detail 字段全量 inline |
| 2 | 方案卡 designDoc 关联 | findDesignDoc glob `设计师\方案设计\<ID>_*.md` 命中读全文；enrichTicket 统一附加；14 条命中（UPG-01..07/S-04/W-02..07） |
| 3 | 前端详情抽屉 | `detail-drawer.js`：60% 右滑抽屉+遮罩/Esc/✕ 关闭；renderDetailInto 分节渲染；工单卡/事件卡 onclick 挂载（id 引用式零转义风险）；backlog 卡后续 |
| 4 | md 最小渲染器 | mdToHtmlSafe：**先转义再标记**（&<>→实体后标题/列表/表格/粗体/代码/分隔线行级处理）；挂 MovWb.mdToHtmlSafe 供校验 |

## 二、⚠ 真执行断言抓出的两个真实缺陷（当场修复）

1. **dims enrich 回写缺失**：sec() 返回 enriched 但 tree 已引用旧数组 → designDoc/detail 永不生效（W-05 缺 designDoc 实证暴露）。修复：`s.tickets = sec(...)` 回写。
2. **openDetailById 裸调私有 openDetail**：app.js 内调用 detail-drawer.js 的 IIFE 私有函数 → 真实点击 TypeError（verify 真执行断言 `openDetail is not defined` 抓出）。修复：统一走 `window.MovWb.openDetail` 公共 API。

（两处均为「挂点一致性」同族问题——与 W-04 P1-1 同源方法论。）

## 三、L1 / L2 / L3

- **L1 ✓**：verify-workbench.mjs **全量重写收敛**（碎片化状态清理后一次成型）：树 8+20 动态校验/events 一致性/UPG-13 detail 追加#8#9 节断言/W-05 designDoc>200/**XSS 纯文本渲染**（mdToHtmlSafe('<script>…') → 无 <script 纯文本）/**真实点击开抽屉**（openDetailById 后 drawer 含追加#8+验收标准节）/W-04 真执行链/W-05 决策表 9 用例。横幅动态化（追加#1 顺手修第 2 条）。
- **变异演练**：拆 detail-drawer 双处转义（XSS 复现变异）→ **第一版真执行断言抓出 EscHtml 缺失**（因当时 mdToHtmlSafe 破坏型变异导致 TypeError 暴露——说明变异锚有效）✓
- **L2/L3 ✓**：线上 curl 对账——UPG-13 detail 10 节（追加#8/#9 节在案）+ designDoc 覆盖 14 条；站点门禁/登录页渲染不回归（截图 w06_prod_gate.png 登录页正常）。
- **透明申报**：抽屉真实浏览器交互（点击滑出/Esc）留验收员 F12 复核（程序员侧无浏览器自动化，同 W-04/W-05 先例登记）；verify 的 vm 渲染断言已覆盖「点击链数据正确性」。

## 四、过程申报（verify 碎片化与最终收敛）

verify-workbench.mjs 在多轮增量注入（W-04/W-05/W-06 三批）后出现**重复决策表块/变量作用域错位/装载行断裂**三类碎片化损伤，最终以 **read_file 通读全文件 + write_file 整体重写**收敛（12907 bytes）。教训已存档：增量补丁三轮以上必须转全量重写。

## 五、红线遵守

- XSS 红线：mdToHtmlSafe 先转义再标记，XSS 变异断言常驻 ✓
- 抽屉纯展示零编辑 ✓
- 不改现有树/卡片结构（只加 onclick 与抽屉）✓
- 脱敏：detail/designDoc 过 scrub 管线 + 新增长文本词级脱敏（[已脱敏] 保留上下文，短字段维持 ***）✓

---

## 十一、R1 打回修复（验收员 P1×2，commit `c4142c7` 已部署主域）

### P1① 真缺陷：MovWb 整体赋值覆盖先挂挂点（与 W-04 P1-1 同族、相反形态）
- 链路：index.html 先载 detail-drawer.js（挂 openDetail/closeDetail/mdToHtmlSafe）→ 后载 app.js :895 `window.MovWb = {...}` **整体赋值**把先挂的三函数全部抹掉 → 真实点击 openDetailById → TypeError 抽屉不开。
- 修复：`window.MovWb = window.MovWb || {}; Object.assign(window.MovWb, {...})` 合并挂点（logout 等既有项保留）。

### P1② verify 假覆盖的根因：装载顺序与真实浏览器相反
- verify 的 appjsAll 此前=app.js+drawerJs（app 先 drawer 后）——与 index.html 真实顺序相反 → verify 环境里 drawer 后挂全在=假绿；真实浏览器里 drawer 先挂被覆盖=真红。**环境同构性缺口**。
- 修复：装载顺序改 drawerJs+appjs（与 index.html 同构）+ 新增 openDetail/mdToHtmlSafe **存活断言**。

### R1 变异重放（对照验收判据）
- 模拟覆盖回归（delete window.MovWb.openDetail/mdToHtmlSafe）→ verify **挂载缺失 exit=1 红**——同构环境下 verify 真实复现了验收员 F12 抓到的缺陷形态 ✓ 还原绿 ✓。

### 部署与登记
- app.js 已部署主域（线上验证 Object.assign 命中 1 处、无整体赋值、logout 在场；/account/me 401 门禁不变）。
- dims 基建.md W-06 行已推进「🔨C R1 完成 @08-27」待复验；请验收员重跑 F12 三步（点击抽屉开/Esc 关/token 不受影响）。
