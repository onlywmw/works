# DELIVERY UPG-06 批1 收尾（C4+C5+L2）——2026-08-29

**工单**：UPG-06 确定性防编造回补 · 批 1（P0）｜ **分支**：feat/upg06 ｜ **本次提交**：`de7bf88`（已 push origin）
**进度底**：C1+C2+C3 = `3fe7fe7`+`fc8f2dc`+`5878bc8`（前任程序员，半停工）；本棒收尾 C4/C5/L2。
**已登记两个表**：工单表.xlsx 程序员列 ✅完成 + 备注（分支/hash/报告名）；工单库.md 状态同步（先表后库）。

## 定位声明（方案 §三 v3.2 红线，必写）

本闸**治用户感知、不治模型行为**——确定性启发式闸，形态 C（措辞绕过）/部分 D 边缘漏拦是结构性的；误拦率非零，用户侧文案一律「可能不准确」级低确定性措辞。验收对漏拦不设超预期质疑。

## 本棒交付（三段）

### C4 气泡级标记 + 重放重建（前任半停工点：仅留一行日志占位，本棒重做）

- **live 标记升级为气泡级**：`fabricateListener` 原只写隐藏调试台（logView 默认隐藏，用户不可见——不达方案「用户侧可见标记必须有」）。现：markstream 房间页用 `__room` 既有 API 独立起一条标记泡（`beginStream/appendChunk/endStream` 完整循环，**未动 room.html 冻结件**，API 语义经 minified 产物核实：独立循环即新气泡）；原生降级路径同文 `appendAiMd`。落点 `MainActivity.kt:3927` 区。
- **重放重建**：`renderHistory` 两路径（markstream `setHistory` 整房灌入 / 原生逐条）挂 `GuardMarkMatcher`——恢复会话时以 `guard/fabricate_hit` 事件的 `textSnippet` 前缀（≤40 字符）逐气泡 contains 匹配，**首中消费**（一条事件只标一个气泡），命中泡紧随一条标记泡。匹配器为纯逻辑类（`dsh/guard/FabricateGuard.kt` 尾部），JVM 可测。落点 `MainActivity.kt:5588` 区。
- 文案单源：`GuardMarkMatcher.MARKER_TEXT`（live 版后缀「涉及: 工具列表」）；重放版无工具列表（事件未存 specified，不编造）。
- 前任在 `coord.onIndexed` 的占位日志（只 Log 不标泡）已删——FTS 索引回调不是 UI 重建层。

### C5 误拦验证集 ≥200 条（前任半停工点：语料 130+、9 误拦、EXEC_CLAIM 重构残留断尾，本棒收口）

- **语料 203 条**：正常 196（≥50% 达标）+ 幻觉/编造 7（六形态 A-F 关键形态 + 执行声称）。**如实申报：语料为合成**——本机 emulator-5556 无真实历史会话（adb run-as 探过）；真实分布复跑待验收员环境，测试注释已声明。
- **误拦 0/196**（≤2% 达标），压测口径（任务文本统一「用 shell.exec 处理」最严苛）。
- **前任重构漏拦回归修复**：EXEC_CLAIM 重构丢了「设置成功/读取完成/调用成功」形态（幻觉样本 2 条漏拦 + FabricateGuardTest「调用成功」红——前任未跑全量即停工）。补词：裸动词组加 `读取|加载|设置|调用|执行|运行`。
- **三条误拦豁免落码**（方案 §四：每个误拦案例须有豁免/词表落码）：
  1. 「你已经完成了全部配置。」→ `(?<!你)(?<!您)` lookbehind 豁免（「你-主语完成」非 AI 执行声称）；
  2. 「保存成功后会有提示。」→ `(?:成功|完成)(?!后)` lookahead 豁免（预测句非声称）；
  3. 「列表、表格、路径是三种常见的数据展示形态。」→ RESULT_LEAD 弱引子收紧（`(以下是|以下为|结果如下|结果为|清单如下|分别为|分别是)`，删裸 列表/清单/当前/现有/包括/如下/共有）。
- **漏拦归因**：验收期若见漏拦，形态 C（措辞绕过）为结构性已知项（见定位声明）；本集 7 幻觉样本全命中。

### L2 测试桩四点断言（方案 §四 L2，v3.1 接口钉死）

新文件 `FabricateGuardAgentLoopE2ETest.kt`：桩点 = `LlmStreamer` 测试替身（伪造「已设置为 30 分钟后的提醒 ✅」零 tool_call 完成声明），走**真实 ReactLoopAgent 全链**（经 toolCalls.isEmpty()→Completed 分叉，无测试旁路；MockPreparer/MockToolScheduler 为既有件）。四点断言齐全：
① 命中记录（listener 回调 category=EXEC_CLAIM、specified 含 timer.set）；
② guard 审计事件落 journal（turnId/agentId/matchedPattern/textSnippet/toolCallsZero/actionTaken 六字段齐全 + 非 ignorable + turn 包裹：前有 TurnStart 后有 TurnEnd）；
③ 重放重建数据通路（GuardMarkMatcher 对事件 snippet：正常气泡不误标 / 编造气泡命中 / 首中消费不重复标）；
④ 模型上下文零注入（journal 仅 1 条 user 消息且无标记文案；适配器入参全文无「疑似编造」）。
对照组：正常闲聊不触发（变异敏感度锚——删挂载则 ①②全红）。
③的「UI 可见+截图」属真机侧，验收员按剧本补。

## 验证实物（不信自报信实物）

- **L1 全量**：`--rerun-tasks` 亲跑 **335/0/0（48 类）** @2026-08-29 06:47（XML 时间戳新鲜）；guard 系 13+4+2+6 全绿。
- **assembleDebug**：绿（manifest/资源合并过）。
- **check-token-effect.mjs**：通过（提交后跑）。
- 变异锚：删 guard 挂载 → L2 ①②红（结构性，非另跑）；对照组闲聊不误拦。

## Token 影响 / KV Cache 影响（AGENTS.md 硬规则 1）

- **Token 影响：0**——更正标记只入 UI/journal 不进模型上下文（方案 §五 v3.2 定案）；RepeatDetector 提醒达阈值才注入模型面（尾部追加不动前缀），常态不变。
- **KV Cache 影响：前缀字节稳定**——tools/system prompt 会话中途不变，本批零请求链路改动。

## 申报与遗留

1. **「R3 释法」条目存疑**：工单库 UPG-06 待续列「+R3 释法」，但方案 v3.2 与出卡补充段均无 R3 对应物——疑似 UPG-05 行串行（UPG-05 的 R3 边界案释法已出、随 UPG-22 卡⑤发布）。请设计师核实销项或补定义。
2. **分支基线**：feat/upg06 基 = 5170421（UPG-21 合后）；main 已演进至 8a205d6（UPG-02+04 合入）。合 main 前需 rebase 评估——本批改动区（:3927/:5588/renderHistory）与 UPG-02+04 改动区（:1615 装配/:5849 schema 链）不同段，预计可解；**合 main 归设计师**。
3. **真机待验项**（验收员）：guard 命中真机呈现（标记泡可见+重载重建截图）——L2 桩已钉死数据通路，真机只验呈现不赌模型幻觉。
4. **UPG-02+04 M3 接线补救**（本棒另一线，已单独交付）：`feat/upg02 70db6c6` 已 push，挂账登记在册（处理中心挂账登记表「UPG-02+04 合 main 版接线缺失」P1）——待设计师定夺转工单+验收排期。
