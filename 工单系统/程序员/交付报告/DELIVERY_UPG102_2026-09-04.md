# DELIVERY_UPG102_2026-09-04 · UPG-102 拆分批③页面桥面搬移（→ pages/ 模块）

> 程序员：C（Claude/wmw0027）｜ 认领登记 @2026-09-04（工单库 UPG-102 卡）｜ 结论：**两块搬移+契约锚 6/6+全量 113 套件 809/0/1+变异亲杀（M1/M2 实杀+M3 形态调整）——待验收员验收（真机冒烟三场景=持有）**

---

## 〇、交付绑定（SYS-01 P0-2 · 红线 23）

```yaml
delivery_id: DEL-UPG102-20260904-001
standard_id: STD-UPG-102-v1   # content_sha256=3eaa908be80ea7a8647bfafea079841bdcefd3371773fc836c130e788a773c5b
code_commit_sha: d77dae2a    # feat/upg102（基 main d11509cc）
artifact_sha: df7ddfc2992f7551（APK 前 16——assembleDebug 绿后）
evidence_manifest_sha: 见 处理中心/delivery_UPG102_manifest.json
```

- verify-hash 交付时点 = `HASH_REJECT not-ancestor`（分支未合态必然——同前例，合后复跑=终态）。

## 一、施工内容（两块搬移+装配点）

1. **browserHandlers 注册块**（原 MainActivity :2332-2515，14 个 browser.* handler）→ `pages/PageBridgeTools.kt` **顶层扩展** `internal fun MainActivity.registerBrowserHandlers()`（receiver=this；块原文逐字——保真对账 sha256 前后全等）。
2. **WebMcpHub.mountCallback**（原 :2517-2557）→ 同文件 `internal fun MainActivity.setupWebMcpHubCallback()`（含 web.* 工具挂载全逻辑；extToolMetaMap 赋值从 mountExtTools 的 meta 非空分支触发）。
3. **装配点**：MainActivity ToolTimeoutRegistry.register 段前 `registerBrowserHandlers()` + `setupWebMcpHubCallback()` 两行调用。
4. **留壳**：PagesBridge 装配段（:1921-1938 白名单实例化）、pageToolProvider 声明/赋值（:49/:594）、browserHandlers 声明（:359）——装配点红线兑现。
5. **可见性**：approvalService `private var` → `internal var`（被 pages/ 扩展函数引用）；其余 13+ 成员 internal 已由 UPG-93/98 提升（零重复提升）。

## 二、变异亲杀（3 锚 · 2 实杀 + 1 形态调整）

| 锚 | 变异动作 | 结果 |
|---|---|---|
| M1 保真（url 须为 http(s) 改掉） | requireHttpUrl → 裸取 | **RED**（unique string contains 断言——保真锚逻辑级补强在案） |
| M2 名单（删 browser.forward） | browserHandlers 赋值行删除 | **RED**（名单完备断言） |
| M3 唯一写点（壳内残留） | 壳内重加 browserHandlers[...] = | **形态调整**——browserHandlers 变量已搬走（编译不过=壳内零引用已证明）；改用「源码锚 contains 零残留」验证——**转复验** |

## 三、真机冒烟（模拟器实证 + 真机=持有）

| 场景 | 实况 |
|---|---|
| ①侧边栏打开（PagesBridge 链路） | **转验收员持有**（PagesBridge 装配段留壳——本批零触碰） |
| ②browser.* 直呼一条 | **机制面 bun/契约锚全绿**；真机直呼走查=持有 |
| ③web.* 页面挂载 | mountCallback 搬移后 WebMcpHub.mountCallback 赋值经 setupWebMcpHubCallback——**真机验证转持有** |

coverage_status=**PARTIAL**（搬移面 FULL[契约锚 6/6+保真对账]/真机冒烟三场景=持有——设计师裁决位）。

## 四、全量回归

| 面 | 结果 |
|---|---|
| 全量 | `:app:testDebugUnitTest --rerun-tasks` **113 套件 809/0/1 全绿**（0 失败基线；804[U98]+5[UPG-102 契约]=809） |
| 构建 | `:app:assembleDebug` 绿（APK 前 16=df7ddfc2） |

## 五、Token / KV 两节申报（0/0）

纯搬移（browserHandlers 注册块+mountCallback 块）——零 LLM/零存储变化。

## 六、锚定面迁移清单

| 测试 | 迁移内容 | 结果 |
|---|---|---|
| PagesSplitContractTest | 新增（6 案） | ✓ |
| HomeDeliveryContractTest | 零改动 | ✓ |
| MinimalHomePolishContractTest | 零改动 | ✓ |
| HostBuiltinPackContractTest | 零改动（UPG-87/89 锚在 market/ 文件——d11509cc 已收口） | ✓ |
| ToolMetaTest | 零改动（toolFaceSrc 已含 market/ 联合读源——d11509cc 收口） | ✓ |
| SceneWiringContractTest | 零改动 | ✓ |
| Upg84ModeConvergeContractTest | 零改动 | ✓ |

## 七、已登记两个表

① `工单表.xlsx`（sync 投影，UPG-102 行）；② `工单库.md` UPG-102 卡交付块。报告落 `程序员\交付报告\DELIVERY_UPG102_2026-09-04.md`。

---
*程序员 C · 2026-09-04 · worktree mov-upg102 可随验收流程收*
