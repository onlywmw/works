# W-02 交付报告：mow.kim 主域切工作台（提前版）

**程序员**：C ｜ **日期**：2026-08-27 ｜ **性质**：纯服务端部署层（Caddy 配置），无代码改动
**配置留档分支**：`feat/w02-root-workbench` @ `aa5a6e0`（docs/deploy/ 含变更前快照与终态配置，已脱敏）
**服务器**：mow.kim ｜ **备份**：`/etc/caddy/Caddyfile.bak-w02-pre-0408`

> **已登记**：dims 基建.md W-02 行改「待验收」；本报告落 `程序员\交付报告\`。
> 本单不涉 LLM 链路，Token/KV 无影响。

---

## 一、变更集（L1 · diff 仅三条，市场/API 一行未动）

| # | 变更 | Caddyfile 行 |
|---|---|---|
| 1 | 根路径 fallback `root /var/www/mow-admin` → `/var/www/workbench-web`（工作台上主域） | :68 |
| 2 | `@wbLegacy path /wb /wb/*` → `redir / 301`（旧入口收编主域） | :58 |
| 3 | `@marketRoot path /market /market/` → `redir /tools/ 301`（市场首页入口，精确匹配两路径） | :42 |

- `/market/*` 其余路径（**registry.json、dev-guide.md 等 App 在用文件）保持原静态服务一行未动；
- account/market-submit/market-admin/relay/api 等全部反代块逐行比对一致（security_review 复核通过）；
- 变更流程：远端备份 `Caddyfile.bak-w02-pre-0408` → 上传 → `caddy validate`=Valid → `systemctl reload caddy` 成功（journalctl reload 日志在案）。

**一键回滚**：
```
ssh root@mow.kim "cp /etc/caddy/Caddyfile.bak-w02-pre-0408 /etc/caddy/Caddyfile && systemctl reload caddy"
```
（注：301 为永久重定向已被浏览器缓存，回滚后浏览器需强刷或清缓存——security_review INFO 项）

## 二、施工中途纠偏（透明申报）

1. **handle 内 `redir <to> 301` 解析错位**：首版将 redir 写在 handle 块内，adapt 后 JSON 显示 Location 变字面 `"301"`、status 302（真机表现为 /wb/ 200 不跳转）。改用文档标准「顶层 redir + named matcher」后编译正确、实测 301 生效。
2. **marketRoot matcher 曾误吞 registry.json**：matcher 初版为 `/market /market/*`，实测 `/market/registry.json` 被 301 走（违反红线）→ 立即收窄为 `/market /market/` 两精确路径并复验 200。该错误存活时间约 2 分钟，已由 curl 全链复验闭环堵截。

## 三、验证证据

### L1 ✓
配置 diff 三条如上；validate 通过；reload 成功。

### L2 ✓（真实浏览器 = 本机 Edge headless 截图 + DOM 文本断言）
截图入 `验收员\证据数据\W-02\`：
- `W02_L2_root_login.png`：未登录访问 `https://mow.kim/` → **MOV 工作台登录页**（DOM 断言命中「MOV 工作台/登录(内测版)/获取验证码/账号密码登录」）✓
- `W02_L2_wb301_to_root.png`：`https://mow.kim/wb/` → 301 到 `/` 渲染同一登录页（curl 实测 `301 -> https://mow.kim/`）✓
- `W02_L2_market_home.png`：`https://mow.kim/tools/` 市场站首页正常渲染 ✓
- 登录后五维工作台视图：门禁逻辑未动（account-service 原校验），凭既有测试账号可在浏览器人工复核——验收员按原 UPG-08 账号路径复核即可。

curl 冒烟矩阵（现网实测）：

| URL | 结果 |
|---|---|
| `/` | 200 工作台 index |
| `/wb/`、`/wb/workbench.json` | 301 → `https://mow.kim/` |
| `/market/`、`/market` | 301 → `https://mow.kim/tools/` |
| **`/market/registry.json`** | **200**（App 命根子无恙） |
| `/market/dev-guide.md` | 200 |
| `/tools/guide.html`、`/tools/index.html` | 200 |
| `/account/me`（无 token） | 401（门禁正常） |
| `/healthz` | 200 |

### L3 ⏳ 部分降级申报（App 实测受限）
- **已完成**：App 消费面复现——以 node fetch 按 App 同一消费方式拉取 `registry.json` 解析成功（结构 `{version, updatedAt, packages}` 完整）+ 各 API 服务端口存活冒烟（8401/8402/8403/8080/8790）。
- **降级原因**：模拟器（MOV_Test, SwiftShader 软渲染）外网 NAT 故障（ICMP/TCP 双断、冷重启 QEMU 进程重建 NAT 后依旧；软重启无效），期间伴随 System UI ANR。「已装 App 打开市场页实测」无法端到端执行。
- **处置**：已在处理中心登记「真机补验挂账」，请验收员在可用网络环境对已装 App 做一次市场页浏览实证后闭环。**服务端零影响的核心事实已由 curl/node 双通道钉死。**

## 四、安全审查

security_review 结论：三处变更均未引入安全问题（无暴露面扩大/无 open redirect/无走私面/反代未被削弱）。 LOW（存量）项处置：入库快照中 basic_auth bcrypt 哈希已脱敏为占位符（commit aa5a6e0），真值仅在服务器本地。INFO：HSTS 等安全头缺失属全局存量事项，不在本单范围。

## 五、遗留

- mov-ai.cn 站块本次零触碰（其 fallback 仍为 mov-ai-public 公众站）。
- 原 mow-admin 页面文件保留于服务器（可随时恢复）；如确认不再需要建议设计师定夺清理时机。
