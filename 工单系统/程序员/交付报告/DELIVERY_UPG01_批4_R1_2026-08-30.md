# DELIVERY UPG-01 批4 R1（agent 面外部工具元数据接线——验收 L3 打回 P1 修复）

> 程序员 C ｜ 2026-08-30 ｜ 分支 `feat/upg01-b4`（worktree `mov-upg01-b4`；rebase 至 `origin/main 8f8debd`；修复 commit `6aaa1fc`）
> 挂账：`处理中心/挂账登记表.md`（挂账-upg01批4-agent面ext元数据断链，P1 · 验收员 L3 发现）
> **已登记两个表**（工单表.xlsx + 工单库.md，先表后库）

## 一、修复内容（验收修复方向 ①②③ 全落）

| 方向 | 实现 |
|---|---|
| ① extMetaMap 成员 | 新增 `@Volatile private var extToolMetaMap`：外部发现挂载处 `extToolMetaMap = ext.meta` + `mountExtTools` meta 非空时 `extToolMetaMap + meta` 并入（热挂载同步 agent 面） |
| ② rebuildAgentTools 投影 | agent 面：`ext.*` 前缀 → `extToolMeta(name, extToolMetaMap, toolRegistry, toolParamSchemas)` 三态（外部元数据→登记层→外部模板串）；宿主工具仍走 `projectToolMeta`（「MOV 工具:」回落语义不变，两态不混） |
| ③ ToolMetaTest 锚 | 新增 `批4-R1 agent 面外部工具投影接线锚`：ext 分支先于宿主分支（indexOf 序）/成员写入点两处/声明存在 |

**根因闭环**：验收 L3 journal——AI reasoning「工具描述是 'MOV 工具: ext.MockServer.echo'，没有参数定义」→ `arguments={}`。修复后 agent 面 `ext.*` desc=inputSchema 与 MCP 面同源（外部真描述），AI 可正确选参。

## 二、验证证据

- **定向**：`ToolMetaTest` 22 用例全绿（原 21 + R1 接线锚 1）
- **变异亲杀 1/1**：rebuildAgentTools 改回恒 `projectToolMeta` → 「agent 面未接 ext.* 三态投影（P1 回归）」必红 ✅（先 commit 后变异，恢复后工作区干净）
- **全量**：55 类 **409 用例 0 失败**（rebase 8f8debd 后总基线）
- assembleDebug **BUILD SUCCESSFUL**（37 tasks）
- check-token-effect 通过（exit=0）
- MainActivity 纯 CRLF（字节级复核）

## 三、Token/KV

- **Token 影响**：0/0（agent 面外部工具元数据与 MCP 面同源对齐——原 MCP 面已申报量级；外部工具入面 token 见批 4 主报告）
- **KV Cache 影响**：0/0（无会话中途新增字段；rebuild 为既有变动点）

## 四、L3 复验（留给验收员，口径复述）

- 真机 21770d7d：外部 mock MCP（filesDir/mcp_servers.json 引导）→ AI 对话「用 ext.MockServer.hello/echo」→ 工具描述=外部真描述 + arguments 带参（如 `{"name":"..."}`）→ journal 见 tool/call 带参 + tool/result；同批复跑批 4 L2（MCP 面 8389 透传 + browser 热挂载 + 全量 158 工具模板串 0 残留）。
- 注：分支已 rebase 至 8f8debd（原 5f5219c/8cf7a2f → 45f5e20/cde86ac，语义不变），验收侧 hash 引用以本次 push 为准。

## 五、挂账销项

- 挂账-upg01批4-agent面ext元数据断链：**✅C 修复 → 待验收员复验 L3 销项**。
