# DELIVERY_UPG01_批2 — browser 校准批交付（2026-08-29）

> 程序员 C ｜ 分支 `feat/upg01-b2` ｜ 提交 `d33c112`（基底 main 0fdfe87，已 push origin；回滚 = revert 单 commit）｜ 方案 v2.3 §三批 2
> 已登记两个表：工单表.xlsx UPG-01 行备注（✅C 批2 完成 + hash）+ 工单库.md 状态同步（先表后库）
> 与批 1 关系：不同文件（BrowserMcpTools.kt/tools/ vs MainActivity 装配区）可并行；批 2 基线 0fdfe87 = 最新 main（批 1 基线 8af7da9 为其祖先，合入顺序无冲突）；取值链接入在批 1 分支，两批合 main 后自然汇合

---

## 一、交付内容

### 1. browser 14 工具三件套（`BrowserMcpTools.kt`）

- **description（只增不改，红线 3）**：原 14 条一句话描述逐条保留原文前缀，差异点/限制以增句补齐（≤60 字）——如 snapshot 增「可交互元素标注 ref=eN，供 click/fill 引用」、click 增「支付/提交类按钮先弹审批（D5），ref 过期报 REF_STALE」、login 增「凭据需先经 vault.credSet 保存，明文不进 AI 上下文」、back/forward 互写差异点。
- **inputSchema 字段级**：`jsonSchema` 升级 Triple 形态（name/type/desc），每字段中文 description ≤15 字，语义逐字段查源码核实（`MainActivity browserHandlers` + `assets/browser/agent-layer.js`，如 extract 的 schema 值格式 css/css@attr、fillForm 的 type 勾选/下拉、scroll 后 ref 失效等行为语义均来自实现代码）。
- **output 声明（新增 `ToolMeta.output` 字段）**：点名顶层返回键，全部按实现实测——snapshot=`ok,text,refs,docVersion`、click=`ok,tag,text,isSubmit`、fill=`ok,ref,chars`、scroll=`ok,x,y`、waitFor=`ok,by`、waitUser=`ok,waited`、detectForms=`ok,forms`、fillForm=`ok,filled,submitted,message,errors`、login=`ok,filled,submitted,formAction`、extract=`ok,data`、markdown=`ok,markdown`、back/forward=`ok`；错误态统一 `ok:false+error`（+hint 时注明），错误码（REF_STALE/APPROVAL_DENIED/NO_HISTORY/TIMEOUT/USER_TIMEOUT/ERR_NO_PAGE）按实际存在附语义，无编造。

### 2. journal 频次榜脚本（`tools/journal_freq_top.mjs`，新增）

- 「近 N 天 journal 工具调用频次聚合 Top 榜」一次性统计脚本（**不新打点**，方案 §五）：扫会话 JSONL 目录/单文件，宽松兼容多种事件形态（type 含 tool 的事件中 toolName/tool/tool_name 字段，callId 去重），输出频次 Top 榜 + 总行数/解析失败数对账行。
- 消费方：Token 拍板数据（常用集裁剪）+ 批 3 子单排序轴（「AI 调用频次 × 领域关键度」）。`node --check` 语法过。

### 3. 校准使命（批 2 立单目的）

- 「撰写+来源标注+验收断言」全流程跑通：14 工具三件套撰写 + 断言测试 + 变异亲杀 + 交付全链在本批完成。
- **单工具实测工时**：14 工具三件套（字段语义查源码 + 描述撰写 + output 实测 + 压缩至限内迭代 5 轮）合计 ≈ 45 分钟，**≈3.2 分钟/工具**（含测试与对账）；批 3 宿主工具约 116 个（147−22 已登记−9 uiOnly，其中部分无 schema 债）按 3~5 分钟/工具估算 ≈ 6~10 小时净施工，建议拆 3~4 个子单并行。

## 二、验证

| 口径 | 结果 |
|---|---|
| L1 BrowserToolsMetaTest | **6 用例全绿**：14 工具命名契约/描述真且原文保留（只增不改逐条前缀断言）/字段级描述非参数名占位（≤15 字含中文）/output 点名键（抽样语义核对实测返回键）/真无参合法（detectForms/back/forward）/代理端契约（connectProxy/rawName 双向） |
| 变异亲杀两条 | ①描述改占位 → 红 ✓ ②字段描述回占位（description=字段名）→ 红 ✓（还原后绿） |
| L1 全量 | **51 类 368 用例 / 0 失败 / 0 错误 + 1 跳过**（rm -rf 强制重跑；feat/upg01-b2 树口径 = main 0fdfe87 + 本批 6 用例） |
| assembleDebug | BUILD SUCCESSFUL |
| Token 脚本 | `check-token-effect` 通过 |
| 行尾纪律 | BrowserMcpTools.kt 原文件 CRLF，重写后恢复 CRLF（unix2dos，blob 无 diff） |

## 三、Token / KV 申报

- **本批分支内 AI 面 token 无变化**：browser 14 元数据的取值链接入在批 1 分支（`buildToolRegistry`），批 2 只改数据源本身；两批合 main 后 AI 面 browser 14 描述从模板串（≈12 字）→ 真描述（≈20-30 字）≈ +250 字 ≈ +170 token/轮（与批 1 申报合计，仍远低于方案全量推导量级）。
- **KV Cache**：数据源为静态只读（TOOLS 常量），零新增会话中途变动点（红线 5 ✓）。

## 四、红线复核

1. 不改工具行为/调用签名 ✓（BrowserMcpTools 是元数据资源端，connectProxy handler 转发零改动）；McpServer 签名不可变 ✓（本批未触碰）
2. 描述真实 ✓（字段语义/output 键逐项查实现核实，错误码零编造）；「待核实」0 个 ✓
3. 现有已验收描述只增不改 ✓（原文前缀断言固化进测试）
4. 登记层与接线同批 ✓（本批数据源 + 批 1 接线为同发两支，合 main 即接通；批 2 测试自证数据形态合规）
5. 请求前缀恒定 ✓；6. 回滚 = revert 单 commit ✓（d33c112 单提交）；7. 禁平行结构/禁落盘 ✓（TOOLS 常量即唯一事实源，频次榜脚本只读）
8. 回滚单 commit ✓；9. MainActivity 未触碰 ✓（CRLF 红线不适用；改动文件行尾与原文件一致）

## 五、交付物

| 项 | 值 |
|---|---|
| 分支 / hash | `feat/upg01-b2` / `d33c112`（push origin ✓，新分支） |
| 改动面 | BrowserMcpTools.kt（14 工具三件套 + jsonSchema/ToolMeta 升级）+ BrowserToolsMetaTest.kt（新增 6 用例）+ tools/journal_freq_top.mjs（新增）；3 文件 +307/-16 |
| 报告 | 本文件 `程序员\交付报告\DELIVERY_UPG01_批2_2026-08-29.md` |
| 校准数据 | ≈3.2 分钟/工具（详见 §一.3，批 3 拆子单依据） |
