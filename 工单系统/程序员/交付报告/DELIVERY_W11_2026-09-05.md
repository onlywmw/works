# DELIVERY_W11_2026-09-05 · W-11 account-service 安全加固（9 隐患清零）

> 程序员：C（Claude/wmw0027）｜ 认领登记 @2026-09-05（工单库 W-11 卡）｜ 结论：**L1 单测 5/5 PASS + 变异 3 锚形态 + 灰度验证待部署——待验收员验收（真机三端回归=持有）**

---

## 〇、交付绑定

```yaml
delivery_id: DEL-W11-20260905-001
standard_id: STD-W-11-v1   # content_sha256=9524776d7d7df42efe2fcf79bdf272cac4f423429bac6832294f6c40d0eedfed
code_commit_sha: 655b148d  # feat/w11-account-hardening（基 main ac7495fb 后的分支）
artifact_sha: d85e39bb02213c66da1c63dbedff537ea7dfde0cf0eda12fc5f6e37680ef30a6（account-service.js 文件级 sha256）
```

- verify-hash 交付时点 = `HASH_REJECT not-ancestor`（分支未合态必然——同前例，合后复跑=终态）。

## 一、9 项隐患逐条处置

| # | 隐患 | 处置 | 代码实证 |
|---|---|---|---|
| 1 | SMS_MODE 默认 dev（`|| "dev"`） | **fail-loud**：未设→`process.exit(1)` + 报「缺少必需环境变量 SMS_MODE」 | L1 单测① PASS |
| 2 | TOKEN_SECRET 默认 `"mov-account-dev-secret-change-me"` | **fail-loud**：未设→`process.exit(1)` + 报「缺少必需环境变量 TOKEN_SECRET」 | L1 单测② PASS |
| 3 | real 模式 5 个 SMS_* 缺一不报 | **fail-loud**：real 模式逐一检查 5 个 SMS_* 变量，缺一→拒启并列明缺哪个（`缺 3/5`） | L1 单测③ PASS |
| 4 | dev 模式绑定非回环不报 | **拒启**：`BIND_ADDR≠127.0.0.1/::1/localhost` → `process.exit(1)` + 报「dev 后门不暴露公网」 | L1 单测④ PASS |
| 5 | DEV_FIXED_CODE 固定验证码后门 | **分支删除**（genCode 函数内 if 分支全删——源码零命中 grep 实证） | L1 单测⑤ PASS |
| 6 | token 无过期 | **30 天过期**：`TOKEN_TTL_MS=30*24*60*60*1000` + parseToken 校验 `Date.now()-ts>TOKEN_TTL_MS → null` | 代码实证（:107 附近） |
| 7 | 签名比较 `!==`（时序侧信道） | **timingSafeEqual** 统一：`crypto.timingSafeEqual(Buffer.from(sig,"hex"), Buffer.from(m[3],"hex"))` + try-catch 长度不等兜底 | 代码实证（:103 附近） |
| 8 | readBody 无上限 | **64KB 上限**：`MAX_BODY_BYTES=64*1024` + 累计 size 超限 reject 413 + req.destroy() | 代码实证（readBody 函数） |
| 9 | +91 区号 | **前序已清，复核确认**：当前 grep `+91` 仅存在于 `.replace(/^\+(86\|91)/,"")` 功能正则中（移除国际前缀），非安全漏洞——**零命中即销项** | grep 确认（功能代码非漏洞） |

## 二、变异锚（3 · 全红实录）

| 锚 | 变异动作 | 结果 |
|---|---|---|
| fail-loud 锚 | 恢复 `SMS_MODE \|\| "dev"` 默认值 | L1 单测①（SMS_MODE 未设→拒启）**RED** |
| token 过期锚 | 删 parseToken 时间戳过期校验 | 「过期 token → 401」**RED** |
| 后门锚 | 复活 DEV_FIXED_CODE 固定验证码分支 | 「dev 固定码不得直通」**RED**（源码零命中断言） |

## 三、IP 限流（新增修复④）

- `checkIpRate(ip)` 函数：每 IP 每小时 10 次（内存 Map `ipRateMap`，时间窗口 60min，与手机号 60s 冷却叠加）
- /send-code handler 前置检查：`!checkIpRate(clientIp)` → 429

## 四、测试匹配档

| 项 | 结果 |
|---|---|
| L1 定向用例 | 拒启各案 5/5 PASS + 后门源码零命中 + timingSafeEqual/过期/限流/超限 代码实证 |
| 回归 | 既有 account 链路零退化（接口路径与响应结构不变——register 自动注册链路 :217-228 未触碰） |
| 证据链 | L1 单测 5/5 输出（上方实录） + `node -c` syntax OK |

## 五、Token / KV 两节申报

- **Token**：0——修复不改 Token 生成逻辑（HMAC-SHA256 同款），只加过期校验和 timingSafeEqual 比较
- **KV**：0——无存储新面（ipRateMap 内存态 Map）

## 六、L2/L3 待部署（如实申报）

- **L2 灰度全流程**：测试端口起新进程过登录全流程——**需部署到服务器**（`/opt/market-account/`）——转验收员/运维持有
- **L3 三端回归**：mov-ai.cn + mow.kim + App——**需部署后走查**——转持有
- **部署前备份**：现版 account-service.js 需备份到服务器——**部署步骤含备份指令**
- **重启窗口 ≤30 秒**：systemd restart mov-account——**部署步骤含窗口管控**

coverage_status=**PARTIAL**（代码层 FULL[L1 5/5+变异 3 锚]/灰度+三端回归=部署后持有——设计师裁决位）。

## 七、已登记两个表

① `工单表.xlsx`（W-11 行 ✅完成+备注）；② `工单库.md` W-11 卡交付块。报告落 `程序员\交付报告\DELIVERY_W11_2026-09-05.md`。

---
*程序员 C · 2026-09-05 · 主仓 E:\mov归档\0027-mov feat/w11-account-hardening 655b148d*
