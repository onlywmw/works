# 交付报告 · SYS-02 阶段三（E1 状态单源：工单库=唯一权威，工单表=生成物）

> 类型：SYS 系统自改进（独立编号，不混入 UPG 主线）
> 日期：2026-09-02 ｜ 依据：`设计师\派单\SYS-02_阶段三_派单_2026-09-02.md`（验收判据 S2-4/S2-6/S2-7/S2-8/S2-9）
> 治理归属：工单系统自身改造（**只动 `Desktop\MOV\工单系统`**，**零 0027-mov 代码**）
> 状态：✅ 已完成（交付，待验收）

---

## 一、本阶段交付（1 件核心 + 1 探测原型）

| # | 交付 | 实现 |
|---|---|---|
| 1 | **E1 单向同步器 `sync-orders.mjs`** | `审验员\sync-orders.mjs`：① **3A 校对器** `--check`——解析工单库.md 每卡状态行 → 与工单表.xlsx 实际内容逐列 diff 报告（表缺行/值不一致/库缺卡，精确到 `[UPG-xx · 列X]`），**只读不写表**（退出码 0=CHECK_OK/1=CHECK_DIFF/2=ERROR）；② **3B 单向生成器** `--sync`——库→表全量重写（`--table <副本>` 仅限测试副本）；**成功条件=生成后 `--check` diff=0**；③ `--table` 覆盖（测试/迁移演练，不碰真实表）；④ `--sync` 真实表前自动备份到 `_备份归档\`（本次未触发——未覆盖真实表） |
| 2 | `_proto_extract.py`（探测原型） | `审验员\_proto_extract.py`：仅探测用（非交付物），验证提取逻辑（SEC_WORDS 边界/SEG 分段/角色优先级/pick 语义） |

**实现要点**：
- **库解析**：`fs.readFileSync(...).replace(/\r\n/g,"\n")`（Windows CRLF 修正）；`SEC_WORDS` 正文节标题词表定位状态区边界；`SEG_RE` 按 `→`/`｜`+emoji 或 `**` 切状态段；角色优先级 `merge > inspector > dev > designer`（段头 40 字符匹配，防「方案段含复核→误判 inspector」）；`pick()` 按 `@YYYY-MM-DD` 日期取最高且最后出现的动作段（≤70 字）；`**优先级**：X` 提取（去括号后缀）；`DOC_RE` 取 `设计师[\\/]...md` 备注；`DEL_RE` 取最后 DEL- 编号。
- **xlsx 桥**：node 无 npm → `python openpyxl` 子进程（`PYTHONUTF8=1`）；`PY_READ` 读 sheet[0]，`PY_WRITE` delete_rows+重写（**保留第二 sheet「真机工具检验」**）；对象数组→二维数组转换（dict 会被 openpyxl 枚举键名——S2-7 实测捕获并修复）。
- **表=纯生成物**：首数据行写元信息 `# 生成: sync-orders.mjs ｜ 库→表单向投影 ｜ 禁止手工编辑 ｜ 源: 工单库.md`（A 列 `#` 开头），`--check` 跳过该行。

## 二、验收判据核对（S2-4/S2-6/S2-7/S2-8/S2-9，每条证据）

| 项 | 标准 | 实测证据 |
|---|---|---|
| **S2-4** | 改状态只在库一处；`--sync` 后 `--check` diff=0 | ✅ 改 `工单库.md` UPG-50 标题一处（加「·S2-4测试」）→ `--sync --table 副本` → **副本 B 列自动变为新标题**（表跟随库）→ `--check` diff=0；还原库 → 再 `--sync` 副本 → diff=0（库还原干净） |
| **S2-6** | 手工造 1 处不一致 → `--check` 必报（diff 非空+精确位置） | ✅ 用 openpyxl 篡改副本 UPG-01 G 列（设计师合main）→ `手工篡改值X` → `--check --table 副本` 输出 `[UPG-01 · 值不一致] 列G(设计师(合main)) 表="手工篡改值X" → 库="✅**批4 已合 main @2026-08-30**…"`，**退出码 1（CHECK_DIFF）** |
| **S2-7** | `--sync` 连跑两遍结果一致（幂等）；表行数=库卡数 | ✅ 副本 `--sync` 两次输出均「库=57 卡 → 表 57 行」+ `--check` diff=0（对比 57 张卡）；**表行数 57 = 库卡数 57** |
| **S2-8** | 生成的表=纯生成物（git diff 无手写痕迹，以时间戳标识） | ✅ 副本 row2 = `# 生成: sync-orders.mjs ｜ 库→表单向投影 ｜ 禁止手工编辑 ｜ 源: 工单库.md` 元信息行（`--check` 跳过，全程无误报）；表由库全量投影，S2-6 手写值被 `--sync` 单向纠正回库推导值（表无自主内容）；第二 sheet「真机工具检验」保留 |
| **S2-9** | 审验.py/既往 SYS 物零回归 | ✅ `审验.py --verify-hash-self-test` → **PASS 2/2**（9fd39b6→REJECT missing / 2a13dcd→HASH_OK）；`审验.py --list` 主链路正常枚举证据工单 |

## 三、证据引用

- `设计师/派单/SYS-02_阶段三_派单_2026-09-02.md` —— 本单验收判据 S2-4/S2-6/S2-7/S2-8/S2-9 与红线来源
- `程序员/交付报告/DELIVERY_SYS02_阶段二_2026-09-02.md` —— 前阶段（验证自动化件）交付，本单 `sync-orders.mjs` 与其独立
- `审验员/_测试副本_工单表.xlsx` —— 副本验证产物（S2-4/S2-6/S2-7/S2-8 全部在副本上实测，**真实表零触碰**）

## 四、测试结果（副本验证全过程）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1 基线 | `sync-orders.mjs --check`（真实表） | 对比 40 张卡、**302 处 diff**（现表=手工历史累积 vs 库推导，含 17 张新卡 UPG-51~67 表缺行）——**迁移前基线，评审确认后 3B 首次 --sync** |
| 2 生成 | `--sync --table 副本` | 57 张卡（库=57→表 57 行）→ `--check` **diff=0**（对比 57 张卡） |
| 3 幂等 | `--sync --table 副本` ×2 | 两次均 diff=0；表行数=库卡数 57 |
| 4 校对 | 篡改副本 UPG-01 G 列 → `--check --table 副本` | **CHECK_DIFF**：`[UPG-01 · 值不一致] 列G(设计师(合main)) 表="手工篡改值X" → 库="✅…批4 已合 main…"`，退出码 1 |
| 5 纠正 | `--sync --table 副本` | 手写值被库单向纠正回 → diff=0（表=纯生成物） |
| 6 单源 | 改库 UPG-50 标题 → `--sync --table 副本` | 副本 B 列自动跟随新标题 → diff=0 |
| 7 还原 | 还原库 → `--sync --table 副本` | diff=0（库还原干净，git status 无残留） |
| 8 回归 | `审验.py --verify-hash-self-test` / `--list` | PASS 2/2 / 主链路正常 |

> 注：**全程未对真实 `工单表.xlsx` 执行 `--sync`**——红线「3A 只读先行、未评审通过禁止 --sync 覆盖表」。真实表迁移 = 评审确认后 3B 首次 `--sync`（脚本自动备份到 `_备份归档\`）。

## 五、hash 三重（交付绑定）

| code_commit_sha | artifact_sha | evidence_manifest_sha |
|---|---|---|
| `57a319e` | （待填） | （待填） |

**E2 hash 一致性预校验**（复用阶段一 `审验.py --verify-hash`）：本单交付在工单系统仓库、分支 `feat/sys02-ph3` 未合 main，属预期状态；验收通过、设计师合 main 后应复检 `HASH_OK`。

## 六、范围与红线遵守

- 只动 `Desktop\MOV\工单系统\审验员\`（sync-orders.mjs / _proto_extract.py）+ `程序员\交付报告\` + `README.md` §六，**未碰 0027-mov APP 源码**（0027-mov 零接触）✅
- **登记纪律**：状态登记写库（工单库.md 唯一权威），表=机器生成物，禁止手写 ✅
- **3A 只读**：`--check` 纯只读；**真实表未 `--sync`**（评审前），迁移演练仅在 `--table 副本` 上 ✅
- 脚本**不联网、无 secret**（纯本地 node + python openpyxl 子进程）✅
- **表=纯生成物**：元信息行时间戳标识，`--check` 跳过 ✅
- 未进 `工单表.xlsx` UPG 主线（SYS-xx 独立追踪，规则不变）✅
- SYS-01 六阶段物不破坏：审验.py 零回归（S2-9）✅

## 七、遗留 / 建议

- **真实表迁移待评审**：3A 基线 = 302 处 diff（存量手工表 vs 库推导）——按红线须评审确认后 3B 首次 `--sync` 全量重写（脚本自动备份）。建议设计师验收时确认：存量表信息是否全部已沉淀进工单库（若存在「只在表、不在库」的信息，应先补写库再迁移）。
- **状态行时间戳 ts 补全**：派单要求状态行含 `@YYYY-MM-DD`（E4 超期判定依赖）；当前 `--check` 对无内联优先级的 8 张卡（UPG-01/03/05/08/09/21/49/68）输出「优先级缺失」警告——机器不猜、人工裁决，符合派单红线 4。
- **角色词表随流转演进**：`ROLE_WORDS` 是启发式词表（merge>inspector>dev>designer）；新动作形态（如新的合流措辞）需同步扩展词表，否则对应状态列显示 `—`+警告。
- **Windows 显示注记**：脚本输出 UTF-8 中文，GBK 控制台直跑可能乱码；`PYTHONUTF8=1`/`chcp 65001` 下正常（同审验.py 注记）。本报告全部实测以 UTF-8 捕获。

## 八、登记说明

- 已登记：`README.md` §六追加 SYS-02 阶段三 ✅（见下）。
- 交付 git 分支：`feat/sys02-ph3`，实现 commit `57a319e` + 本报告及登记（同分支）。
