# 交付报告 · UPG-108

> 类型：阶段 1 demo（扫码配对 + 用户面代理端 + 只读投影） ｜ 日期：2026-09-06 ｜ 依据：`设计师/派单/UPG-108_官网用户面_手机即服务器_派单_2026-09-05.md` + 设计稿 v1.1 + `STD-UPG-108-v1`
> 治理归属：0027-mov（feat/upg108 @ d1d87aa1a10acef79693ac9394aa1f3157c17cc5，已 push origin） ｜ 状态：✅ 已完成（交付，待验收）
> 验收标准：**STD-UPG-108-v1**（content_sha256 `7c3ac2ddbbc6b185f8ffe49649aa8c5a4d7feb14c3fc6785974593d150907fed`）

## 交付绑定

- **delivery_id**: `DEL-UPG-108-20260906-001`
- **code_commit_sha**: `d1d87aa1a10acef79693ac9394aa1f3157c17cc5`（feat/upg108 头，父=2886821d=origin/main）
- **artifact_sha**: `2ea38b2f177efbd6024a039423286fb49ac991d5e93dc0efa9882a41eaeb5265`（app-debug.apk，56140642 字节，gradle :app:assembleDebug）
- **evidence_manifest_sha**: `8c2845811daaeed1721fa87e6e2b8c7da08564622bec93650aa25558917ba818`（`处理中心/delivery_UPG108_manifest.json`，deliver-gen.mjs 机制产出）
- **manifest 自检**：`审验.py --manifest delivery_UPG108_manifest.json` → **ok:True**（E-001~E-005 exists=True hash_matches=True）
- **verify-hash（E2 闸）**：`审验.py --verify-hash feat/upg108 d1d87aa1` → **HASH_REJECT <not-ancestor>**（未合分支交付时点常态——同 UPG-88/90~98/W-11 全部先例；hash 存在性已过 ① cat-file=commit；已 push origin feat/upg108；**设计师合 main 后复跑=HASH_OK 终态**）

## 一、本阶段交付（6 件）

| # | 交付 | 实现 |
|---|---|---|
| 1 | MiniHttpServer bindHost 参数化 | `app/src/main/kotlin/com/hermes/mov/mcp/MiniHttpServer.kt`——新增第三参数 bindHost（默认 `"127.0.0.1"`，**置于 handler 之前**使 trailing lambda 仍绑定 handler，McpServer:50 调用点零改动）；8389 本机 MCP 面语义不变（盘点锚①兑现）；+boundPort 只读属性（测试/诊断） |
| 2 | 用户面代理端 | `UserProxyService.kt`（FGS dataSync 类型，0.0.0.0:8841 仅配对会话存活期间启动）+ `UserProxyCore.kt`（纯函数核心：鉴权/只读路由/投影组装，DataSource 接口注入 JVM 可测）+ `UserProxyDataSource`（Service 侧独立构造 InfoVault/RoomStore/SemanticPool，零 Activity 引用） |
| 3 | 配对管理 | `UserPairManager.kt`——parsePairUrl fail-closed（只认 `https://mov-ai.cn/user/pair?ref=<hex16..64>`）+ 256-bit SecureRandom 会话 token（Base64URL 43 字符）+ localWifiIp（NetworkInterface site-local）+ claim/revoke（HttpURLConnection） |
| 4 | 信令端点（服务器零存储） | `market-web/account-service.js` 增 `/account/pair/create|claim|state|revoke`——内存 Map+TTL 惰性清理（pending 5min/claimed 10min）+revoke 即焚；**零 DB 零日志**（/pair/* 全程无 console 输出——U108-4）；create/claim 套用既有 W-11 IP 限流，state 轮询不占限流 |
| 5 | 官网出码页 + 投影视图 | `market-web/user.html`（create→QRCode→1.5s 轮询 state→claimed 后展示 `http://<ip>:8841/#t=<token>` 入口；零 localStorage/零缓存）；`app/src/main/assets/userproxy_page.html`（手机 8841 直接 serve 同源零 CORS；四卡片+断开→离线态清空数据+5s 轮询重连恢复；token 经 URL fragment 不进 HTTP 请求） |
| 6 | 配对 UI 链 + 快捷方式 | MainActivity：qr.scan 捕获链 pair URL 检测→`showPairDialog` 确认框（配对=显式用户动作红线 2）→confirmPair/disconnectPair；快捷方式「官网配对」（res/xml/shortcuts.xml+Manifest meta-data）extra 经 PrivacyGate/Login 逐跳透传（+5 行）→ 直接开相机扫码（确定性入口，零 AI 依赖） |

## 二、验收判据核对（STD U108-1~5 逐条）

| 项 | 标准 | 实测证据 | 状态 |
|---|---|---|---|
| U108-1 | 配对建立：官网出码→手机扫码→手机确认→sessionId+token | **信令侧线上全链 curl 5/5 闭环**（create→pending→claim→claimed 回读 sessionId/token/ip→revoke→expired，见报告「线上信令实证」节）；**App 侧透传链真机走通**：快捷方式 intent→PrivacyGate→Login→Main→startPairScan→系统相机打开（ActivityTaskManager 实证）→拍照落盘 filesDir/camera→TakePicture 回调回 App（照片 75KB 实物 `UPG108-evidence/emulator_virtualscene_photo_nocode.jpg`）；**扫码解码环节模拟器受限**（见「受限项申报」）→ 转验收员真机人工 | ⚠️ 信令侧过/App 侧 L1 过+真机转人工 |
| U108-2 | 投影四类正确+脱敏（凭证类不出现） | L1：四类端点 200+JSON 结构断言+「资产投影不携带 preview 值」「凭据只出 count 与平台名」11 测试锚全绿；真机端到端截图随 U108-1 同转人工 | ⚠️ L1 过/真机转人工 |
| U108-3 | 断开→离线态；重连→恢复 | 投影视图实现断开→离线态（数据区清空不缓存旧数据——红线 6）+5s 轮询重连恢复；disconnectPair 单方立即断（revoke+stopService+端口关闭=浏览器自然离线）；L1 层结构就位；真机演示转人工 | ⚠️ 实现+L1 过/真机转人工 |
| U108-4 | 服务器零存储 | 代码层：/pair/* 内存 Map+TTL+revoke 即焚（`account-pair.test.mjs` 全生命周期测试实证 revoke 后 state=expired）+零 DB 写（无任何 pair 相关 prepare/INSERT）+零日志（/pair/* 路由零 console 输出）；官网页零 localStorage；线上部署后 `POST https://mov-ai.cn/account/pair/create` 生产 200（`{"ok":true,"ref":"1e5f…","expiresIn":300}`） | ✅ 代码层+线上实证 |
| U108-5 | 鉴权：无 token→401；错 token→401；8389 仍仅回环 | L1：无会话 403/无 token 401/错 token 401/缺 Bearer 前缀 401/恒时比较 11 锚全绿；**真机双地址实证**：模拟器内 `nc 127.0.0.1 8389`→`HTTP/1.1 401 Unauthorized`（回环可达+MCP token 鉴权生效）+`nc 10.0.2.15 8389`→`Connection refused`（非回环不可达——bindHost 默认值未破坏，UPG108-evidence 两份探测文件） | ✅ L1+真机 8389 锚实证（8841 随配对建立后可验，转人工） |

## 三、证据引用

- `程序员/UPG108-evidence/emulator-8389-loopback.txt` —— 模拟器内回环探测：8389 HTTP 401（token 鉴权生效）
- `程序员/UPG108-evidence/emulator-8389-nonloopback.txt` —— 模拟器内非回环探测：Connection refused（8389 仅回环）
- `程序员/UPG108-evidence/node-account-pair-test.txt` —— 信令 node:test 4/4 全绿输出
- `程序员/UPG108-evidence/gradle-l1-targeted.txt` —— L1 定向测试 BUILD SUCCESSFUL
- `程序员/UPG108-evidence/emulator_virtualscene_photo_nocode.jpg` —— 快捷方式透传链真机拍照实物（链路走通实物证据；场景呈现无码见受限申报）

## 四、测试结果（XML 汇总）

| 文件 | tests | failures | errors |
|---|---|---|---|
| `TEST-com.mov.android.UserProxyCoreTest.xml` | 11 | 0 | 0 |
| `TEST-com.mov.android.MiniHttpServerBindHostTest.xml` | 3 | 0 | 0 |
| `TEST-com.mov.android.UserPairManagerTest.xml` | 4 | 0 | 0 |
| **合计** | **18** | **0** | **0** |

- 全量回归：`gradle :app:testDebugUnitTest` → **862/0/1 skipped**（BUILD SUCCESSFUL）
- 信令 node:test：4/4（account-pair.test.mjs，node:test+子进程形态对齐 container-mcp/server.test.mjs 先例）
- **线上信令实证**（生产闭环，2026-09-06）：`POST https://mov-ai.cn/account/pair/create` → `{"ok":true,"ref":"a6fa…","expiresIn":300}` → state=pending → claim → state=claimed（回读 sessionId/token/ip）→ revoke → state=expired（**全链 5 步 curl 实测**）；`https://mov-ai.cn/user.html` → HTTP 200
- **部署**：account-service.js 已部署 `/opt/market-account/`（备份 `account-service.js.pre-upg108`，systemctl restart mov-account 后 active+health ok）；user.html 部署 `/var/www/mov-ai-public/user.html`

## 四之二、变异亲杀（5 刀全红实录）

| 刀 | 锚 | 变异 | 结果 |
|---|---|---|---|
| M1 | MiniHttpServerBindHostTest`默认bindHost字面量锚_回环` | 默认 bindHost "127.0.0.1"→"0.0.0.0" | ✅ FAILED（字面量锚红）→还原 |
| M2 | UserProxyCoreTest`错token一律401` | authorize 删 `startsWith("Bearer ")` 判断 | ✅ FAILED（缺前缀放行）→还原 |
| M3 | UserProxyCoreTest`无会话一律403` | 403 分支改 401（行为变异） | ✅ FAILED（状态码漂移）→还原 |
| M4 | account-pair.test`输入校验 fail-closed` | claim 删 ref 正则校验 | ✅ not ok 4（pass 3 fail 1）→还原 |
| M5 | account-pair.test`claim 全生命周期` | claim 删 pending 态检查（防重放劫持失效） | ✅ not ok 3（二次 claim 不再 410）→还原 |

## 五、hash 三重（交付绑定）

| code_commit_sha | artifact_sha | evidence_manifest_sha |
|---|---|---|
| `d1d87aa1a10acef79693ac9394aa1f3157c17cc5` | `2ea38b2f177efbd6024a039423286fb49ac991d5e93dc0efa9882a41eaeb5265` | `8c2845811daaeed1721fa87e6e2b8c7da08564622bec93650aa25558917ba818` |

## 六、范围与红线遵守（逐条）

1. **用户内容零服务端存储** ✅：信令内存态+TTL+零日志；官网页零缓存；投影内容不经服务器（LAN 直连）
2. **配对=显式用户动作** ✅：确认框（AlertDialog）为唯一建立/解除通道；快捷方式点击本身为显式动作
3. **用户面鉴权强制** ✅：会话 token 恒时比较；无/错 401（L1+真机 8389 双地址实证）；`mcp_token` 永不参与本面（L1 形态锚）；8389 绑定回环不动（默认值+真机 refused 实证）
4. **只读** ✅：非 GET 一律 405；无写端点
5. **脱敏 InfoVault 白名单（凭证 NEVER）** ✅：cred 只出 count+平台名（L1 锚「凭据只出count与平台名_绝无凭据值字段」）；字段只出 label+filled（比 list() 的 preview 更严）；照片只出已存状态
6. **断开=离线态不缓存** ✅：投影视图断开清空数据区；端口关闭=自然离线

**Token/KV 申报（派单 §八）**：MCP 面无新工具 0/0 ✅（配对入口走 qr.scan 既有工具+快捷方式 UI，零工具面变化）；无新 SharedPreferences 键 ✅（会话纯内存态，未用 filesDir 临时文件）

**Code-LOC 申报（ponytail-bench 口径）**：+1136 / -6 / 净 +1130（其中测试文件 347 行——产品代码净约 +783）

## 六之二、施工期重大回归与自纠（强制节）

- **W-11 既有缺陷上线暴露并顺带修复**：部署新版 account-service 后 fail-loud 拒启（服务短暂 down 约 3 分钟）——根因=W-11 检查键名 `TENCENT_SECRET_ID/KEY` 与实际消费键名 `SMS_SECRET_ID/KEY` 不一致（env 完备仍拒启，服务器旧版无 fail-loud 故此前未暴露）；已回滚止血→修键名对齐→本地 4/4 绿→重部署 active+health ok；**影响面=mov-account 服务**；本 commit 内一并修复并在此申报
- 演示数据还原：模拟器测试会话消息（CleanVM what 房间）未清理（共享模拟器既有他人会话，未动）；线上信令测试 ref 已全部 revoke/expired 即焚——生产态已复核

## 六之三、架构适配申报（2 项，均不改数据流契约）

- **A-1 投影页托管手机侧**：设计稿 §八「官网 /user/ 页」的投影 UI 由手机 8841 直接 serve（`userproxy_page.html`），官网 user.html 承担出码+配对状态+入口链接。依据：https 公网页面 fetch http://<内网IP> 被 Chrome mixed content/PNA 硬阻断（无旁路），手机侧托管是 LAN 直连下浏览器唯一确定可行形态，且「内容直接从手机出」与手机即服务器哲学同构；断开=页面自身断链=离线态天然干净（U108-3）。二维码载荷 path `/user/pair` 为纯约定（App 只解析 ref，host 校验对 mov-ai.cn）
- **A-2 前台服务形态**：盘点已申报的「App 内 ForegroundService（dataSync 类型）」兑现设计稿「独立 Service 保活」意图；独立进程留阶段 2 评审项（不推翻派单文）

## 六之四、受限项申报（真机人工验收项）

- **模拟器扫码解码环节**：快捷方式透传链已真机走通至「拍照→回调」（照片实物在案），但 emulated 虚拟相机的 3D 场景对墙上二维码的呈现（贴图距离/角度/光照）导致照片无可解码码（宿主 jsQR 独立复核 NO-QR 双证）——**模拟器物理呈现限制，非代码缺陷**；二维码生成/信令/解析纯函数/确认框/claim 均各自实证。**真机（真实摄像头对屏幕）U108-1 端到端转验收员人工**（真实场景下人手拍照无此限制）
- 8841 端点的真机 curl 401/200（U108-5 前两分句）随配对建立后可验——同转人工
- 衣柜投影=诚实空态（盘点锚⑦：本机无独立衣柜数据类目，不造假）

## 七、登记说明

- 已登记工单库（set-status 交付态）+ 工单表经 sync 投影；报告落 `程序员/交付报告/DELIVERY_UPG108_2026-09-06.md`
- 挂账：无新增（W-11 键名缺陷已随本单修复闭环）
