# DELIVERY_UPG01_批1 — 登记层收口 + AI 面接线交付（2026-08-29）

> 程序员 C ｜ 分支 `feat/upg01` ｜ 提交 `067aa8e`（基底 main 8af7da9，已 push origin；回滚 = revert 单 commit）｜ 方案 v2.3（评审#5 收口）
> 已登记两个表：工单表.xlsx UPG-01 行（程序员 ✅C 批1 完成 + 备注 hash）+ 工单库.md 状态同步（先表后库）
> 认领登记：`批1 认领 @2026-08-29 15:13: C worktree=mov-upg01 branch=feat/upg01`（工单表备注，在案）

---

## 一、批 1 六件交付（对照派单文本 §一）

### 件② 三通道收敛（核心）

- **登记层统一投影**：companion 纯函数 `buildToolRegistry`——builtin 5 + scene 2（ToolDefinition 实物直充）+ provider 20（`allMeta()` 视图，**例外声明**：DeviceProvider/ObsidianProvider 已是单源实物，不重建 execute 空壳=平行结构，注释在案）+ browser 14（`BrowserMcpTools.TOOLS` 视图，键 = 在面客户端名 `mcp__browser__*`）。
- **单源投影**：`projectToolMeta(name, registry, patches)` 纯函数——description = 登记层优先 → 模板串回落（红线 6：回落即运行时降级路径，不另设开关）；parameters = 登记层优先（**登记层空 properties = 真无参，不落补丁**）→ 静态表补丁 → 空对象。
- **两态单源**：`rebuildAgentTools()` 与 MCP 宿主注册循环**均消费同一投影**（AI 面与 MCP 面描述/schema 永远一致）。
- **通道归并**：`sceneToolDescriptions`/`providerToolMeta` 不再是 AI 面取值通道（声明保留：登记投影数据源 + UPG-23 buildLocalOverview 总览消费，UI 契约不变，注释在案）；禁新建平行结构、零落盘（L6 维持）。

### 件③ paramSchema 真字段描述能力

- `paramSchemaDesc(props: Triple<name,type,desc>)`：字段级中文 description 版构造器（批 2/3 撰写入口；≤15 字/字段 + 「来自 xxx.kt:行」注释要求写在函数 KDoc）。占位版 `paramSchema` 保留给未清偿条目（批 4 日落）。

### 件④ 快赢接入

- builtin 5（file.read/file.write/date/echo/search）+ browser 14 真元数据接入取值链——此前「写了没接上面」两族全部生效；现有描述**只增不改**（红线 3）。

### 件⑤ MCP 面补传 inputSchema

- 宿主注册循环：description 从恒模板串改单源投影 + **补传 inputSchema**（`ToolSpec` 方法签名不可变，只补传参——架构不变量 ✓）。
- `mountExtTools` 外部路径维持原样（外部工具元数据缺位 = McpExtDiscovery 只取 name，批 4 范围）。

### 件⑥ 冲突检测 + 回落清单

- `metaConflicts(registry, patches)`：静态表与登记层同名且 schema 异值 → 冲突（测试期必红）。**首用例**：静态表 builtin 4 条（字段占位）vs BuiltinMcpTools 真字段 → 4 条全判冲突（ToolMetaTest 复现）→ **清偿**（删静态表 4 条 + SceneTools append，登记层接管）→ 生产实态零冲突。
- `fallbackTools(names, registry, patches)`：回落清单纯函数 + `rebuildAgentTools` 每次重建 Log.i 输出（「工具面模式: …（agent 工具面 N 工具；登记层覆盖 X，模板串回落 Y）」+ 回落名单行）——**登记债运行时可见**，批 3 逐批清偿可对账。

### 件① 开工实测（缺口清单，归批 3 首批清偿）

| 族 | 工具 | description | 字段级 inputSchema | output 声明 | 缺口 |
|---|---|---|---|---|---|
| scene 2 | scene.trainQuery / scene.stationLookup | ✓ 真 | ✓ 字段级真描述（date/fromStation/toStation/keyword） | ✗ | **output 缺**（ToolOutputDefinition.schema=null，仅 render 文本投影） |
| device 13 | device.network/storage/toast/appList/appLaunch、sensor.list、camera.capture/ocrCapture、qr.scan、calendar.list/add、device.timer | ✓ 真（≤60 字） | ✓ 字段级真描述（PARAM_DEFS） | ✗ | **output 缺**（Pair 二件套形态无 output 件）；8 个真无参空 properties 为合法形态 |
| obsidian 7 | obsidian.vault.detect/register/check/rescan、file.read/write/search | ✓ 真 | ✓ 字段级真描述 | ✗ | **output 缺** |

**结论**：22 已登记工具三件套缺二（description+inputSchema 已齐）；**output 声明 22/22 全缺**——批 3 首批清偿对象（清偿形态：Provider 增 output 顶层返回键声明，或迁 ToolDefinition 时补）。

## 二、验证（批 1 验收口径对照）

| 口径 | 结果 |
|---|---|
| L1 ToolMetaTest（测投影不测文件） | **7 用例全绿**：41 工具接线断言（desc ≠ 模板串 + 无「占位/待补充」黑名单）/ 投影三态语义 / fallbackTools 回落三态 / 冲突首用例复现（4 条）+ 零冲突态 / 清偿状态（静态表无 builtin 残留 + 无 SceneTools append，防双源回潮）/ browser 14 在面名与元数据一致 |
| SceneWiringContractTest（UPG-03 旧锚） | **随收敛升级**：append 存在断言 → 反向断言（append 回潮 = 双源必红）+ toolRegistry 含 SceneTools 锚；取值链锚 → projectToolMeta 单源投影锚（意图不变：AI 面见真描述） |
| 变异亲杀两条 | ①登记层描述改占位 → 红 ✓ ②接线改回模板串 → 红 ✓（脚本化实跑 rc 直采，还原后绿） |
| L1 全量 | **51 类 369 用例 / 0 失败 / 0 错误 + 1 跳过**（rm -rf test-results 强制重跑 XML 逐件统计；feat/upg01 树口径 = main 8af7da9 全量 + ToolMetaTest 7） |
| assembleDebug | BUILD SUCCESSFUL（app-debug.apk 55346021B @15:27） |
| Token 脚本 | `node scripts/check-token-effect.mjs` **通过** |
| 计数断言 | 不变量式（登记层覆盖集内不存在投影回落 + 清偿状态反向断言），未写绝对数；全量覆盖不变量（「不存在已注册、非 uiOnly、无登记的在面工具」）**随批 3 收口启用**——批 1 时 ≈58 工具既无登记层又无静态表补丁，属批 3 交付物（评审#5 棘轮时序口径：未来批次交付物不入当前批断言） |

## 三、Token / KV 申报（AGENTS.md 硬规则 1）

- **AI 面（prompt 增量）**：19 工具（builtin 5 + browser 14）description 从模板串（≈12 字）→ 真描述（≈20-30 字）≈ **+250 字 ≈ +170 token/轮**（中文 0.67 token/字）；builtin 4 条字段描述占位→真（每字段 +2~6 字）；合计 AI 面 ≈ **+0.2-0.3KB/轮**——远低于方案 §五全量推导（52-72KB/轮为批 3 全量后量级），无需用户拍板。
- **MCP 面**：inputSchema 首次传入 → `tools/list` 响应体增 ≈ 145 工具 × ≈200B ≈ **+29KB/次**（MCP 客户端拉取时一次性，不入 LLM prompt）。
- **KV Cache**：描述会话装配时定、会话中不变；rebuild 触发点零新增（L4 纯文本补全不动工具集合）→ **请求前缀恒定维持**，本单未新增中途变动点（红线 5 ✓）。
- 实测：check-token-effect 通过；L3 选参正确率对比（UPG-27 L3）将验证「精度稀释预警」。

## 四、红线复核（派单 §九条）

1. 不改工具行为/调用签名 ✓（仅元数据投影层）；McpServer 签名不可变 ✓（只补传参）
2. 描述真实 ✓（接入的 19 条均为既有已验收真描述，零编造零新写）；「待核实」0 个 ✓（≤8 限额未占用）
3. 现有已验收描述只增不改 ✓（BuiltinMcpTools/BrowserMcpTools/SceneTools/Provider 元数据零改动）
4. 登记层与接线同批 ✓（收敛+接入+投影同 commit）
5. 请求前缀恒定 ✓（零新增变动点）
6. 回滚 = revert 单 commit ✓（067aa8e 单提交）
7. 禁平行结构/禁落盘 ✓（ToolSchemaLike 视图聚合，唯一事实源 = 各族实物；L6 零持久化维持）
8. 回滚单 commit ✓
9. **MainActivity.kt 纯 CRLF** ✓（施工中曾混入 LF，提交前 unix2dos 修复，file 验证纯 CRLF；块注释无嵌套 ✓；Edit 前已 Read ✓）

## 五、串行约束执行

- UPG-22 并行在施（同动 MainActivity）：本单独立 worktree mov-upg01，改动区 = 元数据区（:241 区/:3558 区/:6153 区 + companion）+ 静态表，**避开 :3555 记忆装配区与 McpToolScheduler 名单区**；先完成者先合、后到 rebase（规则 9）。

## 六、交付物

| 项 | 值 |
|---|---|
| 分支 / hash | `feat/upg01` / `067aa8e`（push origin ✓，新分支） |
| 改动面 | MainActivity.kt（+134/-25 区）+ SceneWiringContractTest.kt（锚升级）+ ToolMetaTest.kt（新增 7 用例）；3 文件 +310/-25 |
| APK | `app/build/outputs/apk/debug/app-debug.apk`（55346021B @15:27） |
| 报告 | 本文件 `程序员\交付报告\DELIVERY_UPG01_批1_2026-08-29.md` |
| L2 抽样辅助 | 验收员：AI 面投影 logcat 看 `MOV-Boot` 行（登记层覆盖/回落清单）；MCP `tools/list` curl 实证 inputSchema 非空 |
