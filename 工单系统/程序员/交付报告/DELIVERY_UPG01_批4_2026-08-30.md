# DELIVERY UPG-01 批 4（MCP 面收口 + 外部发现路径）

> 程序员 C ｜ 2026-08-30 ｜ 分支 `feat/upg01-b4`（worktree `mov-upg01-b4`，基底 `origin/main 801b8fc`）
> 派单：`设计师\派单\UPG-01_批4_派单_2026-08-30.md` ｜ 方案：`设计师\方案设计\UPG-01_批3批4_方案设计_v1_2026-08-29.md` v1.1
> **已登记两个表**（工单表.xlsx + 工单库.md，先表后库）

---

## 一、交付概要（分 commit，可独立 revert）

| 段 | commit | 内容 | 状态 |
|---|---|---|---|
| 前段 | `8cf7a2f` | 外部发现补传元数据（McpExtDiscovery Result.meta + metaOf 结构校验）+ 死码删除（McpToolProvider.kt 文件 + McpServer.registerMovQueryTools）+ 敏感名单镜像四处→三处 + 外部发现挂载点改三态投影 | ✅ 已 push origin |
| 后段 | `5f5219c` | mountExtTools 三态回落（meta 参数 + extToolMeta）+ 静态表日落（toolParamSchemas → emptyMap，占位版 paramSchema/paramSchemaDesc 死函数删除） | ✅ 已 push origin |

派单项对照：①McpExtDiscovery 补传元数据 ✅（前段）｜②外部 inputSchema 结构校验 ✅（前段 metaOf，评审 P3-4）｜③死码删除 ✅（前段）｜④黑名单镜像注释同步 ✅（前段，四处→三处）｜⑤mountExtTools 三态回落 ✅（后段，评审 P2-1 严格版 projectToolMetaOrNull + extToolMeta）｜⑥静态表日落 ✅（后段，批 3 收口后验收条件满足：654c88f 合批已落 main）。

## 二、施工明细

### 前段（McpExtDiscovery.kt / MainActivity.kt / McpServer.kt / 删除 McpToolProvider.kt / McpToolScheduler.kt / DeviceObsidianContractTest.kt / ToolMetaTest.kt）

1. **Result 扩元数据**：`meta: Map<String, Pair<String, Map<String, Any?>>>`（外部名 → (description, inputSchema)），默认 `emptyMap()` 向后兼容；`discover()` 遍历 `t` 时 `metaOf(t)?.let { meta[extKey] = it }`。
2. **metaOf 结构校验（评审 P3-4）**：`inputSchema` 必须为 map 且 `type=="object"` 且 `properties` 为 map 才透传（required 原样透传）；非法/缺失 → 整条返回 null（该工具整体回落登记层/模板串，**不原样透传进 AI 面**）。
3. **死码删除**：
   - `tools/McpToolProvider.kt`（零实例化，仅注释引用）→ 文件删除；
   - `McpServer.kt:233-261 registerMovQueryTools`（零调用；mov.roomList/journalTail 已在宿主 118 注册）→ 方法删除；
   - 敏感黑名单镜像注释：四处→三处（McpMarket/McpExtDiscovery/McpToolScheduler），DeviceObsidianContractTest 同步「四处」→「三处」断言。
4. **外部发现挂载点**（onCreate scope.launch 内）：由写死模板串改 `extToolMeta(name, ext.meta, toolRegistry, toolParamSchemas)` 三态投影。

### 后段（MainActivity.kt / ToolMetaTest.kt）

5. **projectToolMetaOrNull（严格版单源投影）**：仅登记层/补丁**真命中**才非空（不落内部回落串）——三态语义前提，与 projectToolMeta（内部自带回落）语义分家。
6. **extToolMeta（三态回落纯函数）**：外部自带元数据（tools/list 真描述）→ 登记层真命中 → 外部模板串「外部 MCP 工具: $name」；宿主面 rebuildAgentTools 仍走非严格 projectToolMeta（回落「MOV 工具:」），**两套回落不混**（评审 P2-1）。
7. **mountExtTools 升级**：加 `meta` 参数（默认 `emptyMap()`，market install/enable 调用点 :2462/:2479 零改动）；逐工具 `extToolMeta(name, meta, toolRegistry, toolParamSchemas)` → `ToolSpec(desc, inputSchema)`。**browser 热挂载顺带清偿**：browser 14 键 `mcp__browser__*` 已在 toolRegistry，mountExtTools 改投影后自动吃登记层真描述（原模板串）。
8. **静态表日落**：`toolParamSchemas` 原 67 条占位条目 → `emptyMap()`。日落安全前置核对：**静态表 67 键 ⊆ hostToolMeta 118 键（差集为空，脚本提取验证）**；唯一消费点 = projectToolMeta patches（:3618/:6245）+ fallbackTools（:6255），清空后无隐藏消费（buildLocalOverview 只用 providerToolMeta+sceneToolDescriptions，T1 解除）；`metaConflicts` 零冲突（补丁位日落）。占位版 `paramSchema`/`paramSchemaDesc` 死函数同步删除；BrowserMcpTools.kt 注释同步。

## 三、L1 验证（证据）

- **定向**：`gradlew :app:testDebugUnitTest --tests com.mov.android.ToolMetaTest` —— 21 用例全绿（原 16 + 批 4 新增 5）：
  - `批4 外部工具元数据提取`：合法条目携带 desc+schema（required 透传）；desc 缺失/inputSchema 缺失/type!=object/properties 非 map 四类非法 → null（回落）
  - `批4 三态回落`：外部元数据 → 登记层真命中（memory.save = hostToolMeta 真描述）→ 外部模板串；browser 键热挂载 = 登记层真描述；projectToolMetaOrNull 未登记 = null
  - `批4 死码删除`：McpToolProvider.kt 文件不存在 + 全量主源码 grep 零命中（含注释）
  - `批4 外部发现挂载点接线锚`：extToolMeta 调用锚 + 无模板串直传
  - `批4-2 热挂载接线锚`：mountExtTools 走三态投影 + toolParamSchemas=emptyMap 源码锚 + 死函数删除断言
- **全量**：`gradlew :app:testDebugUnitTest` —— **55 类 408 用例 0 失败**（--rerun-tasks 语义：本次为增量编译后全新执行，assembleDebug 同时确认编译链完整）
- **变异亲杀 3 条**（先 commit 后变异，checkout 恢复）：
  1. 外部发现挂载点改回写死模板串 → `外部发现挂载点未使用 extToolMeta 三态投影` 必红 ✅亲杀
  2. metaOf 去掉 `type!=object` 校验 → `type 非 object 应回落` 必红 ✅亲杀
  3. mountExtTools 改回写死模板串 → `mountExtTools 未走 extToolMeta 三态投影` 必红 ✅亲杀
- **死码 grep 零命中**：`McpToolProvider`/`registerMovQueryTools` 主源码 0 命中（检查后修复了注释残留，保证验收口径）

## 四、组装验证

- `gradlew :app:assembleDebug`：**BUILD SUCCESSFUL**（前段 57s 绿 / 后段 19s 绿）
- `node scripts/check-token-effect.mjs 801b8fc`：**通过（exit=0）**——前段/后段提交均含 Token/KV 申报
- 红线：MainActivity.kt 纯 CRLF（6358 CRLF / 0 bare LF，python 字节级校验）；Kotlin 块注释无嵌套 `/* */`；Edit 前均 Read

## 五、死码删除清单

| 死码 | 位置 | 证据 | 处置 |
|---|---|---|---|
| McpToolProvider | tools/McpToolProvider.kt | 零实例化（grep 仅注释引用） | 文件删除 |
| registerMovQueryTools | McpServer.kt:233-261 | 零调用（grep 仅定义处）；mov.roomList/journalTail 已宿主注册 | 方法删除 |
| paramSchema / paramSchemaDesc | MainActivity.kt | 唯一消费 = 静态表（已日落清空） | 死函数删除 |

## 六、静态表日落核对（批 3 收口后验收项）

- 前置：批 3 全收口 ✅（四子单合批 654c88f 落 main，hostToolMeta=B1+B2+B3+B4 118 工具）
- 对照：静态表原 67 键 vs hostToolMeta 118 键 → **差集为空**（node 脚本提取对账，重复键 0）
- 结局：`toolParamSchemas = emptyMap()`（显式例外清单：无）；`metaConflicts(registryFull, emptyMap())` 零冲突（批 3 收口测试已断言）
- 消费核点（T1）：buildLocalOverview 用 providerToolMeta+sceneToolDescriptions（实读代码确认），无静态表隐藏消费

## 七、Token / KV 申报（AGENTS.md 硬规则 1）

- **Token 影响**：外部/热挂载工具面描述由模板串换外部/登记层真描述——新增外部工具入面时才增加其自带描述+schema 的 token（量级 ≈ 外部工具数 ×（描述+schema））；宿主侧增量已计入批 3 收口总账（+8~11K token/轮上界，L2 真机快照补验在册）；未命中工具维持既有回落，无额外体积。
- **KV Cache 影响**：请求前缀恒定（无新增会话中途变动点，红线 5 维持）；mountExtTools/外部发现热挂载 rebuild 为既有变动点（rebuild 本身不变，本单未扩大）；`node scripts/check-token-effect.mjs` 全过。

## 八、L2/L3（留给验收员，派单口径复述）

- L2 真机：配置外部 MCP 服务器 → `tools/list` curl 实证 desc 真描述 + inputSchema 非空；browser 安装后 agent 面 desc 真描述（截图/journal 入 ACCEPTANCE_LOG.md）；移动端可用 emulator-5556：外部 mcp_servers.json 引导（filesDir/mcp_servers.json）+ 市场浏览器自动化包装/卸后 AI 面观察。
- L3：AI 对外部工具正确选参（journal）。
- 说明：批 4 是纯登记层 + MCP 面收口（方案 §七 L1 豁免——无 UI 展示面）；L2/L3 证据由验收员执行落档。

## 九、挂账/待核实

- 挂账 0 条；「待核实」0 条（T1 已实读解除；T2/T3 属批 3 无关本单）。
- 遗留观察项（非本单阻塞）：L2 真机快照 Token 补验（批 3 总账在册）；main `origin/main` 后续移动时本分支已有 rebase 路径（B3-2 先合序已过）。
