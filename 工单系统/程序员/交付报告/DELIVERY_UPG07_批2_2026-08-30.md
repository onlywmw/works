# 交付报告 · UPG-07 批2（P1）审批简化——「允许本目标」goal 级豁免

- **交付人**：程序员 C
- **分支 / hash**：`feat/upg07-b2` **431b3cb**（基底 main 3263f10，已 push origin；ls-remote 同 hash 实证）
- **日期**：2026-08-30 02:1x
- **范围**：前置①（挂账-GoalDomain无日志恢复 修复）+ 主体（goal 级豁免）✅ 完成；前置②（弹窗基线采集实测受阻，脚本+任务定义已交付，详见「五」）

---

## 一、前置①：GoalDomain.restoreFrom（挂账-GoalDomain无日志恢复）

- `Goal.kt`：`GoalDomain.restoreFrom(session)` —— 全量重放 `goal/change`（复用 MemoryEvent 承载，payload["goal"] 纯 map，与 GoalChangeEventTest 往返契约一致）；按 id 后者覆盖=最后状态；终态（COMPLETED/ABANDONED）也入 map 由 `activeGoals()` 状态过滤；已有新目标不覆盖（新目标优先）
- 接线：`MainActivity` 会话恢复点（`renderHistory(s)` 后）——重启/恢复会话后活跃目标重建，goal 级豁免有锚点
- 语义核验：`set g1→complete g1` 恢复后无活跃目标 ✓；`set g1→complete g1→set g2` 恢复后 g2 活跃 ✓；ARMED（轮次上限）入 map、activeGoals 含 ARMED 但**豁免 provider 只取 ACTIVE**（批2 回收口径），轮次驱动不受影响

## 二、主体：goal 级豁免

- `ApprovalService`（不另起平行体系，接线现有服务）：
  - `Answer.ALLOW_GOAL`（B 方案三键扩四键）+ `OUTCOME_ALLOWED_GOAL="allowed-goal"`
  - `goalAllowSet`（goal id → 工具名集合）仿 `turnAllowSet`：纯内存（重启清空=安全方向）+ 上限 100 整体清空
  - `goalIdProvider`（宿主装配 = `goalDomain.activeGoals().firstOrNull { status == ACTIVE }?.id`）——**仅 ACTIVE 可豁免**：complete→provider 不返回（回收✓）、ARMED（轮次上限暂停）→不返回（回收✓ 符合卡面「complete/ARMED 即回收」）
  - request() 注入顺序：allowThisTurn → **allowThisGoal** → 弹窗；豁免命中审计照落（asked reason="本目标已允许(goal=id)" + decided outcome=allowed-goal）
  - ALLOW_GOAL 防御支：无活跃 goal 却选→ `OUTCOME_REJECTED`（fail-closed）+ Log.w（UI 已防——无 ACTIVE 目标无此键，理论不可达）
- 弹窗 UI：`MainActivity` answerer 改 `setItems`——有 ACTIVE 目标四键（允许本轮/允许本目标/允许本次/拒绝），无则三键（顺序/语义不变）；通知栏路径不动（两键=本次/拒绝）
- **Log.i 观测点**（验收员批1建议+批2 L3 埋点）：turn 豁免命中 / goal 豁免命中 / 弹窗决策（once/turn/goal/reject）/ 无 goal 防御拒绝——弹窗次数可 logcat 直接数

## 三、红线遵守
| 红线 | 落实 |
|---|---|
| 批准不永久 | goal 失效（complete/ARMED）即回收 + 重启清空（纯内存） |
| 新工具必弹 | goalAllowSet 按工具名（同 turnAllowSet 粒度）；未放行工具仍弹 |
| 接线现有体系 | 只改 ApprovalService/GoalDomain/MainActivity，无平行 ApprovalService |
| 不改 tools 字段/system prompt | 仅 Answer 枚举/审计/UI/观测点 |
| plan 不自己拦工具 | 未触碰 plan/Plan.kt 与调度分支 |

## 四、验证
- **L1 全量**：**391/0/0**（+1 skipped SceneLiveQueryTest 环境跳过）+ assembleDebug 绿（55.36MB @02:1x）
- **新增测试**：GoalChangeEventTest +3（restoreFrom 重建/终态跳过/多目标/不覆盖——4 项实际）+ ApprovalServiceGoalTest 5 项（goal 内放行不弹窗+审计/失效回收重弹/新工具仍弹/重启清空/无 goal 防御拒绝）
- **变异 2/2 亲杀**（node 精确编辑验证，perl 模式误漏已修正）：
  - M1 删 goal 豁免注入块 → `goal 内放行不弹窗且审计落 allowed-goal` 必红 ✓
  - M2 豁免忽略工具名（goal 命中全放行）→ `新工具仍弹` 必红 ✓
  - 还原后终验绿
- 定向测试 2 类 **BUILD SUCCESSFUL**（12 用例全过；含 suspend answerer 契约修正）

## 五、前置②弹窗基线采集（受阻，如实申报）
- **任务定义已定稿**（3 标准混合任务，各 5+ 实质级工具）：A 环境侦察（shell.exec×6）/ B 网络采集（http.get×2+shell.exec×3）/ C 混合运维（shell.exec×2+vault.search+http.post+shell.exec）
- **脚本已入库**：`scripts/approval-baseline-collect.sh`（MCP 面 tools/call 逐工具调用 + `ApprovalVis answerer:` logcat 计数 + uiautomator 弹窗 tap 应答 + 汇总落档 /tmp/approval_baseline_*.txt；计数口径与 journal approval/asked 一致）
- **实测受阻记录**：模拟器（emulator-5558）App 已装 main 基线版+token 已取（5efd8829-…）；adb reverse 失败连环——8389/8388 段「Address already in use」（**并发会话占用**，互踩实录）→ 19001 建表但不监听（5037 server 半死）→ kill-server 后模拟器 adbd 离线、真机 21770d7d 失连未恢复。**基础设施不可用，非代码原因**。基线数值**待恢复**：基础设施恢复后按脚本跑批1版（main 3263f10）→ 批2版（431b3cb）同任务对比。
- **验收员替代径**：任何可用设备复跑脚本即可（3 任务跑通约 3-5 分钟；弹窗 tap 需 App 前台+模拟器分辨率 1080×2400 或改 tap_allow 坐标）

## 六、挂账
- 弹窗基线实测值：未取得（基础设施）——挂账登记表新增条目（UPG-07 批2 基线数值待补，脚本+任务定义已交付）
- adb 基础设施异常（5037 server reverse 半死/离线）：已 kill-server 尝试恢复，未知影响——其他并行 agent 注意

## 七、后续
- 验收员：L1 复跑+变异 2/2 复验+（可选）基线脚本复跑；L3 对比=批1 vs 批2 同任务弹窗次数（基线脚本跑两遍）
- 合 main 后：完整 APPROVE_PLAN 是否必要→用基线数据说话（方案 §六.4）
