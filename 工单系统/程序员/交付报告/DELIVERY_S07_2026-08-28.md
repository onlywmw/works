# DELIVERY_S07_2026-08-28（程序员）

**工单**：S-07 MOV 公众官网一期（用户下载门户 + 创作者开发者中心）｜ **优先级**：P1 市场线 ｜ **分支**：`feat/s07-official-site`（4 commits，见下）｜ **基线**：main @ 2d23662（代码基线 0ab5d48）

**已登记两个表**（先表后库）：dims 市场表 S-07 行状态 + 工单库 S-07 卡状态/交接段。

---

## 一、交付物与提交链

| commit | 内容 |
|---|---|
| `cf303d4` | chore(dims) S-07 认领登记 |
| `033cee3` | 批 1：/home/ 用户门户（下载区构建变量化 + 真实能力三节 + 引流条 + ICP 页脚）+ verify.mjs |
| `55193ba` | 批 2：/dev/ 开发者中心六板块 + hello world 模板包（实测可跑）|
| `7992cbd` | 批 3：Caddy 两区映射（已部署）+ ACCEPTANCE_LOG L1/L2/L3 登记 |

**站点结构**（两线三区，落 mow.kim）：
- `https://mow.kim/home/` 用户门户：hero + APK 下载区（直链+二维码双形态，版本/日期/大小构建时从真实 APK 读）+ 产品能力三节（真实 App 截图）+ 创作者引流条 + ICP 页脚
- `https://mow.kim/dev/` 开发者中心：总览 / 快速开始 / 工具开发规范 / 工具包结构 / 上架流程 / 接口参考 / FAQ + hello-server.mjs 模板包
- 代码落点 `0027-mov/official-web/`；Caddy 增量随单提交 `docs/deploy/Caddyfile.mow.kim`

## 二、验收证据（详版见 docs/ACCEPTANCE_LOG.md 本单段）

### L1（verify.mjs，exit=0）
① 零自家域硬编码（正文线上 URL 用 location.origin 动态填充，域名单点=build-vars.json）② ICP 悬挂 ③ 版本信息构建变量化（含「槽位写死版本」绕过堵洞）④ 死链扫描 146 链接全通。
**变异亲杀 3/3**：塞域名红 / 槽塞 V9.9 双红 / 直写 47.3 MB 红。

### L2（真实浏览器双端，证据 `验收员\证据数据\S-07\`）
- /home/ 双端截图 + 线上 DOM 实测版本注入（`下载 Android 版（V1.0）`、47.3 MB、SHA-256 前缀）；
- 下载双形态：直链 APK HTTP 200、Content-Length=49581333（与源 APK **字节级一致**）+ Range 首 1MB 实下；二维码 png（构建产物）；
- /dev/ 六板块全通；**hello world 实操**：模板 `hello-server.mjs` 本地实测三连全通（200 / tools/list / tools/call → "Hello, MOV!"）；线上真实包 hello-mcp curl 实测同链（`hello_world` → "Hello world"）。

### L3（公网 + 平移演练）
- 公网：/home/ /dev/ /build-vars.json /quick-start.html /hello-server.mjs 全 200；APK 见上；回归：主域工作台 200、/tools/ 200、privacy.html 200（零触碰）。
- **平移演练**：`--domain mov-ai.cn` 重跑构建 → diff 仅 build-vars.json + 二维码两个产物，html/js/css **零改动**——S-04 平移 = 改域名参数重跑构建 + 拷贝 + Caddy 改配置。

## 三、文档纪律执行（对照源码清单）

- 用只读子代理对源码做了逐项核实（file:line 在案）：超时 5s/30s（McpClient.kt:14-15）、健康检查 200/405（:20-27）、保留黑名单 7 个（McpExtDiscovery.kt:31-34）、write 不挂出（McpMarket.kt:411-412 + enableWrite 默认 false :83）、ext 前缀规则（McpExtDiscovery.kt:90,99,113-114）、MARKET_* 六错误码（McpMarket.kt:34-39）、缓存 1h（:165-174）、提交四道卡+限流 3 次/分+队列 100（review-service.js:260-322,370-373）；
- 官方指南 `docs/MCP_TOOL_DEV_GUIDE.md` 与代码核对一致后作为 spec/publish/api-reference 成文底；
- **口径分歧定案（写进文档）**：id 校验按网页提交口径 `^[a-z0-9][a-z0-9-]*$`（前端更严）；网页提交仅覆盖 byo（declarative 走 app 内对话提交）；
- 拿不准的不写：hello-mcp 服务端源码不在仓库，文档只引用其线上真实应答（curl 实测），未编造其实现。

## 四、红线自查

- 只写真实已上线能力（多房间/多模型/工具调用/能力市场——与 App 实际功能对齐，未写微信登录等未上线项）；截图为模拟器真实运行截图（全新环境无隐私内容）；
- 不碰主域工作台//tools//account（Caddy 只新增两段 handle，回归 200 验证）；
- upload 修复归 S-06，本单文档只描述提交流程（以现网四道卡实况为准）；
- APK 从官方路径出（本站直链）；Token/KV 无影响；签名凭据未触碰（当前发布为 debug 签名包，release 签名管线未在本单范围）。

## 五、遗留/挂账建议

1. **APK 版本通道**：当前直链为 V1.0 debug 包（构建变量已实标来源）。release 签名构建（`品牌管理/签名/mov-release.jks` 已备）建议随后续发布单接入 signingConfig + 部署钩子，届时仅需重跑 `generate-vars.py`；
2. 产品图第二张（能力市场）如需配「AI 对话实测图」待登录态演示数据后再补（现两图真实无风险）；
3. HANDOFF.md market-submit 段存在 GBK 乱码（历史遗留，与本单无关），建议卫生批处理。
