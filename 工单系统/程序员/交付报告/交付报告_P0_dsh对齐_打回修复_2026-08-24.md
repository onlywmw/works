# P0 dsh 对齐——验收打回修复交付

- 分支：feat/p0-align-dsh（基于 main 20db295）
- 提交：`4930c24`（修复，含申报两节）+ 基线 `0f18de1`
- 状态：🔁 修复完成，待验收

## 打回四项 → 修复措施

| # | 打回理由 | 修复 | 证据 |
|---|---------|------|------|
| 1 | 硬规则 1：Token/KV Cache 申报缺位 | 提交信息带两节 + `docs/IMPACT_P0_FIX.md` | `node scripts/check-token-effect.mjs` 通过 ✅ |
| 2 | systemPrompt 动态拼接破坏 KV 缓存 | 目标段改**恒前缀**（固定文案，实时目标走 goal.status 工具查询） | IMPACT 文档「无新增失效源」 |
| 3 | session.reference 真空壳（readSession=emptyList） | readSession 从 **JSONL 日志权威源**复原事件；空日志抛 REFERENCE_NOT_FOUND；注入带 [user]/[assistant] 前缀 | 真机：ghost-xyz → `REFERENCE_NOT_FOUND` ✅；android-session → 非空注入（fakekey test/hello/again second）✅ |
| 4 | GoalRoundDriver 未接线（恒 0/20） | AgentLoop 加 `onAfterStep` 钩子 → step 成功后推进活跃目标（失败 step 不推进，语义合理）；轮次文案移出提示词（恒前缀） | 代码接线 + GoalRoundDriverTest（20 轮上限）；真机 5556 key 失效（401）无法验证成功路径——需有效 key 设备复验 |

## 建议两项

- resolver 去 lazy：每次调用现构（不再单次求值锁死）✅
- goal.status 无会话 fail-closed（与 set/complete 一致）✅——真机实证

## 验证汇总

- 全量单测：BUILD SUCCESSFUL（24 类 / 187 用例 / 0 失败，+1 NOT_FOUND 用例）
- 真机 5556：goal.set/status（fail-closed 一致）/reference NOT_FOUND + 非空注入
- 待复验：**轮次推进成功路径**（需有效 DeepSeek key 的设备；逻辑已被单测+接线覆盖）

## 说明

- 401 invalid key（****7890）为 5556 设备配置问题，非代码缺陷；step 失败不推进轮次属预期语义（只有成功迭代才计一轮）
