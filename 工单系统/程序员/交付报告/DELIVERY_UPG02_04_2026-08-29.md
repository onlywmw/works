# UPG-02+04 合并单交付：设备类工具包 + Obsidian 工具包（20 工具接线）

- **分支/hash**：`feat/upg02` `55f9730` **已 push origin**（a02033b M1 → merge upg07 批1 吸收 dbc4a3e → 55f9730 M2 收尾；基线 main 2648c83）
- **日期**：2026-08-29 02:55
- **登记**：工单表 E3/E5、工单库 UPG-02 主卡（UPG-04 并入卡同步销）——**已登记两个表** ✅
- **Token/KV 申报（硬规则 1）**：tools 字段新增 **20 个 schema**——量级估计：20×(描述+参数) ≈ 6-8K chars ≈ 2-3K token/轮（加权口径），会话开始装配定型（rebuildAgentTools 启动期一次定型，前缀恒定）✓；`scripts/check-token-effect.mjs` 实测输出见交付说明（若脚本缺席需补量级记录）——**申报待实测数字回填** →【实测回填 @2026-08-29 验收员】check-token-effect.mjs 实测 **exit=0 通过**（脚本只出通过/不通过，无数字）；遂按 5c113cb 源码两 Provider PARAM_DEFS 逐字节构造线上 wire JSON（OpenAiCompatAdapter 线格式 `{"type":"function","function":{name/description/parameters}}`，点号转下划线、stringifyObject 紧凑 JSON）实测：20 schema 合计 **4208 chars/轮**（CJK 481 字 + ASCII/结构 3727 chars），token 估算 **≈1.2-1.4K token/轮**（口径：CJK 0.6~1.0 token/字、ASCII 4 chars/token）——原估计口径（6-8K chars ≈2-3K token）偏高约 2 倍；对照基线 2648c83（UPG-02 前 main）两 Provider 均缺席=增量即全量，8a205d6（已合 main）与 5c113cb 同为 4208 chars 无漂移
- **L3**：emulator-5558（已配 key）待跑——如实申报

## 交付清单

**A. 设备类 13**（DeviceProvider.kt，class 持 Context）：device.network（零权限子集：在线/类型/计量）、device.storage（StatFs 内外存）、device.toast、device.appList、device.appLaunch、sensor.list、calendar.list/add（**系统日历权限**——无权限明确引导，manifest 已加 READ/WRITE_CALENDAR）、device.timer（Handler+filesDir/timers.json 落盘重建）、camera.capture（**复用既有 TakePicture/FileProvider 管道**+工具模式分流）、camera.ocrCapture（拍照→OcrEngine 复接）、qr.scan（拍照→ML Kit barcode 解码，**新增依赖 barcode-scanning:17.3.0**）、screen.capture（**MediaProjection 单帧 FGS**：ScreenCaptureService，API 34+ 类型合规）。

**B. Obsidian 7**（ObsidianProvider.kt，SAF 通路）：vault.detect（引导 OPEN_DOCUMENT_TREE，授权管道由 obsidianTreeLauncher 桥接）/vault.register（takePersistableUriPermission+prefs 单写点）/vault.check（persisted 校验——MIUI 回收兜底，失效引导重授权不静默）/vault.rescan（索引重建，**ASK**+UI 文案「正在更新笔记索引，不影响笔记内容」）/file.read/file.write（**ASK，不进 harmless**——goal 写任意义 vault 风险；描述声明知识沉淀边界 vs note.create）/file.search（内容扫描 ≤20 条）。沙盒：`sanitizeSegments` 拒 `..`/`.`/空段/content: 语义。

**接线四点（照 UPG-03 样板，一次装配）**：mcpHandlers 注册段 20 handler；**schema+描述随 Provider 走**（`providerToolMeta` lazy = SceneTools+DeviceProvider.allMeta+ObsidianProvider.allMeta 三方 merge，rebuildAgentTools 双消费）；权限名单（写类 12 进 writeTools/查询类 8 进 harmlessTools）；isHardwareTool 并入（device.*/sensor./camera./qr.scan/screen.capture）+ ToolRegistry 查询类并行声明 8 项。

**黑名单四处镜像对齐 + 等价变体**（复核注意②前置处置）：McpToolProvider/McpMarket/McpExtDiscovery/McpToolScheduler 统一补 `sms.recent`/`contacts.search`/`location.get`（Scheduler 保留 contains 子串语义——已声明）。

**命名避让（复核注意①）**：契约测试逐对断言新写类工具名**不包含**任何 harmless 现有成员子串（contains 反方向陷阱防线）。

## 复验

| 项 | 结果 |
|---|---|
| L1 全量 | `--rerun-tasks` **288/0/0**（DeviceObsidianContractTest 8 案：meta 完整 13+7/写类登记/harmless 登记/命名避让/敏感四处对齐+变体/沙盒穿越） |
| 变异 M1 | writeTools 删 camera.capture（模拟漏登记）→ 写类案红 ✓ |
| 变异 M2 | sanitizeSegments 删 `..` 过滤（穿越放行）→ 沙盒案红 ✓（还原经手，过滤项精确补回） |
| L2 装机 | **互踩转复核**：装机验证期间与 C 的 UPG-05 APK 互相覆盖（MemoryPageActivity 实录，安装方最后胜出）——sensor.list/storage 返真值、camera ASK、SAF 全链待复验 |
| L3 | emulator-5558（已配 key）AI 对话触发 sensor.list / obsidian.file.write——待跑（key 通道就绪） |
| Token | 20 schema 新增：**量级估计 ≈2-3K token/轮**（加权口径），会话开始装配定型论证成立；check-token-effect 实测数字回填待办 |

## 交接/遗留

1. L2 装机复验（不与在施方抢设备）：逐工具激活 journal 实锤。
2. L3（5558）：AI 对话「列出传感器」→ sensor.list tool_call；「把这条建议记到笔记」→ obsidian.file.write。
3. ~~Token 实测：check-token-effect.mjs 跑分回填报告。~~ **已回填（@2026-08-29 验收员）**：check-token-effect.mjs 实测 exit=0（无数字输出）；自行按 wire 线格式实测 20 schema = **4208 chars ≈1.2-1.4K token/轮**，方法与细账见上文 Token/KV 申报节。
4. 官网 12306 卡回补（UPG-03 遗留）与 obsidian 卡（接线后口径）——前端批统一。
