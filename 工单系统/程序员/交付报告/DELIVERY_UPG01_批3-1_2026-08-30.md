# DELIVERY_UPG01_批3-1_2026-08-30（程序员 C）

**工单**：UPG-01 批 3-1 · AI 认知核心（34 工具）——hostToolMetaB1 登记 + 第五族并入 + output 首清偿 22
**分支**：`feat/upg01-b3-1` @ **b748d63**（已 push origin；基底 main 31769a0；worktree=mov-upg01-b3-1）
**认领**：工单表 UPG-01 行 @2026-08-30 00:20 登记在案（先表后库流程遵守）

---

## 一、交付清单（派单 §一四件全交）

1. **hostToolMetaB1 登记（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB1.kt`——
   - `data class HostToolMetaEntry(description, schema, output)`（B2/B3/B4 复用同构）
   - `hostToolMetaB1: Map<String, HostToolMetaEntry>` 34 工具（派单附录 A 权威清单，**ToolMetaTest 清单对账锚**防漏防多）
   - desc ≤60 字（功能>限制>场景，近邻差异点：save↔load↔search、switch↔pin、use↔setDefault、delete↔setEnabled、roomList↔room.list、add↔update）；schema 字段级真描述 ≤15 字/字段；真无参 7 工具空 properties（handler 实现逐个核实不读参数）
   - 每条附「来自 xxx.kt:行」锚点（@31769a0），字段/返回键逐条查 handler 实证（MemoryMcpTools/MovQueryTools/AccountMe/ModelRegistry+ModelStore/goalDomain/SubagentRunner 等实现核对）
2. **MainActivity 聚合（本单唯一触碰点）**：companion `buildToolRegistry` 增 `host` 参数（复用既有 `metaView`，零新投影逻辑）+ 装配点 `host = hostToolMeta`；`hostToolMeta by lazy = hostToolMetaB1`（B3-4 收口并齐四表）；**`projectToolMeta` 契约零变化**；`fallbackTools`/`metaConflicts` 自动覆盖 host 族
3. **output 首清偿 22（批 1 欠账）**：
   - `DeviceProvider.ParamDef` 增 `output: String` 字段（13 条真值：network=ok,online,type,metered / toast=ok,shown / camera.capture=ok,path,bytes / ocrCapture=ok,text,tokens / qr.scan=ok,count,contents / screen.capture=ok,path,width,height 等，逐条对 handler 返回实证）+ `allOutput()`
   - `ObsidianProvider.OUTPUT_DECLS`（7 条：detect/register/check/rescan/read/write/search）+ `allOutput()`
   - `SceneTools.OUTPUT_DECLS`（2 条：scene 两工具 execute 返回**纯文本**——如实声明不编造键名）
   - 非空断言入 L1（`output 首清偿 22` 用例）
4. **journal 频次榜（先行件）**：`tools/journal_freq_top.mjs` 已跑 emulator-5558 实机数据（3 房间 session.jsonl，总行 198）——结果：计数窗口内仅 `calendar.list`×1（测试环境调用稀疏）。**按方案 §1.5 兜底**：子单内排序沿用派单附录 A 领域关键度顺序。产出物 = 本报告附录（对账：总行 198/解析 198/计数 1）
5. **output 试点**：本单 34 工具 output 全量登记即试点批（L1 断言非空非占位；顶层键正确性核对归人工抽查/L2——评审 P3-6 口径）

## 二、验收证据（L1）

- `ToolMetaTest` 7→**10 用例**全绿，新增三用例：
  - `批3-1 登记 34 工具清单对账且 desc-schema-output 三件套质检`（清单对账 + desc 20-60 字 + 黑名单 + 字段描述非参数名占位且 ≤15 字 + output 非空；**变异①锚**）
  - `批3-1 接线断言——34 工具全部进登记投影且从模板串回落清单消失`（fallbackTools 对账；**变异②锚**）
  - `output 首清偿 22——provider 20 加 scene 2 全部非空非占位`（13+7+2=22 计数对账）
- **变异亲杀两条（实证）**：①memory.save desc 改「占位」→ 质检用例 FAILED；②buildToolRegistry 删 host 并入循环（接线改回模板串）→ 接线断言 FAILED；均恢复后全绿
- **L1 全量**：`--rerun-tasks` 真跑 22 tasks 全 executed——**53 类 379 tests / 0 failed / 1 skipped**（12306 LiveQuery）BUILD SUCCESSFUL；`:app:assembleDebug` 绿（app-debug.apk 55MB）
- `check-token-effect.mjs b748d63` **exit=0**
- 断言过程纠偏记录：desc 字数断言亲杀出 9 条超 60 字/1 条不足 20 字（memory.search 77/mov.roomList 66/model.add 93 等）——全部精简重写后达标（断言非摆设实证）

## 三、登记债收敛对账（交付要求）

- 交付前：宿主 118 全部模板串回落（登记层 41 = builtin5+scene2+provider20+browser14）
- 交付后：**本单 34 从回落清单消失**（L1 断言 `fallback.isEmpty()`）→ 回落余 **84** 待 B3-2（26）/B3-3（34）/B3-4（24）逐批清偿，收口后回落=0
- MOV-Boot 日志「模板串回落 N」实机数留验收员 L2 抽样核对（L2 抽样 4 件口径不变）

## 四、Token/KV 两节申报（实测校准点）

- **Token 影响**：AI 面 +34 工具真描述（desc ≤60 字 + 字段级 schema ≤15 字/字段，均非顶格实测均值 ≈40 字/工具 desc）≈ 推导 **+2.5~3.2K token/轮**（方案 §四字口径 140 字/工具上界内；B3-1 即实测校准点——真机会话装配快照实测留 L2，若实测显著超推导触发常用集裁剪拍板）；MCP tools/list 同步增量（不入 prompt）；会话中途 tools 字段**零新增变动点**（红线 5：登记层随装配一次成形）
- **KV Cache 影响**：tools 段前缀一次性加长（首轮），第 2 轮起命中前缀缓存增量≈0；无新增中途变动点
- `check-token-effect.mjs b748d63` exit=0 实证申报齐全

## 五、待核实/挂账

**本单 0 条挂账**（34 工具参数与返回全部查实现实证，无「待核实」残留；同时挂账 ≤8 红线余量全空）。

注记（非挂账）：频次榜数据稀疏（实机仅 calendar.list×1）——Token 拍板的总账校准需待真实使用数据积累，B3-4 收口批建议复跑 `journal_freq_top.mjs`。

## 六、红线自查

九条全过：①工具行为/签名零改动（McpServer 面未触碰）②描述全真实（逐 handler 核实）③desc 字数与差异点达标（断言亲杀）④既有描述只增不改 ⑤登记层与接线同批 ⑥请求前缀恒定 ⑦零平行登记结构/零落盘（hostToolMeta 内存态与 provider 同构）⑧回滚=revert 单 commit ⑨MainActivity 纯 CRLF（node 全程字符串替换保形）

**已登记工单表.xlsx + 工单库.md（feat/upg01-b3-1 b748d63｜报告 DELIVERY_UPG01_批3-1_2026-08-30.md）**

---

### 附录：journal 频次榜产出物（emulator-5558 实测）

```
journal 工具调用频次 Top 15（近 7 天；文件 3 个）
     1  100.0%  calendar.list
对账：总行 198 / 解析 198 / 计数 1 / 超 7 天跳过 0 / 解析失败 0
（数据稀疏 → 子单内撰写优先级按派单附录 A 领域关键度兜底，方案 §1.5）
```
