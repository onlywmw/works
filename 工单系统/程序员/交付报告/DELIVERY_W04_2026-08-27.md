# W-04 交付报告：工作台登出入口

**程序员**：C ｜ **日期**：2026-08-27 ｜ **分支**：`feat/w04-logout` @ `f1db122`（amend 含语法修复）
**部署**：已上线 mow.kim 主域（`/var/www/workbench-web/app.js` 2026-08-27 更新；回滚=`git checkout HEAD~1 -- workbench-web/app.js && 构建重新 scp`，服务器无配置变更）
> **已登记**：dims 基建.md W-04 行「待验收」待本次同步写入（认领行已先登记）；Token/KV 无影响。

---

## 一、实现（单文件 app.js 两处）

1. **入口**：renderTree() 循环后（归档节点之下）追加 `#logout-row`——🚪 图标 + 「退出登录」，样式沿用侧栏树节点 token（sidebar-row/hover 色），黑白灰零新色系。
2. **行为**：`doLogout()` = `confirm('确定退出登录？')` → `localStorage.removeItem('mov_account_token')` → `location.reload()` → checkAuth 失败 → renderLogin 门禁页。
3. 抽屉容器（drawer）同步渲染该行（与侧栏双容器一致性沿既有模式）。

## 二、L1 验证

- verify-workbench.mjs 新增 §⑤ 契约四断言：入口行存在 / MovWb.logout 挂点 / removeItem('mov_account_token') 清除动作 / confirm 文案。全部通过。
- **变异亲杀**：
  - MUT-1 删 `removeItem('mov_account_token')` → 断言红 ✓
  - MUT-2 删整个 #logout-row 入口行 → 真实执行路径 ReferenceError、校验 exit=1 红 ✓
  - 还原后全绿 ✓

## 三、L2 分层申报

- **已完成**：生产域未登录门禁证据——headless Chrome 截图+DOM 断言（`W04_L2_prod_gate_no_token.png`）：https://mow.kim/ 无 token 渲染登录页（请登录/验证码登录/账号密码登录 DOM 在案），curl `/account/me` 无 token=401（门禁不被绕过）✓
- **降级项（已登记处理中心操作复核项）**：「已登录态点退出→confirm→token 清除→回登录页」全交互链需真实浏览器 localStorage 登录态，程序员侧无自动化驱动工具，登记为验收员三步 F12 复核项（步骤详见挂账登记表 W-04 条目）。
- 工作台只读原则未破坏（登出为唯一新增交互）；assets/pages 与 account-service 零触碰。

---

## 九、R1 打回修复（验收员 P1×2，commit `3a8617f` 已部署主域）

### P1-1 真实缺陷：logout 挂点错位（真因）
onclick="MovWb.logout()" 引用的是全局 **window.MovWb**（nav/openDrawer/closeDrawer 字面量），而我此前把 logout 只挂进了校验挂钩 **__MOVWb** → 真实点击 TypeError: MovWb.logout is not a function。
修复：window.MovWb 字面量补 `logout: doLogout`（app.js :894 字面量内）。__MOVWb 校验挂钩保持同步含 logout。

### P1-2 verify 假覆盖：字符串 includes 防不住挂点错位
原 §⑤ 四断言全是文本级 includes——三处文本（入口指 MovWb/挂点在 __MOVWb/removeItem）互相错位时全部仍命中=假绿。R1 重写为**真实执行断言**：
- sandbox 注入 confirm/location.reload 计数器 + 可删改的 localStorage mock；
- 断言 `typeof window.MovWb.logout === 'function'`；
- **模拟真实点击**：直接调用 `sandbox.MovWb.logout()`（confirm 返回 true）→ 断言 confirm 调用 1 次 / removeItem 1 次且 token 变 null / reload ≥1。

### R1 变异演练（对照打回判据②）
从 window.MovWb 字面量摘走 logout（还原 P1-1 错位态）→ verify 以 `TypeError: sandbox.MovWb.logout is not a function` 失败 exit=1 —— 与验收员在真实浏览器抓到的错误同形态；还原后 L1 全绿。

### hash 勘误（P3 备注）
交付报告首节的「f1db122」为 amend 前散列，实际仓库历史=45f3b24(初版) → 本轮 R1=**3a8617f（当前终态，已部署生产）**。两表已按 3a8617f 登记。

### R1 重跑判据对照（验收口径原文）
① MovWb.logout 是 function 且真实点击后 token 清除+回登录页 —— 前半由 verify 真实执行断言锁定（assert function + 三计数链）；F12 三步整体留验收员复跑。
② verify 变异「删 MovWb.logout」必红 —— MUT-R1 实测红 ✓ 还原绿 ✓
