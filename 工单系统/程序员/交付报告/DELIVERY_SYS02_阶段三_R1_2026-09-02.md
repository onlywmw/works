# 交付报告 · SYS-02 阶段三 R1（sync-orders 解析器增强——旧卡字段提取兜底）

> 类型：SYS 系统自改进（独立编号，不混入 UPG 主线）
> 日期：2026-09-02 ｜ 依据：`设计师\派单\SYS-02_阶段三_派单_2026-09-02.md`（R1 追加派单：问题/范围/验收 R1-1~R1-5）
> 治理归属：工单系统自身改造（**只动 `Desktop\MOV\工单系统`**，**零 0027-mov 代码**）
> 状态：✅ 已完成（交付，待验收）

---

## 一、本阶段交付（1 件核心增强 + 1 诊断脚本）

| # | 交付 | 实现 |
|---|---|---|
| 1 | **`sync-orders.mjs` R1 解析器增强**（追加 commit `f4d33a6`） | ① **优先级多形态锚** `extractPriority()`：`**优先级**：X` / `｜ 优先级：X` / `；优先级：X` / `（PX·排期` / `**级别**：PX 五种形态；缺陷级别（如「R1 复验 P1×3 全消」）不命中——**机器不猜**；② **角色锚改 `ROLE_ANCHORS` 正则对象**（merge/inspector/dev/designer 四角色动作锚），段头 40 字符主分类**优先整段提取**，无主角色段时 **`actionSubsegs()` 段内锚切片兜底**——旧卡长状态段多角色并存逐个提取（如 UPG-03 长 merge 段内嵌「验收员通过」独立成 F 列）；③ **段内多锚去重**（同 idx 取最长锚、同角色重叠锚跳过）；`pick()` 保留整段优先防碎片化；④ `statusRegion` 排除 blockquote（`>` 遗留跟进）与 `---` 分隔线（修 UPG-03 状态区污染）；I 列占位符归一（`—（…` → `—`） |
| 2 | `_diag_diff.mjs`（诊断脚本，非交付物） | R1 增强后全量 diff 分类器（A 表缺行 / B 库无值·表有值 / C 库有值·表空 / D 值不一致），用于生成剩余清单 |

**核心收益**：真实表 `--check` diff **302 → 267**（-35）；解析器不足类已全部消除，剩余 267 处全部可归因于**库登记缺失 / 表过时 / 表缺行**（详见 §二 R1-3）。

---

## 二、验收判据核对（R1-1 ~ R1-5，每条证据）

| 项 | 标准 | 实测证据 |
|---|---|---|
| **R1-1** | 8 张卡优先级全命中 | ✅ **UPG-68 命中**：状态区 `（P0·3~5 工作日`（排期形态锚）→ C 列 **P0**，警告集 8→7 卡。其余 7 张（UPG-01/03/05/08/09/21/49）**库状态区无优先级字段**——逐卡实证（见下），非解析器不足，按红线「提取失败→警告不猜（保留）」诚实警告。特别地 **UPG-08 的「P1×3」是缺陷级别**（「R1 复验 P1×3 全消」），非优先级，机器判正确不提取 |
| **R1-2** | UPG-08 抽样一致 | ✅ 逐条核对库文本：状态区唯一一行 `验收员✅全部通过 @2026-08-26（R1 复验 P1×3 全消 + R2 消 P3）→ 设计师✅已合 main @2026-08-26（aa09882...）`。提取结果：F=`验收员✅全部通过 @2026-08-26`、G=`**设计师✅已合 main @2026-08-26**`——与原文逐字一致。派单所述「程序员✅」经全卡检索确认**仅存在于派单交接段指令文本**（「本单状态改为 程序员✅完成，待验收」是派单说明非状态登记），状态区无程序员交付段 → 表 E=`✅R2 修复完成` 为历史手工值未沉淀进库（库信息缺失） |
| **R1-3** | diff<302 + 剩余清单诚实 | ✅ **diff=267 < 302**。剩余清单分类（`_diag_diff.mjs`）：**A 表缺行 17**（UPG-51~67，17 张新卡表未建行）｜ **B 库无值·表有值 79**（库登记缺失——7 卡优先级 + 历史手工表值[方案完成/C 完成/备注来源]从未沉淀进库状态区）｜ **C 库有值·表空 1**（UPG-43·G，sync 可补）｜ **D 值不一致 170**（表过时，库为最新态——如 UPG-49 E/F/G 库版为 09-01/09-02 最新、表版停在 08-30；标题/备注半角↔全角差异）。**无一条属解析器不足** |
| **R1-4** | 真表零 --sync | ✅ **真实 `工单表.xlsx` 从未执行 `--sync`**：A2=UPG-01（无 `# 生成:` 元信息行，保持手工存量态）；全程仅对 `_测试副本_工单表.xlsx` 操作。副本回归：`--sync --table 副本` ×2 幂等 **diff=0**；篡改副本 UPG-68 C 列 → `--check` 精确报 `[UPG-68 · 值不一致] 列C(优先级) 表="手工篡改值X" → 库="P0"`、**退出码 1（CHECK_DIFF）**；`--sync` 恢复 → diff=0、退出码 0 |
| **R1-5** | 审验.py 零回归 | ✅ `审验.py --verify-hash-self-test` → **PASS 2/2**（9fd39b6→REJECT missing / 2a13dcd→HASH_OK）；`审验.py --list` 主链路正常枚举证据工单 |

**8 卡优先级逐卡实证**（为什么 7 张只能警告）：
- **UPG-68** ✅ 命中 `（P0·3~5 工作日` → P0（排期锚 `（PX·`）
- UPG-01/03/05/09/21/49：状态区扫描全文**无任何优先级/级别标注**（表 P1/P0 为历史手工填值，库未登记）→ 诚实警告
- UPG-08：状态区 `P1×3` 为**缺陷级别**（R1 复验全消）非优先级 → 机器不猜，诚实警告

---

## 三、证据引用

- `设计师/派单/SYS-02_阶段三_派单_2026-09-02.md` —— 本单 R1 追加派单（问题/范围/验收/红线）
- `程序员/交付报告/DELIVERY_SYS02_阶段三_2026-09-02.md` —— 阶段三主交付（3A/3B 基座，本单在其上增强解析器）
- `审验员/_diag_diff.mjs` —— R1 增强后全量 diff 分类器（A/B/C/D 剩余清单）
- `审验员/_测试副本_工单表.xlsx` —— 副本验证产物（R1-4 全部在副本实测，真实表零触碰）

## 四、测试结果（R1 回归全过程）

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1 基线 | `sync-orders.mjs --check`（真实表） | **267 处 diff**（R1 增强后，自 302 降 35）；警告 8→7 卡（UPG-68 优先级命中） |
| 2 分类 | `_diag_diff.mjs` | A 表缺行 17 + B 库无值 79 + C 表空 1 + D 值不一致 170 = **267**（无解析器不足残留） |
| 3 抽样 | UPG-08 全卡检索 | F/G 提取与库文本逐字一致；「程序员✅」仅派单指令、P1=缺陷级 → 判定库信息缺失 |
| 4 副本幂等 | `--sync --table 副本` ×2 | 两次均 diff=0；表 57 行=库 57 卡 |
| 5 副本校对 | 篡改副本 UPG-68 C 列 → `--check` | CHECK_DIFF：`[UPG-68 · 值不一致] 列C(优先级) 表="手工篡改值X" → 库="P0"`，**退出码 1** |
| 6 副本恢复 | `--sync --table 副本` → `--check` | diff=0，退出码 0（表=纯生成物，库单向纠正） |
| 7 真表只读 | 检查 `工单表.xlsx` A2 | A2=UPG-01（无 `# 生成:` 元信息行）——**真实表零 --sync** ✅ |
| 8 回归 | `审验.py --verify-hash-self-test` / `--list` | PASS 2/2 / 主链路正常 |

> 注：R1 全程未对真实 `工单表.xlsx` 执行 `--sync`——红线「3A 只读先行、未评审通过禁止 --sync 覆盖表」。真实表迁移仍需评审确认后 3B 首次 `--sync`（脚本自动备份）。

## 五、hash 三重（交付绑定）

| code_commit_sha | artifact_sha | evidence_manifest_sha |
|---|---|---|
| `f4d33a6` | （待填） | （待填） |

**E2 hash 一致性预校验**（复用阶段一 `审验.py --verify-hash`）：本单交付在工单系统仓库、分支 `feat/sys02-ph3` 未合 main，属预期状态；验收通过、设计师合 main 后应复检 `HASH_OK`。

## 六、范围与红线遵守

- 只动 `Desktop\MOV\工单系统\审验员\`（sync-orders.mjs 增强）+ `程序员\交付报告\` + `README.md` §六，**未碰 0027-mov APP 源码**（0027-mov 零接触）✅
- **登记纪律**：状态登记写库（工单库.md 唯一权威），表=机器生成物，禁止手写 ✅
- **不造值**：优先级只认明确标注（`优先级/级别` + 排期锚）；缺陷级别（P1×3）与库状态区缺失字段一律诚实警告，机器不猜 ✅
- **3A 只读**：`--check` 纯只读；**真实表未 `--sync`**（评审前），副本验证仅在 `--table 副本` 上 ✅
- 脚本**不联网、无 secret**（纯本地 node + python openpyxl 子进程）✅
- **表=纯生成物**：元信息行时间戳标识，`--check` 跳过 ✅
- 未进 `工单表.xlsx` UPG 主线（SYS-xx 独立追踪，规则不变）✅
- SYS-01 六阶段物不破坏：审验.py 零回归（R1-5）✅

## 七、遗留 / 建议

- **真实表迁移仍待评审**：R1 后剩余 267 处 diff 已**全部归因**——17 表缺行（UPG-51~67 新卡）、79 库登记缺失（7 卡优先级 + 历史手工表值未沉淀）、170 表过时（库最新）。迁移前建议设计师确认：**存量表信息是否全部沉淀进工单库**（B 类 79 处即「只在表、不在库」信息——若需保留应先补写库再 3B 迁移）；C 类 1 处（UPG-43·G）sync 可自动补。
- **B 类「库信息缺失」是数据补登单候选**：79 处表有值库无值（尤其 7 卡优先级 + 大量「来源: 用户实测…」备注），属历史登记遗漏，非脚本可解——建议后续挂账数据补登（先库后表，E1 单向纪律不变）。
- **角色词表随流转演进**：`ROLE_ANCHORS` 是启发式词表；新动作形态（新的合流/审验措辞）需同步扩展词表，否则对应状态列显示 `—`+警告（不猜）。
- **Windows 显示注记**：脚本输出 UTF-8 中文，GBK 控制台直跑可能乱码；`PYTHONUTF8=1`/`chcp 65001` 下正常（同审验.py 注记）。本报告全部实测以 UTF-8 捕获。

## 八、登记说明

- 已登记：`README.md` §六 SYS-02 阶段三后追加 R1 标注 ✅（见下）。
- 交付 git 分支：`feat/sys02-ph3`，R1 实现 commit `f4d33a6` + 本报告及登记（同分支）。
