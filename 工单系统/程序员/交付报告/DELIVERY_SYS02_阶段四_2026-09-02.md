# 交付报告 · SYS-02 阶段四（E4 全景看板——全局仪表盘，只读投影）

> 类型：SYS 系统自改进（独立编号，不混入 UPG 主线）
> 日期：2026-09-02 ｜ 依据：`设计师\派单\SYS-02_阶段四_E4看板_派单_2026-09-02.md` + STD `验收标准冻结区\SYS-02\STD-SYS02_阶段四_E4看板_v1_2026-09-02.md`（S2-10~20 冻结）+ 设计 v1.1（三审通过）
> 治理归属：工单系统自身改造（**只动 `Desktop\MOV\工单系统`**，**零 0027-mov 代码**）
> 状态：✅ **已交付 @2026-09-02**（C 线实现，待设计侧验收 + 变异抽查）

---

## 一、本阶段交付（1 件核心产物）

| # | 交付 | 实现 |
|---|---|---|
| 1 | **`审验员\orders-overview.mjs`**（E4 全局看板脚本，自包含解析核心） | **只读投影**：E1 负责真相（工单库.md 57 卡），E4 只算允许的派生告警（超期/🟡待合/✅终态/完整性缺口/⚠️无法解析），不写库/表（跑前=跑后 sha256 一致实证，写入看板头部）；**三态子列** 每卡交付/验收/合main 三列独立（S2-10）；✅ 仅已合 main 终态（S2-11）；⚠️ 超期覆盖状态列（S2-12）；severity_rank 稳定排序（S2-13/20）；ts 缺失降级不默认（S2-15）；未知状态=⚠️ 无法解析不静默降级（S2-16/19）；数据源边界=E1 唯一（S2-14）；超期基准=当前态 ts 非 created_at（S2-18） |
| 2 | **看板产物** `处理中心\验证产物\orders-overview.md` | 2026-09-02 快照：57 卡全呈现 100%；⚠️ 超期 2（UPG-04/45）；⚠️ 无法解析 2（UPG-24/26）；❌ 打回回炉 4（UPG-05/25/57/65）；🔨 施工 3（UPG-18/55/68）；🟡 待合 1（UPG-66）；📌 已派 1（UPG-46）；⏳ 排队 1（UPG-43）；✅ 终态 43；挂账待审池 19 条；优先级未排定 UPG-21/49 诚实「未排定」 |

**核心收益**：E1（工单库）→ E4（全局仪表盘）投影链路贯通，一次性看清 57 卡当前态 + 派生告警（超期/待合/打回/无法解析），机器只 flag 人终裁——**不新增状态载体、不改变 E1 真相**。

---

## 二、验收判据核对（S2-10~20，每条证据）

| 项 | 标准 | 实测证据 |
|---|---|---|
| **S2-10** | 三态子列：交付/验收/合main 三列独立标记 | ✅ 看板表头 `\| 交付 \| 验收 \| 合main \|`，每卡三列独立：UPG-66=`✅\|✅\|🟡（待合）`、UPG-46=`📌\|—\|—`、UPG-43=`⏳\|—\|—`、终态 43 卡=`✅\|✅\|✅` |
| **S2-11** | ✅ 仅终态：验收通过未合=🟡（超期=⚠️），不得出现「✅ 未合」 | ✅ UPG-66 验收通过未合 main=`🟡（待合）` 非 ✅；self-test S2-11 fixture（@09-01 待合）断言 `cols.m==="🟡（待合）"`；**变异 A**（把 accepted 误设 ✅）→ self-test 红（FAIL 1） |
| **S2-12** | 超期覆盖状态列：⚠️ 派生替换状态列，非关键点备注 | ✅ UPG-04 合main 列=`⚠️ 超期：待合 main`（>2d 未合 @08-29 起）、UPG-45=`⚠️ 超期：待合 main`（>1d @08-30 起）——⚠️ 直接占状态列，非藏在关键点；self-test S2-12 fixture 同断言 |
| **S2-13** | ⚠️ 置顶排序：超期/施工/待合/排队/终态各 1 → ⚠️→🔨→🟡→📌→⏳→✅ | ✅ self-test S2-13 构造 6 卡混合 → 输出 `UPG-912(超期),UPG-914(超期),UPG-913(施工),UPG-915(待合),UPG-911(排队),UPG-910(终态)` rank 0/0/1/2/4/5 严格；真实看板 rank0 超期+无法解析置顶 → rank1 施工+打回 → rank2 待合 → rank3 已派 → rank4 排队 → rank5 终态 |
| **S2-14** | 数据源边界：状态列=E1 唯一；线/关键点=只读；冲突=关键点末尾 `⚠️ 数据源冲突` | ✅ 状态列唯一来自 `工单库.md` 状态行；`线` 列=状态文本最后 [A-D] 动作字（只读展示不参与判定）；关键点=派生说明；self-test S2-14 演示冲突时关键点末尾标 `⚠️ 数据源冲突` 且状态列保持 E1（`🔨` 不变）。真实数据 E1 单源、无外部事实，天然无冲突 |
| **S2-15** | ts 缺失降级：无 ts 状态 → 不算超期 + 关键点 `⚠️ ts 缺失（超期未计算）`，整板正常产出 | ✅ self-test S2-15 fixture（`🔨 施工中` 无 ts）→ `overdue=null` + `ts=null` + `tsMissing`，关键点标 `⚠️ ts 缺失`；真实数据无可超期卡缺 ts（57 卡状态行均有 ts），降级路径=fixture 覆盖 |
| **S2-16** | 诚实空态+图例：无法解析=⚠️ 无法解析（不猜）；头部图例在位 | ✅ UPG-24/26 状态异常 → `⚠️ 无法解析`（不落 ⏳/📌）；看板头部图例 `线 A=agent-1 · B=agent-2 · C=agent-3 · D=agent-4` + ✅/🔨/🟡/📌/⏳/⚠️/— 全语义在位 |
| **S2-17** | 幂等+零回归：连跑两遍一致；审验.py/阶段一二三脚本零回归 | ✅ self-test S2-20 连跑两遍 render 逐字节一致；真实数据多次 `node orders-overview.mjs` 输出一致；**审验.py `--verify-hash-self-test` PASS 2/2**（9fd39b6→REJECT / 2a13dcd→HASH_OK）——E2 阶段一零回归；未触碰 sync-orders.mjs（阶段三收口，零回归） |
| **S2-18** | 超期基准正确：created_at 旧但当前态 ts 新 → 不超期 | ✅ self-test S2-18 fixture（状态区含 `created_at=2026-07-01` 干扰字段 + 当前态 `施工中 @2026-09-01`）→ ts 取状态区最后更新 `2026-09-01`、`overdue=null`（不误用 created_at）；**实现=maxDate(状态区) 非 created_at** |
| **S2-19** | 未知不静默降级：非法枚举 → ⚠️ 无法解析（不得落 ⏳/📌） | ✅ self-test S2-19 fixture 非法枚举 → `stage=unknown`、`cols.d !== 📌 && !== ⏳`；变异口径=把 unknown 映射 📌 → 必红 |
| **S2-20** | 稳定排序：severity_rank ASC → card_id ASC；连跑两遍 output 逐字节一致 | ✅ 排序键 `rank ASC → cardNum(UPG-xx) ASC`；同 rank 内按卡号稳定（如 rank0 内 UPG-04<24<26<45）；self-test S2-20 连跑两遍 `md1===md2` 逐字节一致 |

---

## 三、变异 3 条（STD §四 变异攻击，实证判据可红）

| # | 变异 | 手法 | 实证结果 |
|---|---|---|---|
| **A（S2-11）** | 验收通过未合 main 误设 ✅ | 副本改 `columns()` accepted 分支 `m:"🟡（待合）" → "✅"` | `--self-test` → **FAIL 1**（S2-11「验收通过未合=🟡（不得出现✅未合）」红，stage=accepted merge=✅）→ 判据有效拦截 |
| **B（S2-12/13）** | 阈值偏移（merge 1 天 → 1 小时） | 副本改 `THRESHOLD.merge` `1*DAY → 3600*1000` | 真实看板超期 **2→3 卡**：UPG-66（@08-31 待合）从 `🟡（待合）` 变 `⚠️ 超期：待合 main`（>0d）→ 阈值是有效判据，偏移必红 |
| **C（S2-16）** | 优先级缺失回落默认 P2 | 真实数据核对（实现无回落逻辑） | 对账区 `优先级未排定（诚实「未排定」，不回落默认 P2）：UPG-21、UPG-49`——库状态区无优先级字段则诚实留空，**未回落**（若回落 P2，对账区该项消失=信息丢失，设计红线禁止） |

---

## 四、证据引用

- `设计师/派单/SYS-02_阶段四_E4看板_派单_2026-09-02.md` —— 本单派单（范围/判据/红线/注意项）
- `处理中心/验收标准冻结区/SYS-02/STD-SYS02_阶段四_E4看板_v1_2026-09-02.md` —— 判据 S2-10~20 冻结档 + §四 fixture/变异 + §五 实现契约
- `公司完整体系设计/SYS-02_阶段四_E4全景看板_设计_v1.1_2026-09-02.md` —— 三审定案设计（面板形态/状态语义/排序/数据流）
- `审验员/orders-overview.mjs` —— 交付脚本（含 `--self-test` fixture 15 项）
- `处理中心/验证产物/orders-overview.md` —— 看板产物（2026-09-02 快照）

## 五、测试结果

| 步骤 | 命令 | 结果 |
|---|---|---|
| 1 自测 | `node orders-overview.mjs --self-test` | **PASS 15 / FAIL 0**（S2-10~13/15/16/18~20 fixture；S2-14 冲突演示） |
| 2 真实数据 | `node orders-overview.mjs` | 57 卡全呈现；⚠️超期 2、⚠️无法解析 2、❌打回 4、🔨施工 3、🟡待合 1、📌已派 1、⏳排队 1、✅终态 43；挂账 19 条；表 57 行无缺口 |
| 3 只读实证 | 跑前/跑后 sha256 | 工单库 + 工单表 hash **跑前=跑后一致**（写入看板头部，退出码 0） |
| 4 变异 A | 副本改 accepted→✅ 跑 self-test | **FAIL 1**（S2-11 红）——✅仅终态判据有效 |
| 5 变异 B | 副本改 merge 阈值 1h 跑真实 | 超期 2→3 卡（UPG-66 变 ⚠️）——阈值判据有效 |
| 6 零回归 | `审验.py --verify-hash-self-test` | **PASS 2/2**（E2 阶段一零回归）；sync-orders.mjs 未触碰 |
| 7 幂等 | 连跑两遍 render | 逐字节一致（self-test S2-20） |

## 六、hash 三重（交付绑定）

| code_commit_sha | artifact_sha | evidence_manifest_sha |
|---|---|---|
| `0028a3b` | `orders-overview.mjs` sha256=`d66ce788018ee41e` ｜ `orders-overview.md` sha256=`73b0ca0d653ccb50` | （待填） |

**E2 hash 一致性预校验**：本单交付在工单系统仓库、分支 `feat/sys02-ph4` 未合 main，属预期状态；验收通过、设计师合 main 后应复检 `HASH_OK`。

## 七、范围与红线遵守

- 只动 `Desktop\MOV\工单系统\审验员\orders-overview.mjs`（新增）+ `处理中心\验证产物\orders-overview.md`（新增）+ `程序员\交付报告\` + `README.md` §六，**未碰 0027-mov APP 源码**（0027-mov 零接触）✅
- **只读投影**：E4 不写 工单库.md / 工单表.xlsx——跑前=跑后 sha256 一致实证，写入看板头部 ✅
- **不新增状态载体**：E1=唯一真相，E4=投影；✅ 仅终态；超期阈值顶部 const 可配；提醒不审批、无预测 ✅
- **机器只 flag 人终裁**：看板只呈现派生告警（超期/待合/打回/无法解析），不代替设计侧终裁 ✅
- **诚实空态**：UPG-21/49 优先级库状态区无字段 → 诚实「未排定」，**不回落默认 P2**；无法解析卡不静默降级 ⏳/📌 ✅
- **不新造告警类型**：只允许设计 §4 列出的派生告警（超期/🟡/✅/完整性/无法解析/ts缺失）✅
- **展示层=纯文本 md**：无 HTML/推送，0027-mov 零接触 ✅
- **不修改 sync-orders.mjs**（阶段三收口）：E4 解析核心为自包含复制（同源同口径，避免跨文件耦合）✅
- 脚本**不联网、无 secret**（纯本地 node + python openpyxl 子进程读表行数）✅
- 未进 `工单表.xlsx` UPG 主线（SYS-xx 独立追踪，规则不变）✅

## 八、遗留 / 建议

- **超期基准口径注记**：看板按「日粒度」取状态发生日 23:59:59 为 ts（当天发生、次日检查 <24h 不算逾期）——符合 STD「超期按天计」语义；如需小时级精度需 E1 状态行记录具体时刻（当前库为日粒度）。
- **UPG-24/26 ⚠️ 无法解析**：两卡状态行文本异常（无任何可归入枚举的当前态动作）——需 E1 侧人工复核补全状态行后看板自动收敛（E4 不猜，诚实呈现）。
- **UPG-04/45 超期待合 main**：>1d/2d 未合 main，需设计师尽快合 main 收口（E4 只 flag，处置=人）。
- **打回 4 卡（UPG-05/25/57/65）**：回炉修复中，属施工态；若打回后超 3 天未重交付，将进入 ⚠️ 超期：交付。
- **角色词表随流转演进**：E4 解析核心为 sync-orders 同源复制；若 sync-orders 后续扩展 ROLE_ANCHORS 等词表，E4 需同步（建议后续以 E1 解析库单源化消除重复）。
- **Windows 显示注记**：脚本输出 UTF-8 中文，GBK 控制台直跑可能乱码；`PYTHONUTF8=1`/`chcp 65001` 下正常（同 sync-orders 注记）。本报告全部实测以 UTF-8 捕获。

## 九、登记说明

- 已登记：`README.md` §六 SYS-02 阶段四标注（已认领 C 线 → 阶段四完成 + 交付待验收）。
- 交付 git 分支：`feat/sys02-ph4`，实现 + 看板产物 + 本报告及登记（同分支）。
