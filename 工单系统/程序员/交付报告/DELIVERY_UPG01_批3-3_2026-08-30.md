# DELIVERY_UPG01_批3-3_2026-08-30（程序员 C）

**工单**：UPG-01 批 3-3 · 内容与设备控制（34 工具）——hostToolMetaB3 登记
**分支**：`feat/upg01-b3-3` @ **e20f03c**（已 push origin；基点 = feat/upg01-b3-1 最新 f6a6a53——共享类型，同 B3-2 策略）
**认领**：工单表 UPG-01 行 @2026-08-30 01:35 登记在案

---

## 一、交付清单

1. **hostToolMetaB3（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB3.kt`——34 工具三件套：
   - md 2（render/renderFile——返回 String 形态如实声明「HTML 文本/URL 路径」）
   - note.create（公共目录边界，与 obsidian.file.write 差异）＋ text2image（文本→图）/ image.info（读属性）/ image.ocr（ML Kit 坐标注）/ pdf.read（PDFBox，>50 页拒）
   - http 3（get 无 body / post JSON body / download 落盘 50MB；截断字段 truncated）
   - spill 2 / shell.exec（sh -c 15s 强杀，限主用户权限语义）/ causal 3（record 写事实 / link 建关联 / query 只读）/ error 2（classify 永不失败 / report 后写 200 字）
   - battery / bluetooth（安卓 31+ 授权语义）/ brightness 2（与 screen.on 区分：亮度值 vs 亮屏）/ screen.on（**desc 豁免清单**，方案 §1.4 件 7）/ torch 3（on/off/status）/ vibrate / volume 2 / wifi / silent 2 / notification.post（POST_NOTIFICATIONS）/ **xiaomi.assist（disabled：只登记名+附注，handler 固定拒绝实证）**
   - 全部 desc 20-60 字 + 近邻差异点；schema 字段级 ≤15 字/字段（`来自 xxx.kt:行` 锚点 @31769a0）；output 顶层键逐条 handler 实证（FileTools/HardwareProvider/SystemControlProvider/AgentEssentialTools/ArchMcpTools/MarkstreamMcpBridge/TorchProvider 全核对）
2. **不碰 MainActivity**（纯新文件；聚合 B3-1/B3-4）
3. **output 随登记照写**（非空断言入 L1）

## 二、cond 分类（本单为批 3 唯一 cond 非 none——随报告交付清单）

| 维度 | 工具 | 说明 |
|---|---|---|
| env=mobile | 全部 34 | 设备本机能力，无 PC-only（方案 §1.6 审计结论） |
| **cond=hardware** | torch.on/off/status、vibrate、bluetooth.status（5） | 模拟器跳过、**挂账「真机补验」区**——L2 抽样该 5 项在 ACCEPTANCE_LOG 标 ⏳ 待真机补验，不阻塞本单合批（派单 §二） |
| **cond=disabled** | xiaomi.assist | 硬禁用（handler 固定拒绝，:1971 实证）；只登记名 + 附注「已禁用：唤起小爱同学会切换前台（AI 不可自动调用）」 |
| **desc 豁免** | screen.on | 单动作无参、语义即名（方案 §1.4 件 7）；豁免清单已入测试（b33DescExempt） |

## 三、验收证据（L1）

- `ToolMetaTest` 10→**12 用例**全绿，新增：
  - `批3-3 登记 34 工具清单对账且 desc-schema-output 三件套质检`（附录 A 对账 + 豁免逻辑 + 20-60 字 + 黑名单 + 字段 ≤15 字 + output 非空；变异①锚）
  - `批3-3 投影验证——对齐 B1+B3 合并 registry 34 工具全命中非模板串`（B3-4 收口前形态先行；变异②锚）
- **变异亲杀两条（实证）**：①screen.on desc 改「占位」→ 质检 FAILED；②测试内 host 只并 B1 → 投影验证 FAILED；均恢复后全绿（本批先 commit 后变异，恢复=git checkout 安全）
- **L1 全量**：`--rerun-tasks` 真跑 43 tasks 全 executed——**53 类 381 tests / 0 failed / 1 skipped** BUILD SUCCESSFUL；`:app:assembleDebug` 绿（app-debug.apk 55MB）
- `check-token-effect.mjs HEAD` **exit=0**
- desc 断言亲杀纠偏记录：33 条中 2 批共 15 条超限（含首自查正则漏报 7 条——二次断言全抓）→ 全部精简后达标；类名笔误 HostMetaToolEntry→HostToolMetaEntry 编译亲杀修正

## 四、Token/KV 两节申报

- **Token 影响：不变**——本单只增登记文件（HostToolMetaB3）+ 测试，零 MainActivity 装配变更；34+26+34 工具并入后总账留 **B3-4 收口批统一实测申报**
- **KV Cache 影响：不变**
- `check-token-effect` exit=0 实证

## 五、登记债对账

- B3-2 后预期 8583 → 本单 34 工具登记（B3-4 收口接线后从回落清单消失；L1 已先行断言「B1+B3 合并 registry 下 34 全命中零回落」）→ 收口预测 **余 24**（B3-4）

## 六、待核实/挂账

**本单 0 条挂账**（34 工具参数与返回全部 handler 实证，无「待核实」残留）。
注记：hardware 5 项的「真机补验」由验收员 L2 按派单 §二口径挂账（模拟器跳过是验收约定，非施工挂账）。

## 七、红线自查

九条全过：①工具行为/签名零改动 ②描述全真实（disabled/豁免/hardware 语义均查实现）③字数/差异点达标（断言亲杀）④既有描述只增不改 ⑤登记层与接线同批（聚合 B3-1/B3-4）⑥请求前缀恒定 ⑦零平行登记结构/零落盘 ⑧回滚=revert 单 commit（e20f03c）⑨纯 CRLF 保形

**已登记工单表.xlsx + 工单库.md（feat/upg01-b3-3 e20f03c｜报告 DELIVERY_UPG01_批3-3_2026-08-30.md）**
