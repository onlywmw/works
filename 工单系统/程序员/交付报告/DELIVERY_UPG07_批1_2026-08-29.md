# UPG-07 批 1 交付：预算口径加权移植 + 窗口下沉 ModelEntry + 双阈值收敛 + usage 聚合层

- **分支/hash**：`feat/upg07` `171acfb`（基线 = main 36d7f6e + upg16 挂账基建 433a0b1，worktree=mov-upg07）
- **日期**：2026-08-29 01:45
- **登记**：工单表 E8/H8、工单库 UPG-07 卡（先表后库）
- **Token/KV 申报（方案 §五）**：批 1 只改估算口径与聚合读侧，**不改请求内容**——token 不变、KV 前缀不变
- **已登记两个表** ✅

## 交付四项（方案 v3 §二批 1）

1. **估算口径移植**：`ContextBudget.estimateTokens` 替换 char/4 → 老版加权（ContextBudget.java:37-52 @ff8d67e 逐字同源）：ASCII×0.3 / CJK×(1/1.5≈0.67) / 其他×1.2，ceil；isCJK=0x3400-4DBF∪0x4E00-9FFF。修真 bug：中文低估 60-65% → 85% 压缩线悬在真实窗口外。
2. **窗口下沉 ModelEntry**：`ModelEntry.contextWindowTokens`（默认=DEFAULT_CONTEXT_WINDOW）+ `ModelRegistry.windowFor(id)` 三级解析（覆盖表→条目→默认）；MainActivity `applyWindowFromCurrentModel` 挂 syncModelRegistry 尾部 + model.use handler——**切模型同步切窗口**；windowBtn 保留为调试临时覆盖（1M↔5K）。
3. **双阈值收敛（销挂账-压缩双阈值口径漂移）**：`ContextBudget.DEFAULT_DEGRADE_RATIO=0.85` 单一事实源；BasicCompactionEngine `thresholdRatio` 默认值 0.8→引用常量（改 internal 可测）。
4. **usage 聚合层**：新类 `UsageAggregator`（dsh/budget）——回放 session.jsonl 的 assistant/message 事件 fold 日/月/模型三轴（老版 TokenMeter 三分量口径 p/c/r）；**只派生视图零写点**（测试 grep 实证）；`usage.summary` 桥 + SettingsSheet allowedPrefixes 加 `usage.`；MONTH_QUOTA=5_000_000 仅提示。

## 复验

| 项 | 结果 |
|---|---|
| L1 全量 | `--rerun-tasks` **275/0/0**（BudgetWeightTest 10 案：加权/中英误差<30%/CJK 常量锚定/ModelEntry 窗口三态/跨月分桶/无写点 grep/双阈值源锚） |
| 变异 | M1 CJK 权重 0.67→1.0（注释口径漂移复活）→ **6 案红**；M2 ModelEntry 窗口默认删→ **1 案红**；M3 schema merge 删→红（UPG-03 批已证同族） |
| L2 真机 | **部分转复核**：模拟器装机后与本机 C 的 UPG-05 APK 互踩（安装方最后胜出，MemoryPageActivity 实录）——压缩时机/切模型跟随的 logcat 实证待复验；**代码链已锚**：applyWindowFromCurrentModel 源码 + windowFor 单测 + 装配点 :3809 自动消费 contextWindow |
| 设置页用量 | 桥可达（usage.summary + 白名单）；Vue 展示 UI 留前端批（如实申报） |

## 关键发现（评审意见澄清）

- **「窗口 1M 拍脑袋」需修正**：实拉 DeepSeek 官方 pricing 页（api-docs.deepseek.com/quick_start/pricing，2026-08-29）——**DeepSeek V4 全系 CONTEXT LENGTH=1M、MAX OUTPUT 384K**。原 :161 的 1M 恰为官方值；评审「下调对齐真实窗口」意见基于未实拉文档。**真 bug 的修正是口径本身**（char/4→加权），窗口数值本就对齐官方。1M/5K 调试按钮保留（语义=临时覆盖，文案已更新）。
- 窗口下沉的价值不变：将来接入其他模型（非 1M 窗口）时按 ModelEntry 配置即跟随，不再全局拍板。

## 排查实录（方法论）

- L2 装机互踩：验证前 `dumpsys topResumedActivity` + MemoryPageActivity（UPG-05 独有页面）识别安装方——**安装方最后胜出**原则；不与在施方抢设备。
- BudgetWeightTest 追加案曾被 `cat >>` 追加到 class 闭合外（JVM 报 should not be static）——`grep -c @Test` 与 testcase 清单核对发现；python 重排时注意保留 case 闭合。

## 交接

- 批 2 前置：①GoalDomain restoreFrom 补齐（挂账已在案）②弹窗基线采集（3 标准混合任务落档）→「允许本目标」goalAllowSet。
- Vue 用量展示 UI：settings.html 消费 usage.summary 字段（桥可达，留前端批）。
