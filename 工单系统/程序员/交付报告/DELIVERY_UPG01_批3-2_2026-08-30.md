# DELIVERY_UPG01_批3-2_2026-08-30（程序员 C）

**工单**：UPG-01 批 3-2 · 业务与数据安全（26 工具）——hostToolMetaB2 登记
**分支**：`feat/upg01-b3-2` @ **209d191**（已 push origin；**基点偏差申报**：基于 feat/upg01-b3-1 最新 f6a6a53——B2 复用 B1 的 `HostToolMetaEntry/hostParamSchema/hostNoParams` 共享类型，b748d63 未合 main 前这是批 3 最稳施工基线；B1 因此新增小提交 f6a6a53「hostNoParams 升 public」；B3-1 合 main 后 B3-2 将无冲突可 ff/rebase）
**认领**：工单表 UPG-01 行 @2026-08-30 00:52 登记在案

---

## 一、交付清单（派单 §一）

1. **hostToolMetaB2 登记（主线）**：新建 `app/src/main/java/com/mov/android/meta/HostToolMetaB2.kt`——
   - 26 工具：biz 13（onboardStart/Set/Photo/Scan/Submit/Status + profInfo + bookingMine/Action + taskOpen/Mine/Claim/Action）+ workflow 3（run/resume/status）+ vault 8（list/set/delete/get/setPhoto/scanPhoto/credSet/credDelete）+ credential 2（getKey/setKey）
   - desc ≤60 字（近邻差异点：onboard 六步流程各写第几步、taskOpen↔taskMine、taskClaim↔taskAction、bookingMine↔taskMine、vault.get↔vault.peek（peek uiOnly 免审批）、setPhoto↔scanPhoto 及与 camera.* 边界、workflow.run↔resume、credential.getKey 脱敏语义）
   - schema 字段级真描述 ≤15 字/字段（`来自 MainActivity.kt:行` 锚点 @31769a0）；真无参 6 工具空 properties
   - output 顶层键逐条对 handler 实证：credential.getKey=masked、workflow.status 双形态、vault.get 审批批准/拒绝双语义、biz.* 走 bizHttp=ok+业务 JSON 透传、onboardStatus=微信侧状态+signUrl 等
2. **不碰 MainActivity**（本单纯新文件；聚合由 B3-1 首并入 + B3-4 收口——交接段 2 遵守，未认领编辑窗口）
3. **output 随登记照写**（非空断言入 L1，随 B3-1 试点口径）

## 二、验收证据（L1）

- `ToolMetaTest` 10→**12 用例**全绿，新增：
  - `批3-2 登记 26 工具清单对账且 desc-schema-output 三件套质检`（附录 A 对账 + 20-60 字 + 黑名单 + 字段 ≤15 字 + output 非空；变异①锚）
  - `批3-2 投影验证——对齐 B1+B2 合并 registry 26 工具全命中非模板串`（B3-4 收口前形态先行：buildToolRegistry(host=B1+B2)；变异②锚）
- **变异亲杀两条（实证）**：①vault.credDelete desc 改「占位」→ 质检 FAILED；②测试内 host 只并 B1 → 投影验证 FAILED；均恢复后全绿
- **L1 全量**：`--rerun-tasks` 真跑 22 tasks 全 executed——**53 类 381 tests / 0 failed / 1 skipped**（12306 LiveQuery）BUILD SUCCESSFUL；`:app:assembleDebug` 绿（app-debug.apk 55MB）
- `check-token-effect.mjs HEAD` **exit=0**（本单无装配变更，未触请求链路）
- desc 字数断言亲杀纠偏记录：12 条（10 超限+2 不足）精简后达标（断言非摆设）

## 三、Token/KV 两节申报

- **Token 影响：不变**——本单只增登记文件（HostToolMetaB2）+ 测试；**不动 MainActivity 装配**，AI 面/MCP 面 tools 集合与描述零变化。34+26 工具并入后的总账影响留 **B3-4 收口批统一实测申报**（B3-1 申报口径上界 3.2K/轮为 max，收口时校准）
- **KV Cache 影响：不变**（无前缀变化）
- `check-token-effect` exit=0 实证

## 四、登记债对账

- B3-1 后回落余 84 → 本单 26 工具登记（B3-4 收口接线后从回落清单消失；**L1 已先行断言**「B1+B2 合并 registry 下 26 全命中零回落」）→ 收口预测 **余 58**（B3-3 34 + B3-4 24）

## 五、待核实/挂账

**本单 0 条挂账**（26 工具参数与返回全部 handler 实证）。

## 六、红线自查

九条全过：①工具行为/签名零改动 ②描述全真实（bizHttpGet 透传形态、vault.get 审批门控、credential 脱敏均查实现）③字数/差异点达标（断言亲杀）④既有描述只增不改 ⑤登记层与接线同批（聚合由 B3-1/B3-4；L1 投影验证先行）⑥请求前缀恒定 ⑦零平行登记结构/零落盘 ⑧回滚=revert 单 commit（+f6a6a53 前置）⑨纯 CRLF（node 字符串替换保形；rebase 后 CRLF 检查通过）

**已登记工单表.xlsx + 工单库.md（feat/upg01-b3-2 209d191｜报告 DELIVERY_UPG01_批3-2_2026-08-30.md）**
