# S-06 交付：市场站 bug 批修 + 死文件清理 + server.mjs 站表修复

- **分支/hash**：`feat/s06` `87ec0a1` **已 push origin**（基线 main 012784e）
- **日期**：2026-08-29 03:15
- **登记**：工单库 S-06 卡状态已更新（S 系列单无工单表行）——**已登记**（工单库）
- **Token/KV 申报**：纯前端静态页与服务端脚本修复，tools 字段/模型面零改动

## 交付清单（9 组全修）

1. **upload.html 提交整页失效（P1 核心）**：`:186` 提交按钮补 `id="submit-btn"`——原 :200 `getElementById` null 致 IIFE 首行抛错、整个上传功能死。另修 `0/20` 静态计数 → 动态计数（input 监听）。
2. **index.html:199**：`href="explorer.html>` 引号未闭合 → 修复。
3. **admin-console.html:102**：审核服务硬编码「正常」→ 诚实口径「运维通道验证（本页不做公网探测）」——不造假探测（8401 无公网 health 端点，fetch 探测恒异常反而误导）。
4. **details.html**：lh3 设计稿占位图×2 移除（icon/头像换本站占位块）；假副标题（设计稿模型文案）→ `#d-desc` 由 registry desc JS 填充；「安装到 MOV」alert 占位 → 页内指引块 + guide.html 跳转兜底。
5. **explorer.html**：分类按钮对齐 **registry 实际 cat**（all/onboarding/demo/browser——原「生产力/开发/创意/逻辑」是设计稿假设分类，registry 3 包实况不符）+ 过滤 JS 绑定（含按钮高亮态）+「加载更多」死按钮移除（registry 全量已渲染）+ `:157` 死变量 q 删。
6. **guide.html:168**：错字「绝费用」→「跳转」。
7. **server.mjs:29 站表 bug（BP-02 server 侧）**：f[0] 拼音缩写 → **f[2] 电报码**（与 UPG-03 App 侧 SceneTools.parseStationTable 同源口径）——**真实站表 3384 站 node 验证：北京→BJP、北京西→BXP** ✓。
8. **死文件清理**：`make_guide.py`/`make_merchant.py`/`run-pc.sh`（git rm，与产物分叉的一次性生成器）；`sms-probe.js`（untracked 一次性探针删除）。

## 复验

| 项 | 结果 |
|---|---|
| L1 语法 | `node --check` server.mjs/nav.js/data.js/account.js 全过 |
| L1 站表 | server.mjs 解析口径真实站表验证：3384 站、BJP/BXP 电报码正确（f[2]） |
| L1 契约 | 分类过滤逻辑/data-cat 标注/cat-btn 绑定（代码在案；浏览器交互复验随部署后） |
| **L2 部署** | **待办转设计师/用户**：market-web 静态站 scp 上线（服务器 ssh 有腾讯云扫码墙，本会话被挡——通道就绪后 `git pull` + 同步即可） |
| L2 浏览器走查 | 部署后：upload 提交全链（登录→填表→提交）/explorer 分类过滤/details 渲染 |

## 交接

- 部署命令建议：服务器侧 `cd <market-web 部署目录> && git fetch && git checkout origin/feat/s06 -- market-web/`（或设计师按既有部署纪律操作）。
- 12306 server 化方向已废弃（IP 风控）——server.mjs 修复仅为存量代码正确性（S-06 挂账口径），不作为查询服务推广。
