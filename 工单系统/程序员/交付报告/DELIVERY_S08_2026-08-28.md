# DELIVERY_S08_2026-08-28（程序员）

**工单**：S-08 官网「能力市场」页（/home/market/，与 App 市场同 registry 数据源）｜ **优先级**：P1 ｜ **分支**：`feat/s08-market-page`（基于 **main @ abc3aa3**；S-07/e07/UPG-15/16 均已在 main）｜ **部署**：已上线

**已登记两个表**（先表后库）：dims 市场表 S-08 行 + 工单库 S-08 卡状态/交接段。

---

## 一、交付物

| 项 | 内容 |
|---|---|
| `https://mow.kim/home/market/` | 能力市场页：**内置能力五卡** + **市场工具包列表**（fetch `/market/registry.json` 与 App 市场同源）+ 双引流（下载区 / /dev/）+ ICP 页脚 |
| `/home/` 首页 | 新增「能力市场」板块：三张摘要卡（拍照识字/文件处理/加密信息库）+ 「进入能力市场 →」入口 |
| `verify.mjs` | 新增 ③b 段（市场页断言，见 L1） |
| Caddy | **零改动**（/home/ 映射覆盖子路径，出单人判断正确） |

提交链：`d9bebb3` 认领 → `441b09b` 认领行列位修正 → `dc1405b` 主体（market 页 + 首页板块 + verify + ACCEPTANCE_LOG）。

## 二、红线执行：能力卡逐卡源码核实（**12306 卡按红线撤下**）

只读子代理对照源码逐卡盘点（锚点详表已入 `docs/ACCEPTANCE_LOG.md` 本单段），结果：

| 卡 | 结论 | 关键锚点 |
|---|---|---|
| AI 对话 | ✅ 上卡 | MA:763/778/590-599 双模式、RoomStore.kt:9-16 多房间、ModelStore.kt+ModelSheet 多模型 |
| 文件处理 | ✅ 上卡 | BuiltinMcpTools.kt:107-109、MA:2793-2846 公共目录、pdf.read/md.render/http.download |
| 拍照 OCR | ✅ 上卡 | MA:725/3650/3721、LightOcr.kt:15-27 离线 ML Kit、image.ocr |
| 浏览器自动化 | ✅ 上卡（文案带「市场一键启用」——builtin 分发默认未启用，MA:1919-2102 共 14 工具） | AgentBrowser.kt:13-18、McpMarket.kt:100/243-247 |
| 信息库 | ✅ 上卡 | InfoVault.kt:7-20 AES/GCM 本机、vault.* 8 工具、vault.get 逐次审批（MA:2380-2407） |
| **12306 出行** | **❌ 撤卡** | SceneTools.kt:17-269 内置实现存在，但 `McpToolProvider.kt:27` 全源码**无调用方**（未接线运行时工具面，App 工具面无 scene.*）；市场侧 scene-mcp 是独立外部 server 且不在 registry。上卡即编造 → 撤；**待接线后随单补卡** |

另：「总结文件」（MA:726 按钮 Toast 即将上线）、「任务计划」「截图」均未核实/未上线，market 页与新增板块**零出现**（verify 假数据禁令固化）。

## 三、市场包区（与 App 同源，两态诚实）

- `fetch('/market/registry.json')`（与 App 市场 tab 同一数据源）→ 渲染真实 packages（name/desc/author/cat + kind 标签：**剧本 / 外部服务器 / 内置**，标签映射出处 market-web/data.js:13 与 McpMarket.kt）；
- **0 包 → 空态「首批工具上架筹备中」**；取数失败 → 「市场目录同步中」降级；均不放假卡；
- 线上实测（registry 现有 3 真实包）：微信商户入驻助手[剧本] / Hello world[外部服务器] / 浏览器自动化[内置] 三卡全渲染。注：出单人写卡时口径为「registry 0 包」，实测当前线上有 3 包——页面按数据实时渲染，两态皆合规。

## 四、验收证据

### L1（verify.mjs exit=0，156 链接全通）
新增 ③b：market 页存在、空态文案、同源 fetch 断言、**假数据禁令**（registry 实际包名 hello world / browser-automation / wx-merchant-onboard 与未核实能力词 12306 / 总结文件 / 任务计划 写死即红）、内置五卡锚点、首页入口链接。**变异亲杀 2/2**：写死假包卡（Hello world）→ 红；删空态文案 → 红。

### L2（真实浏览器双端，证据 `验收员\证据数据\S-08\` 5 张）
线上 /home/market/ 双端截图（真实 3 包渲染 + 五卡 + 双引流）；首页板块截图；**空态实证**（本地构造 0 包 registry，「首批工具上架筹备中」渲染）。

### L3
构造假 registry（演示包）→ 列表渲染正常、空态区正确隐藏（display:none）→ 还原；公网 /home/market/ 200、/home/ 200、/dev/ 200；APK 直链 200 不回归（部署为**精确文件级** scp，未触碰服务器 build-vars.json/二维码/APK——e07 已切 CDN 的线上产物原样保留）。

## 五、红线自查

- 零假数据：能力卡逐卡源码锚点在案；市场包只渲染 fetch 内容；空态诚实；12306 撤卡；
- 不碰 /tools/ 市场站、不碰 App 市场页（App 侧零改动）；
- 视觉沿用 official-web 既有 token（同色板/字体/卡片风）；平移纪律沿用（零域字样，verify ① 全绿）；
- Token/KV 无影响。

## 六、遗留/给出单人的两个口径确认

1. **12306 补卡前置**：需先接线（SceneTools 挂入运行时工具面）才能上「内置能力」卡——建议单独立小单（接线 + 默认隐藏/显示策略）；
2. 出单人「registry 0 包」口径与实测（3 包）不符，页面已按数据驱动两态兼容；若市场侧计划清空演示包（S-05/S-06 范围），清空后官网自动进空态，无需改码。
