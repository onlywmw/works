# DELIVERY_UPG01_批3-4_2026-08-30（程序员 C）

**工单**：UPG-01 批 3-4 · 市场与界面（24 工具）+ 批 3 聚合收口
**分支**：`feat/upg01-b3-4` @ **3f118ae**（已 push origin；基点 = main 3263f10 + merge feat/upg01-b3-2）
**认领**：工单表 UPG-01 行 @2026-08-30 02:05 登记在案

---

## 一、交付清单

1. **hostToolMetaB4（主线）**：新建 `meta/HostToolMetaB4.kt`——24 工具三件套：
   - market 7（list/install/uninstall/enable/disable/status/refresh——install↔enable「装 vs 激活」、uninstall↔disable「卸 vs 停用」差异点；marketGuard/McMarketClient 透传面如实）
   - marketAdmin 3（pending/approve/reject——本地运营口令档风控语义）
   - ui 页面桥 11（openMarket↔openMarketReview 区分、openModels/Orders/Settings/Vault/Workbench 各写「打开 X 页」、prefillInput/getPins/setPins/setLang）
   - 只读查询 3（permission.mode/presentation.mode/approval.getMode——desc 统一「只读查询，改权限走 UI（改类已 uiOnly）」，铁律 1 精神）
2. **批 3 聚合收口（本单独有）**：MainActivity `hostToolMeta = hostToolMetaB1 + B2 + B3 + B4`（四表合并；B3-1 首并入后本单收口；后续批 4 扩表在此续并位）
3. **收口断言（入 L1）**：四表清单并集 == 四表 keys（118 无缺漏，不变量式）+ 重复键检测 + 全在 mcpHandlers 字面注册（源码在面核对）+ **回落=0** + metaConflicts 零冲突 + **生产聚合接线锚**（防聚合缺表——变异②锚）

## 二、基点与对齐说明（申报）

- 三前提：B3-1（c69fa1f+b0e2d86）/B3-3（1e79dfa）已合 main；**B3-2（209d191）未合**——本分支通过 `merge feat/upg01-b3-2` 对齐 B2 文件（收口聚合需要四表同现才能编译/断言 118）
- **合批顺序**：设计师合批时 **B3-2 先合**（209d191 ff/rebase 进 main）→ B3-4 rebase 后 B2 变更归零，剩 B4+聚合改动（无冲突）
- ToolMetaTest merge 冲突（main 侧 B3-3 区 vs b3-2 侧 B3-2 区并行追加）已按「共享区 + B3-2 区 + B3-3 区 + B3-4 区」重构为单一类结构（保留全部用例并集；重构中定位并修复「双类尾 }」结构残渣 2 处 + moduleFile 换成 :150 先例双候选路径）

## 三、验收证据（L1）

- `ToolMetaTest` 10→**16 用例**全绿（B3-1 2 + B3-2 2 + B3-3 2 + B3-4 2 + 原 8），新增：
  - `批3-4 登记 24 工具清单对账且 desc-schema-output 三件套质检`
  - `批 3 收口——四表聚合 118 无缺漏 且 回落为零 且 零冲突`（含生产聚合接线锚）
- **变异亲杀两条（实证）**：①market.list desc 改占位 → 质检 FAILED；②host 聚合删 B2 → **收口断言 FAILED**（**测试盲区修复**：首次变异②漏网——收口用例只测测试内构造的 registry，未验生产聚合；补「生产聚合接线锚」源码断言后必红）
- **L1 全量**：`--rerun-tasks` 真跑 43 tasks 全 executed——**53 类 386 tests / 0 failed / 1 skipped** BUILD SUCCESSFUL；`:app:assembleDebug` 绿（app-debug.apk 55MB）
- `check-token-effect.mjs HEAD` **exit=0**（装配变更 + Token/KV 申报齐全）
- desc 断言亲杀纠偏：11 条（market 族 6 超限+2 不足+3 只读超限）精简后达标

## 四、Token/KV 两节申报（批 3 收口总账）

- **Token 影响**：AI 面 **118 宿主工具全部真描述入库**（批 1 前 41 → 收口 118 全量）——推导上界 **+8~11K token/轮**（方案 §四 140 字/工具 × 118；B3-1 实测校准点口径延续；**真机装配快照实测留 L2**——若实测显著超上界触发常用集裁剪拍板）；MCP tools/list 同步增量（不入 prompt）；无新增会话中途变动点
- **KV Cache 影响**：tools 段前缀一次性加长（首轮），第 2 轮起命中前缀缓存增量≈0；无新增中途变动点
- `check-token-effect` exit=0 实证

## 五、登记债对账（收口达成）

- **收口后回落=0**（L1 断言 + MOV-Boot「模板串回落 N」实机数留验收员 L2 复核）：118 宿主 + 41 既有登记全部进登记层，批 1 以来登记债清零

## 六、待核实/挂账

**本单 0 条挂账**（24 工具参数与返回全部 handler 实证）。

## 七、红线自查

九条全过：①工具行为/签名零改动 ②描述全真实（透传面/风控/只读语义均查实现）③字数/差异点达标（断言亲杀）④既有描述只增不改 ⑤登记层与接线同批（收口=接线完成面）⑥请求前缀恒定（tools 字段无中途变动点）⑦零平行登记结构/零落盘 ⑧回滚=revert 3f118ae（+merge a4858f1 前置）⑨纯 CRLF 保形（重构测试文件时统一 CRLF 写出）

**已登记工单表.xlsx + 工单库.md（feat/upg01-b3-4 3f118ae｜报告 DELIVERY_UPG01_批3-4_2026-08-30.md）**
