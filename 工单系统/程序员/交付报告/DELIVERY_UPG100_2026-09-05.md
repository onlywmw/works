# 交付报告 · UPG-100

> 类型：M2 体系/治理（M9 工具链/治理——测试锚强度升级） ｜ 日期：2026-09-05 ｜ 依据：工单库 UPG-100 卡面（**待派单态**——用户指令直接领单施工；无派单文/无 STD 冻结，验收锚以卡面 A1-1/A1-2/A3-1 为准，STD 补冻转设计师追认[UPG-103 先例口径]）
> 施工：程序员 Kimi/kimi-cli ｜ worktree mov-upg100 ｜ branch feat/upg100 ｜ 基 origin/main 31640fc8 ｜ 状态：✅ 已完成（交付，待验收）

---

## 一、本阶段交付（3 件）

| # | 交付 | 实现 |
|---|---|---|
| 1 | **A1 锚②升级**（计数+局部性组合断言） | `HomeDeliveryContractTest.kt` 锚②：`if (homeDeliveryActive)` 恰好 2 处（chunk 转发点+完成文本回流点双点冻结）+ `MovHomeHost.onLlmChunk(` 恰好 2 处 + 每分支块 260 字符窗内必含 onLlmChunk 转发（防空壳 if）；原 contains 断言保留 |
| 2 | **A2 方法论入库** | `设计师/知识库/已知坑.md` #10：「多点同文本 contains 组合锚只防全删」现象→根因→药方（计数+局部性/单点不动/行为级优先/Vue 侧两档更弱形态） |
| 3 | **A3 同族抽检** | 静态 8 文件排查（结论表=证据 upg100_a3_sweep_table.md）+ 实测变异 4 处（S1/S2/S4 死角实证绿 + S3 强锚对照红） |

**形态选择论证（红线「行为级测试优先」的取舍）**：锚②守护的回路是 MainActivity 内联 WebView 回调（runOnUiThread + homeWeb.evaluateJavascript）——JVM 单测无法驱动 WebView 回路，行为级=真机层（验收员持有，UPG-96 七场景在案）；计数+局部性组合是 JVM 可执行面的最强锚（单点短路/空壳/双点/全删四变异全红实证）。卡面「不改业务行为」与「行为级优先」在本单不可兼得，取后者精神（锚强度）守前者边界（零业务改动）。

## 二、验收判据核对（卡面锚逐条）

| 项 | 标准 | 实测证据 |
|---|---|---|
| A1-1 | 单处短路（恢复原审验变异）→ 升级版锚红（亲杀） | ✅ M1：chunk 转发点条件反转 → 锚② FAILED（亲杀记录在案） |
| A1-2 | 双处短路→红；全删→红；原状态→绿 | ✅ M3 双点红 / M4 全删红 / 原态绿 / M2 空壳红（加强档）——全部快照还原复绿 |
| A3-1 | 同族抽检结论表（锚/强度/是否升级） | ✅ 8 文件 18 处疑似（Appearance 16 + Approval 2）+ 实测 4 处；两档死角模式命名（定义兜底命中/注释令牌污染） |
| 红线 | 不引入 flaky；不改业务行为 | ✅ 计数/局部性断言=确定性文本统计；git diff 仅 1 测试文件 +23/-0 |

## 三、证据引用

- `程序员/UPG100-evidence/upg100_mutation_kills.txt` —— M1-M4 亲杀 + S1-S4 抽检变异逐条记录
- `程序员/UPG100-evidence/upg100_a3_sweep_table.md` —— A3 同族抽检结论表（静态 8 文件 + 实测 4 处）

## 四、测试结果（XML 汇总）

- app testDebugUnitTest 全量：**842/0/0/1**（842 总=841 过+1 跳过；0 失败 0 错误；XML 119 文件全 fresh）
- 基线对账：origin/main 31640fc8 同口径；UPG-107 提交信息载「app 843/0/1」——±1 计数口径注记（本单=原位升级零计数变化，差 1 非本单引入）
- 本单测试计数变化：+0（锚②原位升级，HomeDeliveryContractTest 仍 5 例）

## 五、hash 三重（交付绑定）

| code_commit_sha | artifact_sha | evidence_manifest_sha |
|---|---|---|
| `a63b7088fe146a2f68fb52098c456cce67fa9cac` | 无产物绑定（纯测试锚升级单，零业务代码改动） | `fa151af21ca5…`（完整值见 manifest 文件） |

**manifest 自检**：ok:True 重算一致 ｜ 文件：`程序员/UPG100-evidence/delivery_UPG100_manifest.json`
**verify-hash**：HASH_REJECT not-ancestor——未合 main 常态，如实申报，合后复跑=终态
**standard_id 申报**：STD-UPG-100 未冻结（卡面待派单态）——manifest 内嵌卡面验收锚段实测 sha256（2f3e20ea…）作临时代，正式冻结转设计师追认

## 六、范围与红线遵守

- 不改业务行为（纯测试/文档锚升级）✔；不动 UPG-96 已合逻辑 ✔
- 不引入 flaky：计数断言目标文本为稳定代码形态（`if (homeDeliveryActive) {`/`MovHomeHost.onLlmChunk(`），无时序/环境依赖 ✔
- 未做：其他 18 处疑似死角的升级（结论表建议列——超本单范围，留后续单）；STD 冻结（设计师职权）
- Token 影响：不变（纯测试锚升级，不触请求链路） ｜ KV Cache 影响：不变
- Code-LOC 申报：+23 / -0 / 净 +23（1 测试文件原位升级）

## 六之二、施工期重大回归与自纠

- 自纠 1：M2 变异脚本首版 bytes 字面量含中文 → SyntaxError 未注入（跑了原态绿）——发现后 ASCII 重注入复杀成功；影响面=零
- 自纠 2：新 worktree 首跑缺 local.properties（SDK location）+ printf 转义写错一次——环境修正后全绿；均为施工环境项，不涉交付物

## 七、登记说明

- 工单表.xlsx 程序员列 ✅完成 + delivery_id 列 `DEL-UPG100-20260905-001`（已登记）
- 工单库.md UPG-100 卡面状态快照同步（先表后库）
- 遗留转办：①STD-UPG-100 冻结 → 设计师；②A3 结论表 18 处疑似死角的升级 → 建议立后续单（P3，Appearance 族优先）
