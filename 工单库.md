# 工单库

> 验收铁律一律：L1 变异亲杀 + 全量绿 + L2/L3 真机证据链；测试必须用真实数据形态，禁用假覆盖。

---

# UPG-01 全量工具元数据补全

**状态**：✅已派单(待领单) @2026-08-26 ｜ **优先级**：P1 ｜ **出单人**：设计师 ｜ **日期**：2026-08-26

## 标题

全量工具元数据补全：description / inputSchema / output 三件套（对齐 dsh 参考实现）

## 背景

MOV 工具面 = 内置工具（`BuiltinMcpTools.kt`）+ browser 工具族（`BrowserMcpTools.kt`）+ 桥/宿主面（`dsh/tools/Tools.kt`）+ 注册 MCP。现状实测：`BuiltinMcpTools.kt` 仅 5 条 description；`BrowserMcpTools.kt` 与 `dsh/tools/Tools.kt` **均为 0 条**。AI 引导层看不到工具用途与参数含义 → 错误调用；市场能力详情缺描述。dsh 参考实现为真描述 + 完整 schema + 返回结构。

## 验收标准

- **L1**（全量绿 + 变异亲杀）：`ToolMetaTest` 遍历全部工具：description 非空/≥20 字/无占位；inputSchema 每字段有中文 description；output 声明非空。变异：任一描述改占位 → 必红。
- **L2**（真机）：`emulator-5556` 能力/工具详情面显示真描述 + 参数说明 + 返回说明（截图入 `docs/ACCEPTANCE_LOG.md`）。
- **L3**（AI 实证）：AI 读取描述后正确选参，journal 可查；禁用假覆盖。

## 红线

- 不改工具行为/调用签名；`McpServer` 方法签名不可变。
- 描述必须真实（字段语义查源码核实）；拿不准标「待核实」进挂账登记表，禁止编造。
- 现有 5 条已验收描述只增不改（除非事实性错误）。

## 方案文档

`设计师\方案设计\UPG-01_全量工具元数据补全_方案.md`（批次拆分 / 元数据登记层设计 / 施工口径详见文）

---

## 派单交接段（程序员领单时按此执行）

1. 先认领：工单表该行备注追加 `认领: <agent> worktree=mov-upg01 branch=feat/upg01 @<时间>`（worktree 命名强绑定工单号）。
2. 施工范围：按方案文档四批执行（批 1→批 4 顺序，每批独立可测）。
3. **完成后必须登记两个表**（先表后库）：
   - `工单表.xlsx`：程序员列 `✅完成`，备注列 `feat/upg01 <hash>（报告 DELIVERY_UPG01_*.md）`；
   - `工单库.md`：本单状态改为 `程序员✅完成，待验收`。
4. 交付报告落点：`程序员\交付报告\DELIVERY_UPG01_*.md`；报告里明确写「已登记两个表」。
5. 全部产物（源码/测试）提交主仓库 `0027-mov` 分支 `feat/upg01`，交付时报 hash。


---

# UPG-05 记忆体系回补

**状态**：✅方案完成，⏳待专家审 ｜ **优先级**：P0 ｜ **出单人**：设计师 ｜ **日期**：2026-08-26

**背景**：新版 MemoryLifecycle 只有引用晋升/衰减骨架；基因化、memory.* 工具、预算投影、causal 增强全部缺失（老版 MemoryGeneCompactor/UserGene/Journal memoryCover 实测在案）。

**方案**：①基因层移植（纯规则 `{k,s,a}` + failureAvoids 自动提取）②memory.load/save/search/delete/cover/list 接线 ③预算覆盖投影（逐字/塌缩+指纹+懒提炼）④causal 活性/衰减/擦除（可拆子单）。

**红线**：基因只渲染不渲染原文；投影只改读侧不删原文；不破坏现有 Lifecycle 语义。

**决策点**：见设计文档§五（字段够用性/误报容忍/折叠阈值/账本链成本）。

---

# UPG-06 确定性防编造三件套

**状态**：✅方案完成，⏳待专家审 ｜ **优先级**：P0 ｜ **出单人**：设计师 ｜ **日期**：2026-08-26

**背景**：新版 guard 仅 RepeatDetector；FabricateGuard/GoalGate/AcceptanceJudge 三类确定性防编造机制缺失（老版 AgentLoop 三件套挂载线索在案）。

**方案**：FabricateGuard（4 提取模式+豁免+诚实降级）→ GoalGate（掌握度 0.5 封顶/0.9 过线）→ AcceptanceJudge（expected 服务端持+剔键+对拍）→ 统一挂载执行循环。

**红线**：expected 绝不发给模型；只加观察+反馈层不改工具签名；建议性语句不得拦截。

**决策点**：与 F1 声称检测线边界、GoalGate 适用场景、expected 来源、误拦体验。

---

# UPG-07 计划授权 + 预算口径 + 设备能力裁剪

**状态**：✅方案完成，⏳待专家审 ｜ **优先级**：P0 ｜ **出单人**：设计师 ｜ **日期**：2026-08-26

**背景**：审批风暴（逐工具弹窗）；中文字数预算低估 63%（char/4 口径）；无用量计量；工具面不裁剪不可用工具。

**方案**：A 计划级审批（APPROVE_PLAN 一次批准，计划外仍弹兜底）→ B 预算 Unicode 加权口径（误差<30%）+ TokenMeter 计量（只提示不硬拦）→ C 设备探测裁剪（fail-open）。

**红线**：批准不永久（计划外必弹）；预算切换先单测对比；计量不硬拦；探测失败不裁剪。

**决策点**：计划授权适用场景、加权精度、月配额默认、设备裁剪是否本期。
