# MOV 网页设计总纲 v1（2026-08-31）

> 设计师产出。只写已实证内容（文件结构/交付记录/工单卡），页面级视觉细节标注「待补全」——下次设计迭代逐页补，不编造。

---

## 一、公众站（mov-ai.cn /home/）

### 1.1 官网首页（index.html）
- **定位**：用户下载门户（S-07 一期：APK 下载入口 + 创作者/开发者中心入口）
- **已实证结构**：`home/index.html` + `assets/` + `build-vars.json`（构建变量注入）+ `market/`（能力市场子页）
- **事件板块**（W-05）：parseEvents 白名单 fail-loud（状态/类别取值域校验）、红点语义（临期≤3天/超期红）、SVGs 注册、事件卡⏰截止渲染——L3 端到端验证过（改表→sync→线上断言→还原）
- **设计要点**：下载入口醒目（用户转化第一目标）；能力卡与 registry 同源
- **待补全**：视觉稿/栅格/配色 token（下轮设计迭代补）

### 1.2 能力市场页（/home/market/，S-08）
- **定位**：与 App 市场同源的能力展示页
- **设计要点**：registry 驱动（禁假数据）；**12306 撤卡案例** = 五卡源码核实的诚实口径样板；空态诚实（无包显示无包）
- **待补全**：卡片视觉规范/筛选交互稿

### 1.3 隐私政策页（W-07）
- **设计要点**：与 App 内 PrivacyGate 弹的 privacy.txt **同一份内容**（App 改政策 → 网页同步）；文本冻结技术保障链（防篡改）
- **V1.2 新增**：App 内协议已升级 V1.2（信息加工与个性化推荐五条）——**网页版同步 V1.2 待核对**（若未同步列为待办）

## 二、开发者中心（mow.kim /dev/）

### 2.1 页面清单（已实证，2026-08-29 同步代码基线）
| 文件 | 内容 |
|---|---|
| index.html | 开发者中心首页（工具面模式/目录层） |
| api-reference.html | tools[] 表——**description/inputSchema 已从「建议」升格「✅ 必须（空/占位上架审核即打回）」**（修正与 index 硬规矩「禁占位」的内部矛盾） |
| package.html | 包结构说明 |
| publish.html | 发布流程 |
| quick-start.html | 快速上手 |
| faq.html | 常见问题 |
| llms.txt | 「开发者文档一键给 AI」入口（Token 预算口径） |
| assets/mov-page-server.js | 自研 UMD ≈250 行（JSON-RPC 2.0 over postMessage + 三错误码 + 白名单 + 数据非指令） |

### 2.2 设计要点
- **工具面契约双轨**：dev 文档描述与实际 tools/list **同源同更新**——App 侧工具面演进（UPG-01 批4 已全量 158 工具真描述）后 dev 文档必须跟上（0829 已同步一轮，后续每批跟）
- **页脚基线标注**：七页页脚「最后同步：2026-08-29 · 代码基线 @ af68d22」——每次工具面演进必须更新基线标注

## 三、能力市场工具页（mow.kim /tools/，S-05）

- **registry 驱动**：S-05 去 demo 化——撤 2 个 demo 包，线上 1 真包，消费面即时生效
- **待扩展**：UPG-42 批2 的 /tools/ 三工具（list_packages/get_package/market_stats）将让此页可被 AI 直读

## 四、站点 WebMCP 化（UPG-42，浏览器双本质）

- **一期 ✅**（批1）：mov-page-server.js + dev 站 3 工具 + llms.txt + spec.md 镜像；线上 200；7/7 绿
- **二期 🔨**（批2 派单文本已备）：/tools/ 三工具 + /home/ + mov_site_info + 徽标
- **三道门已纳管**：站点 mov_* 工具注册进 AI 工具面后自动走 SkillGate（JS_ARTIFACT 拒收/门 2 回归）与 A-1 Manifest 演进契约

## 五、待办与缺口（诚实清单）

1. **隐私政策网页版 V1.2 同步核对**（App 已 V1.2，网页若未跟则为待办）
2. **各页视觉稿/栅格/配色 token**：全部待补全（下轮设计迭代逐页出）
3. **mov-ai.cn 与 mow.kim 的视觉一致性**：双域名双受众，视觉语言是否统一待设计定夺
4. **dev 站七页页脚基线标注**：UPG-01 后续批次合入后需再次更新（流程已固化：页脚基线=最后同步日期+代码基线 hash）

## 六、设计变更流程

新设计/改版 → 设计稿落 `网页体系建设\设计\<页面名>\` → 定稿后拆工单进 `工单系统\` → 施工在 0027-mov → 验收（verify.mjs + 截图）→ 上线（scp dev 先行）→ 涉及 mov_* 工具契约的走 A-1 Manifest。
